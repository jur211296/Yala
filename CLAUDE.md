# Yala (iOS)

Yala es una app iOS de finanzas personales. Objetivo: entender gastos, cuentas, presupuestos y reportes con claridad.

## Stack

- Swift, SwiftUI, SwiftData (.xcodeproj)
- **Target iOS 26+** — APIs nativas (Liquid Glass, ToolbarSpacer, etc.)
- Schemes: **Yala** (producción) | **Yala Dev** (con toggle Pro y `DEV_BUILD`) | Tests: YalaTests
- Simulador: **iPhone 17 Pro**
- 20 SwiftData models. ModelContainer via `SwiftDataConfiguration`. Divisas SSOT en `Yala/Utils/CurrencyUtils.swift` (`CurrencyCode`, 48 divisas).

## Docs (leer cuando sea relevante)

| Archivo | Propósito |
|---------|-----------|
| `$VAULT/planning/CODEBASE-MAP.md` | Tablas de Services / Calculators / ViewModels / Tests con paths |
| `$VAULT/planning/UI-PATTERNS.md` | Design System, gotchas de SwiftUI, formularios, glass |
| `$VAULT/planning/SWIFT-STYLE.md` | ViewModel pattern, idioms modernos, DS.Semantic / DS.Gradients, "añadir preferencia" |
| `$VAULT/planning/L10N.md` | 16 locales, workflow para añadir keys, tests CI |
| `$VAULT/planning/DEVICE-QA.md` | Setup Yala Dev + agent-device patterns |
| `$VAULT/planning/BRAND-VOICE.md` | Tono y estilo de marca |
| `$VAULT/planning/WORKFLOW.md` | Workflow detallado de skills |
| `$VAULT/planning/PROJECT.md` · `ROADMAP.md` · `STATE.md` | Producto, plan, progreso |
| `$VAULT/planning/DECISIONS.md` | Registro de decisiones arquitectura |
| `$VAULT/planning/QA-SCENARIOS.md` | Escenarios de prueba |
| `qa/README.md` | QA automatizado (agent-device + suites) |

`$VAULT` = `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/YalaWiki/` (Obsidian, sync vía iCloud).

Carpetas del vault: `Backlog/` · `Ideas/` · `Bugs/` · `Attachments/` · `planning/`. Skills: `/backlog`, `/spec`, `/promote`.

## General Rules

- Hacer SOLO los cambios explícitamente solicitados. No mover UI, refactorizar adyacente, ni añadir mejoras no pedidas.
- Antes de editar, listar archivos a modificar y qué cambia. Esperar aprobación si son más de 3 archivos.
- Confirmar que rutas referenciadas existen antes de proceder. Si no, preguntar al usuario.
- Evitar refactors grandes innecesarios. No introducir dependencias nuevas sin justificación.
- Mantener separación UI / lógica / SwiftData.

## Reglas inviolables

### Errores y unwraps
- NUNCA `try?` que silencia. Usar `do { try } catch { print("Service: Error: \(error)") }`.
- NUNCA force unwraps sin validación previa (`guard let x = ... else { return }`).
- Logs SIEMPRE dentro de `#if DEBUG` — nunca datos sensibles en producción.
- API keys: NUNCA hardcodear. Usar `Secrets.xcconfig` + `Info.plist` via `Bundle.main.object(forInfoDictionaryKey:)`.

### SwiftData
- SIEMPRE `@Relationship(inverse:)` en relaciones bidireccionales.
- Verificar `deleteRule` en cada relación.
- `@MainActor` en servicios que manipulan `ModelContext`.
- **CloudKit compat:** NUNCA `@Attribute(.unique)`, propiedades con default obligatorias, ni relaciones non-optional.

### State Management (SwiftUI)
- `@Observable` SIEMPRE con `@MainActor`. `@State` SIEMPRE `private`.
- NUNCA `Binding(get:set:)` en body — usar `@Binding` + `.onChange()`.
- NUNCA `@AppStorage` dentro de `@Observable` (no triggerea updates).
- Preferir `@Observable` + `@State`/`@Bindable` sobre `ObservableObject`/`@Published`/`@StateObject`.
- **Preferencias persistentes → `AppPreferences` inyectado via `@Environment`.** NUNCA `@AppStorage` directo en views nuevas.

### Gotchas críticos
- **`containerRelativeFrame(.horizontal)` en `ScrollView(.vertical)` con `.contentMargins`** → deadlock de layout, splash nunca dismissa, sin crash log. Usar `onGeometryChange`. Detalles en UI-PATTERNS.md.
- **`YalaFormatter` no auto-refresca prefs** — lee `UserDefaults` directo. Vista que lo use con `decimalPlaces` o `currencyDisplayFormat` debe inyectar `@Environment(AppPreferences.self)` y leer `let _ = appPreferences.X` en body para registrar dependencia.
- **Forms con `TextField`/`TextEditor`/`SecureField`** (sin `Form`): obligatorio `dismissKeyboardOnTap()` desde el primer commit. Detalles en SWIFT-STYLE.md.

### iOS 26 Liquid Glass (OBLIGATORIO)
- `ToolbarSpacer(.fixed, placement: .topBarTrailing)` — placement es OBLIGATORIO.
- `.glassEffect()` para chips, barras flotantes, elementos translúcidos.
- Si existe API iOS 26 que mejore integración con sistema, USARLA.

### Tests (OBLIGATORIO)
Detalles completos en `$VAULT/planning/TESTING-STRATEGY.md`. Reglas mínimas:
- `makeTestContext()` es seguro desde Fase 5 (2026-04-29) — usa UUID suffix + `isRunningTests` detecta Swift Testing. Aún así prefiere `@Model` directos sin contexto cuando la lógica lo permite (más rápido).
- NUNCA `UserDefaults.standard` directo en tests → `UserDefaults(suiteName: "test.\(UUID().uuidString)")!` (helper `makeIsolatedDefaults()`).
- NUNCA tocar singletons `.shared` sin `@Suite(.serialized)` + `defer { restore }` o `_testReset()`.
- NUNCA `Task.sleep(.seconds(N))` con N>0.5 — usar señales determinísticas. Excepción: `≤50ms` para forzar dealloc.
- NUNCA `Date()` / `Calendar.current` en lógica testeada — inyectar vía param opcional `now: Date = .now` (patrón canónico, ya en `FinancialScoreCalculator`/`BudgetAlertService`).
- NUNCA `@Test(.disabled(...))` sin entrada en Lista Negra (TESTING-STRATEGY.md) con owner + deadline.
- NUNCA declarar fix completo si un test falla. "Preexistente" no es excusa: arreglar o registrar en Lista Negra con plan.
- Ejecutar con `-parallel-testing-enabled YES` (recomendado, -30% tiempo). `NO` solo como fallback si aparece race en singleton no cubierto.

### Audit markers
- `// A11Y-DT:` justifica font size hardcodeado (Dynamic Type).
- `// A11Y-DM:` justifica color hardcodeado (Dark Mode).

### Design System (en cambios UI)
- SIEMPRE `DS.Spacing`, `DS.Radius`, `DS.Typography`, `DS.Semantic.*`, `DS.Gradients.*` — NUNCA hardcoded.
- SIEMPRE filas clicables con `Button` + `contentShape(Rectangle())`.
- Componentes estándar: `YalaPrimaryButton`, `YalaEmptyState`, etc.
- Tablas DS.Semantic / DS.Gradients en SWIFT-STYLE.md.

### Documentation & copy
- Describir features desde la perspectiva del USUARIO, no técnica.
- NUNCA fabricar features — solo lo confirmado en scope.
- Copy nuevo: leer BRAND-VOICE.md.

## Workflow

Skills disponibles vienen en el system-reminder de cada sesión. Flujos canónicos:

```
Feature:    /clear → /next → Plan Mode → /review-plan → implementar →
            /verify-ios → /test-smart → /device-qa → /swift-audit → /commit-one → /clear
Bug fix:    /next → implementar → /verify-ios → /device-qa → /commit-one
Autónomo:   /clear → /next → Plan Mode → /review-plan → /yolo
Complejo:   añade /analyze-impact antes de Plan Mode + /simplify antes de commit
```

**Regla QA-SCENARIOS:** cada feature nueva requiere escenarios en `$VAULT/planning/QA-SCENARIOS.md` ANTES del commit.

## Testing

| Tipo de cambio | Comando |
|----------------|---------|
| Modelo / servicio | `/test-smart` (relevantes) |
| Solo UI (Views) | `/verify-ios` |
| Antes de commit | `/test-smart` siempre |
| Tras merge / refactor grande | `/test-ios` (todos) |

Después de implementar cualquier cambio, SIEMPRE ejecutar `/verify-ios` o `xcodebuild` antes de presentar como completado.

## Corrección de errores

- SIEMPRE buscar TODAS las instancias del mismo patrón antes de declarar fix completo.
- NO confiar ciegamente en "BUILD SUCCEEDED" — verificar todos los casos.
- Si hay errores tras build exitoso, limpiar cache: `xcodebuild clean`.

## Self-Maintenance

Cuando se modifican modelos / servicios / ViewModels:
- Actualizar tablas en `$VAULT/planning/CODEBASE-MAP.md`.
- Actualizar conteo de tests si se agregaron nuevos.
- Agregar gotchas descubiertos a "Reglas inviolables" de este archivo.

Para preferencias nuevas (`UserDefaults`): ver checklist en SWIFT-STYLE.md sección "añadir una preferencia nueva".

## Control de Ejecución

Después de implementar código:
1. Mostrar resumen de cambios **en lenguaje de usuario** (qué cambia para él / qué problema resuelve), no descripción técnica de archivos editados.
2. Ejecutar `/verify-ios` SIEMPRE para confirmar que el build pasa.
3. Sugerir siguiente paso.
4. DETENERSE y esperar instrucción del usuario.
5. NO ejecutar tests, device-qa o commits automáticamente — solo el build.

**Git:** ejecutar cada comando de lectura UNA SOLA VEZ, secuencialmente, nunca en paralelo. No matar shells con git en curso.

**Tags:** SIEMPRE semver con prefijo `v` → `v1.0.0`, `v1.1.0`. Nunca sin prefijo o sin 3 componentes.

## Decisiones Recientes (TTL: hasta cierre de fase)

[Formato: [FECHA] Decisión breve — se archiva en DECISIONS.md al cerrar fase. Máx 3-5 entradas activas.]

- [2026-05-11] **Onboarding restyle iter3 — refinamiento Step 3 + Step 6 (commit `e42488bc`)**. Sprint single-commit con 2 cambios precisos sobre iter2. **Step 3 reorden**: cards intercambiadas — card 1 ahora "Varias cuentas" (multiple, iconTint `.priorityNeedNew`) preseleccionada por default + card 2 "Una sola cuenta" (single, iconTint `.hotPink`). **Default global**: `selectedUsageMode` cambia de `.dayToDay` → `.fullControl` — el user llega a Step 3 con multi-cuentas marcada. Callback Step 2 card "control" cuando viene de expensesOnly: setea `.fullControl` (era `.dayToDay`) para coherencia. Step 4 (.accountType) ya no se skipea por default — el user explícitamente debe elegir "Una sola cuenta" en Step 3 para skipearlo. Cero tests dependen del default (verificado grep). **Step 6 redesign**: eliminado completamente `balanceLivePreview` arriba — cada modo muestra el saldo en su propia sección debajo de la card seleccionada, sin duplicación visual. En `.manual` el form editable expande TextField con el monto. En `.guided && hasValue` expande nuevo computed `guidedBalancePreview` con `OnboardingFieldSection` (mismo wrapper que el manual): label "Saldo inicial" + monto formateado en `largeTitle.semibold` (vía `appPreferences.currency()` con thousands separator + decimales) + **botón "Editar"** inline (icon `square.and.pencil` + label en `Color.hotPink`) que reabre el sheet del calculator. Tap card 2 directo también reabre el sheet. En `.guided && empty` sigue el hint amber de iter2. Tap card 1 desde `.guided` con valor: cambia a `.manual` y el monto previo del calc se carga en el TextField. **L10n**: reuso de keys existentes (`Onboarding.accountBalanceLabel` "Saldo inicial" + `Action.edit` "Editar") — **0 keys nuevas**. **Decisión owner consolidada**: livePreview arriba era redundante porque ambos modos ya muestran el saldo en su card propia (manual via TextField, guided via guidedBalancePreview). Estructura más limpia. **Tests existentes verdes** (24/24). Sin tests nuevos. 28 escenarios QA documentados (OB-VS-03 actualizado _(iter3 update — REORDEN)_, OB-VS-06 actualizado _(iter3 update — sin livePreview arriba)_, **OB-VS-28 nuevo** preview read-only + botón Editar). Plan: conversación inline sin doc separado. Pendiente: device QA con 28 escenarios OB-VS-01..28.

- [2026-05-11] **Onboarding restyle iter2 — feedback adicional sobre iter1 (commit `48367147`)**. Sprint single-commit con 2 ajustes precisos. **Step 1**: eliminado completamente el saludo interactivo "Hola, %@. Yala está listo para ti" (computed `welcomeGreetingText` + callsite en `nameStep`). Decisión owner: sobrecargaba sin sumar. Keys `Onboarding.greetingBubble` + `Onboarding.greetingBubbleEmpty` eliminadas en 16 locales + accessors en `L10n.swift`. Step 1 ahora va directo de subtitle → form del nombre. **Step 6 rediseño**: el CTA card prominente de iter1 no comunicaba claramente que era tappeable. Reemplazado por **2 binaryCards estilo Step 2/3/4**: card 1 "Conozco mi saldo" (icon `pencil` halo `.priorityNeed`, subtitle "Lo escribes a mano") **preseleccionada por default** + card 2 "Calculemos tu saldo juntos" (icon `arrow.triangle.2.circlepath` halo `.hotPink`, subtitle "Te guiamos paso a paso"). Tap card 1 expande el form manual abajo (sign segmented + TextField gigante con currency symbol — diseño intacto desde iter0). Tap card 2 abre sheet del calculator. **`balanceLivePreview` siempre arriba** (independiente del modo, feedback constante). **Tras sheet cancelado sin elegir**: card 2 sigue seleccionada (stroke 2pt `.hotPink`) y aparece **hint amber** debajo con icon `exclamationmark.triangle.fill` `.essentialNeed` + caption "Aún no completaste el cálculo. Toca para retomar." (voseo es-AR "Tocá"). Re-tap card 2 reabre el sheet. **Color amber NO rojo** (decisión: brand voice cálido, alineado con "atención pendiente" no "algo roto"). State management: nuevo enum `BalanceMode { manual, guided }` privado + `@State balanceMode: BalanceMode = .manual`. Nuevos helpers: `manualBalanceForm` (form expandido — extraído del balanceStep original sin tocar diseño) + `balanceIncompleteHint` (hint amber con `fixedSize(horizontal: false, vertical: true)` para wrap correcto). Reuso del `binaryCard` existing — no nueva abstracción. **Eliminado** computed `balanceGuideCTA` de iter1. **L10n net iter2**: 3 keys nuevas (`balanceManualOption` "Conozco mi saldo", `balanceManualHint` "Lo escribes a mano" — es-AR "Lo escribís a mano", `balanceGuidedIncompleteHint` "Aún no completaste el cálculo. Toca para retomar.") + 2 eliminadas (`greetingBubble`, `greetingBubbleEmpty`) × 16 locales = +16 strings net (vs +80 de iter1). Aplicado vía script Python idempotente. Reuso de keys existentes para card 2: `accountBalanceLearnMore` (title) + `balanceGuideHint` (subtitle). **Tests existentes verdes** (24/24): 7 OnboardingTelemetryEventBuilder + 10 OnboardingPrefillResolver + 7 FullModeActivationLogic. Sin tests nuevos (no hay nueva pure-logic — `BalanceMode` enum + estado SwiftUI, todo UI). 27 escenarios QA documentados (OB-VS-01 actualizado _(iter2 update — saludo eliminado)_, OB-VS-06 actualizado _(iter2 update — REDISEÑO)_, **OB-VS-27 nuevo** sheet cancelado → hint amber + retap reabre). **Decisiones consolidadas en sesión**: (1) `.manual` preseleccionada por default — reduce un tap, mayoría usa input manual. (2) Tras sheet cancelado `.guided` sigue marcada con hint (no auto-revert a `.manual`) — preserva intent del user. (3) Re-tap card reabre sheet sin CTA extra "Recalcular" — la card ya es interactiva por contrato. (4) `balanceLivePreview` siempre arriba en cualquier modo. (5) Color hint amber `essentialNeed` (no rojo error) — brand voice cálido. Plan: conversación inline sin doc separado (alcance reducido vs iter1). Pendiente: device QA con 27 escenarios OB-VS-01..27 (incluyendo SE + AX5 + Reduce Motion + VoiceOver + 5 temas themedPanel).

- [2026-05-10] **Onboarding restyle iter1 — QA visual feedback aplicado (commit `6f0040a5`)**. Sprint single-commit que aplica feedback de QA visual del owner sobre `3f4cdf95`. **4 reglas universales R1-R4** en heroFlow: R1 elimina `titleAccentGradient` (titles primary), R2 hero icons primary (Steps 2-7), R3 paleta de iconos de cards restringida a `.hotPink/.priorityNeed/.priorityNeedNew/.essentialNeed` (sin indigo, sin cyan), R4 progressIndicator activo+inactivo bifurcado (`.white` + `.white.opacity(0.2)` heroFlow / `theme.accent` + `theme.secondaryText.opacity(0.2)` themedPanel — antes inactivo era invisible sobre indigo). **Step 1**: eliminada struct `WelcomeBubblePreview` → Text inline simple sin contenedor con `.smooth(0.45)` keyed en text. Copy nuevo "Hola, %@. Yala está listo para ti" en 16 locales (voseo es-AR "para vos", vouvoiement fr, Sie de, です/ます ja, 你 zh-Hans). **Step 6**: drop auto-launch del calculator + CTA card prominente (icon halo + chip "Recomendado" amber). **Reusa `accountBalanceLearnMore`** existente (NO eliminar — tiene 2 callsites externos en `BalanceCalculatorSheet.swift:164` y `AccountFormView.swift:65` que el plan original no contemplaba; hallazgo del review escéptico). 1 key nueva `balanceGuideHint`. **Step 7**: grid categorías 52pt → 40pt (reusa `categoryIconSize` existing). Eliminada propiedad huérfana `notifIconSize`. **Step 8 rediseño tipo Apple Settings**: eliminado `readyHeader` (Circle 96pt + sparkles 44pt). Title `largeTitle.bold`, subtitle `body`. Items en lista compacta con **Rectangle 1pt bifurcado** (heroFlow `Color.white.opacity(0.1)` / themedPanel `Color(.separator)` — `Divider()` default invisible sobre indigo). Privacy en grid 2x2 mini-cards individuales. 4 keys L10n nuevas (versiones cortas): `privacyLocalShort/NoTrackingShort/IcloudShort/NoSharingShort`. **Hallazgos del review escéptico** (todos incorporados): callsites externos `accountBalanceLearnMore`, R4 inactivo también bifurcado, `iconTint` default `.electricIndigo` viola R3 → `.hotPink` defensivo, `Divider()` invisible sobre indigo → Rectangle bifurcado, `.padding(.top, DS.Spacing.xl)` migra al VStack tras eliminar readyHeader. **L10n net**: 1 modify + 5 new + 0 delete × 16 locales = +80 strings. Aplicado vía script Python idempotente. **Tests existentes verdes** (24/24): 7 OnboardingTelemetryEventBuilder + 10 OnboardingPrefillResolver + 7 FullModeActivationLogic. Sin tests nuevos (no hay nueva pure-logic — todo UI/style/copy). `/simplify`: 2 fixes (eliminada referencia "tras QA iter1" del comment de `styleAccentColor` por regla CLAUDE.md no narrar ticket; constante nombrada `iconColumnWidth` en `rowDivider`). 26 escenarios QA documentados (OB-VS-01..08 actualizados con `_(iter1 update)_` + OB-VS-26 nuevo R4 capsules bifurcadas). **Aprendizajes**: `/review-plan` cazó 5 problemas críticos pre-implementación; el grep defensivo `accountBalanceLearnMore` reveló los 2 callsites externos. SourceKit cross-file diagnostics ruido durante edits (resuelven al compilar). Plan: `~/.claude/plans/ajusta-el-plan-considerandot-sharded-boot.md`. Pendiente: device QA con 26 escenarios OB-VS-01..26 (incluyendo SE + AX5 + Reduce Motion + VoiceOver + 5 temas themedPanel).

- [2026-05-09] **Bug `review-deeplinks` — centralizar selectMainTab para tabs ocultos en Más** (commit `bde61bb2`). 6 fases atómicas. **Causa raíz**: `TabView` nativo iOS 18+ con `selection` binding requiere que `Tab(value:)` esté montado en `ForEach(visibleTabs)` del mismo tick — asignar `selectedMainTab = X` cuando el tab está oculto en "Más" deja el binding en el vacío. El patrón correcto (`temporaryTab` + `Task.sleep(50ms)` + `selectedMainTab`) estaba solo en `.recordsStandalone` y `.groups`; otros 16 callsites lo violaban (chevrones del Panel, hero, Score, deeplinks externos, MorePlaceholderView). **Solución**: helper único `SessionState.selectMainTab(_:)` con cancellation de `pendingTabSelectionTask` + GC-08 guard centralizado (eliminado guard duplicado en `handleMainTabIntent`). Helper pure-logic `MainTabSelectionLogic.decide(requested:config:)` para testabilidad. Extension inversa `AppTab.asConfigurable` (nil para `.more`/`.search`). `TabBarConfiguration.loadFromStandardDefaults()` deduplica lectura `UserDefaults`. **Cancellation crítico**: dos deeplinks rápidos consecutivos podrían dejar `temporaryTab` limpio antes que el Task posterior llegara al `selectedMainTab = X`, regenerando el bug — el cancel previene esto. **Migración**: 16 callsites (4 funciones síncronas SessionState + 10 cases handleMainTabIntent + MorePlaceholderView.hiddenTabRow + GroupsContainerView.openPanel + FullModeActivationView + PanelViewModel.navigateToStatistics). **Tests**: 9 pure-logic en `MainTabSelectionLogicTests.swift` (visibleTab/hiddenTab/panel/more/search/groupInvite x2/emptyConfig/hiddenPlanning) — todos verdes. **Decisiones clave**: helper centraliza el invariante "monta-luego-selecciona" en un sitio que es imposible saltarse (vs Approach B "todo via AppRouter" que convertía nav síncrona en async, y Approach C "fix puntual" que dejaba bug latente en deeplinks externos); helper pure-logic separado vs lógica inline (alineado con `RemoteWipeSignalDecider`/`OnboardingPrefillResolver`/`FullModeActivationLogic`); guard GC-08 en SessionState no en helper (helper queda libre de conocer `OnboardingMode`); mantener semántica de overwrite de `temporaryTab` ya ocupado (consistente con patrón anterior — follow-up si UX confusa). **NO migrar** `SessionState.resetToDefaults:370` (transient durante wipe). **Aprendizajes**: `/review-plan` cazó 5 problemas pre-implementación (cancellation faltante en diseño inicial, race con `didSet` de selectedMainTab cuando otra mutación concurrente limpia `temporaryTab`, callsites adicionales en `MorePlaceholderView`+`GroupsContainerView`+`FullModeActivationView` no listados en plan inicial, verificación tras F4 antes de tocar handleMainTabIntent, escenario iPad sidebar DL-08). `/simplify` cazó 1 oportunidad real: helper `loadFromStandardDefaults` en TabBarConfiguration (deduplica triple-llamada UserDefaults+fromJSON). Sanity check `grep -rn "selectedMainTab\s*="` confirmó 0 callsites residuales fuera del helper. SourceKit cross-file diagnostics ruidosos (mismo target — resuelven al compilar; ignorables). 8 archivos / +236/-50 LOC. Plan: `~/.claude/plans/proxede-a-planificar-parsed-wall.md`. Pendiente: device QA con 8 escenarios DL-01..DL-08 (incluye iPad sidebar landscape).

- [2026-05-09] **Onboarding completo restyling — Welcome Hero/Chooser + Yala AI**. Sprint único 9 commits F1-F9 que llevan el lenguaje visual del `WelcomeFlowContainer` (heroIndigoBlack + logo 128pt + cards translúcidas Liquid Glass) y del `YalaAIOnboardingView` (PanelBackgroundView + capability cards + readyHeader gradient + toolbar leading X/chevron + progress en `.principal` + CTA único `YalaPrimaryButton`) al `OnboardingView` completo (8 steps fresh-install) **Y** al `FullModeActivationView` (wrapper para users mode `groupsOnly` activando full). **Bifurcación contextual** vía nuevo wrapper `OnboardingFlowScreen<Content>` + enum `OnboardingBackgroundStyle` (`.heroFlow` post-Welcome dark indigo continuo + paleta blanca rígida; `.themedPanel` FullModeActivation `PanelBackgroundView()` adapta tema con texto themed). 6 computed style helpers privados en OnboardingView (`primaryTextStyle`/`secondaryTextStyle`/`mutedTextStyle`/`cardStroke`/`styleAccentColor`/`titleAccentGradient`) bifurcan según el style. **Cambios estructurales**: (1) `binaryCard` reescrito sin checkmark/circle, icono con halo (RoundedRectangle 44×44 con `iconColor.opacity(0.18)` + Image gradient 20pt semibold), stroke 2pt accent en seleccionada (vs 1pt cardStroke). 6 callsites actualizadas con iconColor diferenciado por step (purpose: accent/essentialNeed; accounts: neonCyan/priorityNeedNew; accountType: 4 tints; categories yes/no: priorityNeed/secondary). Step 4 ForEach inline consolidado a binaryCard via helper `iconTint(for: AccountType)`. (2) Nuevo `OnboardingFieldSection<Content>` private struct reemplaza `SectionBox` solo en Steps 5/6 (label arriba `secondaryTextStyle` + content `.solidCard()` background-aware). `SectionBox` global no se toca. (3) Step 1 nameStep dibuja YalaLogo 128pt con `colorMultiply(.white)` solo en `.heroFlow` consumiendo `logoTopSpacing` del wrapper (mismo nivel visual que WelcomeFlowContainer). En `.themedPanel` mantiene app icon 120pt como fallback (FullMode skipea Step 1 vía prefilled, defensivo). Title con `titleAccentGradient` (`.hotPink → .white` heroFlow / `.hotPink → theme.accent` themedPanel). Nuevo `WelcomeBubblePreview` subview (saludo en vivo `"Hola, %@ 👋 — voy preparando tu Yala…"` + placeholder `"¿Cómo te llamas?"`) con `.smooth(0.45, extraBounce: 0.15)` keyed en text + Reduce Motion respetado. (4) Step 6 nuevo `balanceLivePreview` computed: `largeTitle.semibold` con monto formateado en vivo + `.contentTransition(.numericText())` keyed en `initialBalanceText`/`balanceIsPositive`. Empty state muestra `"S/ 0"` en `mutedTextStyle`. (5) Step 8 readyHeader inline (Circle 96pt gradient `.hotPink → styleAccentColor` + sparkles 44pt en gradient match) reemplaza el `confirmationMotivation` largeTitle previo — el motivacional pasa a subtitle `title2.semibold`. Items con `.thCard` + stroke + iconColor variado (styleAccentColor/hotPink/essentialNeed/priorityNeed). Privacidad como card translúcida con header `privacyTitle` (reusa key existing) + 4 bullets. CTA Step 8 cambia a `L10n.Onboarding.startUsingYala` ("Empezar a usar Yala", más identitario que `finish`). (6) **Toolbar bifurcado**: `OnboardingView` envuelve body en `NavigationStack` propio + `toolbarBackgroundVisibility(.hidden)`. `topToolbar` con `leadingToolbarButton` switch en `(backgroundStyle, currentStep)`: heroFlow Step 1 → X cancel solo si `onCancelFromStep1!=nil` / heroFlow Steps 2-8 → chevron back llama `goBack()` / themedPanel todos los steps → X cancel total via `onCancel`. Progress capsules en `.principal`. **Eliminado obligatoriamente** el `.welcomeBackButton(tint: .secondary, ...)` modifier en F1 — coexistía con el toolbar como overlay, hubiera causado dos chevrons en Step 1 (regresión OB-VS-23). (7) **Bottom CTA bifurcado**: `floatingNavigationButtons` solo renderiza fade gradient + back capsule en `.themedPanel` (PanelBackground claro necesita separación). En `.heroFlow` solo CTA único `YalaPrimaryButton` (back vive en toolbar). Helper `goBack()` extraído como función privada (instrumentado con telemetría). `advance()` extraído de la closure inline del primary button con `trackPickIfApplicable(forStep:)` interno. (8) **`FullModeActivationView` refactor**: drop NavigationStack envolvente + drop toolbar Cancel — pasa `.themedPanel` + `mode: .fullActivation` + `onCancel: { onComplete() }` al OnboardingView. (9) **Bugfix preexistente double-fire** `.onboardingCompleted`: hoy disparaba 2 veces en FullMode (uno desde `OnboardingView.completeOnboarding` con `expensesOnly`/`usedSeedCategories`, otro desde `FullModeActivationView.completeFullActivation` con `mode: fullActivation`). Fix: `OnboardingView.completeOnboarding` agrega param `mode` derivado del `init`; `FullModeActivation.completeFullActivation` deja de disparar `.onboardingCompleted` (mantiene solo `.fullModeActivationCompleted`). Resultado: 1 fire único por flow con `mode` correcto (verificable en OB-VS-24). **Telemetría granular** (9 cases nuevos en `AnalyticsEvent`): `onboardingStarted/StepViewed/PurposePicked/AccountsPicked/AccountTypePicked/CurrencyPicked/CategoriesPicked/BackTapped/Cancelled`. Pure-logic helper `OnboardingTelemetryEventBuilder` con 4 funciones (`paramsForStarted/StepViewed/Cancelled/BackTapped`) en `Yala/App/Logic/` + 7 tests Swift Testing verdes. **Disparo**: `started`+`stepViewed(.name)` en `.task` raíz; `stepViewed(next)` y picks (state final efectivo, no por handler tap) en `advance()`; `backTapped`+`stepViewed(prev)` en `goBack()`; `currencyPicked` en `accountCurrencyPickerSheet onDismiss` solo si `currencyAtPickerOpen != accountCurrency`; `cancelled` en callbacks toolbar X ANTES del callback original. Step enum gana `trackingName: String` computed (lowercased "name"/"purpose"/etc.) desacoplado del Int rawValue. **L10n**: 3 keys nuevas (`greetingBubble`+`greetingBubbleEmpty` Step 1, `startUsingYala` Step 8 CTA) × 16 locales = 48 strings con traducciones reales (voseo es-AR "Empezá", Sie de "wie heißt du", vouvoiement fr "Comment vous appelez-vous", です/ます ja "Yalaを始める", 你 zh-Hans "你叫什么名字", tu pt-PT "te chamas", você pt-BR "seu nome"). Helper Python `/tmp/yala-l10n-insert.py` insertó idempotente en 14 locales (10 con anchors INSERT, 4 sin anchors APPEND con marker). 25 escenarios QA documentados (OB-VS-01..25) cubriendo per-step × per-style + Dynamic Type AX5 + Reduce Motion + VoiceOver + 5 temas en .themedPanel + regresiones críticas (OB-VS-23 un chevron, OB-VS-24 telemetría única) + riesgo NavigationStack chrome (OB-VS-25 logo position). **Aprendizajes**: `/review-plan` cazó 6 problemas + 3 riesgos antes de implementar — eliminación obligatoria del `.welcomeBackButton` modifier; bugfix double-fire `.onboardingCompleted` aprovechable; logo `logoTopSpacing` solo se EXPONE (responsabilidad del Step 1 content, no del wrapper); `toolbarBackgroundVisibility(.hidden)` para chrome NavigationStack; instrumentar picks en `advance()` no en handlers (1 evento por step efectivo, no por tap); helper `OnboardingFieldSection` privado en lugar de tocar `SectionBox` global. F2 visual SE para `confirmationMotivation` largeTitle → subtitle (3 keys cortas paralelas `confirmReadyExpenses/DayToDay/FullControl` documentadas como contingentes en F7 si QA reporta overflow). `.numericText()` con currency string puede tener jitter al cambiar signo (validar OB-VS-06). Pendiente: device QA con 25 escenarios + decisión final sobre keys cortas Step 8 según overflow visual.

- [2026-05-09] **M6 — Cuenta personal real en Caso A del bridge SplitExpense ↔ TransactionItem**. 9 fases F1-F8 cierran épico Groups Pulido Final (items diferidos del Sprint Pulido 3). Cambio arquitectónico: Caso A (yo soy payer) ahora crea TX cuenta real `-totalAmount` con subcat manual del gasto + TX virtual `+lentAmount` sistema "Préstamo a grupos". Resuelve paradoja M5 donde mi BCP no veía la plata que salió (panel personal mentía). **Contrato editorial**: "lo que tú escribes queda como tú lo escribiste" — bridge **preserve+update** Caso A NUNCA pisa campos personales (cuenta/note/subcat/tags) después de creación inicial; solo actualiza monto/fecha/moneda/splitTotalAmount/splitType en eventos remotos. **Sincronía 100% personal cross-device**: TX cuenta real va por CKSync privado del user (no shared zone), preserve+update preserva cuenta local cuando llega SplitExpense remoto del mismo user. **Race autodelete**: nuevo notification `transactionsImportedFromSync` (cada import successful, no solo first) + `GroupBridgeRaceCleaner.cleanupPendingDraftsWithMatchingTX` borra drafts pendientes Caso A cuando ya existe TX real para mismo `splitExpenseID`. **Items entregados**: F1 InboxDraft + 3 fields opcionales (`originReasonKey`/`originActorName`/`originGroupName` snapshot al crear, lightweight migration safe). F2 bridge refactor `bridgeExpense` (sig: `accountForCurrentUser: Account?` + `isRemoteSync: Bool`) — preserve+update Caso A si TX real existe Y currency compatible (NUNCA pisa account/subcat/note/tags); incompat currency → delete + draft con hint impersonal "currencyChanged"; primera vez con cuenta proveída → crea TX real; sync remoto sin cuenta → draft con hint "remoteCreate" actor=payerName snapshot; helper `createDraftCaseA` interno + flag `skipDraftCleanup` para race con DraftService. F3 GroupExpenseService.createExpense/updateExpense propaga `accountForCurrentUser` end-to-end. F4 form chip "Cuenta" condicional (solo Caso A `.full/.completed`) + VM `selectedAccount`/`isReady`/`isCaseA`/`isAccountRequired`/`canSave` extender + `resetAccountIfIncompatible()` onChange currency + `isGroupInviteOverride` para tests + 12 tests pure-logic verdes. F5 race cleaner + sync hook (`iCloudSyncService.applyRemoteChanges` post-save dispara notification cada import) + `GroupBridgeRaceCleaner` static (`cleanupPendingDraftsWithMatchingTX` runtime + `computeCleanupPlan` pure-logic) + observer en `AppBootstrapper` step 14.5 + 6 tests verdes. F6a NewTransactionView edit parcial (computed `isBridgedCasoA`, refactor `Group { }.disabled` → granular: centralContent siempre disabled si bridged, bottomChips/registerButton habilitados Caso A) + `currencyFilter` en AccountSelectorSheet edit Caso A + delete/duplicate `.disabled(isBridgedCasoA)` (regenera draft, UX confusa) + banner copy contextual `editPartialBanner`. F6b nuevo `GroupExpenseAccountFinalizationSheet` (~190 LoC, replica Settlement Caso D pattern) + `DraftService.approveGroupExpenseAccountDraft` (10 pasos atómicos: guards, fetch SplitExpense+SplitGroup, crea TX cuenta real con splitExpenseID, set skipDraftCleanup=true, invoca bridge para regenerar virtual lent, defer reset, cache+delete draft+save) + InboxView routing por `targetTransactionID==nil && needsUserInput.contains("account")`. F7 GroupCardView D3 estilo Splitwise puro (max 3 rows + chip "+N más" CTA detail, sort by amount desc, perspective `.iOwe`/`.theyOweMe` con counterpartyName resuelto) + helper pure-logic `GroupsViewModel.computeCurrentUserDebts` + 8 tests + cleanup NO defaults universal (eliminadas funciones `defaultSettlementAccount/setDefaultSettlementAccount` de `GroupPersonalPreferences`, callsites en `SettlementFormView.resolvePreselectedAccount`, `GroupSettlementDraftFinalizationSheet`, bridge `bridgeSettlement` Caso C/D — `removeAll` mantenido para limpiar legacy). F8 L10n 6 keys × 11 locales completos (es-AR voseo "Podés"/"Elegí", es/es-419, de Sie "Sie können", fr vouvoiement "Vous pouvez", it, ja です/ます, zh-Hans 你, pt tu form Portugal, pt-BR você, nl, pl) + es-ES/en-GB/pt-PT heredan via fallback iOS + 14 escenarios QA M6-01..M6-14 incluyendo race cleaner, cross-device sync, edit parcial, currency change. **Decisiones clave**: subcat NO sincroniza (Opción I conservadora — categorías son privadas CloudKit private DB; en M6 incluso solo el payer no propaga, simetría perfecta), hint copy con actor snapshot para `remoteCreate` y impersonal para `currencyChanged` (sync remoto no expone "modifiedBy" → más honesto), `splitTotalAmount` semántica diverge en TX virtual lent (`amount=lent, splitTotalAmount=total` — ningún consumer asume equivalencia, validado pre-F2 con grep), no migrar M5 (owner archivará grupos manualmente), delete TX cuenta real bloqueado en NewTransactionView (regenera como draft, UX confusa). **Aprendizajes**: `/review-plan` cazó 8 problemas pre-implementación (F1 incompleto solo 1 field → ampliado a 3, DraftService branch sin documentar orden → 10 pasos numerados con defer, actor name semántico vs payer name → impersonal para currencyChanged, delete gating no documentado → M6-13 escenario, F2 path "sin cuenta" sin guard → assertionFailure DEBUG, splitTotalAmount semántica → grep pre-F2 confirmado seguro, `currentUserMemberID` nil edge case → `isReady` gate, F8 copy currencyChanged debe ser impersonal). Refactor a `computeCurrentUserDebts` static helper hecho post-F7 inicial al detectar tests requerían pure-logic (instanciación de `private(set) var` no posible). **Race cuidado**: `skipDraftCleanup` flag + `defer` en DraftService garantiza reset si bridge falla mid-flight; draft permanece pendiente para retry (idempotency natural). 31 tests pure-logic nuevos (5 InboxDraftM6Fields + 12 GroupExpenseViewModelM6 + 6 GroupBridgeRaceCleaner + 8 GroupsViewModelDebtsM6) — todos verdes. Tests de `GroupPersonalPreferences` reescritos a 2 cases sobreviviendo (removeAll cleanup legacy). Build verde end-to-end en cada fase. Pendiente: `/device-qa` con 14 escenarios M6-01..M6-14 + verificación cross-device sync personal (M6-04, M6-07, M6-14 críticos — necesitan 2 devices misma Apple ID). TODO post-launch: añadir debounce 1s al observer `transactionsImportedFromSync` para mitigar invocaciones múltiples en cold launch. Closes items diferidos Sprint Pulido 3 (cambio arquitectónico, #34 selector moneda, D3 doble fila multi-moneda).

- [2026-05-08] **Yala AI Onboarding (4-step tutorial first-use post-consent)**. 7 fases F0-F7. Flag `hasShownYalaAIOnboarding` (per-device, no synced) + 5 cases telemetría (`yalaAIOnboardingShown/Completed/Skipped/Dismissed/TonePicked`) + L10n.YalaAI.Onboarding (28 keys × 16 locales = 448 strings, voseo es-AR + Sie de + vouvoiement fr + です/ます ja + 你 zh-Hans + tu/você pt-PT/pt-BR) + pure-logic `YalaAIOnboardingLogic.nextScreen(consent×onboardingShown)` con 4 tests verdes evita drift entre 3 launchers (PanelView/RecordsStandaloneView/DetailContainerView). Animación dummy del chat en Step 1 con loop infinito + delay inicial 800ms (deja leer título) + pausa total con VoiceOver + solo fades con Reduce Motion + cancel safety en `.onDisappear`. Reusa componentes reales (`ChatMessageBubble`, `ChatLoadingIndicator`, `ChatMessage` struct mockeable). `ChatDraftPreviewCard` nuevo (read-only) en lugar de `ChatTransactionDraftCard` (que requiere SwiftData `@Query`). Step 2 destaca "Registra gastos al instante" con badge "Nuevo" en `Color.essentialNeed` (amber, universal en los 9 temas — evita clash con `theme.accent==.hotPink` en tema rosa Pro). Step 3 con `InsightTone`/`InsightFocus` selectors persistencia inmediata al tap (consistente con `AIPersonalizationSheet`). Step 4 con back chevron permite volver a step 3. **Encadenamiento sheets vía `onDismiss:` callback nativo iOS** (no `Task.sleep(350ms)` frágil) — `pendingOpenChatAfterOnboarding` flag setea solo en path `onComplete`, callback dispara `showChatSheet=true` post-animación de cierre nativa. **Background `PanelBackgroundView()`** (no `.thBackground`) — coherente con `ChatTopicsSheet` y aprovecha gradient atenuado tipo Welcome Hero (post-commit `32e3b355`) en los 4 temas glass/translucent. **Comportamiento de cierre**: complete (CTA Step 4) abre chat + setea flag; skip (topRight steps 1-3) cierra + setea flag; X (topLeft) cierra SIN setear flag (vuelve a aparecer). 20 escenarios QA documentados (AI-OB-01..AI-OB-20). **Aprendizajes**: `/review-plan` cazó 4 problemas críticos antes de implementar (sheet collision con Task.sleep frágil → onDismiss nativo, badge clash en rosa → essentialNeed amber, telemetría faltante en inventario, animation distrayendo título → delay 800ms) + 3 mejoras opcionales aplicadas (pure-logic helper con tests, a11y labels explícitos, escenario QA AI-OB-19 documentando UX side-effect del Saltar tras cambio de tono). Auditoría temas confirmó 9 themes (4 free + 5 PRO) y guió decisión `PanelBackgroundView()` post-commit `32e3b355`. Ticket: `Backlog/onboarding-yala-ai.md` (status: implemented-pending-qa). Pendiente: Device QA con 20 escenarios AI-OB-* en simulador (5 temas mínimo).

- [2026-05-08] **Sprint Pulido 3 — cierre items diferidos** (6 commits `f547cd85..7267d552`). Cierra 4 bloques pendientes del épico Groups Pulido Final post Sprint Pulido 2. (1) **C0 #16-bis copy notif** (`f547cd85`): title con emoji `👋 {nombre} quiere unirse a {grupo}` + body separado "Aprueba o rechaza desde Yala." (1 key actualizada + 1 nueva × 16 locales). Voseo es-AR "Aprobá o rechazá", impersonal de "In Yala annehmen oder ablehnen", vouvoiement fr "Approuvez ou refusez", です/ます ja, 你 zh-Hans. (2) **C1 #36 cleanup masivo** (`f3d66817`, +32 −1332 LoC): eliminado todo el path `autoCreate=false` del bridge — `SplitGroup.autoCreateTransaction` campo + 7 funciones `GroupPersonalPreferences` (preserva `defaultSettlementAccount` per-currency) + `GroupTransactionBridge.resolveAccount`/`importGroupHistory*`/`updateTransaction`/`updateDraft`/`unbridgeAllForGroup`/`makeBridgedDraft`/`matchSubcategoryFromList`/`case noAccountsAvailable` + `GroupSettingsView.mySettingsSection` completa + 3 archivos sheets (`BridgeActivationSheet`, `BridgeDeactivationSheet`, `BridgeRadioOption`) + tests legacy + 11 keys L10n × 16 locales = 176 strings + catch en `GroupExpenseService` (F0e). **Sin migración SwiftData** — schema plano confía en lightweight migration nativa (campo Bool con default). **Decisión "no legacy users"** del owner habilitó cleanup en lugar de gating. (3) **C2 #29+#30+#31 visuales feed** (`c85a5a28`): nuevo `GroupExpenseAmountResolver` (pure-logic 3 cases) + `GroupExpenseAmountView` componente + prefetch `txBridgeMap` en VM con filter logic correcto (TX1 con subcat=nil PREFERIDA sobre TX2 sistema "Préstamo a grupos") + `mySharesByExpense` + `currentMemberID`. Row del feed ahora muestra: icono+color de subcat per-user (fallback `splitTypeBadge` si no hay TX), copy "Pagaste S/X" / "{name} pagó S/X", monto perspectiva personal con caption "Prestaste"/"Te prestaron"/"No participaste". 5 keys L10n × 12 locales (4 heredan) + 1 eliminada (`paidBy` huérfana). 3 tests pure-logic verdes. (4) **C3 #32+#33 visuales form** (`88242ab3`): chip "División" cambia a teal (2 tokens DS nuevos `splitMethod*` para mejor contraste dark, NO infoBackground azul) + tamaño label. Placeholder "Agregar nota" → `Color(.tertiaryLabel)` (más claro). (5) **C4 #15 wipe vs grupos B1** (`c911a0f3`): alert del wipe ahora aclara "Esto no incluye tus grupos. Para salir, ve a Grupos, salda lo pendiente y abandónalos." (decisión owner: NO crear fila "Salir de todos los grupos", user va manual). 1 key × 16 locales (3 heredan, es-AR voseo "andá/saldá/abandonalos"). (6) **C5 routing inteligente invite link** (`7267d552`): `ReconnectMode` enum (7 cases) + custom CKShare key `isArchived` propagada por `GroupService.setArchived` + `inviteRouteDecision` extendida con `isArchived` + `currentMemberStatus` + `SplitSyncManager.currentMemberStatus(zoneName:)` helper + `GroupReconnectView` UI condicional por mode. **6 escenarios extra**: archived (CTA "Entendido", no acepta) / alreadyMember (nav groupDetail) / pendingDuplicate (nav groups lista NO detalle) / rejectedRetry+leftRetry+removedRetry (acceptShare + ensureCurrentUserMemberExists con reactivate). **Race archived mitigado**: fallback safe — key ausente → `.standardReconnect`; sync posterior trae estado al SplitGroup local. **`.left` vs `.removed` BIFURCADOS** (copys distintos "Saliste" vs "Te removieron"). 17 keys L10n × 13 locales (3 heredan) con voseo es-AR. 7 tests `inviteRouteDecision_*` nuevos en `AppBootstrapperTests` (13/13 verdes). **F0 pre-flight cazó**: SwiftData lightweight nativo OK, `bridgeExpense` M5 path ya cubre `.groupInvite` (importGroupHistoryAsDrafts redundante post A0-Bridge V2.0), `CKRecordNameZoneWideShare` existe en SDK, `paidBy` confirmada huérfana, `noAccountsAvailable` catch en `GroupExpenseService`. **Aprendizajes**: `/review-plan` cazó 11 problemas + 3 graves (SwiftData migration verificada con F0; `importGroupHistoryAsDrafts` impact resuelto leyendo bridgeExpense; `txBridgeMap` filter `?? false` siempre `false` para subcat nil → corregido con `guard let sub`); `onJoin` mantenido como `() -> Void` (no over-engineering con `(ReconnectMode) -> Void`); tokens DS `splitMethod*` (alineado con `splitMethodChip` que los consume, no `splitDivision*`). Diferido a sesión M6 aparte: cambio arquitectónico bridge Caso A con cuenta personal real seleccionable + #34 selector moneda editable + D3 doble fila multi-moneda en GroupCardView (estilo Splitwise). Pendiente: device QA full con 15 escenarios SP3-* (instruir testers borrar app + reinstalar antes por SwiftData schema change).

- [2026-05-08] **Sprint Pulido 2 — 9 bugs + simplify cleanup post QA A3-04**. 8 commits `a8722338..cfc16ff9`. **Items cerrados**: (1) **#16 instrumentación** (`a8722338` chore): 6 prints DEBUG en SplitSyncManager.applyRemoteRecord (status+isPending+isAdmin), GroupNotificationService.processRemoteChanges (isActive+summary+rateLimited+scheduling), NotificationService.sendNotification (authStatus+alert/center/lockScreen settings + add() result). Decisión: separar diagnose de fix — el commit C0 instrumenta, el fix root cause (C1) está DIFERIDO al post device QA con Console.app capturando logs `[#16-debug]`. Hipótesis ranked en `Ideas/apuntes-grupos.md`: rate limit comiendo el primer pending, isAuthorized=false con permisos OK, isActive flag no persistido, isAdmin race de cache. (2) **#23 + #24** (`9e9e05eb` fix): eliminar "Regenerar enlace" completo (state + dialog + UI block + 3 keys × 13 locales = 41 strings — A3 reemplazó la necesidad) + renombrar "Agregar a mis registros" → "Llevar también a mis registros" alineado a BRAND-VOICE.md (12 locales con traducciones reales). **Decisión voseo es-AR**: el copy es infinitivo + impersonal → NO requiere voseo gramatical → NO añadir entrada en es-AR (hereda vía iOS fallback). Coherente con patrón actual (es-AR tiene 306/4167 strings — solo verbos en 2da persona conjugados). (3) **#22 + #20-bis + #17-bis** (`417a70b0` fix): invite banner identidad con `BrandedMetadata` propagada end-to-end (AppBootstrapper → ContentView → GroupInviteOnboardingView, reusa key `welcomeWithGroup` existente — 0 keys nuevas) + botones aprobar/rechazar inline +height (xxs→sm) sin dim row para pending + counts horizontal `X · Y` con dot separator. (4) **#26 + #27** (`ea0f4812` feat): `GroupCardDisplayMode` enum + helper pure-logic `GroupCardDisplayLogic` + 6 tests + chip warning amber "Esperando aprobación" (tap disabled) / chip rojo "Solicitud rechazada" (tap → alert padre "¿Salir del grupo?") + `SplitMember.isVisible` + filter en GroupSettingsView miembros. **Decisión #25 droppeado**: con #26 bloqueando entrada al GroupDetailView para pending/rejected, gear gating es innecesario. Alert pattern: en padre `GroupsContainerView` con callback desde card (single-modal global vs N alerts montados). (5) **#35** (`3f7d6f88` fix): chips "gelatina" + pull-to-refresh espurio en form gasto. Causa raíz: `.refreshable` del `GroupDetailView` padre se propaga al sheet del form en iOS 26 (NewTransactionView con estructura idéntica pero sin padre refreshable NO reproduce). Fix: `.refreshable {}` no-op absorbe gesto. (6) **#28** (`a6b4501e` feat): header del GroupDetailView con `ToolbarItem(.principal)` + `GroupIconBadge` (28pt) + nombre. Cambio `.large` → `.inline`. (7) **/simplify cleanup** (`cfc16ff9` refactor): tupla anónima → `InviteLinkService.BrandedMetadata` struct Equatable+Sendable + nonisolated `.empty`. Componentes shared `StatusChip` (chip pill icon+text+capsule) + `GroupIconBadge` (Circle+icon configurable) en `Yala/App/Views/Shared/`. Helper VM `groupCardRow` (callsites duplicados de GroupCardView en filteredGroups + archivedGroups). `notificationSettings()` consolidado a 1 fetch (era double-fetch en DEBUG). Comentarios narrando ticket eliminados — solo se mantienen los que explican WHY no obvio. **L10n total**: 4 keys nuevas × 12 locales (#26 chips + alert) + 12 locales × 2 keys renombradas (#24) + 41 strings eliminadas (#23) = ~+34 net. **Tests**: 6 pure-logic GroupCardDisplayLogic 100% verdes. **Aprendizajes**: `/review-plan` cazó 7 problemas pre-implementación (callsite metadata invite, voseo es-AR no aplica a infinitivos, alert padre vs card, helper isVisible compuesto, double-fetch consolidación, comentarios narrando ticket, struct dedicado para tupla pública). Reusar `welcomeWithGroup` existente evitó 12 strings nuevas. **Diferidos**: fix root cause #16 (post device QA) + bloque feed/detalle gasto (#29-#34 + #36 — requiere decisión producto sobre 5 escenarios moneda+cuenta) + #15 wipe vs grupos (decisión arquitectónica). Detalles completos en `Ideas/apuntes-grupos.md`.

- [2026-05-08] **Sprint Bug #21 + #14 — Subcategorías vacías + FullModeActivation rediseñado**. 4 commits (C0+C1+C2+C3) cierran A3-05 reportado + bug latente #14 de apuntes-grupos. **Decisión arquitectónica**: el bug NO era filtro mal puesto — era conflicto entre 3 invariantes (A0-Bridge `isSystem` excluido de pickers manuales + modo `.groupInvite` minimal + form que pide subcat). **Approach J elegido** sobre H (ocultar chip) y D (seed automático): empty state con CTA "Crear categorías" → seed bajo demanda. Storage 0 hasta que user opta. Reversible. (1) **C0 cleanup**: eliminada `createGeneralAccountIfNeeded` de `GroupInviteOnboardingView.performSilentSetup` — la cuenta "General" era vestigio del diseño previo a A0-Bridge V2.0; bridge usa cuenta sistema "Grupos [moneda]" y `SettlementFormView` ya gatea `accountSection` por `!isGroupInviteMode`. (2) **C1 fix**: `SubcategorySelectorSheet` empty state usa `YalaEmptyState.actionTitle/action` (API ya existente) — icon `tag.fill`, copy "Aún no tienes categorías" + CTA que ejecuta `seedCategoriesIfNeeded` + `seedSystemGroupCategoriesIfNeeded` + `viewModel.loadData()`. 3 keys × 16 locales (es-ES/pt-PT heredan via `ls()` fallback). Voseo es-AR ("tenés"/"creá"/"vas a poder gestionar"). (3) **C2 rediseño**: `FullModeActivationView` reescrito de 290 LoC custom 2-step (account + notification) a 125 LoC wrapper de `OnboardingView` con `prefilledData: ICloudAccountSummary?` (rama B existente). Skipea `.name` + `.currencyName` + `.categories` (si Approach J ya sembró). Steps activos: purpose → accounts → accountType → balance → categories (condicional) → confirmation. **Decisión D2**: eliminado `requestNotificationPermission()` — permission iOS lazy en eventos explícitos posteriores. NavigationStack envolvente con toolbar Cancel. `cleanupResidualGeneralAccount` paranoid safeguard para QA legacy. `prefilledSummary` cacheado en `@State` (evita recompute en re-renders). Helper pure-logic en `Yala/App/Logic/FullModeActivationLogic.swift` (`buildSummary` + `shouldDeleteResidualGeneralAccount`) — 9 tests sin context (evita flake R8). (4) **C3 polish**: `GroupStatsView` muestra placeholder "Asigna categorías a tus gastos..." con icon `chart.pie` + `solidCard` cuando `categoryBreakdown` solo tiene "Sin categoría" (computed `allUncategorized` en VM). Evita pie chart de 1 segmento confuso. 1 key × 16 locales. **Total L10n**: 4 keys × 16 = 64 strings. **Tests**: 19 verdes (10 SubcategorySelectorViewModel + 9 FullModeActivationLogic, 100% pure-logic). **Aprendizajes**: `/review-plan` cazó 4 problemas pre-implementación (L10n.swift accessors omitidos, keys con sub-dots vs camelCase flat convención, prepareSummary async innecesario, micro-orden por commit). `/simplify` cazó 3 fixes (hardcoded `"defaultCurrencyCode"` → `AppPreferences.Keys.defaultCurrencyCode`, `"onboardingMode"` → `OnboardingMode.userDefaultsKey`, computed `prefilledSummary` → `@State` cacheado). Tests con `makeTestContext()` descartados temprano — verificación end-to-end via Device QA A3-05-bis + C2-01 + C2-02 + C3-01 (QA-SCENARIOS.md). Pendiente: `/device-qa` acumulado.

- [2026-04-28] Traducciones reales keys-por-keys de los 4 locales nuevos (nl `b3f16aba`, pl `d6b3748e`, zh-Hans `d6697ec5`, ja `8891408f`). Cierra épico l10n M14 — reemplaza rule-based v0. Approach validado: ~17 Edits grandes (50-150 keys), `grep -c` por Edit, sesión ~60 min. Decisiones específicas por locale (forma です/ます ja, pronombre `你` zh, voseo es-AR, glosarios financieros, counter words contextuales, placeholder reorder con notación positional para idiomas SOV). Pendiente: device QA visual en simulador para los 4 locales. Siguiente: M16 (ASC metadata localizada).
- [2026-04-28] 3 fixes runtime logs post-QA chat-registrar-transacciones (`773fe651` + `35bd11d1` + `aade3042`). (1) Sheet collision chat→NTV — gate del consumer `.panel` mientras `showChatSheet=true` vía markUnready/markReady del AppRouter. (2) ProfileImageStorage — guard `fileExists` antes de `Data(contentsOf:)` (caso normal vs error real). (3) UserSegmentService race — `currentSegment` se leía como `.dormant` durante cold start (recalculate antes del primer importEvent). Fix con NotificationCenter (patrón establecido) + 2 flags separadas (`hasCompletedFirstImport` en iCloudSyncService + `hasRecalculatedAfterFirstImport` en UserSegmentService) + helper `markRecalculatedAfterFirstImport()`. AppBootstrapper:555 gate-ea con la flag — caía a "require interaction" si segmento no confiable. Tests: 2 nuevos (`@Suite(.serialized)` en UserSegmentService). Pendiente: device QA con cold launch + cuenta poblada >100 tx (verificar log `(pre-import)` en primer scan).
- [2026-05-04] Subset 2 del épico Groups Pulido Final (`8945a30b`) — A10 + A9 + A13 + A12. (1) **A10**: `GroupTransactionBridge.resolveAccount` cambia de `-> Account?` a `throws -> Account` con `noAccountsAvailable`. Propagación selectiva: creación bubble-up al alert; batch import + updates `try?` (resilient). Pattern matching catch directo en GroupExpenseService. Logger.error reemplaza print debug-only. 1 key l10n × 13 locales. (2) **A9**: eliminadas 43 líneas de heurísticas (nameMatch único + admin único / oldest joinedAt) en `ensureCurrentUserMemberExists`. Identidad solo desde `cloudKitUserRecordID`. Path defensivo legacy backfill intacto. Riesgo duplicación legacy mitigado por `deterministicUUID(zoneID, recordName)`. (3) **A13**: nuevo `GroupService.updateCurrentUserDisplayName(_:)` con cache de grupos por zoneID + filtro `displayName != trimmed` (idempotente). Llamada en `GroupInviteOnboardingView.performSilentSetup` paso 8.5 + boot-time reconciliation `AppBootstrapper.reconcileCurrentUserDisplayNameIfNeeded` (paso 16.6) cubre kill-app entre acceptShare y onboarding-complete. Self-healing sin flag. (4) **A12**: función pura `AppBootstrapper.inviteRouteDecision(hasCompletedOnboarding:onboardingMode:) -> InviteRouteDecision` reutilizada por `acceptShareFromURL` y `YalaAppDelegate` — simetría perfecta entre branded link y CKShare nativo. Verificado: `ContentView.onJoin` ya invoca `acceptShare(metadata:)`. Tests: +8 (resolveAccount + inviteRouteDecision con 4 estados). Cleanup `/simplify`: hardcoded `"userName"`/`"hasCompletedOnboarding"` → `AppPreferences.Keys.*`. Pendiente: device QA full al final del épico (acumulado, no por subset). Siguiente: Subset 3 (A6 race bridgeExpense + A7 dedup balance + A8 leave/remove con deuda).
- [2026-05-05] **A0-Bridge** del épico Groups Pulido Final — rediseño completo del bridge `SplitExpense` ↔ `TransactionItem` (modelo M5 simplificado). 16 commits F1-F16, 14 fases técnicas + L10n + QA docs. **Decisiones clave**: (1) TX expenses van TODAS a cuenta virtual `Grupos [moneda]` (auto-creada por `ensureSystemAccount`) — desacopla bridge de conciliación bancaria. (2) Settlements sí tocan cuenta real (Caso C/D) pero solo `.full`/`.completed`; `.groupInvite` solo virtual con upsell. (3) **5 subcategorías sistema** en 2 categorías (`Grupos` expense + `Cobros de grupos` income) con naming contable claro: Préstamo a grupos, Cobro de préstamo, Pago de liquidación, Liquidación enviada, Liquidación recibida. (4) **TX-puntero** para drafts groupExpense: TX1 se crea con subcat=nil + draft con `targetTransactionID` → finalize UPDATEa subcat (saldo virtual cuadra desde t=0). (5) **Idempotency vía delete+recreate** en bridge — robusto a edits de payer/monto y sync race. (6) **Drafts groupExpense/groupSettlement no eliminables** — solo desde el grupo origen. (7) **3 toggles AppPreferences** synced: includeGroupTransactionsInFeed, includeGroupsInPanelTotal, includeGroupTransactionsInStats. (8) **Auto-archive** cuenta virtual cuando vacía + recreación lazy on demand. **Diferidos a iteración posterior**: BridgeActivationSheet/DeactivationSheet UI (lógica core completa via `importGroupHistory` async + `unbridgeAllForGroup`); GroupExpenseDraftFinalizationSheet/GroupSettlementDraftFinalizationSheet visualmente distintivos (drafts funcionan en Inbox normal con icons distintivos `person.2.fill`/`arrow.left.arrow.right.circle.fill`); GroupDraftsPendingBanner del Panel; bloqueo edición `EditTransactionView` de TX bridgeadas (protección operativa via pickers que excluyen sistema + bridge idempotency). **Tests A0-Bridge**: 61 tests pure-logic (F1=24, F2=9, F3=8, F4=12, F15=8). Tests integration con `makeTestContext()` en blacklist R8 — validación funcional via Device QA F16 (21 escenarios documentados en `$VAULT/planning/QA-SCENARIOS.md`). **L10n**: 37 keys × 13 locales con traducciones reales (voseo es-AR, です/ます ja, 你 zh-Hans, vouvoiement fr, Sie de). **Sin migración**: grupos no estaban en producción; deploy aplica desde cero (instruir testers borrar app + reinstalar). Pendiente: completar UI sheets diferidas + device QA full.
- [2026-05-05] **A0-Bridge V2.0** del épico Groups Pulido Final — completa los 4 items P0+P1 pendientes que quedaron diferidos del A0-Bridge inicial (commits `414dc75b` F1, `f2e08e6d` F2, `eb59c9b7` F3, `179e621b` F4, `ab786490` F5). 5 commits F1-F5 + tests/verificación F6. **Items entregados**: (1) **P0-1 Settlement form Caso C proactivo** — `SettlementFormView` con AccountSelector filtrado por currency, preselect desde `defaultSettlementAccount`, `createSettlement`+`confirmSettlement` con `accountForCurrentUser` para disparar bridge inmediato (2 TX al instante). `.groupInvite` oculta selector. `AccountSelectorSheet` extendido con parámetro opcional `currencyFilter` (retrocompat). (2) **P0-2 Sheets activación/desactivación tardía** — `BridgeActivationSheet` con opciones "Empezar desde ahora" / "Importar todo el historial" (corre `importGroupHistory` async con `ProcessingProgressView` determinate); `BridgeDeactivationSheet` con "Eliminarlas" / "Mantenerlas como histórico". Toggle `personalAutoCreate` intercepta cambios cuando hay data pre-existente. **Anti-loop guard D8** via flag `isInternalToggleRevert` (revert visual del binding sin re-disparar `onChange`). **Alert error** propaga `error.localizedDescription` al user (en lugar de solo log DEBUG). (3) **P1-3 Bloqueo edición TX bridgeadas** — `NewTransactionView` detecta `splitExpenseID/splitSettlementID` y entra en read-only: oculta `transactionTypeSelector`, `ContextualGuideBanner` y toolbar trailing favorites; `disabled + opacity 0.5` en form; banner CTA contextual (`assignFromInbox` / `editFromGroup` / `editSettlementInGroup`) navega a Inbox o `groupDetail(zoneID)` (fallback `groups`). (4) **P1-4 Sheets finalización drafts especializados** — `GroupExpenseDraftFinalizationSheet` (read-only header + selector subcat, path TX-puntero existente), `GroupSettlementDraftFinalizationSheet` (read-only header + AccountSelector con preselect + persiste `defaultSettlementAccount`). `InboxView` switch por `draft.sourceType` con fallback a `InboxDraftEditSheet` para sources personales. **Decisión D7 crítica**: `approveDraft` rama `groupSettlement` crea TX manual INDEPENDIENTE — **NO setea** `splitSettlementID`/`splitGroupZoneID`. Razón: el bridge hace delete+recreate sobre TX con esos IDs al editar el settlement; mantener los IDs haría que una edición posterior borre la TX manual del user. La TX virtual (lado bridge) sigue ligada al settlement y se regenera. **Currency** se resuelve desde origen (`SplitExpense`/`SplitSettlement` por ID) en `.onAppear` — `cachedCurrencyCode` solo se popula en `approveDraft`. **L10n**: 11 keys × 16 locales con traducciones reales (voseo es-AR "Asigná"/"finalizá", です/ます ja, 你/账户 zh-Hans, vouvoiement fr, Sie de, "Posteingang" de para Inbox, "Caixa"/"Skrzynkę" pt/pl, "Finalise" en-GB). **Aprendizajes**: `/review-plan` cazó 7 problemas reales antes de implementar (loop infinito en onChange, `approveDraft` borrando TX manual, currency no cacheado, alert error faltante, read-only incompleto en toolbar/typeSelector). `@Query` reemplazado por fetch directo en `.onAppear` para evitar re-fetch. Tests A0-Bridge integration siguen verdes; flake R8 preexistente confirmado en `resolveAccount_throwsNoAccountsWhenEmpty` (pasa aislado, falla en suite paralela — race con `makeTestContext()` documentado). Pendiente: `/device-qa` full con 21 escenarios F16 acumulados (borrar app + reinstalar antes). Bloquea: A3 (`pendingApproval`) + A4 (Welcome Chooser) — desbloqueados ahora.
- [2026-05-05] **A3 — pendingApproval flow (opción B)** del épico Groups Pulido Final. 6 fases F1-F6 (modelo + servicios + notificación + UI admin + UI invitado + L10n + tests/docs). **Items entregados**: (1) **F1 modelo**: `SplitMemberStatus` extiende con `.pendingApproval` y `.rejected` (rawValue String CloudKit-compat); helpers `isPendingApproval`, `isRejected`, `canWrite`. `GroupService` añade `pendingMembers(in:)`, `approveMember(_:in:)`, `rejectMember(_:in:)` (todos `requireCurrentUserAdmin`). `ensureCurrentUserMemberExists` modificado: invitados nuevos entran como `.pendingApproval` (owner siempre `.active`); existing `.rejected`/`.removed` re-pone en `.pendingApproval`; **case explícito para `.pendingApproval` no-op** evita bug donde re-tap link convertía pending a active. `validateCurrentUserCanWrite` en `GroupExpenseService` lanza nuevo `pendingApproval`. `bridgeExpense`/`bridgeSettlement` endurecen guard a `currentMember.isActive` — pending/rejected no triguean bridge. (2) **F2 notificación admin**: `RemoteChangeSet.newPendingMembers` + `buildPendingMemberNotification` con prioridad sobre newMember. `SplitSyncManager` bifurca al clasificar `splitMember` records leyendo `record["status"]` directo del CKRecord, con **pre-cache `isCurrentUserAdminOfGroup` por zoneID** dentro del batch — solo admins reciben notif. (3) **F3 UI admin**: `pendingApprovalSection` en `GroupSettingsView` (gated `isCurrentUserAdmin && !pendingApprovalMembers.isEmpty`) con avatar + nombre + 2 botones Aprobar/Rechazar y confirmation dialogs. `GroupDetailViewModel.pendingApprovalMembers` computed. (4) **F4 UI invitado**: `PendingApprovalBanner` (pending + rejected states) en `GroupDetailView` con `DS.Semantic.warningBackground` / `errorBackground`. **FAB ya bloqueado** por `canCurrentUserParticipate` existing — sin tocar. **Refresh wiring** ya cubierto por `.onChange(of: sessionState.dataVersion)` existing. Step 3 onboarding "Solicitud enviada" en `GroupInviteOnboardingView` con detección async vía `mostRecentGroup()` + `ensureCurrentUserMemberExists`. (5) **F5 L10n**: 14 keys × 16 locales con traducciones reales (voseo es-AR, です/ます ja, 你/管理员 zh-Hans, vouvoiement fr, Sie de, "ligação" pt-PT, "Anfrage" de) + 1 plural `pendingRequestsCount` × 12 stringsdict (4 reglas pl, sin distinción singular ja/zh). **stringsdict pattern**: keys plain (sin `%d` literal), `%#@count@` resuelve plural. (6) **F6 tests**: +8 (4 SplitModels: rawValue stable, pending/rejected not active, garbage rawValue fallback; 2 GroupService errorDescription; 2 CKRecordTranslator round-trip pending/rejected). **Decisiones clave**: `rejected` case nuevo (no reusa `removed`) — mejor UX copy + telemetría. Re-pone en pending tras rechazo/removed (sin límite reintentos V2.0). Owner siempre `.active`. Last-write-wins en aprobación concurrente. Sin unbridge en reject (pending nunca creó gastos). Deeplink reusa `groups/{UUID}` (un tap más a Settings). Trade-off bridge: removed/rejected dejan de bridgear updates remotos (caso edge, aceptado). **F0 pre-flight cazó 5 hallazgos**: FAB ya cubierto por `canCurrentUserParticipate`, refresh wiring ya existe en GroupDetailView:153, `GroupInviteOnboardingView` no recibe `group` (usa `mostRecentGroup()`), stringsdict keys plain confirmado, bug `else { active }` en branch reactivateInactive. **Aprendizajes**: `/review-plan` cazó el bug del branch reactivateInactive antes de implementar (caso explícito `.pendingApproval` no-op crítico). `SectionBox` no acepta `subtitle` (count va inline como label). `DS.Typography.callout` y `title3` no existen — usar `label` y `title2`. `L10n.Common.continue` no existe — usar `Action.continueAction`. Pendiente: device QA acumulado (21 escenarios F16 A0-Bridge + 9 escenarios A3-01..A3-09 nuevos). Siguiente: A4 (Landing + AASA + Welcome Chooser).
- [2026-05-06] **A4 — Landing + AASA + Welcome Chooser pre-onboarding** del épico Groups Pulido Final. ÚLTIMO P0 de Fase A — cierra Fase A. 7 fases F0-F7. **Items entregados**: (1) **F1 Web**: `Web/src/pages/invite.astro` multilenguaje SSR via `detectLanguage(Accept-Language)` reutilizando `Web/src/i18n/translations.ts` (6 idiomas: es/en/de/fr/it/pt); 9 keys × 6 idiomas. `Vary: Accept-Language` header. `<html lang>` dinámico + `og:locale`. `PUBLIC_DEEP_LINK_SCHEME` env var (yaladev preview / yala production). `Web/.env.example` documentado. AASA sin cambios. (2) **F2 iOS infra**: `iCloudSyncService.forceFetchAndWait(timeout: 15)` async observa `Notification.Name.iCloudFirstImportCompleted` (existing) con `withCheckedContinuation` + `NSLock` single-resume; `ICloudAccountSummary: Equatable` struct + `@MainActor extension ModelContext.iCloudAccountSummary(appPreferences:)` excluye infra A0-Bridge (cuentas sistema, TX bridgeadas, subcat sistema); `AppPreferences.hasShownWelcomeChooser` per-device. Tests: 3 `forceFetchAndWait_*` + 11 `ICloudAccountSummary` (flake R8 conocido en suite paralela, pasan individuales). (3) **F3 Welcome Chooser + Rama A**: `WelcomeChooserView.swift` (~150 líneas) full-screen 3 cards (sparkles/icloud.and.arrow.down/person.2.badge.plus). Inserción en `ContentView.checkInitialSyncState` reemplazando el `showOnboarding=true` (NO antes — preserva el toast mágico para returning users con data en iCloud). Race CKShare: gate `.fullScreenCover(isPresented: Binding { showWelcomeChooser && !showGroupInviteOnboarding })` auto-cierra el chooser. Invariante: `hasShownWelcomeChooser` se setea SOLO tras tap consciente. Telemetría event `welcomeChooserBranchSelected`. (4) **F4 Rama B**: `WelcomeRestoreView.swift` (~280 líneas) state machine 5 estados + reintento + confirmation dialog "empezar de cero". Cancelación task en `.onDisappear` (`searchTask?.cancel()`) + `guard !Task.isCancelled` antes de cada mutación de state — previene resume sobre vista no presentada. `OnboardingView.init(prefilledData: ICloudAccountSummary?)` API híbrida (`Step` permanece privado): `prefilledSkippedSteps` UNION `usageModeSkippedSteps`; `.task` popula `userName`, `selectedCurrency` desde summary ANTES del fallback UserDefaults. Reset zombi: `prefilledOnboardingData = nil` en `onChange(of: hasCompletedOnboarding)` + `hasShownWelcomeChooser = false` en `performLocalWipeForRemoteSync` (path `skipOnboarding=false`) — chooser se vuelve a mostrar tras wipe. (5) **F5 Rama C**: `InviteRecoveryView.swift` (~150 líneas) con `UIPasteboard.hasURLs` auto-detect (evita toast clipboard hasta confirmar URL) + validación inline live + reusa `InviteLinkService.isInviteLink` + `AppBootstrapper.handleInviteLink` existing. `InviteLinkService.alternateHosts`: array con 4 hosts canónicos + alternos (yala-app.pe, yala-pe.com, yala-app.com.pe + variantes www) — paste manual NO sigue redirects HTTP. `extractShareURL` y `isInviteLink` aceptan los alternos. 11 tests `InviteLinkServiceTests` (canónicos, alternos, custom schemes yala/yaladev, foreign rejected, decode con alternate). (6) **F6 L10n**: 35 keys × 16 locales con traducciones reales por locale (voseo es-AR "Pegá"/"empezá"/"contános", です/ます ja, vouvoiement fr "Vous", Sie de, 你 zh-Hans cercano, "ligação" pt-PT, "tu" forms pt-PT). Plurals via `String(format: ls(...))` con `%d` literal (sin `.stringsdict` — adjetivo invariante en todos los idiomas). (7) **F7**: cierre épico (CLAUDE.md + STATE.md + QA-SCENARIOS.md A4-01..A4-12 + backlog [x]). **Decisiones clave**: (a) Welcome Chooser inserta al final de `checkInitialSyncState`, NO antes — evita degradar el toast mágico de returning users. (b) Rama B usa `forceFetchAndWait(15s)` adicional sobre el wait existente (8s) — control explícito al user "estoy seguro que tengo cuenta, espera más". (c) `OnboardingView.init(prefilledData:)` híbrida en lugar de `Set<Step>` externo — encapsula skip-logic, cero acoplamiento. (d) Web SSR con `Accept-Language` + `Vary` header en lugar de client-side i18n — sin FOUC, SEO multi-idioma correcto, single source of truth con `translations.ts`. (e) `hasShownWelcomeChooser` per-device (no synced) + reset en wipe path. **F0 cazó hallazgos**: `skippedSteps` solo 2 referencias (no 6 como estimado), `TelemetryService` enum simple sin schema migration, vercel.json hosts confirmados (4 alternos exactos). **Aprendizajes**: `/review-plan` cazó 6 problemas reales antes de implementar (cancelación task `.onDisappear`, reset zombi prefilled tras wipe, dominios alternos paste manual, verificación post-deploy Vary header, telemetría event check, suite race con singleton). Build verde y tests pasan en cada fase. Pendiente: device QA acumulado (21 escenarios F16 A0-Bridge + 9 A3-01..A3-09 + 12 A4-01..A4-12 = 42 escenarios; borrar app + reinstalar antes). Siguiente: Fase B (P1+P2 backlog).
- [2026-05-05] Subset 3 del épico Groups Pulido Final (`dbce20a6`) — A6 + A7 + A8. Cierra Fase A salvo A3/A4 (no son código Swift). (1) **A6**: NSLock descartado tras `/review-plan` — habría introducido regresión (descartar second-call legítimo de update). `@MainActor` + sync function + idempotency check via fetch existing TX/Draft ya garantizan atomicidad por construcción. Comment documenta el invariante en `bridgeExpense` línea 80. Sin test E2E (runner crashea con globals del bridge — patrón documentado Subset 1). (2) **A7**: dedup `expenses` por `id` con `Dictionary(grouping:by:\.id).values.compactMap(\.first)` en `calculateBalances` y `rawDebts` (cubre `calculateDebts` + `globalSummary` por propagación). +4 tests cubren las 3 funciones + regresión de IDs distintos. (3) **A8**: eliminada restricción "balance == 0"; `removeMember` y `leaveGroup` ya no lanzan `outstandingBalance`. UI: warning copy en confirmation dialog (mensaje dinámico), helper `hasNonZeroBalance(for:)` extraído por `/simplify` (reuse). Limpieza simétrica: `case GroupServiceError.outstandingBalance` + branch + helpers privados `memberHasOutstandingBalance`/`memberBalances` + key `leaveGroupDisabledHint` × 13 locales. 2 keys nuevas con traducciones reales por locale (voseo es-AR, です/ます ja, 你 zh-Hans, vouvoiement fr). Aprendizajes: `/review-plan` cazó la regresión del NSLock antes de implementar; greps previos zanjan ambigüedades de "qué eliminar"; `/simplify` post-implementación detecta duplicación de computeds. Pendiente: device QA full al final del épico. Siguiente: A3 (decisión `.readOnly` vs iOS 26 access requests) + A4 (landing page web + AASA).
- [2026-05-08] **A4 v3.2 — Sprint 2.1 polish refinado CERRADO en 8 commits** post-QA del Sprint 2 (commit `0e638931`). Cerrado en cadena: `a2cefd42` (base Sprint 2.1) + `44858f59` (logo centrado + back cards eliminadas) + `e718ede9` (logo 128pt) + `211579cc` (Chooser mirror VStack) + `b261b8c1` (Hero minLength 80) + `63931f91` (`WelcomeFlowLayout.logoTopSpacing` adaptativo proporcional 10%/min 60pt vía GeometryReader) + `3e8b22f0` (`/simplify`: extraído `WelcomeFlowScreen<Content>` wrapper SSOT — GeometryReader + background gradient + frame del content) + `f6444eb9` (Chooser invite card icon `essentialNeed` amber, evita cyan duplicado vs `neonCyan`). **Sub-iteraciones post-QA visual**: (a) Logo Hero centrado verticalmente entre top safe area y cards (Spacer flotante); back cards eliminadas (case ±2) — solo front + 1 side card a cada lado, `offsetSideX` 140→200pt para que NO se crucen con la front (resuelve cruce visual de translucencia). (b) Logo duplicado a 128pt en Hero + Chooser; Chooser refactorizado de `ScrollView { VStack }` a `VStack` con `Spacers` flexibles (misma estructura que Hero) → logo a la misma altura visual exacta en ambas pantallas, transición Hero→Chooser sin "salto" de logo. **TODO pendiente** (anotado en `apuntes-grupos.md`): rebranding del OnboardingView (steps 1-8 post-Chooser) para igualar background indigo→negro del flow Welcome y adaptar elementos al theme liquid glass — sigue con `.thBackground` claro, requiere sesión completa de polish (~4-6h). 8 ítems originales del Sprint 2.1: (1) **Cards Hero reducidas ~mitad**: 200×130/160pt (de 280×200/240). Iconos 26pt con gradient brand sobre frame 56×56 con tinte 0.18. (2) **Cards laterales sobresaliendo + page indicator dots**: ZStack con todas las 8 cards renderizadas con posiciones derivadas signed mod (`(idx - currentCardIndex + n) % n` + signed wrap a `[-2, +2]`). Posiciones: front (1.0, 0, 0), ±1 (0.82, ±140, ±5°, opacity 0.55), ±2 (0.7, ±185, ±8°, opacity 0.22). 8 capsules debajo del stack (3pt × 6pt no-activo, 3pt × 18pt activo blanco), tap navega + reset timer rotationTask. (3) **Cards translúcidas estilo Liquid Glass del Panel** (NO Material genérico ni accentColor sólido): `.background(.thCard)` + stroke `Color.white.opacity(0.1)` + `DS.Panel.widgetRadius` (20pt). Diferenciación brand SOLO en el icono (gradient + tinte 0.18 del accentColor). (4) **Carousel transition rediseñado**: animation `.smooth(duration: 0.45, extraBounce: 0.15)` (más elegante que spring 0.6/0.75 previo); SIN `.id(currentCardIndex)` + transition asymmetric (rompía sincro back/middle); SIN `.clipped()` para no cortar shadows en bordes. Las 8 cards se mueven juntas porque sus propiedades visuales (offset/scale/rotation/opacity) cambian con `currentCardIndex` dentro del `withAnimation`. (5) **Copy subtitle nuevo**: "Tú captura. Yala se encarga." × 16 locales con traducciones reales (voseo es-AR "Vos capturá", you en/en-GB, du Sie de, vouvoiement fr, te tu it, あなたは撮るだけ ja, 你拍下来 zh, jij vastlegt nl, ty rejestrujesz pl, você captura pt/pt-BR, tu capturas pt-PT). (6) **Logo Hero 64pt** (de 36pt) para alinear con Chooser. (7) **Chooser cards con `.solidCard(padding: 0, radius: DS.Radius.xl)`** reemplaza `.ultraThinMaterial` que se veía grisáceo sobre dark — usa `.thCard` del theme `liquidGlass` + stroke `theme.cardBorder` (mismo look que widgets del Panel). (8) **Hero+Chooser unificados en 1 fullScreenCover** (`WelcomeFlowContainer.swift` nuevo): enum `WelcomeFlowStep { .hero, .chooser }`, state interno + transitions `.opacity` con animation `.smooth(duration: 0.5, extraBounce: 0.1)`. Alert "Detectamos tu cuenta" vive DENTRO del container (no en ContentView). Resuelve el "frame azul vacío" entre dismiss del Hero y present del Chooser que aparecía con dos covers separados — el background gradient persiste continuo. ContentView state: eliminados `showWelcomeHero`, `showWelcomeChooser`, `showDetectedDataAlert`, `detectedICloudSummary`; agregados `showWelcomeFlow: Bool` + `welcomeFlowInitialStep: WelcomeFlowStep`. Cover separado del Chooser en ContentView eliminado. `presentNextOnboardingScreen()` y InviteRecovery `.onBack` cableados al nuevo modelo (`welcomeFlowInitialStep = .chooser` cuando InviteRecovery hace back). **Decisiones clave**: (a) Approach ZStack con 8 cards renderizadas a la vez (en lugar de matchedGeometryEffect o `.id`+transition asymmetric) — más simple, sin race entre back/middle/front, performance OK porque cards con opacity 0 se omiten en draw pass. (b) `.solidCard()` modifier (existente, usado por widgets del Panel) en lugar de `.glassEffect()` iOS 26 nativo — mantiene look consistente con el resto de la app, sin riesgo de Material genérico. (c) Cross-fade `.opacity` simple entre Hero y Chooser (sin matchedGeometryEffect ni `.move`) — el background compartido hace que la transición se sienta continua sin necesidad de tricks geométricos. (d) Page indicator capsules en lugar de Circles — más moderno, `frame(width: 18 vs 6)` da un "dot largo" visual claro del activo. (e) `WelcomeFlowContainer.initialStep` parameter inmutable (no binding) — el ContentView setea `welcomeFlowInitialStep` ANTES de poner `showWelcomeFlow = true`, y el container respeta su valor inicial. **Aprendizajes**: matchedGeometryEffect descartado tras review (riesgo glitches con array que rota); `.id(currentCardIndex)` + transition asymmetric descartado (rompe sincro de back/middle al cambiar índices). Build verde, 10 tests pure-logic OnboardingPrefillResolver siguen pasando. Pendiente: device QA con escenarios A4-26 (cover unificado sin frame azul) + A4-27 (page indicator + cards translúcidas + tap navigation) + re-test A4-23 con dims nuevas.
- [2026-05-07] **A4 v3.2 — Sprint 2 polish visual/UX** post-QA del épico Groups Pulido Final. 4 hallazgos consolidados (#5, #8, #11, #9c) en una sesión unificada. **Items entregados**: (1) **#9c fix bug userName override**: nuevo `Yala/App/Logic/OnboardingPrefillResolver.swift` con 2 funciones puras (`resolveUserName`, `resolveCurrencyCode`) — si `prefilledData != nil` (rama B) devuelve SOLO summary aunque sea nil; si `prefilledData == nil` (rama A) lee defaults con guards (`!isEmpty`, `!= "Usuario"`). 10 tests pure-logic. `OnboardingView.task` refactorizado para invocar el resolver — ya no pisa prefilled con UserDefaults residual. (2) **#8 copy unificado "Empezar desde cero" × 16 locales**: 4 españoles cambian todas las "configuración"/"de cero" → "desde cero" (`startFresh`, `startFreshConfirm.title`, `startFreshConfirm.confirm`, `notFoundBody`, `detectedData.startFresh`). 12 locales no-español alinean `welcome.restore.startFresh` con su `welcome.detectedData.startFresh` correspondiente (ej. en "Start setup" → "Start fresh", de "Einrichtung starten" → "Neu beginnen", fr "Commencer la configuration" → "Repartir de zéro"). Out of scope: `icloud.remoteWipe.confirm` (contexto wipe), `account.deleteBalanceError` (uso matemático). (3) **#5 Welcome Hero rediseño completo**: cards 200/240pt (mitad de altura previa), fondo sólido con difuminado claro/oscuro suave (no translúcido), paleta calmada rotando indigo/pink/teal/amber/purple sin neonCyan saturado, stack lateral pronunciado (offsets 32/-32, rotations 7°/-9°, scales 0.88/0.78), background `DS.Gradients.heroIndigoBlack` (token nuevo) gradient indigo→negro vertical, **8 cards** con Grupos vector viral + Asistente IA + Presupuestos como nuevos diferenciadores (eliminadas voice/multiAccount/currencies, fusionadas en assistant/multiAndCurrencies, reformuladas capture/import/icloud/more). **Carousel horizontal Estrategia 1**: front `.id(currentCardIndex)` + `.transition(.asymmetric(insertion: .move(edge: .trailing).combined(with: .opacity), removal: .move(edge: .leading).combined(with: .opacity)))` + spring 0.55/0.78. Back/middle interpolan implícitamente con `withAnimation(.spring)`. Title sobre fondo dark: `.white` para "Tus finanzas personales," + gradient `.hotPink → .white` para titleAccent (contraste >4.5:1 garantizado, evita gradient indigo→pink que daría contraste insuficiente sobre indigo). Logo Yala con `.colorMultiply(.white)`. (4) **#5 background extendido al Chooser** (decisión user "Hero + Chooser, NO Onboarding"): mismo `heroIndigoBlack`, cards re-estilizadas con `.ultraThinMaterial` (glass sobre dark), stroke `Color.white.opacity(0.12)`, text `.white` / `.white.opacity(0.7/0.4)`, icon backgrounds `iconTint.opacity(0.25)`, iconTints brand (.hotPink/.neonCyan/.priorityNeed). (5) **#5 chevron back desde Chooser → Hero** (F6a): `.overlay(alignment: .topLeading)` con Button 44pt touch target — NO NavigationStack (evita navigation bar overhead). Callback `onBack` cableado en `WelcomeFlowModifier` para `showWelcomeChooser=false; showWelcomeHero=true` SIN setear `hasShownWelcomeChooser` (sigue false hasta tap consciente). (6) **#5 chevron back desde Onboarding step 1 → Hero** (F6b): nuevo callback opcional `onCancelFromStep1` en `OnboardingView.init` (preserva API existente). Overlay topLeading gateado por `currentStep == .name && onCancelFromStep1 != nil`. ContentView: `showOnboarding=false; hasShownWelcomeChooser=false; prefilledOnboardingData=nil; presentNextOnboardingScreen()` — vuelve al Hero. State del onboarding se descarta silenciosamente (fresh install). (7) **#11 RestoreView `.found` rediseño**: header `sparkles` con `foregroundStyle(LinearGradient(colors: DS.Gradients.heroIntense, ...))` reemplaza `icloud.and.arrow.down.fill` plano azul. Counts pasan de un único box con filas a **layout adaptativo según `visibleCountItems.count`**: 1 → card centrado max-width 280pt; 2-3 → HStack columnas iguales; 4 → `LazyVGrid` 2×2. Cada card: VStack con icon brand (theme.accent) + número grande (`DS.Typography.amount`) + label caption, fondo `.thCard` + `dsCardShadow()` + `DS.Radius.lg`. Refactor reemplaza `summaryRow` por struct `CountItem` + `countCard` + `countCards(for:)`. **Decisiones clave**: (a) Hero+Chooser fondo unificado con fade default entre fullScreenCovers — mismo gradient hace transición imperceptible. (b) Carousel **Estrategia 1** elegida: front `.id` + transition asymmetric, back/middle interpolan implícitamente. Estrategia 2 (TabView paginated) descartada (page indicator overhead). (c) Cards fusionadas (voz+IA, multi+divisas) reducen 7→8 cards manteniendo conciso pero más completo. (d) Copy chevron back reusa `L10n.Welcome.Invite.back` ("Volver" × 16 locales). (e) Voseo es.lproj limpio (audit confirma tuteo correcto). **F0 cazó**: brand colors viven en `Yala/App/Views/Shared/UIHelpers.swift:228-248`; SF Symbol `target` ya usado en onboarding purposeStep:303; 6 keys del flow Welcome con "de cero"/"configuración" descubiertas vs 5 del plan inicial (notFoundBody adicional). **Aprendizajes**: `/review-plan` cazó 4 problemas reales antes (carousel incompleto, NavigationStack overhead, copy `.title` faltante, keys L10n inconsistencia) + 5 riesgos (brand colors, contraste a11y, SF Symbol, re-fetch iCloud, logo asset). Fixes preventivos: gradient titleAccent `.hotPink → .white` (mitiga contraste), `.ultraThinMaterial` correcto sobre dark. Build verde end-to-end (`/verify-ios`). Pendiente: device QA con simulador (A4-22..A4-25 nuevos + A4-01/A4-02/A4-06 re-test post-polish), reanudar 39 escenarios A0-Bridge/A3/A4 pendientes. **Out of scope diferido**: TODO #10 auditoría sistémica `Dictionary(uniqueKeysWithValues:)` + telemetría.
- [2026-05-07] **A4 v3.2 — Sprint 1 Fix P0 #2 + #6** post-QA del épico Groups Pulido Final (commits `435c67d8` + `9a818f28`). (1) **#2** (P0, diferido desde 2026-05-06): banner `dormantGroupsAsMain` "Tienes gastos compartidos pendientes" aparecía en users post-onboarding sin grupos. Causa: `NudgeTriggerEvaluator.shouldShow(.dormantGroupsAsMain, ...)` retornaba `true` incondicionalmente — las otras 3 nudges dormant ya gating por contexto, esta era la única excepción. Fix: gateado por `groupMemberCount > 0`. Test `dormantGroupsAsMain_alwaysTrue` reemplazado por 2 cases (with/without groups). (2) **#6** (P0 regresivo de A4 v3.1): tras reinstall fresh con misma Apple ID, Hero aparecía ~1s y se reemplazaba automáticamente por PanelView "Hola, [name residual]" + alert "Datos no disponibles". Causa raíz real (NO la hipótesis original que asumía KV-Store sincronizando `hasCompletedOnboarding` — esa flag ya era `synced: false` en `AppPreferences:353`): `performLocalWipeForRemoteSync(skipOnboarding: true)` en `ContentView:553` autopromovía la flag al recibir `.remoteWipeDetected` con `onboardingAlreadyDone=true` desde KV-Store del Apple ID. Los timestamps escritos por `signalOnboardingCompleted`/`signalWipeInitiated` en instalaciones previas sobreviven al uninstall — el KV-Store es per-Apple-ID, no per-app. Fix A: nuevo `RemoteWipeSignalDecider` (pure-logic, `Yala/App/Logic/RemoteWipeSignalDecider.swift`) decide si procesar signal según `hasCompletedOnboarding` + `isWipingData` + presencia timestamp. Devices fresh-install (`hasCompletedOnboarding=false`) ignoran el signal y marcan AMBOS timestamps (wipe + onboarding) localmente para idempotencia y silenciar también el "Caso B" `.remoteOnboardingCompleted` de `checkForRemoteWipeSignal`. `handleRemoteOnboardingCompleted` recibe guard simétrico `hasCompletedOnboarding` + elimina asignación espuria de la flag (no debe completar onboarding por signal cuando user mid-onboarding sin estado local). 6 tests pure-logic del decider cubren matriz fresh/returning/wiping. **Fix B explorado (gate `hasExistingData` en línea 87 del ZStack) DESCARTADO**: `/review-plan` cazó la regresión grave — desmontaba MainTabView ante cualquier borrado legítimo de cuentas/categorías. Defensa-en-profundidad especulativa puede romper más de lo que arregla. Sin regresión cross-device wipe (devices con estado local siguen procesando signal). **Aprendizajes**: hipótesis del user puede ser parcialmente errónea — investigación profunda en código antes de aceptar diagnóstico al pie de la letra. `/review-plan` cazó el problema del Fix B antes de implementar. Pure-logic decider habilita tests sin SwiftData ni UI (evita flake R8 conocido). Pendiente QA con 3 devices (Físico-A, Sim, Físico-B) — pre-condición CRÍTICA: KV-Store contaminado por install previa para reproducir bug #6 (sin contaminación → "pasa" sin validar). Sprint 2 (#5 polish visual Hero, 9 puntos) y Sprint 3 (#8 copy "Empezar configuración" en `WelcomeRestoreView`) en sesiones separadas tras validar QA con A4-01..A4-21 limpio. Reanudar QA del épico desde A4-01 con los 3 devices. 21 escenarios A0-Bridge + 9 A3 quedaron ⏳ pendientes (no afectados por el bug #6).
- [2026-05-07] **Sprint extra — TODO #10 cleanup CloudKit duplicates** (commits `fe897dd3` F1+F2, `a4b70fe4` F3+F4+F5, `20ac4223` F6 tests, + commit final post-`/simplify`). Cierra el TODO #10 que quedó diferido en A4 v3.2 Sprint 2 polish — el fix `f07a26be` resolvió el crash inmediato pero era defensivo, no curativo. **Items entregados**: (1) **Auditoría sistémica de 14 sitios `Dictionary(uniqueKeysWithValues:)`** vía 3 agents Explore en paralelo: **0 P0 reales**; `AppBootstrapper:321` reclasificado a P3 (UUID local round-trip CloudKit, sin race posible — el TODO original lo asumía P1 erróneamente); 4 sitios P2 (claves String editables = sort-by-name) endurecidos con `uniquingKeysWith: { first, _ in first }` (`AccountsSettingsListViewModel:48`, `TagsSettingsListViewModel:51`, `TrendsTabView:38`, `PanelViewModel:939`). Resto (PersistentIdentifier, UUID local memoria, enum cases) sin cambios — riesgo cero confirmado. (2) **Telemetría `cloudkitDuplicateDetected`**: nuevo case enum `AnalyticsEvent` + enum `DuplicateDetectionContext: String` (`.bootCleanup`, `.runtimeFetch`, `.syncApply`, `.uniquingFallback`) + helper `TelemetryService.cloudkitDuplicateDetected(model:count:context:keySuffix:)` (extraído por `/simplify` — 4 callsites de ~7 líneas a 1 línea cada uno). **Composite key local incluye zoneID** para no perder cuenta de zones distintas — la `key` es solo dedup local en `trackedOnceKeys: Set<String>`, NO se envía al backend (privacy-first). Payload: `model`/`count`/`context.rawValue`. (3) **Cleanup proactivo `SplitGroupDeduplicationService`** (`Yala/Services/Groups/`, patrón `CategoryDeduplicationService` / `NotificationService.deduplicateNotifications`): pure-logic `computeDedupPlan(groups:) -> DedupPlan` testeable sin context (R8 evita `makeTestContext()` flake) + service wrapper `deduplicateSplitGroups(in:) -> Int` que fetch → plan → `context.delete(extras)` → save + telemetría por zone duplicada. **Keeper = oldest `createdAt`**. Members/Expenses/Settlements **NO se tocan** — vinculados por `groupZoneID` String, no por `@Relationship` (F0 confirmó ausencia). Integrado como **paso 16.4 sync** en `AppBootstrapper` (entre init Group services y retryPendingBridges/reconcileDisplayName Tasks) — sync porque consumers downstream Tasks pueden tropezar con duplicados si corre async. 6 tests pure-logic verdes. (4) **Telemetría runtime defensiva**: `GroupService.updateCurrentUserDisplayName` y `refreshCurrentUserFlags` comparan `dict.count < array.count` post-`uniquingKeysWith` → dispara `.uniquingFallback` (sin crashear). `SplitSyncManager.group(for:)` cambia a fetch ordenado por `createdAt asc` (canónico determinístico) + dispara `.runtimeFetch` cuando results.count > 1 (red de seguridad para race post-boot no cubierto por el cleanup boot-time). `groupName(for:)` sin sortBy (sin callsites — `/simplify` removió defensa innecesaria). **Causa raíz del race confirmada**: `SplitGroup` se crea por DOS paths con `id` distintos pero `cloudKitZoneID` idéntico — (a) CloudKit sync via `SplitSyncManager.applyGroupMeta` (id desde `record.recordID`), (b) acceptShare local (id fresh `UUID()`). Race fetch-then-insert en `applyGroupMeta:957-963` sin transacción atómica. Mitigación cleanup proactivo + defensa runtime cubre el caso en práctica. **Cascade-delete CloudKit aceptado conscientemente**: `context.delete` propaga a otros devices via SwiftData CKSyncEngine; si telemetría muestra recurrencia post-deploy iterar a heurística más fina (ej. `isOwner` flag o sync state). **Out of scope** (segunda iteración): cleanup post-fetch en `applyRemoteRecord`, transacción atómica fetch+insert, dedup de SplitMember/SplitExpense/SplitSettlement (sus `id` provienen del recordID round-trip, sin race). **Aprendizajes**: 3 agents Explore en paralelo (audit `Dictionary`, telemetría infra, CloudKit dedup) cazaron numéricamente el alcance antes de planificar — sin esto, el TODO original habría guiado a un over-fix de `AppBootstrapper:321` (P3, no P1). `/review-plan` cazó 5 problemas pre-implementación: composite key sin zoneID perdía info de N zones, cascade-delete CloudKit no documentado, race post-boot no cubierto (red de seguridad runtime telemetry), falta update CODEBASE-MAP.md, F0 verificación pre-flight de `SplitGroup.swift`. `/simplify` cazó 5 oportunidades: extraer helper telemetría (4→1 línea cada callsite), enum quita stringly-typed code, eliminar refs `Sprint extra TODO #10` de comments (regla CLAUDE.md no narrar tarea), remover sortBy defensivo en `groupName(for:)` sin callsites, agregar docstring R8 al test helper. Build verde end-to-end. Pendiente: device QA cold launch verificar log "SplitGroupDedup: removed N duplicates" + telemetría TelemetryDeck dashboard si recurrencia detectada.
- [2026-05-06] **A4 v3.1 — Welcome Hero + Chooser fix unificado** (cierra bugs #1 P1 + #4 P0 de `apuntes-grupos.md` en una sesión). Insight arquitectónico: el **Welcome Hero pre-Chooser** es el lugar natural para fetch background de iCloud — el user lee 7 cards animadas mientras decidimos si tiene data, y el alert "Detectamos tu cuenta" ocurre ANTES del Chooser, no post-onboarding. **Esto elimina 3 paths de autopromote silente** sin necesidad de wipe+reseed flow ni helpers complejos. **Items entregados**: (1) **F1 (1 commit atomic)**: `WelcomeHeroView.swift` (~330 líneas) — stack de 3 cards visibles (front/middle/back con offset/rotation/scale + spring animation 3.5s) sobre 7 cards data (capture · voice · import · multi-account · 48 divisas · iCloud · "y mucho más"). Title con accent gradient indigo→pink. Background theme.background + glow asimétrico pink→indigo desde abajo. iCloud fetch task corre invisible (`forceFetchAndWait(timeout: 10)` + `iCloudAccountSummary`). `handleEmpezar` SIN recursión (single-shot guard `hasTappedEmpezar` + dispatch único tras timeout 3s extra). Pause card rotation con VoiceOver activo (`@Environment(\.accessibilityVoiceOverEnabled)`). Refactor `ContentView.checkInitialSyncState`: eliminar autopromotes (líneas ~602-668), eliminar `iCloudSyncWaitingView` + `isWaitingForSync` state + `showICloudDataFound` alert. Extraer `runReturningUserPostChecks()` (SSOT post-onboarding checks) y `consumeDetectedSummary(_:)` (lógica WelcomeRestoreView callbacks SIN re-fetch). `presentNextOnboardingScreen` cambia `showWelcomeChooser` por `showWelcomeHero` cuando `!hasShownWelcomeChooser`. **`WelcomeFlowModifier`**: extraído como ViewModifier (patrón `GroupInviteModifier`) — Hero cover + alert "Detectamos tu cuenta" + onChange reset state cuando llega CKShare. Necesario para evitar typecheck timeout en body grande. **Extension SSOT** `ICloudAccountSummary.isFullyPrefilled` en `iCloudSyncService.swift` reusada por `WelcomeRestoreView` (1 línea cambio) + `WelcomeFlowModifier`. (2) **L10n**: 23 keys × 16 locales (5 hero meta + 14 cards × 2 + 4 detected data alert) con traducciones reales por locale: voseo es-AR ("Apuntá", "Decilo", "Registrá", "Viajá", "Cargá"), tuteo es estándar, Sie de, vouvoiement fr, です/ます ja, 你 zh-Hans, "tu" forms pt-PT, en/en-GB diferencia "organizes" / "organises". Python script appendea 368 strings × 16 archivos. (3) **F2 tests**: +7 pure-logic en `ICloudAccountSummaryTests.swift` (5 `isFullyPrefilled` casos + 2 `hasAnyData` casos puros). 100% passing. Tests integration `makeTestContext()` siguen con flake R8 conocido (no regresión, no míos). (4) **F3 QA-SCENARIOS.md**: 18 escenarios A4 (12 originales + 6 nuevos): A4-01/03/11 modificados, A4-13 (alert detected data + 2 CTAs), A4-17 (slow network), A4-18 (rotation 1min), A4-19 (VoiceOver pause), A4-20 (Dynamic Type XXL), A4-21 (race CKShare). (5) **F4 cierre**: `apuntes-grupos.md` #1 + #4 marcados resueltos. **Decisiones clave**: (a) Hero approach elimina necesidad de v2 alert post-onboarding + wipe+reseed flow + `resetFirstImportFlag` + sleep 2s + revalidación summary multi-device race — todo eso desaparece porque la decisión es PRE-onboarding, no POST. (b) **7 cards inicial** (no 11) — reducido tras `/review-plan` para shipping inicial; las 4 restantes (siri, widgets, alerts, export) en iteración posterior si ROI lo justifica. (c) Sin destructive role en alert primary — no se borra nada hasta que user tap card concreta del Chooser. (d) Returning users (`hasCompletedOnboarding=true` cached) → MainTabView directo, sin Hero, sin cambios. (e) Group invites bypassean Hero via universal link + reset state en `onChange(showGroupInviteOnboarding)`. (f) `Image("YalaLogo")` reusado del SplashScreenView/WelcomeChooserView (theme-agnostic). (g) `PBXFileSystemSynchronizedRootGroup` en `.xcodeproj` significa auto-discovery de archivos nuevos (sin tocar pbxproj). **Aprendizajes**: `/review-plan` (2 rondas) cazó 7 problemas + 4 riesgos antes de implementar (handleEmpezar recursivo bug, F0 verificación DS tokens, race CKShare en alert, 11→7 cards, post-checks no extraídos, mixto String(localized:)/L10n.X.Y, F1+F2 ordering, theme-aware logo, transition splash→Hero, a11y VoiceOver/DT, Timer→Task-based loop). F0 grep verificaciones cazaron mappings reales: `DS.Semantic` solo status colors (no marca → usar `Color.electricIndigo/hotPink/neonCyan/priorityNeed/essentialNeed` del proyecto), `DS.Gradients.heroNeutral/heroIntense` ya tienen pink→indigo, `YalaPrimaryButton(isLoading:)` API existing. Build typecheck timeout en body forzó extracción a ViewModifier (patrón GroupInviteModifier). #2 P0 (banner saldos pendientes) diferido fuera de scope (decisión user). Pendiente: device QA en físico B (cuenta poblada) + simulador A (cuenta limpia) — A4-13 + A4-21 son críticos.
