# Yala (iOS)

Yala es una app iOS de finanzas personales. Objetivo: entender gastos, cuentas, presupuestos y reportes con claridad.

## Stack

- Swift, SwiftUI, SwiftData (.xcodeproj)
- **Target iOS 26+** — APIs nativas (Liquid Glass, ToolbarSpacer, etc.)
- Schemes: **Yala** (producción) | **Yala Dev** (con toggle Pro y `DEV_BUILD`) | Tests: YalaTests
- Simulador: **iPhone 17 Pro**
- 21 SwiftData models. ModelContainer via `SwiftDataConfiguration`. Divisas SSOT en `Yala/Utils/CurrencyUtils.swift` (`CurrencyCode`, 48 divisas).

## Docs (leer cuando sea relevante)

| Archivo | Propósito |
|---------|-----------|
| `$VAULT/planning/CODEBASE-MAP.md` | Tablas de Services / Calculators / ViewModels / Tests con paths |
| `$VAULT/planning/UI-PATTERNS.md` | Design System, gotchas de SwiftUI, formularios, glass |
| `$VAULT/planning/SWIFT-STYLE.md` | ViewModel pattern, idioms modernos, DS.Semantic / DS.Gradients, "añadir preferencia" |
| `$VAULT/planning/L10N.md` | 16 locales, workflow para añadir keys, tests CI |
| `$VAULT/planning/DEVICE-QA.md` | Setup Yala Dev + agent-device patterns |
| `$VAULT/planning/BRAND-VOICE.md` | Tono y estilo de marca |
| `$VAULT/planning/WORKFLOW.md` | Workflow detallado de skills |
| `$VAULT/planning/PROJECT.md` · `ROADMAP.md` · `STATE.md` | Producto, plan, progreso |
| `$VAULT/planning/DECISIONS.md` | Registro de decisiones arquitectura |
| `$VAULT/planning/QA-SCENARIOS.md` | Escenarios de prueba |
| `qa/README.md` | QA automatizado (agent-device + suites) |

`$VAULT` = `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/YalaWiki/` (Obsidian, sync vía iCloud).

Carpetas del vault: `Backlog/` · `Ideas/` · `Bugs/` · `Attachments/` · `planning/`. Skills: `/backlog`, `/spec`, `/idea`.

## General Rules

- Hacer SOLO los cambios explícitamente solicitados. No mover UI, refactorizar adyacente, ni añadir mejoras no pedidas.
- Antes de editar, listar archivos a modificar y qué cambia. Esperar aprobación si son más de 3 archivos.
- Confirmar que rutas referenciadas existen antes de proceder. Si no, preguntar al usuario.
- Evitar refactors grandes innecesarios. No introducir dependencias nuevas sin justificación.
- Mantener separación UI / lógica / SwiftData.

## Reglas inviolables

### Errores y unwraps
- NUNCA `try?` que silencia. Usar `do { try } catch { print("Service: Error: \(error)") }`.
- NUNCA force unwraps sin validación previa (`guard let x = ... else { return }`).
- Logs SIEMPRE dentro de `#if DEBUG` — nunca datos sensibles en producción.
- API keys: NUNCA hardcodear. Usar `Secrets.xcconfig` + `Info.plist` via `Bundle.main.object(forInfoDictionaryKey:)`.

### SwiftData
- SIEMPRE `@Relationship(inverse:)` en relaciones bidireccionales.
- Verificar `deleteRule` en cada relación.
- `@MainActor` en servicios que manipulan `ModelContext`.
- **CloudKit compat:** NUNCA `@Attribute(.unique)`, propiedades con default obligatorias, ni relaciones non-optional.
- **Schema del container de GRUPOS — campo nuevo = deploy a Production en el MISMO PR:** todo field key nuevo en records del container de grupos (`CKConstants`/`CKRecordTranslator`) exige desplegar el schema a Production (CloudKit Console) Y actualizar `cloudkit-groups-production.ckdb` (raíz del repo, snapshot-contrato del schema vivo) en el mismo PR — enforced por `CloudKitGroupsSchemaParityTests`. El sim corre grupos SIN CloudKit ⇒ el campo no se autocrea ni en Development; en Production el server RECHAZA todo save del record type y **CKSyncEngine descarta el record de su cola en silencio (NO reintenta rechazos definitivos)** → sync muerto bidireccional e invisible (incidente `isOpeningBalance` 27-jun→1-jul, 4 días). Redes de seguridad en runtime: `recoverUnsyncedRecordsIfNeeded` re-encola al arrancar los records con `ckSystemFieldsData == nil` (nunca hicieron round-trip) y `cloudkitGroupRecordSaveRejected` en TelemetryDeck es el canario (>0 = incidente de schema/permisos activo).
- **`#Predicate` GENÉRICO-PROTOCOLO crashea (`DataUtilities.swift:85`) — usa concreto por tipo:** NUNCA un fetch genérico `func f<T: SomeProto>(…) { FetchDescriptor<T>(predicate: #Predicate { $0.id == id }) }` donde `id` es un **requisito del protocolo** — `$0.id` resuelve al keypath del **witness del protocolo**, que SwiftData NO puede casar con el keypath concreto `\ConcreteType.id` del schema → `Fatal error: Couldn't find \X.<computed …> on X` al EJECUTAR el fetch (SIGTRAP, no atrapable). Síntoma: dos keypaths distintos para `id` en el mensaje. SIEMPRE `#Predicate<ConcreteType> { $0.id == id }` **concreto por tipo** (los concretos resuelven en CUALQUIER `ModelContext`; si necesitas DRY, un genérico que solo EJECUTE un `FetchDescriptor<T>` ya construido concretamente es seguro). Costó el crash de "generar enlace / forzar sync" en Grupos (`fetchByID<T: HasUUID>`/`deleteModel` → 5 helpers concretos `splitGroup/Expense/…(byID:in:)`, protocolo `HasUUID` ELIMINADO como footgun; commit `c74349fc`). El `ModelContext(container)` dedicado NO era la causa (los `#Predicate` concretos del mismo archivo nunca crashearon ahí). **Variante 2 (device, 2026-07-09, spike S6): `localizedStandardContains` sobre una propiedad OPCIONAL coalescada (`(x.note ?? "").localizedStandardContains(y)`) genera `TERNARY(...) CONTAINS[cdl]` que el SQL de SwiftData NO implementa → `NSInvalidArgumentException` en SQL generation, excepción ObjC que el `do/catch` de Swift NO atrapa (crash).** Compila limpio; solo revienta al EJECUTAR contra el store. Patrón seguro probado en device: igualdad exacta (`$0.note == x`, disjunción de variantes conocidas si hace falta). Si necesitas substring de verdad sobre opcional, testéalo en device ANTES de confiar en él (commit `4a270b4a`).
- **Acciones post-accept de un CKShare = intent PERSISTENTE reconciliable, nunca one-shot (bug Pia 2026-07-11):** toda acción tras `container.accept` que dependa de estado local sincronizado (p.ej. crear el `SplitMember` del invitado, que exige el `SplitGroup` de la zona) NUNCA debe ser un one-shot gateado por `if let` sobre estado que puede no haber llegado — la zona compartida tarda ≥60s en bajar (ventana export-only) y el skip silencioso deja al usuario "unido" sin member: el owner jamás recibe la solicitud y NADIE lo reintenta. Patrón vigente: `PendingJoinStore` (intent persistente, TTL 7d) + `GroupJoinReconciler` (4 triggers: acceptShare/remoteInsert/boot/foreground, decisión pura en `GroupJoinReconcileLogic`) + `GroupJoinIntentTracker` (@Observable — la UI muestra la fase REAL; el "¡Todo listo!" del onboarding SOLO con member confirmado, jamás como fallback defensivo). Corolario de notifs: en el PRIMER import de una zona recién unida los members preexistentes clasifican como "nuevos" → `MemberChangeNotificationLogic` exige baseline (`SplitGroup.initialMemberImportStartedAt`, set en applyGroupMeta rama NUEVO, clear en `didFetchRecordZoneChanges`, ventana 15 min auto-sana) + autoexclusión por identidad (`memberID` del record vs `cachedRecordName`) — sin eso, el invitado recibía "Jür se unió al grupo" por el member del owner.
- **Lazy M2M con CloudKit — CSV mirror:** cuando una `@Relationship` M2M `[Type]?` es crítica para correctness (cálculos, filtros, displays) y la app corre en cold start con sync activo, **duplica el filtro como CSV de UUIDs** en el mismo record. Patrón replicado en `Budget.subcategoryIDs/accountIDs/tagIDs` y `TransactionItem.tagIDs`. Helpers: `setFilters(...)`/`setTags(from:)` para dual-write; `resolvedXIDs(scheduleBackfill:)` para read CSV-first con M2M fallback y auto-cura async. La relación M2M queda solo para cascade `.nullify` automático. Razón: SwiftData/CloudKit puede entregar el record con relaciones lazy `nil` mientras espera que lleguen los Tag/Subcategory/Account records — el código pre-fix interpretaba `nil` como "sin filtros" → comportamiento catastrófico. Naming asimétrico aceptado: `Tag.id`, `Subcategory.shortcutID`, `Account.shortcutID`, `Budget.id`. Backfill eager en bootstrap **solo para Budget** (pocos records); TX usa lazy on hot path (miles de TX, eager sería caro).
- **CSV mirror — stale ≠ nil al regenerar un UUID de identidad (commit `899c1c25`):** `resolvedXIDs(scheduleBackfill:)` (y los `resolved*` de Budget) leen **CSV-first y SOLO caen a M2M si el CSV es `nil`**. Un CSV *stale-pero-presente* (apunta a un UUID que ya no existe — p.ej. tras regenerar un id de identidad) NO cae a M2M → `byIDLookup` no lo resuelve → display/filtro queda **huérfano permanente** (no auto-sana). ⇒ **regenerar cualquier UUID de identidad (`Tag.id`/`Account.shortcutID`/`Subcategory.shortcutID`) OBLIGA a reconstruir TODOS los mirrors que lo referencian con _nuke-on-nil_**: si la M2M forward viene `nil` (ventana lazy en paths sin quiescencia: force-sync/DEBUG), escribir `encode((m2m ?? []).map(\.id))` (= nukea el CSV → fuerza el fallback a M2M en la próxima lectura), NUNCA saltar (dejar el CSV stale orfanaría permanente). El colapso de estos UUIDs (CloudKit entrega el record sin el campo → default `UUID()` evaluado UNA vez y compartido) lo auto-cura `CategoryDeduplicationService.repairCollapsedIdentityUUIDs` (repetible, gateado por quiescencia, regenera solo colisionados vía `collidedUUIDItems`/`regenerateDuplicateUUIDs` + reconstruye mirrors); la migración one-shot **difiere** si el primer import de CloudKit no se asentó (`MigrationGateLogic.shouldDeferMigration`) para no regenerar sobre datos parciales y quedar bloqueada por su sentinel.
- **Sync de Grupos (CKSyncEngine) NO debe arrancar/`save()` sobre el `mainContext` compartido antes de que el primer import personal de CloudKit se asiente — crash-loop en restore de iCloud:** la app monta **un solo `ModelContainer` con dos stores en el mismo `mainContext`** — personal (`cloudKitDatabase: .private`, espejado por NSPersistentCloudKitContainer) y grupos (`.none`, CKSyncEngine manual). En un device **restaurado de iCloud**, el store personal se importa a medias mientras el sync de Grupos hace `modelContext.save()` sobre ese mismo contexto → SwiftData dispara un **`_assertionFailure` interno (`EXC_BREAKPOINT`/SIGTRAP)** al persistir el grafo a medio hidratar. **El `do/catch` NO lo atrapa** (es un trap que mata el proceso, no un `throw`) → crash-loop en cada cold launch porque CKSyncEngine re-entrega su cola persistida. ⇒ **Fix vigente (device-confirmed 2026-06-22, build 32): el delegate del sync opera sobre el `mainContext` COMPARTIDO, protegido por gates de QUIESCENCIA — el `ModelContext` dedicado fue un intento intermedio y está REVERTIDO.** Historia: el contexto dedicado (`groupSyncContext = ModelContext(container)`, `6360d964`, build 29) NO bastó — comparte el `NSPersistentStoreCoordinator` con el store personal, así que build 29 SIGUIÓ crasheando, pero en un `save()` del store PERSONAL durante el import [seed de notificaciones / invite-onboarding / reconciles de boot], NO en el del sync; se revirtió a `mainContext` en `0e8b40e7` (build 31) porque la quiescencia lo hace redundante (solo añadía aislamiento). El crash de "generar enlace / forzar sync" que se investigaba en paralelo lo mataron los `#Predicate` CONCRETOS por tipo (`c74349fc`, build 32 — ver regla `#Predicate` arriba), NO el contexto: build 31, ya revertido a mainContext, seguía crasheando idéntico. Hoy: **0 referencias a `groupSyncContext` en `Yala/`** (solo el doc-comment histórico en `SplitSyncManager.swift:47-53`). Consecuencia estructural: la separación personal/grupos es de SCHEMA/STORE, no de contexto (los `Split*` se vinculan por IDs planos, sin `@Relationship`) — un `save()` del delegate comitea también el estado dirty personal pendiente (y viceversa), por eso el gate de quiescencia es obligatorio. **Fix COMPLETO:** gatear por QUIESCENCIA del import [`iCloudSyncService.isImportQuiescent`, adaptador runtime de `SubcategoryDedupGate.decide`] TODOS los `save()` automáticos del store personal — seed/dedup notificaciones, scheduled-payments, exchange-rate, provisional-tx, race-cleaner, dedups de grupo, y los boot Tasks freeze/reconcileTransferPairs/reconcileDisplayName/refreshCurrentUserFlags/sendDueReports/migrateToLiveBalance — vía guard sync por-save o `awaitPersonalImportForBootSave` [que ahora espera primer-import + quiescencia, NO solo el primer evento]; y promover el sync de grupos por QUIESCENCIA, no por el primer `importEvent` [`SplitSyncStartGate.resolveWaitByQuiescence`, exige `hasCompletedFirstImport && isQuiescent`]. `SaveBreadcrumb` [Console.app, fuera de `#if DEBUG`, sin PII] instrumenta cada save site → si reaparece, el último `SAVE <site>` lo nombra sin necesidad de dSYM. **Residual documentado**: aceptar un invite con tap EXACTO durante el import [`ensureCurrentUserMemberExists`/`updateCurrentUserDisplayName` vía `handleJoinTap`] NO está gateado — es user-tap, no el auto-loop reportado; gatearlo rompería la UX de unirse. Del intento revertido sobrevive (inofensivo) el param `context:` en los 3 métodos de `GroupService` invocados desde el delegate (`ensureCurrentUserMemberExists`/`performRemovedSelfCleanup`/`refreshCurrentUserFlags`), default `mainContext`. **Refresh de UI**: el delegate señala `markRemoteChangePending()` (diferido junto al save mientras el import no esté quiescente; `dataVersion` → `loadData()` re-fetch); las vistas de Grupos no usan `@Query`. **El bridge personal** escribe `TransactionItem`/`InboxDraft` en `YalaModel` (el store que se importa) → NO aislable por contexto; se gatea por **QUIESCENCIA del import** (`SubcategoryDedupGate.decide`, por `isImporting` no `isSyncing` para no bloquear exports normales); al diferir retiene los IDs EN MEMORIA (`pendingBridgeExpenseIDs`/`pendingBridgeSettlementIDs` — NO persiste `bridgePending` en ese path: ese `save()` flushearía el grafo personal a medio importar) + retry en sesión (`scheduleBridgeRetry`); app matada durante esa ventana pierde la recuperación in-session (residual documentado — CKSyncEngine re-entrega después). `bridgePending=true` persistido lo escribe el Caso A user-action (`GroupExpenseService`) y lo recupera `retryPendingBridges` (boot), **también gateado por quiescencia**. Settlements diferidos: sin flag persistido, cubiertos solo por el retry en-sesión (residual documentado). **Fase 1 mantiene como RED redundante** todo el gate/export-only de abajo; **Fase 2 (limpieza del gate) solo tras validar en device vía TestFlight**. ⇒ **(red, Fase 1) `SplitSyncManager.initialize()` crea SIEMPRE los engines pero en modo "export-only" (`automaticallySync = false`)** hasta que el import se asiente (`SplitSyncStartGate.decideStart`; offline/import asentado → `autoSync=true` directo). **Por qué crear-siempre y no diferir la creación** (refactor 2026-06-20, regresión del primer gate): diferir dejaba `privateEngine = nil` → crear grupo / **invitar por enlace** / `enqueueSave` fallaban toda la ventana (`createZone` no-op silencioso → grupo sin zona en CloudKit, no invitable; `createShare` lanzaba `engineNotInitialized`). En export-only el engine **existe** (export funciona vía `sendChanges` manual, que `createShare` ya invoca) pero **NO fetchea solo**, y CADA `save()` del delegate se difiere vía `deferMainContextWork()` (gate `autoSyncActive`): los handlers de fetch no corren (sin auto-fetch; `acceptShare` también gatea su `fetchChanges`), y los de sent/conflict (export — ej. system fields) hacen early-return ANTES de mutar el mainContext (se reconcilian tras promover vía un `serverRecordChanged`, **sin pérdida**). Apple: `automaticallySync=false` no programa sync; uno llama `fetch/sendChanges`. Al asentar el import (observer `.iCloudFirstImportCompleted` / poll 15s / tope 300s) → **`enableAutoSync()` RECREA ambos engines con `automaticallySync = true`** (vuelve al modo automático probado: push/coalescing/retry). **Cuidados de la recreación**: transfiere los `pendingRecordZoneChanges/DatabaseChanges` en-memoria del engine viejo al nuevo (el `.stateUpdate` persiste async → `loadState` del disco puede no tener un encolado de último ms; transferir directo evita perderlo); actualiza `_privateEngineRef`/`_sharedEngineRef` y el delegate **descarta callbacks de engines viejos vía `isCurrentEngine`** (un `.stateUpdate` tardío del viejo se guardaría con nombre equivocado → corromper el otro store). **Recuperación de grupos rotos**: `recoverOwnedGroupZonesIfNeeded()` (en `startEngines`, read-only sobre mainContext) re-encola zona+records de grupos owner sin GroupMeta subido (`SplitSyncStartGate.needsZoneRecovery`: `isOwner && ckSystemFieldsData==nil`) — recupera grupos creados en una ventana export-only previa (incl. el "Jurpi" del owner). **D2**: `retryPendingBridges`/`migrateShareGroupZoneIDs` (boot) también esperan el import (`awaitPersonalImportForBootSave` → `forceFetchAndWait`) antes de su `save()` del mainContext personal (idempotentes: difieren al próximo launch si no asienta). Diferir NO pierde eventos (cola en CloudKit + `stateSerialization`). **Cero silencios** (decisión owner): todo `try?` del subsistema de Grupos → `do/catch` con log; user-actions (invitar/eliminar/liquidar) → alert localizado (`Groups.Errors.*`, copy clara en vez de "CKSyncEngine not initialized"). Logging de `SplitSync*` INTENCIONALMENTE fuera de `#if DEBUG` (excepción consciente: el bug solo reproduce en CloudKit **Production**, verificable solo vía TestFlight Release en Console.app; sin PII).

### State Management (SwiftUI)
- `@Observable` SIEMPRE con `@MainActor`. `@State` SIEMPRE `private`.
- NUNCA `Binding(get:set:)` en body — usar `@Binding` + `.onChange()`.
- NUNCA `@AppStorage` dentro de `@Observable` (no triggerea updates).
- Preferir `@Observable` + `@State`/`@Bindable` sobre `ObservableObject`/`@Published`/`@StateObject`.
- **Preferencias persistentes → `AppPreferences` inyectado via `@Environment`.** NUNCA `@AppStorage` directo en views nuevas.

### Gotchas críticos
- **`containerRelativeFrame(.horizontal)` en `ScrollView(.vertical)` con `.contentMargins`** → deadlock de layout, splash nunca dismissa, sin crash log. Usar `onGeometryChange`. Detalles en UI-PATTERNS.md.
- **`YalaFormatter` no auto-refresca prefs** — lee `UserDefaults` directo. Vista que lo use con `decimalPlaces` o `currencyDisplayFormat` debe inyectar `@Environment(AppPreferences.self)` y leer `let _ = appPreferences.X` en body para registrar dependencia.
- **Forms con `TextField`/`TextEditor`/`SecureField`** (sin `Form`): obligatorio `dismissKeyboardOnTap()` desde el primer commit. Detalles en SWIFT-STYLE.md.
- **Swift Charts `.annotation { }` NO propaga environment objects `@Observable`** → el contenido de una annotation de un mark (`BarMark`/`LineMark`/etc.) se hostea fuera del árbol de la vista; una sub-View que lea `@Environment(AppPreferences.self)` (ej. `AmountText`) NO lo resuelve y dispara `SIGTRAP` (`_assertionFailure` en `EnvironmentValues.subscript.getter`) al renderizar — sin crash log claro. Dentro de annotations usar `Text(...)` con el valor YA resuelto del callsite (`appPreferences.currency(...)` desde `self`) o un formatter estático (`YalaFormatter`), NUNCA una sub-View con `@Environment`. **`.chartOverlay { }` SÍ propaga** (ahí `AmountText` funciona, ej. `CashFlowWidget`). Causa del crash del chip Estadísticas en grupos (`8bb5ace8`).
- **Presentaciones (sheet/fullScreenCover) — 4 reglas del bug "toolbar muerta" (TestFlight 2.0.5):** (1) NUNCA un binding de presentación con setter no-op (`set: { _ in }`) ni sin `onDismiss:` de respaldo — si UIKit tumba la cadena presentada (p.ej. dismiss del sheet debajo de un cover), el estado queda pegado → cover fantasma irrecuperable; el reset del flag JAMÁS puede depender solo de un `Task { sleep; ... }` interno de la vista presentada. (2) **`opacity(0)` NO desactiva hit-testing**: un backdrop full-screen invisible con `.onTapGesture` se traga todos los taps de la app — siempre `allowsHitTesting(isVisible)`. (3) Toda presentación nueva que cuelgue del anchor de ContentView DEBE entrar a `ShellReadinessState`/`blocker()` (matriz de readiness), y los sheets de MainTabView/PanelShell están gateados por `RouterConsumerGateLogic` (peek-first: un intent que presenta se RETIENE en cola mientras un nodo superior tape — nunca se consume tapado). Corolario one-shots: flags tipo `markXShown` se queman en el `onAppear` del sheet real, nunca en el drain ni en el productor. (4) **NUNCA dos anchors presentando ante el mismo observable** (bug device sign-out 2026-07-14): UIKit no presenta dos veces y la reconciliación puede tumbar AMBAS cadenas dejando los flags en `true` sin que NINGÚN `onDismiss` corra (jamás se presentaron) → red muerta e irrecuperable; "SwiftUI materializa la presentación pendiente al despejarse el anchor" es FALSO. Un solo DUEÑO de la presentación (los demás anchors ceden — p.ej. cierran su sheet) + verificación de presentación EFECTIVA: solo el `onAppear` del contenido real prueba que UIKit presentó — si no llegó, re-intentar con toggle false→true tras un runloop turn (molde `SignOutRelaunchNetModifier` + `RelaunchNetLogic`); para blockers de la matriz, la CONDICIÓN VIVA del dominio es el input, el `@State` del cover es solo la red visual.
- **`DateInterval` es CERRADO en ambos extremos (`start` Y `end` inclusivos)** — al construir un `end` que coincide exactamente con el `start` de otro período/bucket adyacente (ej. `DateInterval(start: startOfLastMonth, end: startOfThisMonth)`), ese instante compartido se cuenta en AMBOS lados: `.contains(_:)` devuelve `true` para los dos, y con predicados manuales (`date >= start && date <= end`) el mismo double-count. Ocurre en la práctica porque `DatePicker(displayedComponents: [.date])` normaliza la hora elegida a medianoche exacta (00:00:00) — una transacción fechada "1 de julio" cae en `.lastMonth` (junio) en vez de solo `.thisMonth`. **Fix**: restar 1 segundo al `end` cuando representa "hasta el inicio de X, sin incluir X" (`calendar.date(byAdding: .second, value: -1, to: startOfX)`). Costó el bug de "Mes/Año pasado incluye día 1 del actual" replicado en 4 archivos independientes (`DetailPeriod.dateInterval()` en `SharedModels.swift`, su mirror en `WidgetDataService.swift`, `PreviousPeriodHelper.previousPeriodInterval` [ancla en `.end` que cambia → re-anclar en `.start`, invariante], y `FullFinancialContextBuilder.buildIntervals` [cadena de buckets adyacentes `lastMonth`/`twoMonthsAgo`/`threeMonthsAgo`/semanas] + `ReportNotificationService.getIntervalForReportType`). Al añadir un período/bucket nuevo con boundary en medianoche, verificar el mismo patrón.
- **`PreferenceSyncService.remove/set` propaga a la CUENTA, no al device — NUNCA limpiar un consent desde un camino con `.cloud` VIVO (commit `bdbc46d1`):** el service ramifica por `behavior`, que es **computed y se resuelve en CADA llamada** (`CloudSyncFlags.storageMode` + descriptor secundario): `.icloud` → local + iKV; `.cloud` → `enqueuePref(.int(0))` al outbox de prefs, que **SUBE al backend**; secundaria M1 → `.localOnly`. Para la familia `intPresence` un `0` **ES** "no aceptado" (`isAccepted` es `> 0`) y el wire de prefs **no tiene tombstone**, así que ese `0` pisa por LWW el epoch de aceptación en `user_preferences`: borra el **registro GDPR de una cuenta que sigue VIVA** y lo propaga a sus otros devices. Un sign-out es "hasta luego", no una retirada de consentimiento — el precedente del repo es append-only (`cloudConsentAcceptedAt` no lo borra NINGÚN camino). ⇒ el olvido del consent en un cierre va **donde el modo persistido ya es `.icloud`**: hoy el boot-hook `performSignOutWipeIfArmed`, DESPUÉS de `StorageModePersistence.write(.icloud)` (rama sin backend) y ANTES de `resetPrefs()` (que podría barrer la key que gatea el clear, dejándolo mudo en silencio). Pinneado en AMBAS direcciones: `SignOutNotificationWiringTests.inSessionConsentClears_neverOnCloudModePaths` resuelve la función contenedora y **nombra al culpable** si alguien cuela el clear en un camino `.cloud`, y `modeAtClear` en `SignOutWipeHookTests` captura el modo **DENTRO** de la closure — leerlo tras el return no prueba nada del instante del clear (un mutante que mueva el `write(.icloud)` por debajo quedaría verde). **Corolario general:** todo `set`/`remove` de prefs en un camino de cierre o de frontera de cuenta debe preguntarse en qué rama de `behavior` caerá **en ese instante**, no en cuál cae "normalmente".

### Localización (l10n)
- **iOS NO hace fallback per-key con idioma de SISTEMA.** El patrón "marker-only hereda vía fallback iOS" es FALSO: si la variante (`es-ES`/`es-AR`/`en-GB`/`pt-PT`) es el idioma del sistema, iOS resuelve `Bundle.main` contra esa `.lproj` y devuelve la **key cruda** si falta (afecta `ls()` Y los ~290 `NSLocalizedString` directos). El fallback manual de `ls()` solo corre con **override in-app** (`parentBundle != nil`). → **Toda key nueva DEBE quedar en las 4 variantes completas, no sparse.** Usa `qa/scripts/add-l10n-key.sh` (ya las materializa) y `LocalizationParityTests.variants_haveAllKeys_fromParent` lo enforcea. `es-419`/`en`/`pt-BR` son las referencias completas. Detalle en L10N.md.
- **Keys duplicadas en un mismo `.strings`: la ÚLTIMA declaración gana, silenciosamente** (ni el compilador ni la paridad lo detectan — `NSDictionary` colapsa al parsear). `LocalizationParityTests.noDuplicateKeys_inAnyLocale` lo enforcea leyendo los archivos FUENTE vía `#filePath` (app + widget target). Si alguna vez hay que dedupe: keys usadas para **matching por nombre persistido** (ej. `subcategory.balanceAdjustment` — seed + CSV import + InitialBalanceService) deben conservar el valor runtime actual o se duplican entidades de usuarios.
- **Idioma para prompts LLM: SIEMPRE `AppLocale.current.identifier`, NUNCA `Locale.current.identifier`.** `Locale.current` refleja la región de FORMATO (`AppleLocale`, ej. `es_PE`) y no cambia con el idioma de la UI (`AppleLanguages`) → app en inglés recibía respuestas del chat en español. `AppLocale.current` (→ `LanguageManager.resolved.locale`) es el SSOT: override in-app → `Locale.preferredLanguages` → `en`. `Locale.current.region` sí es la fuente correcta para PAÍS/modismos. Output LLM cacheado (ej. sugerencias de chat) debe incluir el idioma en la cache key.

### iOS 26 Liquid Glass (OBLIGATORIO)
- `ToolbarSpacer(.fixed, placement: .topBarTrailing)` — placement es OBLIGATORIO.
- `.glassEffect()` para chips, barras flotantes, elementos translúcidos.
- Si existe API iOS 26 que mejore integración con sistema, USARLA.

### Tests (OBLIGATORIO)
Detalles completos en `$VAULT/planning/TESTING-STRATEGY.md`. Reglas mínimas:
- `makeTestContext()` **REUSA un `ModelContainer` in-memory por archivo (`#fileID`)**, no uno por llamada (2026-07-04): crear >~15 containers in-memory en un proceso acumula estado global de SwiftData → `EXC_BREAKPOINT` no atrapable (crash-loop; iOS 26.2 y 26.5). ⇒ **toda suite con ≥2 `makeTestContext()` DEBE ser `@Suite(.serialized)`**. Residual conocido: los SUTs que spawnean `Task { @MainActor in save() }` huérfanos (CSV backfill, dedup repair) racean con el reuso → 2 tests flaky bajo suite completa (Lista Negra en TESTING-STRATEGY.md). Prefiere `@Model` directos sin contexto cuando la lógica lo permite (más rápido y sin este acoplamiento).
- NUNCA `UserDefaults.standard` directo en tests → `UserDefaults(suiteName: "test.\(UUID().uuidString)")!` (helper `makeIsolatedDefaults()`).
- NUNCA tocar singletons `.shared` sin `@Suite(.serialized)` + `defer { restore }` o `_testReset()`.
- NUNCA `Task.sleep(.seconds(N))` con N>0.5 — usar señales determinísticas. Excepción: `≤50ms` para forzar dealloc.
- NUNCA `Date()` / `Calendar.current` en lógica testeada — inyectar vía param opcional `now: Date = .now` (patrón canónico, ya en `FinancialScoreCalculator`/`BudgetAlertService`).
- NUNCA `@Test(.disabled(...))` sin entrada en Lista Negra (TESTING-STRATEGY.md) con owner + deadline.
- NUNCA declarar fix completo si un test falla. "Preexistente" no es excusa: arreglar o registrar en Lista Negra con plan.
- Ejecutar con `-parallel-testing-enabled NO` (2026-07-04): con el reuso per-file de `makeTestContext`, parallel OFF es determinista (~7s) y no crashea; parallel YES destapa una race de singleton (`AppLanguageSyncTests`). Antes se recomendaba YES.

### XCUITest (regresión UI determinista) — `YalaUITests`
- Scheme **Yala Dev**. Lanzar con `XCUIApplication().launchForUITest(...)` (helper en `YalaUITests/Support/`).
- Launch args (`#if DEBUG`, `UITestHooks`): `-uitest` (modo + store local sin CloudKit), `-uitest-reset`, `-uitest-skip-onboarding`, `-uitest-pro`, `-uitest-seed <minimal|realista|pesado>`.
- **Seed `minimal` por default** (rápido); `realista`/`pesado` solo si el test necesita volumen (arranque más lento, riesgo watchdog).
- Esperar **`waitForUITestReady()`** (señal `uitest_ready`) antes de interactuar — NUNCA `sleep`.
- Targetear por `accessibilityIdentifier` (`feature_element` / `feature_row_<claveEstable>`), NUNCA texto localizado ni coordenadas.
- Al cubrir un área `deterministic`: poner `coverage: "xcuitest:<File#test>"` en `qa/coverage-index.json` y bajar `_meta.backlogBaseline`.

### Audit markers
- `// A11Y-DT:` justifica font size hardcodeado (Dynamic Type).
- `// A11Y-DM:` justifica color hardcodeado (Dark Mode).

### Design System (en cambios UI)
- SIEMPRE `DS.Spacing`, `DS.Radius`, `DS.Typography`, `DS.Semantic.*`, `DS.Gradients.*` — NUNCA hardcoded.
- SIEMPRE filas clicables con `Button` + `contentShape(Rectangle())`.
- Componentes estándar: `YalaPrimaryButton`, `YalaEmptyState`, etc.
- Tablas DS.Semantic / DS.Gradients en SWIFT-STYLE.md.

### Backgrounds de vista
- TODA View root, sheet, fullScreenCover NUEVA → `.yalaScreenBackground(_:ignoredEdges:)`.
- NUNCA aplicar `.background(theme.background)`, `.background(.thBackground)` ni default iOS sin background.
- Forms/Lists dentro de sheet → `.scrollContentBackground(.hidden)` MANUAL antes del modifier (el modifier no lo hace automático para preservar predictibilidad).
- **Regla SSOT (reestructura 2026-06-08)**: el fondo lo determina el **contenedor de presentación RAÍZ** del stack. Una vista navegada (push) HEREDA el fondo de su stack — dentro de un tab → `.panel`; dentro de un sheet → el del sheet. **Un `.sheet` NUNCA es `.panel`.**
- Variantes (`YalaBackgroundVariant`) — 3 tras eliminar `.compact`:
  - `.panel` (default) — PanelBackgroundView gradient temático. SOLO vistas COMPLETAS: tab roots, vistas navegadas (push), fullScreenCover normal.
  - `.subtle` — `theme.background` plano. CUALQUIER sheet desde el bottom (sin detents o `[.large]`); TODO el stack del sheet de Profile (los ~20 Settings + sub-navegación); y success/celebración.
  - `.transparent` — sin fondo (DESNUDO, muestra el fondo de sistema del sheet — decisión owner, NO glassSheet). Sheets con detent PARCIAL (`.medium`/`.height`/`.fraction`). **Idiom-aware**: si el detent viene de `DS.Adaptive.sheetDetents(...)`, usar `DS.Adaptive.usesLargeSheets ? .subtle : .transparent` (iPad fuerza `.large` → debe ser subtle). Dual-detent `[.medium,.large]` con `selection: $selectedDetent` → `isLargeDetent ? .subtle : .transparent`.
- `.compact` ELIMINADA. `AnimatedMeshBackground` + `MeshConfigResolver` borrados (dead code). `DS.Adaptive.usesLargeSheets` (= isPad||isiOSAppOnMac) expuesto para el fondo idiom-aware.
- Param `ignoredEdges: Edge.Set?`: si `nil`, default `.all`. Para vistas con `safeAreaInset` pasar edges específicos (`[.top]`, `[.bottom]`).
- OUT: WelcomeFlow/onboarding (`DS.Gradients.heroIndigoBlack` especializado), InboxAlertModal (custom modal Color.black backdrop), popovers (iOS nativo).
- `GlassSheetModifier.glassSheet()` se mantiene como helper de sheets que SÍ quieren material (`.presentationBackground(.ultraThinMaterial)` + drag indicator). `.transparent` NO usa material (es desnudo).

#### Patrones aceptados temporalmente (deuda incremental, migrar al tocar el archivo)

- **Pattern B**: `ZStack { PanelBackgroundView(); content }` manual. Migrado masivamente al modifier (2026-06-08). Residuales aceptados: `PanelView` (tab root complejo con overlays) + las 6 vistas de onboarding/welcome (fondo hero propio, OUT). Sanity: `grep -rn "PanelBackgroundView(" Yala/` solo muestra esas + `PanelBackgroundView.swift` (def) + `ViewModifiers.swift` (modifier).
- **Pattern Subtle**: `ZStack { theme.background.ignoresSafeArea(); ...overlays; content }` en success screens (TransactionSuccessView, SubscriptionSuccessView, InboxApproveSuccessView, InboxBulkApproveSuccessView). Reescribir el ZStack rompería overlays propios (ConfettiView, RadialGradient glow). Semánticamente ES `.subtle`.

### Documentation & copy
- Describir features desde la perspectiva del USUARIO, no técnica.
- NUNCA fabricar features — solo lo confirmado en scope.
- Copy nuevo: leer BRAND-VOICE.md.

## Workflow

Skills disponibles vienen en el system-reminder de cada sesión. Flujos canónicos:

```
Feature:    /clear → Plan Mode → /review-plan → implementar →
            /verify-ios → /test-smart → /device-qa → /commit-one → /clear
Bug fix:    implementar → /verify-ios → /device-qa → /commit-one
Complejo:   añade /code-review high + /refine antes de commit
```

`/commit-one` ya incluye `/swift-audit` + tests antes del commit. No es necesario ejecutarlos por separado salvo que quieras arreglar issues mid-implementación.

Aplica el flujo **Complejo** cuando el cambio involucre lógica densa donde un bug sería costoso: sync/race (CKShare, bridges, NotificationService), cálculos financieros (FinancialScoreCalculator, GroupBalanceService, BudgetAlertService), migraciones SwiftData, bridge SplitExpense ↔ TransactionItem. Para rebranding visual, l10n o polish UI no aporta.

**Regla QA (contrato anti-drift):** la SSOT de cobertura es `qa/coverage-index.json` (validar: `bash qa/validate-coverage.sh`). Al tocar código bajo `Yala/`, en el MISMO commit actualizar el área correspondiente del index + sus tests/escenarios asociados (`coverage`, `lastVerified`). Cobertura por clasificación: `deterministic` → XCUITest en `YalaUITests`; `agentic` → `/device-qa`; `manual` → documentada. El **ratchet** del validador BLOQUEA si el backlog determinista (áreas `deterministic` sin XCUITest) crece respecto a `_meta.backlogBaseline` — escribe el test o baja el baseline conscientemente. `QA-SCENARIOS.md` queda como narrativa humana; la verdad ejecutable es el index + los tests + CI.

## Testing

| Tipo de cambio | Comando |
|----------------|---------|
| Modelo / servicio | `/test-smart` (relevantes) |
| Solo UI (Views) | `/verify-ios` |
| Antes de commit | `/test-smart` siempre |
| Tras merge / refactor grande | `/test-ios` (todos) |

Después de implementar cualquier cambio, SIEMPRE ejecutar `/verify-ios` o `xcodebuild` antes de presentar como completado.

## Corrección de errores

- SIEMPRE buscar TODAS las instancias del mismo patrón antes de declarar fix completo.
- NO confiar ciegamente en "BUILD SUCCEEDED" — verificar todos los casos.
- Si hay errores tras build exitoso, limpiar cache: `xcodebuild clean`.

## Self-Maintenance

Cuando se modifican modelos / servicios / ViewModels:
- Actualizar tablas en `$VAULT/planning/CODEBASE-MAP.md`.
- Actualizar conteo de tests si se agregaron nuevos.
- Agregar gotchas descubiertos a "Reglas inviolables" de este archivo.

Para preferencias nuevas (`UserDefaults`): ver checklist en SWIFT-STYLE.md sección "añadir una preferencia nueva".

## Control de Ejecución

Después de implementar código:
1. Mostrar resumen de cambios **en lenguaje de usuario** (qué cambia para él / qué problema resuelve), no descripción técnica de archivos editados.
2. Ejecutar `/verify-ios` SIEMPRE para confirmar que el build pasa.
3. Sugerir siguiente paso.
4. DETENERSE y esperar instrucción del usuario.
5. NO ejecutar tests, device-qa o commits automáticamente — solo el build.

**Git:** ejecutar cada comando de lectura UNA SOLA VEZ, secuencialmente, nunca en paralelo. No matar shells con git en curso.

**Tags:** SIEMPRE semver con prefijo `v` → `v1.0.0`, `v1.1.0`. Nunca sin prefijo o sin 3 componentes.

## Decisiones Recientes (TTL: hasta cierre de fase)

[Formato: [FECHA] Decisión breve — se archiva en DECISIONS.md al cerrar fase. Máx 3-5 entradas activas.]

- [2026-07-21] **fix(session): el consent de GRUPOS ya no sobrevive al wipe del sign-out en Modo Nube (`bdbc46d1`, branch `2.0.5`)** (encargo APARTE destapado por el review adversarial de las colas del App Group — no es regresión de aquel fix; método: workflow de comprensión [7 mapeadores + síntesis, veredicto gap-vs-decisión] → 2 decisiones owner vía AskUserQuestion → impl → workflow de review adversarial [6 lentes + refutación por hallazgo: **13 hallazgos, 0 confirmados / 13 refutados**] → endurecimiento → gates). El boot-cleanup del cierre `.cloud` borraba los archivos de los stores y reseteaba prefs pero **nunca limpiaba el consent de Grupos** (§C5): `removeUserPreferenceKeys` no nombra esas keys **ni en su lista ni en sus exclusiones deliberadas**. Era **el ÚNICO superviviente de un trío que el repo trata como unidad** — `PendingJoinStore`/`GroupJoinIntentTracker` ya morían en ese mismo hook dentro de `resetPrefs()` vía `AppRouter.resetAll` ⇒ firma de olvido, no de política; y la única justificación escrita que existía (doc-comment de `AppGroupInboundPurge`) razonaba sobre el flag OFF y era **stale por partida doble**. **Impacto con el flag ON — no es solo trazabilidad:** la cuenta SIGUIENTE en el device no vería la pantalla de consent (`GroupBackendInviteEntryLogic.nextStep`/`GroupCreateRoutingLogic`/`GroupJoinReconciler`) y el **uploader de migración, gateado POR consent** (`AppBootstrapper:424` → `GroupMigrationUploader:97`), subiría grupos, gastos y liquidaciones bajo un `user_id` que jamás consintió. **2 CORRECCIONES AL ENCARGO, verificadas contra código vivo:** (1) **`performPrivateReset` NO limpia el consent** — la línea citada pertenece a `exitYalaOnThisDevice`, doblemente gateada por flag + quiescencia; (2) **meter el clear en `performCloudSecureSignOut` (donde el encargo lo pedía) habría sido un bug NUEVO y peor que el gap** — ver la regla nueva de "Gotchas críticos": allí el modo persistido sigue siendo `.cloud` ⇒ rama `.cloudOutbox` ⇒ `.int(0)` al backend ⇒ borra por LWW el registro GDPR de una cuenta VIVA. ⇒ **el fix vive en el BOOT-HOOK**, entre `write(.icloud)` y `resetPrefs()`: rama `.icloudKeyValue` ⇒ local + iKV, **cero backend** (*el device olvida, la cuenta recuerda* — al re-entrar por adopt el pull de prefs lo devuelve sin volver a preguntar), y mata además la resurrección **CR-2** (el iKV era la única capa que ningún paso del wipe tocaba). **Decisión owner #1: gate por PRESENCIA de la key**, no por `signOutWipeIncludesGroups` — con el flag OFF las keys nunca se escriben ⇒ la closure JAMÁS se invoca ⇒ **byte-identidad literal**, y cubre el hueco que deja el marker si el flag se apaga en remoto entre el `register()` y el sign-out. Cubre **GRATIS** `closeLocalAfterAccountDeletionCloud` (mismo arm); `performSecondaryCloudSignOut` ya estaba cubierto por `SecondarySessionBoundaryPurge`; el hook solo-grupos **conserva su asimetría** (allí el consent se limpia in-session y el store personal sobrevive). **Decisión owner #2:** incluida la corrección del doc-comment stale. **2 ENDURECIMIENTOS del review (lección replicable): (a) el invariante load-bearing NO estaba aserido** — `#expect(read(defaults) == .icloud)` se evalúa TRAS el return y no dice nada del instante del clear ⇒ el modo se captura **DENTRO de la closure** (`modeAtClear`), único modo de cazar el mutante que mueve `write(.icloud)` por debajo; **(b)** la posición resultó load-bearing **en las dos direcciones**: también debe ir ANTES de `resetPrefs()`, porque el gate LEE la key que ese reset podría barrer — añadirla a `removeUserPreferenceKeys` es el movimiento "natural" y con el clear detrás el gate enmudecería en silencio (iKV sucio, gap reabierto). Tests +6 (4 comportamiento + 2 source-scan que **nombran la función culpable**) con **5 MUTANTES VERIFICADOS** (4/3/4/3/3 tests rojos; los dos últimos solo caen gracias al endurecimiento). Gates: builds ×2 sin warnings nuevos · **suite completa 4870/4870 parallel OFF** · swift-audit LIMPIO · validate-coverage OK. **Todo DARK tras `groupsBackendEnabled`** ⇒ cero impacto en producción hoy; el valor es cerrar el gap **antes del encendido** (gate §12 / Bloque C). **PENDIENTE owner: push** (3 commits locales: `9f16ce58`, `3d421f7d`, `bdbc46d1`).

- [2026-07-21] **chore(groups-backend): `transfer_group_ownership` (g10_01) PROMOVIDA A PROD — drift del chip D10 CERRADO** (prod `kefvaiymtgytemwbltlz`, versión `20260721212213`; método: verificación adversarial en workflow [5 lentes + refutación por hallazgo] → 4 decisiones owner → precheck → apply → post-check endurecido). Reduce fricción del encendido: era el último SQL de grupos con drift declarado. **md5 `dd3a049c793f6fe2479552ac0c7fba3f` idéntico staging↔prod** (paridad 33→**34/34** funciones). **3 LECCIONES REPLICABLES:** **(L1) el md5 registrado de una migración NO es el del archivo `.sql`, es el de `pg_get_functiondef`** — el archivo del repo da `c2c7845b…` y eso NO es una discrepancia; confundirlos hace abortar la promoción por un falso positivo. **(L2) `md5(pg_get_functiondef)` NO cubre `proowner` ni `proacl` ⇒ el gate de paridad histórico (33/33) tenía un hueco REAL:** en un SECURITY DEFINER cuyo dueño no fuera el de las tablas (o sin `rolbypassrls`), como las policies de `split_groups`/`group_members` están declaradas SOLO para `authenticated`, el `select` interno cae en default-deny → la RPC devuelve **`{already:true}` EN SILENCIO** con el md5 cuadrando perfecto. ⇒ **todo post-check de promoción exige la terna completa** (md5 · `proowner`/`prosecdef`/`proconfig` · EXECUTE anon=false + authenticated=true + `proacl` sin PUBLIC): 6/6 OK, molde en el README. **(L3) el archivo del repo NO siempre es lo aplicado:** staging recibió la variante con COMENTARIOS CONDENSADOS (2741 car. vs 5616 B) y `pg_get_functiondef` SÍ incluye los comentarios del cuerpo ⇒ aplicar el archivo a prod habría ROTO la paridad. Se promovió el texto byte-exacto del historial de staging (método gate §12), verificando que el código es idéntico por **normalización independiente en ambos lados** (quitar `--`, colapsar whitespace → `5ac870418e96cfdd80ba024ad35c8c18`) y transcripción **autoverificada por md5** (`ca0c8414…`). Decisión owner: paridad primero; si algún día se quiere archivo == DB, el molde es `g6_01b_verbatim_comments` en AMBOS envs (y entonces el md5 contractual cambia en sus 4 registros). Precheck 39/39 (18 columnas con tipo Y nullability · `server_hlc`/`auth.uid`/`stamp_group_seq` · 3 tablas · 2 triggers · 2 FKs · `owner=postgres` y FORCE RLS apagado · `rolbypassrls` · `pgrst_ddl_watch` activo · función AUSENTE ⇒ aditivo puro). Advisors: sin clase nueva. **⚠️ PENDIENTE OWNER, BLOQUEA EL ENCENDIDO (NO el SQL): el Worker `yala-gateway-production` es del 2026-07-16, ANTERIOR al commit `a9ed3785` que metió la RPC en la `PARAM_ALLOWLIST` ⇒ prod la rechaza con 404 `unknown rpc` ANTES de PostgREST.** Impacto hoy CERO (doble-dark), pero el cliente mapea ese 404 a `.transient` → **reintento perpetuo sin TTL** si se enciende el flag con el Worker stale ⇒ redeploy del gateway a prod obligatorio antes de D9 (arrastra todo lo acumulado en `gateway/` desde julio 16 → merece review propio). **Residuales anotados (no bloquean):** sin `for update` entre el select del heredero y el update (misma ventana que `groups_forget_user` loop1, YA viva en prod — no es clase nueva); los goldens G10 ya estaban ACTIVOS y corren solo contra staging con URL hard-codeada ⇒ **no verifican prod** (lo hacen el precheck/post-check). Docs: README §g10 reescrita + addendum en el bloque del gate §12 (el «33/33 — DRIFT CERRADO» es foto del 2026-07-16, no se falsifica), texto stale de goldens corregido en README y en el `.ts`, coverage-index y vault.

- [2026-07-21] **fix(notifications): el dedup de recordatorios ya no sobrevive al sign-out ni colisiona por nombre de cuenta (`9f16ce58`, branch `2.0.5`)** — encargo APARTE destapado por el review adversarial de §5.2.1, **no regresión de aquel fix**. Tres defectos preexistentes en escalera, con **2 lecciones replicables**: **(L1) una lista EXPLÍCITA de keys NO puede cubrir keys generadas dinámicamente** — `DataWipeService.removeUserPreferenceKeys` enumera keys una a una, pero `creditCardNotif_`/`scheduledPaymentNotif_`/`budgetAlert_` se construyen con UUID + fecha ⇒ sobrevivían al «Vaciar mis datos» Y al boot-cleanup del sign-out. **Toda key nueva de UserDefaults con parte variable exige (a) barrido POR PREFIJO en ese reset y (b) su prefijo expuesto como `static let` en el tipo dueño** (aquí `ScheduledPaymentNotificationTracker.{keyPrefix,creditCardKeyPrefix}` y `BudgetAlertTracker.keyPrefix`) — literales duplicados en el wipe se desincronizan en silencio. El barrido **enumera prefijos concretos y JAMÁS uno genérico**: `GroupNotifications.lastNotified.*` y `groupPrefs_*` quedan EXCLUIDOS a propósito (el dominio Grupos sobrevive el wipe por diseño; barrerlos duplicaría una notificación legítima), invariante anotada en el doc-comment de las exclusiones. **(L2) llavear un dedup por el NOMBRE de una entidad es un bug latente, no solo un problema de frontera de cuenta**: `creditCardNotif_<account.name>_<fecha>` hacía que dos tarjetas **homónimas** con el mismo día de pago compartieran entrada (**solo una notificaba**) y que **renombrar** la cuenta re-disparara el aviso el mismo día — además de que una entrada de la cuenta anterior **SILENCIABA** el recordatorio de la entrante tras el sign-out. Ahora se llavea por `Account.shortcutID` (UUID de identidad `.preserveValueOnDeletion`, misma forma que el `paymentID` del tracker hermano) y **la key se construye DENTRO del tracker** (`hasNotifiedCreditCard`/`markNotifiedCreditCard`), no inline en el servicio. Tercer defecto: `cleanupOldEntries` filtraba solo por `scheduledPaymentNotif_` ⇒ las de tarjeta **crecían sin límite**; ahora cubre ambos prefijos y en el de tarjeta **lee la fecha por el ÚLTIMO componente**, lo que caduca también el formato legacy por nombre (incl. nombres con «_», que por posición fija habrían quedado huérfanos permanentes) **sin migración one-shot**. **Residual asumido:** la primera ejecución tras actualizar puede repetir UNA notificación de tarjeta, solo si el día de pago cae exactamente ese día y ya se había avisado con la key vieja. Tests +6 con **mutante verificado en las TRES capas**. Gates: builds ×2 · **suite completa 4864/4864 parallel OFF** · validate-coverage OK. **GOTCHA DE PROCESO (sesiones concurrentes sobre el mismo repo):** otra sesión trabajaba en paralelo ⇒ (a) sus builds bloquearon la build DB tres veces (`database is locked`) — la salida es correr con **`-derivedDataPath` propio**, nunca matar su build; (b) `qa/coverage-index.json` es un SSOT compartido y **lo commitea quien llegue primero**: mi edición del área `session-sign-out` acabó absorbida en su commit `d33bc518` (contenido correcto, atribución no — no se reescribió historia ajena).

- [2026-07-21] **fix(session): §5.2.1 CERRADO — notificaciones fantasma post-sign-out `.cloud` (`1e11f540`, branch `2.0.5`)** (método: workflow de comprensión [8 mapeadores + síntesis] → 3 decisiones owner vía AskUserQuestion → impl → workflow de review adversarial [6 lentes + refutación por hallazgo: **11 confirmados / 22 refutados**] → gates). El boot-cleanup borraba los ARCHIVOS de los stores pero no tocaba `UNUserNotificationCenter`. **La premisa del estudio era demasiado benigna: no es «hasta que los reconcilers re-programen» — NO HAY QUIEN LOS MATE** (`ensureNotificationsScheduled` solo REPROGRAMA y su guard exige `!activeItems.isEmpty`, falso post-wipe ⇒ los `repeats: true` de la cuenta saliente sobreviven **indefinidamente**). **3 correcciones al encargo, verificadas contra código vivo:** (1) **`performSecondaryWipeIfArmed` NO cancelaba** (lo hace la ENTRADA, no la SALIDA) y ahí el daño es **doble** — como al dueño se le cancelaron todas al entrar la invitada, si ella dejó tantas como él la heurística `pending.count < activeItems.count` resulta FALSA y **las de él jamás se restauran**; cancelar deja `pending == 0`, justo la condición que dispara la reprogramación; (2) **`performGroupsOnlySignOutWipeIfArmed` NO debe cancelar pending** — ahí el store personal SOBREVIVE y es dueño de todas las programadas: sería el **bug inverso** (asimetría documentada en el SUT + **pinneada por source-scan** para que nadie la «arregle» por simetría ciega); solo retira las ENTREGADAS de grupos (filtro `userInfo["deepLink"]` prefijo `groups/`); (3) el fix NO va dentro de `resetForSignOutWipe` pese a que el bug la nombraba (como closure propia del hook queda asertable; el NotificationCenter no es prefs/caché/archivo). **Decisiones owner:** incluir salida M1 · delivered selectivo de grupos · capa in-session además del boot-hook. **Impl:** `performSignOutWipeIfArmed` gana variante inyectable (`resetPrefs` es seam OBLIGATORIO — toca `ProfileImageStorage`/`AppRouter`/`ProTourManager`/`SetupChecklistManager`/`WidgetDataCache`/`Tips` y barre `.standard`) y retira pending+delivered **DESPUÉS del guard abort-S3** (si el archivo BASE no se borró el store sigue vivo ⇒ sus recordatorios son válidos) y del reset de prefs; arm limpiado AL FINAL. Barrido TOTAL (ningún emisor es de ámbito DEVICE). `clearDeliveredNotifications()` **separado** de `cancelAllNotifications()` A PROPÓSITO: aquel corre dentro de `rescheduleAllNotifications` ⇒ en cada toggle de Ajustes y cada foreground; fusionarlos borraría el historial entregado del usuario. **2 ENDURECIMIENTOS LOAD-BEARING del review adversarial (lección replicable):** (a) **la capa in-session por sí sola era ANULABLE — un guard en el PRODUCTOR no sirve contra `Task` no estructurados**: `AppBootstrapper.handleBecameActive` lanza `Task { ensureNotificationsScheduled; sendDueReports }` con decenas de puntos de suspensión y **su guard de arm se evalúa AL LANZARLO**; un background→active durante el push-all (fase `.working`, aún sin arm — son segundos de red) deja la tarea suspendida y al reanudar —ya armado— reprograma y puede **ENTREGAR un banner con montos de la cuenta saliente sobre el cover de relanzamiento** (agravante: el propio cancel deja `pending == 0`, que vuelve VERDADERA la heurística del reconciler) → fix **en el CHOKE POINT**: guard `NotificationService.isPersonalWipeArmed` evaluado **en el instante del `add`** en `sendNotification` Y `scheduleNotification` (cubre cualquier productor presente o futuro sin rastrear Tasks; NO incluye `groupsOnlyWipeArmed`); (b) el clear de entregadas de grupos pasa a **in-session** en `finalizeGroupsOnlyClose` (donde hay `await`) — en el boot-hook solo cabía un `Task` que el arm, limpiado sincrónicamente a continuación, deja de respaldar (única ventana que el próximo boot no reparaba). **Suite source-scan `SignOutNotificationWiringTests`** (molde `GroupsSyncHardeningTests.signOut_allThreePaths_wireGroupsTeardown`): **los wrappers de producción abren con `guard !isRunningTests` ⇒ son inejecutables en unit test POR CONSTRUCCIÓN**, así que vaciar una closure `cancelNotifications:` reintroducía el bug ÍNTEGRO con todo verde; pinnea el cableado en AMBAS direcciones. **Mutante verificado en las dos capas.** Gates: builds ×2 sin warnings nuevos · **suite completa 4856/4856 parallel OFF** (+15) · validate-coverage OK (4 áreas; `session-sign-out` **gana el glob `SwiftDataConfiguration.swift`**, antes ciego al drift). **Residuales inline:** la capa in-session es best-effort (el abort S3 es el único camino en que esas notifs vuelven a ser válidas; el reintento del wipe las recancela — la red kill-safe es el boot-hook); los ENTREGADOS de grupos se retiran también en el wipe personal con flag OFF a propósito (el camino devuelve el device a «recién instalado»). **Encargado aparte (preexistente, otras superficies, destapado por el review):** claves de dedup `creditCardNotif_`/`scheduledPaymentNotif_`/`budgetAlert_` que sobreviven al wipe —`creditCardNotif_` se llavea por **nombre de cuenta** ⇒ puede **silenciar el aviso de la cuenta entrante**—; colas App Group (Apple Pay/Siri/imágenes) que se materializan como `InboxDraft` en la cuenta siguiente; `WidgetDataCache` pintando saldos de la sesión cerrada hasta el próximo LANZAMIENTO. **PENDIENTE owner: push (2 commits locales) + device-QA del camino `.cloud` con sesión backend real** (DARK en sim; el botón DEBUG «Arm sign-out wipe» cubre el boot-cleanup, pero la capa in-session exige el flujo completo).

- [2026-07-21] **fix(l10n): los aliases catch-all `pt` y `es` RESTAURADOS como copia de su base (`9dd9fcd3`) + causa mecánica cerrada en el tooling + test que lo enforcea** (método: workflow de 3 sondas [resolución de bundle · cuantificación · historia/contrato] → síntesis → fix en 3 capas). **`pt.lproj` había derivado a portugués EUROPEO en 272 de 3925 keys (+1 en `.stringsdict`) entre 2026-04-30 y 2026-07-21**, violando un contrato declarado TRIPLICADO: `SupportedLocale.swift:19` («Alias catch-all — copia idéntica de pt-BR»), `L10N.md`, y los commits del split (M7 `d868009d`, M9 `4fe8abcc`). **NO era teórico:** iOS entrega el idioma preferido CON región y CFBundle solo enruta a `pt-BR`/`pt-PT` cuando la región está en su cadena CLDR ⇒ **`pt.lproj` sirve a 46 de 59 regiones probadas** (medido con `Bundle.preferredLocalizations` contra las 16 localizaciones del `.app` real) — toda la **diáspora** lusófona con región no-BR (US/JP/DE/PE/MX…), que veía un **híbrido** (244 «você» junto a 224 «tu/teu/tua», a veces en la misma pantalla). `pt` tampoco era pt-PT (476 divergencias) — peor que cualquiera de las dos opciones puras. `es` solo divergía en 7 keys (moneda de los ejemplos: `S/12`→`$12`). **CAUSA MECÁNICA (lección replicable): `add-l10n-key.sh` tenía rama de alias para `es` (valor real ⇒ idéntico sin intervención) pero NINGUNA para `pt`, que caía al `else` y recibía `[NEEDS_TRANSLATION]`; como `noNeedsTranslationMarker_anywhere` BLOQUEA el commit, el script convertía a `pt` de facto en un locale a traducir y quien lo rellenaba escribía registro europeo — desde el primer día (`b996be17`, 3 días post-split). Ningún test comparaba VALORES entre locales ⇒ entró y creció en silencio 3 meses.** **Fix en 3 capas:** datos (copia byte-a-byte `pt`←`pt-BR`, `es`←`es-419`, `.strings`+`.stringsdict`; cero pérdida verificada — 0 keys exclusivas del alias) · tooling (`es`/`pt` FUERA de `BASE_LOCALES`, regenerados por copia idempotente al final del script ⇒ **son artefactos GENERADOS, `cmp -s` es el guard trivial**) · test `aliases_haveIdenticalValues_toTheirBase` **verificado que CAZA la regresión** (inyectada a propósito antes de commitear). `L10N.md` corregido: su justificación era **factualmente falsa** (decía que `pt` servía a pt-MZ/pt-AO y `es` a es-CR/es-EC — medido: esos resuelven a `pt-PT` y `es-419`; la audiencia real es la contraria, la diáspora). `pt` NO se elimina pese a ser redundante (tocar el case `.pt`/`bestMatch`/tests de drift = más superficie, cero beneficio); `es` NO puede eliminarse (sin él, 36 regiones caerían a `es-ES` peninsular). Widgets ya estaban en paridad — intactos. Gates: build · **suite 4841/4841 parallel OFF** · batería l10n 51/51 · script probado e2e con key de sonda.

- [2026-07-21] **chore(qa): XcodeBuildMCP con `ui-automation` HABILITADO — el QA visual agentic vuelve a estar disponible** (`~/.claude.json` → `mcpServers.XcodeBuildMCP.env.XCODEBUILDMCP_ENABLED_WORKFLOWS = `session-management,simulator,ui-automation,project-discovery,utilities,coverage`; backup en `~/.claude.json.bak-d3-*`; **requiere reiniciar la sesión para tomar efecto**). El servidor registraba solo **24 tools** (`session-management,simulator`) ⇒ `snapshot_ui`/`screenshot` funcionaban pero **no existía `tap`/`swipe`/`type_text`/`wait_for_ui`**. Con el cambio: **37 tools**. **NO confundir con el symlink de SimulatorKit** (`DEVICE-QA.md` §1): ese aplica solo a **Xcode-beta** y se manifiesta con `snapshot_ui` FALLANDO; aquí `xcode-select` es Xcode **release** y `snapshot_ui` funcionaba — el diagnóstico correcto es «¿cuántos tools registró el servidor?». **AXe no hay que instalarlo** (bundled en el paquete + Homebrew). Deliberadamente fuera: `device`/`macos`/`debugging`/`swift-package`/`project-scaffolding`/`xcode-ide` (no aplican a Yala e inflan el prompt). Sección nueva en `DEVICE-QA.md` con el diagnóstico diferencial. **Alternativa documentada y ya usada en D3** (sirve además como evidencia reproducible en CI): capturas desde XCUITest vía `XCTAttachment(screenshot:)` + `lifetime = .keepAlways` + `-resultBundlePath` + `xcrun xcresulttool export attachments` — molde vivo en `GroupBatchLeaveUITests.attachScreenshot`.

- [2026-07-21] **fix(groups): D3 CERRADO — botón «Detener» del batch «salir de todos mis grupos» (branch `2.0.5`; método: workflow de comprensión [7 mapeadores + síntesis] → decisión owner vía AskUserQuestion → impl → workflow de traducción [14 traductores nativos + auditor adversarial] → gates + QA visual).** El diferido D3 del batch D10 quedaba sin decidir; **el análisis corrigió la premisa de «no hace falta»: matar la app NO detenía el batch, lo POSPONÍA** (`AppBootstrapper.swift:443` + `ContentView.swift:556` llaman `resume(.boot/.foreground)` y lo relanzan solos) ⇒ un batch lanzado por error era imparable, con cada iteración irreversible (salir de un grupo exige re-invitación). **Decisión owner: opción (a) semántica segura.** **Cancelación COOPERATIVA entre grupos** — el guard vive al tope del `for` de `drive`, junto al gate de quiescencia que ya estaba ahí (cero refactor); **JAMÁS interrumpe una operación en vuelo** (transfer/leave son server-first: cortar el `await` dejaría un ack perdido post-COMMIT). `requestStop()` es **WRITE-AHEAD**: persiste el marcador + retira las `.pending` (`stopPending`) ANTES de nada ⇒ el resume no re-arma lo detenido; relanzar desde «Vaciar» sí (`replaceAll` limpia marcadores). **INVARIANTE que obligó al diseño Y al copy: las `.inProgress` SOBREVIVEN a la parada** (su RPC pudo haber salido server-side; el resume debe completarlas) ⇒ **residual honesto: tras detener, un grupo más puede terminar de salir** — por eso el copy dice «no seguiremos con los que faltan», nunca «cancelado»; corolario: `acknowledge()` pasa de `clearAll()` a **`clearFinished()`** (conserva las en vuelo; byte-equivalente sin parada). **BUG DE UX destapado y cerrado de paso:** con `.inProgress` diferidas por red, `isComplete` NUNCA se cumplía ⇒ **la vista quedaba clavada en el spinner sin ningún botón** (único escape: swipe-down) → `isComplete` gana la rama `wasStopped`, y `total` se recalcula como `entries + skipped` para no mentir el «3 / 3» de un batch de 11. Botón `batch_leave_stop` **directo, sin diálogo** (una presentación en ese anchor es la clase de trampa toolbar-muerta); headline propia que MANDA sobre allDone/someNeedDecision + fila «Sin procesar». **Sin `context.save()` en el camino de parada ⇒ quiescencia intacta.** Canario `groupBatchLeaveStopped`. 4 keys l10n ×16 (traducción nativa por workflow; el auditor cazó 2 defectos reales — tiempo verbal de `en` desalineado de sus hermanas, override injustificado en `en-GB` — más un carácter japonés colado en `zh-Hans`). **Gates:** builds ×2 · **suite completa 4840/4840 parallel OFF** · OrchestratorTests +5 (corte entre grupos · detenido-no-resume · `.inProgress` preservada y completada · relanzar limpia marcadores · acknowledge conserva no-terminales) · **XCUITest 3/3** (nuevo, seam `-uitest-groups-batch-running`: siembra el punto de partida, el tap recorre la mecánica REAL) · validate-coverage OK. **QA VISUAL VERDE por vía nueva reutilizable:** capturas extraídas del `.xcresult` vía `XCTAttachment` — los tools de interacción de XcodeBuildMCP (`tap`/`swipe`) NO están habilitados en esta sesión (config de workflows del MCP, NO el symlink de `DEVICE-QA.md:5`, que aplica solo a Xcode-beta; aquí `xcode-select` es Xcode release y `snapshot_ui` sí funciona). **Hallazgo l10n preexistente NO corregido (fuera del delta):** la invariante «`pt` = copia exacta de `pt-BR`» está ROTA a escala de archivo (594 líneas divergentes en HEAD; 8 de las 12 hermanas de `batchLeave` ya diferían — `pt` mezclado, con ese bloque íntegramente en portugués europeo). `LocalizationParityTests` no lo caza (valida presencia/placeholders/duplicados, jamás igualdad de VALORES entre alias) → sesión aparte, o actualizar `L10N.md` si es intencional. **Pendiente owner:** device-QA del flujo ENABLED con backend real (la ejecución de leave/transfer sigue siendo imposible en sim).

- [2026-07-21] **feat(settings): pantalla «Tu cuenta de Yala» (§3.3.5) + reorganización de Ajustes (§3.2) — 2 commits `41ddfd0f`+`d25198fe` (branch `2.0.5`)** (del estudio `MODO-NUBE-GESTION-DATOS-UX.md`; método exploración [4 archivos centrales + workflow 6 investigadores] → plan → /review-plan [NECESITA AJUSTES, 5 folds F1-F6] → 2 decisiones owner vía AskUserQuestion → impl 2 commits → QA visual sim VERDE). **HALLAZGO clave: H4 (fusión Apple↔Google del mismo email) NO es detectable client-side** — `storedProvider()` solo devuelve el ÚLTIMO método usado, `SIWARefreshTokenStore` guarda `appleUserID` (no el sub) y `GoogleUserPairStore` el `sub`, sin accessor combinado, y el cliente es mono-provider por diseño (regla anti-sangrado cross-provider) ⇒ **Q1 owner: nota general de vínculo SIEMPRE** («Si usas el mismo correo, Apple y Google abren esta misma cuenta»), no condicional a una fusión indetectable. **Commit 1 (pantalla, DARK — `hasSession` imposible en prod):** `YalaAccountView` (navegada, `.subtle`, tokens DS, `#Preview` cloud/groupsOnly) + `YalaAccountLogic` (pura, tabla provider×storageMode×canDeleteAccount) — mapa/explainer del enlace privado↔nube: método (`storedProvider()` leído VIVO, lección I4) + nota general + «dónde viven los datos» (dinámico `.cloud`/solo-grupos) + 3 desenlaces (Cerrar sesión / Volver a iCloud [solo `.cloud`] / Eliminar cuenta [**F1: gate `AccountDeletionRowLogic`, MISMO que la fila de Seguridad** — sin él un group-invite+sesión vería «Eliminar» en la pantalla pero no como fila]) que **enlazan a los flujos EXISTENTES — cero mecánica nueva — vía closures a ProfileView (DUEÑO ÚNICO de hojas/observers/cover-root; plumbing opción (a), re-hostear observers de `.phase` violaría «dos anchors ante el mismo observable», bug device 2026-07-14)**; NavigationLink a `.storageMode` para la Reversa. Reemplaza el letrero mudo `groupsAccountRow` (keys `groupsAccountRow*` retiradas de los 16 `.strings`) por fila navegable `profile_yala_account`. `StorageSettingsView.revertCard` gana `storage.revert.desenlaceNote` (la Reversa ES el desenlace funcional). **Commit 2 (reorg, VIVO+DARK):** DATOS en escalera de gravedad (Exportar → Importar → «Dónde viven tus datos» [`storage.title` renombrada de «Almacenamiento» ×16 + subtítulo dinámico `dataLocationSubtitle{ICloud,Cloud}`; gate `StorageRowGateLogic` intacto] → Vaciar; cambio VIVO intencional) + subsección «Tu cuenta» (`settings.accountSubsectionTitle`) en Seguridad **CONDICIONAL a `showsYalaAccountRow` (sesión)** ⇒ **byte-idéntico para VIVO sin sesión** (nada se inserta; las filas de sign-out/eliminar-cuenta NO se mueven → `profile_security_signout`/`exit_yala`/`delete_account` intactos — Q2 owner «filas en Seguridad + mapa», mínimo riesgo). **2 decisiones owner (AskUserQuestion):** Q1 nota general siempre; Q2 dual-surface (no mover D2/D6). Nota de sign-in (item 4) OMITIDA (BRAND-VOICE — la pantalla ya la cubre; `WelcomeCloudSignInView`/`StorageSignInChooserView` intactos). Título de sección ya era «Seguridad y cuenta» (sin rename). 18 keys l10n ×16 (voseo es-AR; traducción nativa por 2 workflows adversariales por idioma — 12 locales cada uno). Seam QA `-uitest-fake-backend-session` (D5, inerte en release). Gates: build Yala+Yala Dev sin warnings nuevos · **YalaAccountLogicTests 7/7 + LocalizationParityTests 10/10** (parallel OFF) · build-for-testing XCUITest `YalaAccountUITests` (2 casos: fila+pantalla con seam / ausencia sin seam; runner flaky local → CI) · validate-coverage OK (áreas `settings-profile-general` +globs+XCUITest, `cloud-migration-ui` +revert/storage.title) · **QA VISUAL sim VERDE end-to-end** (Yala Dev, iPhone 17 Pro, es-419, seam: DATOS reordenado + «Dónde viven tus datos» [`iCloud privado`] + subsección «Tu cuenta» → pantalla con «Iniciaste sesión con Apple» + nota de vínculo + ubicación solo-grupos + desenlaces Cerrar sesión/Eliminar cuenta [sin «Volver a iCloud» en `.icloud`] → «Cerrar sesión» dispara la hoja de alcance `.signOutPrivate` race-free y cancela). **Diferido:** «Borrar mi copia antigua en iCloud» (§3.3.5 sub-pieza separable, mecánica CloudKit nueva — fase 4). **Pendiente owner: push (2 commits locales) + device-QA del flujo con sesión backend real (todo DARK en sim — cubierto por unit+XCUITest+#Preview).**

- [2026-07-20] **feat(D1): retención «Seguir con mis grupos» tras vaciar IMPLEMENTADA — 4 commits `471e9f3d`+`3ce48c94`+`ddc021f8`+`773ad255` (branch `2.0.5`)** (del estudio `MODO-NUBE-GESTION-DATOS-UX.md` §3.3.2/§2.2, D1 ratificada; método plan → /review-plan [NECESITA AJUSTES, 4 fixes folded: bug reset-on-absent stale + 3 precisiones de orden] → 2 decisiones owner vía AskUserQuestion → impl 3 piezas → review adversarial Opus [2 serios verificados: 1 UNCERTAIN gap-latente cerrado + 1 REFUTED] → gates + QA visual sim). Tras «Vaciar mis datos» CON grupos vivos, pantalla de éxito (fullScreenCover raíz) con bifurcación **«Solo mis grupos»** (shell reducida a Grupos) / **«Empezar de cero»** (Welcome = comportamiento actual). Cierra el momento de retención que hoy se pierde (app vacía → desinstalar). **Mecanismo: flag de PRESENTACIÓN nuevo `usageFocus: full | groupsOnly` en `AppPreferences`** (LWW simple synced — `PrefSyncKey.stringGuardNonEmpty`, NUNCA never-downgrade; **reset-on-absent en `loadFromDefaults`** para no dejar `.groupsOnly` stale tras el wipe — el observer de `didChangeNotification` lo revierte a `.full` in-session). **NO toca `onboardingMode`** (un `.groupsOnly` sigue `onboardingMode==.full` — VETADO reusar `.groupInvite`/degradar onboardingMode: merge never-downgrade + oculta salidas). Derivador ÚNICO puro **`ShellModeLogic.effective(onboardingMode:usageFocus:)`** (`.groupsFocused` si groupInvite O groupsOnly) + `SessionState.effectiveShellMode`/`isGroupsFocusedShell` (point-read imperativo); **VISTAS reactivas leen `appPreferences.usageFocus`**. **Byte-identidad total con `usageFocus==.full`** (`effectiveShellMode==.groupsFocused ⟺ isGroupInviteMode`). **2 decisiones owner (AskUserQuestion):** **Q1** «Activar Yala completo» = **re-onboarding guiado** (reusa `FullModeActivationView`; único cambio: `completeFullActivation` resetea `usageFocus=.full` ANTES de `selectMainTab(.panel)` para des-reducir — se descartó el «un tap» literal del brief: el usuario ya vació y necesita re-armar finanzas); **Q2** alcance = **tab bar + secciones personales** (MoreView CTA en vez de dashboard vacío; ProfileView oculta «Organización»; **CONSERVA sesión/cuenta/export/preferencias** — automático: un groupsOnly es `onboardingMode==.full`, esos gates `isGroupInviteMode` ya las muestran; los gates de behavior/bridge NO se tocan — un groupsOnly TIENE store personal). **Presentación (zona toolbar-muerta):** cover DUEÑO-ÚNICO en ContentView; `@State showGroupsRetentionCover` (red visual) driven por `syncGroupsRetentionCover()` desde onChange de `groupsRetentionPending`/`isWipingData` (**un `Binding` computado sobre el singleton NO re-evalúa la presentación** — lección: el patrón del inbox-cover funciona porque lee un `@State` body-tracked, no un singleton); acción en `onDismiss` (cover fuera); gate del `onChange(hasCompletedOnboarding)` por `groupsRetentionPending` **bloquea el ruteo automático a Welcome**; blocker `groupsRetention` en `ShellReadinessState` tras `wipingData`; re-arm en `case nil`/scenePhase `.active` contra teardown externo (endurecimiento del review — gap latente, no alcanzable hoy). Con deudas se destaca «Solo mis grupos» (no dark pattern). **l10n:** 7 keys `settings.retention*` ×16 (voseo es-AR; traducción nativa por workflow). Seam DEBUG `-uitest-retention-demo` + reset de `usageFocus` en `-uitest-reset`. Gates: build Yala+Yala Dev · **suite completa 4828/4828 parallel OFF** · ShellModeLogicTests+TabBarConfigurationSecondaryTests+ContentViewReadinessLogicTests+AppPreferencesTests+PreferenceMergeLogicTests(taxonomy 38→39)+LocalizationParityTests · validate-coverage OK · XCUITest `GroupsRetentionUITests` · **QA VISUAL sim VERDE end-to-end** (seed `grupos`, wipe REAL): retención intercepta (gate bloquea Welcome) → «Solo mis grupos» reduce a Grupos → Perfil «Activar Yala completo» + Organización oculta → guiado abre; «Empezar de cero» → Welcome. **GOTCHA del entorno:** `xcodebuild build` (Bash) escribe a `~/Library/Developer/Xcode/DerivedData` PERO `get_sim_app_path`/`install_app_sim` de XcodeBuildMCP usan `~/Library/Developer/XcodeBuildMCP/workspaces/.../DerivedData` — instalar del path equivocado corre una app STALE (perdí ~1h con el cover «no presentando» que era la build vieja); usar `build_sim` de XcodeBuildMCP para QA visual, o instalar del DerivedData correcto. **Residual documentado:** el offer (`groupsRetentionPending`, in-memory) no persiste — kill en la ventana → Welcome al próximo launch (default seguro). **Pendiente owner:** push (4 commits locales) + device-QA (multi-device: `usageFocus` LWW sincroniza → device B también se reduce). **Diferido:** sign-in solo-grupos (R9/c) y variante `.cloud` (DARK).

- [2026-07-20] **feat(D10): batch «salir de todos mis grupos» en Vaciar IMPLEMENTADO — 3 commits `a9ed3785`+`2f8378ba`+`9392f4e1` (branch `2.0.5`)** (del estudio `MODO-NUBE-GESTION-DATOS-UX.md` §2.1 actualizado + D10; método exploración [workflow 8 clústeres] → 2 decisiones owner vía AskUserQuestion → plan → /review-plan [NECESITA AJUSTES, 5 problemas + 2 diferidos folded] → impl 3 piezas → 3 agentes diseño/red-team → QA). Ofrece, dentro de «Vaciar mis datos» (fila 👥 sin deudas), **«También salir de mis grupos»**: orquesta primitivas SEGURAS por grupo — **jamás wipe de filas `Split*`**. Todo **DARK** tras `groupsBackendEnabled`; corre ANTES del wipe pero es **paso separado (NO lo dispara)**. **2 decisiones owner (2026-07-20):** **A1** — grupo CloudKit-legacy owner+co-members cae DIRECTO a «necesitan tu decisión» **sin migrar** (supersede el fallback ratificado: confirmé en `g6_01`/`g7_02` que `migrate_group` deja co-members `user_id NULL` → transfer no halla heredero Y migrar dispara «Moved» a terceros; no vale la disrupción); **B1** — flujo dedicado headless, no encadena el vaciado. **Clasificación pura** (`GroupBatchLeaveLogic`, tabla): member→`leaveGroup` · owner-solo→`softDelete` · owner+comembers-backend-con-heredero→RPC nueva `transfer_group_ownership`+leave · resto→`.needsDecision`. Herederos/deuda EN MEMORIA (jamás `#Predicate` sobre `userID` opcional). **Piezas:** (1) RPC `transfer_group_ownership` (heredero verbatim de `groups_forget_user`, JAMÁS tombstonea, idempotente-suave; aplicada a staging md5 `dd3a049c…` y **promovida a prod el 2026-07-21 con el mismo md5** (ver entrada propia abajo); gateway allowlist + cliente + goldens G10); (2) orquestador kill-safe (`GroupBatchLeaveStore` sin cap + TTL fase-aware; `GroupBatchLeaveOrchestrator` write-ahead + resume boot/foreground + gate quiescencia por-grupo + guard re-entrancy; `GroupBatchLeaveTracker` @Observable; `GroupService.transferOwnershipThenLeave/batchLeave` inline el backend-leave [esquiva el guard `!isOwner` — `isOwner` NO se emite] tolerante a `member_not_found` en resume); (3) UI `SecondaryKind.leaveAllGroups` + `GroupBatchLeaveView` (resultado honesto N/M/K + lista «necesitan tu decisión» + «Ver mis grupos») + `AccountDeletionGroupsSummary.hasGroups` (aditivo). **l10n:** 13 keys ×16 (voseo es-AR; **revisión nativa adversarial por idioma vía workflow — 16 correcciones aplicadas**). Gates: build Yala+Yala Dev ✅ · unit GroupBatchLeaveLogicTests(8)+OrchestratorTests(5, kill-sim)+DestructiveScopeLogicTests+LocalizationParity+AccountDeletion*(55) ✅ · **XCUITest `GroupBatchLeaveUITests` 2/2 ✅** (offerAndResultFlow + notOffered-flag-OFF; el 1er run flakeó por arranque-en-frío del runner, pasó warm) · **QA VISUAL sim VERDE** (Yala Dev, iPhone 17 Pro, es-419, seam DEBUG `-uitest-groups-batch-demo`): oferta sin deuda → vista batch resultado (Salidos 2·Transferidos 1·«Amigos del gym» necesita decisión) → «Listo» cierra; **gate de deuda validado** (con deuda muestra «Ver mis grupos», no la oferta) · validate-coverage OK (4 áreas). **Hazards del review-plan resueltos:** transfer→leave, `member_not_found` en resume, re-entrancy, quiescencia pre-grupo, `#Predicate`/opcional, deuda-sobrevenida re-chequeada, archivados incluidos (`!isHiddenForAll`), toolbar-muerta (batch headless). **Diferidos SIN decidir del owner:** D1 (XCUITest — HECHO igual), D3 (botón «Detener» del batch — no incluido). **Pendiente owner:** device-QA del flujo ENABLED con backend real (leave/transfer/migrate imposibles en sim — cubierto por unit+device-QA-demo+breadcrumbs). ~~re-aplicar `g10_01` a prod en D9~~ **HECHO 2026-07-21** (ver entrada propia). **Push:** los 3 commits (+ este docs) pusheados.

- [2026-07-20] **feat(settings): Vaciar v2 sobre la hoja de alcance IMPLEMENTADO — fila 👥 accionable + «Exportar antes» + C4** (branch `2.0.5`, commit `d5f76648`; del estudio `MODO-NUBE-GESTION-DATOS-UX.md` §3.3.1, decisiones D9/D10 ratificadas; método plan → /review-plan [APROBADO, riesgos R1-R3 folded, D2 documentar + D4 Preview incluidos por el owner] → impl → review adversarial Swift [APROBAR, 0 bugs de corrección] → QA visual sim VERDE). Construye SOBRE el componente D4; la mecánica `wipeAllUserData` NO cambia (capa de presentación + detección read-only). **Piezas:** (1) detección al TAP de Vaciar (`SplitSyncManager.accountDeletionGroupsSummary`, molde D5, **CERO saves** — quiescencia (b); **primer uso VIVO en `.icloud`**, confirmado en QA); (2) fila 👥 accionable — con deuda: `wipeScopeGroupsDebt` + botón «Ver mis grupos» (id `wipe_view_groups` → `SessionState.selectMainTab(.groups)` + **cierra Ajustes vía `onRequestCloseSettings`**, renombre de `onUserDataWiped` — `UserDataResetView` es PUSHED, su `dismiss` solo popearía a Profile [B1 del review adversarial]); sin deuda: «No se tocan»; el batch D10 «salir de todos» queda como SEAM documentado (OTRO chip); (3) botón «Exportar antes» (id `wipe_export_before` → sheet `ExportFiltersStepView` autocontenido, regresa al flujo; caption «única copia» en `.cloud`) — implementa la decisión §m.4 del owner (2026-07-03) nunca hecha; (4) **C4** `DestructiveScopeLogic.wipeOperation(isGroupInviteMode:)` — un solo input: 5b (onboarding completed + sesión backend solo-grupos) ve el copy personal completo con su fila 👥; la sesión NO baja el scope (documentado, **sin dead param** — el review descartó `hasLiveBackendSession`); (5) **D9** (residual multi-device en `.cloud`) ya venía de D4, verificado. **Estructura:** `Model.hasSecondaryAction` → `secondaryActions:[SecondaryKind{viewGroups,exportBefore}]` (conjunto **y orden** en la lógica pura, no en la factory); `SecondaryAction` gana `caption`; `onDismiss` capture-all→reset-all→act (3 flags mutuamente excluyentes, anti-carrera toolbar-muerta). `deleteAccount` preserva ids `delete_account_view_groups`/`delete_account_continue` (`DeleteAccountDialogUITests` INTACTO; `ProfileView` solo cambia el rename del callback). **l10n:** 3 keys ×16 (voseo es-AR; revisión nativa adversarial por idioma vía workflow — «Yala»/«iCloud» verbatim, «pestaña Grupos» calcada). **Punto 6 (gate QA, NO bloqueante):** `WipeMassRowsCloudDrainTests` caracteriza el wipe masivo por filas en `.cloud` (container ON-DISK 3 stores + **drain intermedio load-bearing** o serían identity-gaps): **~1:1 tombstones por fila sync-eligible, sin patología**; documentado en `qa/cloud/README §Vaciar` (distinción `.cloud`-backend/outbox vs `.icloud`-CloudKit/mirror — NO confundir). Gates: build Yala+Yala Dev sin warnings nuevos · YalaTests focalizados verdes (DestructiveScopeLogicTests + WipeMassRowsCloudDrainTests + LocalizationParityTests + DataWipe*) · validate-coverage OK · **QA VISUAL sim VERDE** (Yala Dev, iPhone 17 Pro, es-419, seed `grupos`, `.icloud`): fila 👥 con deuda REAL (S/330+$46.67) + «Ver mis grupos» → tab Grupos (Ajustes cerrado) + «Exportar antes» → wizard + regresa + paso 2 «¿Seguro? Esto es definitivo.» sin carrera → cancelar sin borrar. **R3 (nota):** en detent `.medium` con 4 botones (2 steer-away + destructiva + Cancelar) el contenido scrollea; el steer «Ver mis grupos» queda sobre el fold — aceptado, consistente con el detent medium de D4. **Diferido/pendiente:** batch D10, retención D1, pantalla «Tu cuenta de Yala» §3.3.5, «Borrar copia antigua iCloud» §fase 4; QA de la variante `.cloud` de la hoja (DARK en sim — cubierta por tests+código+#Preview); **push pendiente** (commit local, ahead del remoto).

- [2026-07-20] **feat(settings): D4 hoja de alcance destructiva FASE 3 IMPLEMENTADA — componente único de confirmación para los flujos destructivos** (branch `2.0.5`, commits `803e7d72` [lógica+tests] + `92c9efef` [componente+adopciones+l10n+coverage], **pusheados**; del estudio `MODO-NUBE-GESTION-DATOS-UX.md` §3.1/§3.3, decisión owner D4 ratificada 2026-07-19; método plan → /review-plan [NECESITA AJUSTES, 4 problemas resueltos] → impl → review adversarial Swift [APROBAR, 0 bugs de corrección, 4 mejoras menores aplicadas: dead-code confirmMessage + test, reset doble-flag, comentarios stale, force-unwrap] → QA visual sim). **2 decisiones owner de esta sesión (vía AskUserQuestion):** (1) la hoja reemplaza **TODAS** las confirmaciones de sign-out **incl. el privado normal VIVO** (un solo lenguaje visual, sin ramificar por path); (2) **sin botón «Exportar antes»** en Vaciar. **Componente `DestructiveScopeSheet` (Views/Shared):** sheet con 3 filas escaneables SIEMPRE iguales (📱 En este dispositivo / ☁️ En iCloud|En tu cuenta de Yala — **dinámica por `storageMode`, mata C2 de raíz** / 👥 En tus grupos), nota de conservación, líneas condicionales, botones [secundaria][destructiva][Cancelar]. Componente TONTO que renderiza un `Config`; la factory `Config.make` mapea `DestructiveScopeLogic.model` + `L10n` en UN sitio. SF Symbols (`iphone`/`icloud`/`person.2`), tono por fila (destructive=errorForeground / preserved=secondary / neutral=primary), detent `.medium` idiom-aware, botones pinneados fuera del ScrollView. **Lógica pura `DestructiveScopeLogic`** (molde `AccountDeletionMessageLogic`): 12 operaciones concretas → filas+tonos+etiqueta☁️+extraLines+secundaria; `deleteAccount*` reutiliza `AccountDeletionMessageLogic.lines` (D5) menos `.base`; `wipeDataFull` en `.cloud` añade `multiDeviceResidual` (D9). **Adopción (reemplaza el 1er diálogo; la confirmación corta final se conserva):** Vaciar (`UserDataResetView`), Eliminar cuenta (`ProfileView`, conserva «Ver mis grupos»/id `delete_account_view_groups` + «Continuar»/id `delete_account_continue` → `DeleteAccountDialogUITests` intacto), Cerrar sesión todas las variantes + Salir de Yala (`ProfileView`, operación por `signOutScopeOperation`), Borrar copia congelada (`GroupSettingsView`). **Anti-carrera (regla toolbar-muerta):** cada hoja es un `.sheet` con su @State bool; la acción/siguiente-paso corre en el `onDismiss` (hoja YA fuera) vía flags `pending*` — crítico en `.privateReset` privado → Welcome in-session. **Mecánica INTACTA:** `DataWipeService`/`CloudSessionSignOut`/`AccountDeletionService`/`performDeleteFrozenCopy` byte-idénticos (solo cambia la capa de confirmación). **l10n:** 39 keys nuevas ×16 (voseo es-AR; traducción nativa con revisión adversarial por idioma vía workflow — sentinel «Usuario eliminado» con el término y comillas tipográficas de cada locale, iCloud/Yala verbatim, «pantalla de inicio»/«pestaña Grupos» calcados); retiradas 13 keys huérfanas (mensajes concatenados → filas) + `CloudSignOutFlowLogic.confirmMessage`/`ConfirmMessage` (dead code) + su test. Gates: build Yala+Yala Dev sin warnings nuevos · LocalizationParityTests (16 locales, sin huérfanas/placeholders) · DestructiveScopeLogicTests (17, tabla operación×etiqueta☁️×deuda×huella) · CloudSignOut*/AccountDeletionDebt verdes · validate-coverage OK · coverage-index 4 áreas actualizadas (`settings-profile-general`/`account-deletion`/`session-sign-out`/`groups-crud-balances-settlements`). **QA VISUAL sim VERDE** (Yala Dev, iPhone 17 Pro, es-419): **Vaciar** (hoja 3 filas + nota + **doble confirmación onDismiss race-free**) y **Cerrar sesión privado** (todas las filas preservadas) — ambos VIVOS. **Diferido:** Borrar copia congelada es DARK/migrado (no seedeable en sim) → cubierto por `#Preview` + unit test + código; device-QA de las hojas DARK (eliminar-cuenta/sign-out `.cloud`/copia congelada) pendiente owner. Fuera de scope (piezas fase 3 separadas): batch D10 «salir de todos mis grupos», retención D1, pantalla «Tu cuenta de Yala» §3.3.5, 6ª config «Borrar copia antigua iCloud» (fase 4 — el componente ya la acomoda).

- [2026-07-20] **feat(settings): D5 + D7 gestión de datos UX FASE 2 IMPLEMENTADAS — aviso de deudas en «Eliminar mi cuenta» + higiene faro/marcador post-borrado** (branch `2.0.5`, commits `2c6a51fe` [D5] + `125a2a0f` [D7] + `b49fa3ce` [fix test preexistente D2]; del estudio `MODO-NUBE-GESTION-DATOS-UX.md` §3.3.4/§3.3.4.2 y §5.2.3, decisiones owner D5+D7 ratificadas 2026-07-19; método impl → review adversarial multi-lente [5 lentes + verificación] → 0 defectos de corrección, 3 gaps de cobertura cerrados con tests). **Todo DARK/[FLAG]** — la fila «Eliminar mi cuenta» solo existe con sesión backend viva (imposible en prod). **D5:** el primer diálogo de eliminar-cuenta gana, CONDICIONAL a saldos pendientes del usuario en grupos, el aviso `deleteAccountDebtWarning` («… para los demás quedarán como deuda de «Usuario eliminado»; salda primero desde la pestaña Grupos») + botón `deleteAccountViewGroups` («Ver mis grupos» → `SessionState.selectMainTab(.groups)` ANTES del `dismiss()`, molde `FullModeActivationView`). **INFORMA, JAMÁS BLOQUEA** (línea roja GDPR del owner — «Continuar» sigue disponible). Además 2 líneas nuevas: `deleteAccountFrozenICloudNote` (solo `.cloud` — la copia iCloud congelada sobrevive, semántica reinstalación) y `deleteAccountLegacyFootprintNote` (solo con huella CloudKit — el nombre persiste en zonas legacy; `groups_forget_user` anonimiza SOLO el backend). Detección: `AccountDeletionDebtLogic` (agregado puro, umbral 0.01) + `AccountDeletionMessageLogic` (composición de líneas por modo/deuda/huella, pura) + `SplitSyncManager.accountDeletionGroupsSummary(context:)` **READ-ONLY** (fetches + cálculo, CERO saves — invariante quiescencia (b) intacto; `context` inyectable ⇒ testeable sin mutar el singleton; member por `isCurrentUser`, grupo sin resolver ⇒ sub-conteo JAMÁS sobre-aviso; huella `ckSystemFieldsData != nil` en memoria; molde `GroupSettingsView.recomputeOutstandingDebt`). Desvío cruzado a Vaciar YA estaba (Fase 1). **D7:** en `AccountDeletionService.deleteAccount`, tras el delete server-side (paso 3) y antes del cierre local (4c), paso 3.5 BEST-EFFORT: `clearCloudBeacon` (limpia el faro iCloud-KV — **CIERRA el falso mismatch R9**: `ProviderMismatchLogic.decide` lee SOLO el faro; se limpia PRIMERO e incondicional) + `deleteCloudKitMarker` local (`guard !markers.isEmpty` ANTES de todo `save()` ⇒ solo-grupos 5b [mirror `.icloud` vivo] no tiene marcador → jamás toca el mainContext compartido; en `.cloud` el mirror está OFF ⇒ NO exporta a CloudKit [residual documentado: el record CloudKit sobrevive, pero a R9 lo cubre el faro]). Reusa los efectos de la Reversa PERO **sin retry** (la cuenta ya murió; a diferencia del re-throw journaled de `MigrationStateMachine`) — jamás condición del arm ni del cierre (kill-safety (d)); fallo del marcador se traga. Breadcrumbs sin PII `CloudSyncBreadcrumb.accountDeletion{BeaconCleared,MarkerDeleted,MarkerCleanupFailed}`. `CloudSessionSignOut`/`DataWipeService` INTACTOS. **Fix preexistente cazado por la suite completa (`b49fa3ce`):** el source-scan `signOut_allThreePaths_wireGroupsTeardown` esperaba 6 teardowns de grupos, pero D2 (`573c3b8e`) añadió el 5º path `exitYalaOnThisDevice` → conteo real 7 (D2 no actualizó el test). **l10n:** 4 keys ×16 (voseo es-AR; traducción nativa con revisión adversarial por idioma vía workflow, ancladas al sentinel «Usuario eliminado» y a «pestaña Grupos» de cada locale; iCloud/Yala verbatim; comillas tipográficas por familia). Gates: build Yala+Yala Dev sin warnings nuevos · **suite completa YalaTests 4773/4773 parallel OFF** · LocalizationParityTests verde · validate-coverage OK · área `account-deletion` actualizada (+`AccountDeletionDebtLogic.swift`, coverage D5+D7, lastVerified 2026-07-20). **4º commit `3656f74a` (seam + XCUITest de QA):** el diálogo de D5 era invisible en sim (la fila exige `hasSession` real, y SIWA/Google no corren en sim). Seam `-uitest-fake-backend-session` (UITestHooks, `#if DEBUG` ⇒ **inerte en release** vía `hasArg`; fuerza SOLO el input `hasSession` de la fila en ProfileView vía `deleteAccountRowHasSession` — NO crea sesión Supabase real; byte-idéntico en prod) + accessibilityIdentifiers `delete_account_view_groups`/`delete_account_continue`. XCUITest `YalaUITests/DeleteAccountDialogUITests` (2 casos: seed `grupos` → «Ver mis grupos» + «Continuar»; seed `minimal` → sin «Ver mis grupos»). **QA VISUAL en sim VERDE** (Yala Dev + `-uitest-fake-backend-session` + `-uitest-seed grupos`): la fila aparece → el diálogo muestra el aviso de deudas D5 + «Ver mis grupos» + «Continuar» → «Ver mis grupos» navega al tab Grupos («Te deben S/ 330.00 + $ 46.67»). El RUNNER de XCUITest no inicializa en este Mac (Xcode-beta AX timeout, issue local conocido) → el test compila (build-for-testing) y corre en CI; la QA visual es la prueba local. **PENDIENTE owner: push (4 commits locales) + device-QA en device real/TestFlight del flujo ENABLED backend** (D7 no reproducible en sim: borrado backend + revoke SIWA/Google; cubierto por unit tests + breadcrumbs). Nota de seeds: `DevSeedGroups` (perfil `grupos`) YA seedea grupos con deuda; el único gap para la QA en sim era el `hasSession`, resuelto con el seam.

- [2026-07-19] **feat(settings): D2 gestión de datos UX — split de "Cerrar sesión" en privado+grupos con sesión backend IMPLEMENTADO** (branch `2.0.5`; del estudio `MODO-NUBE-GESTION-DATOS-UX.md` §3.3.3, decisión owner D2; método impl → review adversarial multi-agente [5 lentes + verificación] → 1 SERIO + 2 nits, 3/3 aplicados). Cuando el path resuelto es `.groupsOnlySignOut` ([FLAG] `groupsBackendEnabled` + sesión viva) la fila única "Cerrar sesión" (que daba `.groupsOnlySignOut`, sin el "volver al Welcome" esperado) se **DIVIDE en dos** — **la precedencia de `CloudSignOutFlowLogic.path(...)` NO se toca**: (1) **"Cerrar sesión de grupos"** → `.groupsOnlySignOut` actual (copys H-6/H-7 intactos) + (2) **"Salir de Yala en este dispositivo"** → `.privateReset` FORZADO vía `CloudSessionSignOut.exitYalaOnThisDevice(context:)` (bypass de precedencia; diálogo DEDICADO `showExitYalaGroupsConfirm`, contenedor distinto = anti-carrera same-anchor). Lógica pura nueva `CloudSignOutFlowLogic.rowLayout` (split solo con `.groupsOnlySignOut`; **byte-idéntico flag-OFF** = `shouldShowRow`/`shouldShowExitYalaRow` de antes; `.plainSignOut`/`.exitYalaOnly`/`.none`). **Decisión owner #3 "encadenar cierre de grupos":** `exitYalaOnThisDevice` purga cursor+outbox de grupos (gateado por QUIESCENCIA, invariante b) + arma el boot-wipe de `YalaGroups` como **UNIDAD** — el boot-wipe NO toca `YalaSyncMeta`, así que sin purgar el `GroupSyncCursor` superviviente suprimiría el re-pull incremental tras el wipe → grupos invisibles + canario `groupMerkleDivergence` espurio [**hallazgo del review adversarial: era el único armador que no purgaba, a diferencia de `finalize`/`account-deletion`**]; timeout de quiescencia → ni purga ni arma (filas + cursor intactos, consistente). Re-sign-in de grupos DESARMA un wipe colgado (`GroupsBackendInviteModifier` → `clearGroupsOnlyWipeArm` + `GroupsSignOutBannerMarker.clear`). **Decisión owner #2 "solo banner (sin forzar tab)":** banner one-shot de re-entrada sobre el empty state H-7 (`GroupsSignOutBannerMarker` armado en `finalizeGroupsOnlyClose`, quemado en el `onAppear` del banner real en `GroupsContainerView`; gateado por `isGroupsSignedOutEmptyState` — jamás sobre tarjetas de grupo; **NO se fuerza el tab**). 5 keys l10n ×16 (voseo es-AR; ancladas a la terminología shippeada — `signOutConfirmMessageGroupsOnly`/`exitYalaConfirmMessage`/`groups.empty.signedOut.*`). Gates: build Yala+Yala Dev sin warnings nuevos · `CloudSignOutRowLayoutTests` + `GroupsSignOutBannerMarkerTests` + `LocalizationParityTests` (48 tests, todos verdes) · validate-coverage OK. **Todo DARK tras `groupsBackendEnabled`** (el split, el diálogo dedicado, el boot-wipe encadenado y el banner son device/sim-only). **Residual ratificado por el owner:** re-entrada in-session ("Restaurar iCloud" sin cold boot) muestra las filas `Split*` hasta el próximo boot. **PENDIENTE owner: device-QA del split + del banner (guion G6, path DARK).**

- [2026-07-19] **feat(settings): D6 gestión de datos UX FASE 2 — salida y export del solo-grupos legado 5a IMPLEMENTADA** (`b5b65145`, branch `2.0.5`; del estudio `MODO-NUBE-GESTION-DATOS-UX.md` §3.3.6, decisión owner D6). El escenario solo-grupos legado (onboarding `.groupInvite`, VIVO en prod) ya no queda sin ninguna salida ni forma de exportar. **Fila "Salir de Yala en este dispositivo"** (`profile_security_exit_yala`) → invoca el path `.privateReset` EXISTENTE (vuelve al Welcome, NO toca datos ni grupos, que siguen en el iCloud del usuario); copy propio "qué se borra / qué se conserva / vuelta atrás". `CloudSignOutFlowLogic.shouldShowRow` gana el parámetro `hasLiveSession` (`!isGroupInviteMode || hasLiveSession` — un group-invite CON sesión backend viva [FLAG] necesita superficie para `.groupsOnlySignOut`) + `shouldShowExitYalaRow` NUEVO (`isGroupInviteMode && !hasLiveSession`), mutuamente excluyentes (test de barrido). **"Exportar datos" DESOCULTADO** en solo-grupos → sheet `GroupsExportView` DEDICADO (el wizard personal `ExportFiltersStepView` EXIGE seleccionar una cuenta ⇒ inutilizable sin transacciones personales; el nuevo reusa `GroupsExportBuilder`/`ExportedFile`/`ShareSheet`, red defensiva empty-state), gateado por `ProfileViewModel.hasExportableGroups`; **Importar sigue oculto** (opera sobre TX personales). `confirmationDialog` COMPARTIDO con título/acción/mensaje adaptados al contexto exitYala (`isExitYalaContext`). 10 keys l10n ×16 locales (voseo es-AR; traducción nativa con revisión adversarial por idioma vía workflow — ancladas a la terminología ya shippeada: "pantalla de inicio", "grupos", "CSV"; `Yala`/`iCloud` verbatim). **`CloudSessionSignOut`/`DataWipeService` INTACTOS** (solo visibilidad/filas). Gates: build Yala+Yala Dev sin warnings nuevos · LocalizationParityTests 10/10 · CloudSignOutFlowLogicTests 12/12 (4 nuevos) · validate-coverage OK · **verificación visual en sim (group-invite): fila + diálogo + export desocultado confirmados; Importar ausente, sin "Cerrar sesión"/"Eliminar cuenta"** (correcto sin sesión backend). **Diferido (no en scope D6):** el flujo ENABLED de `GroupsExportView` con grupos reales (device-QA owner — el sim `minimal` no seedea grupos en group-invite); D2 split de sign-out privado+grupos, D5 aviso de deudas (fase 2 aparte). **PENDIENTE owner: push (commit local, ahead 1 sobre Fase 1) + device-QA del export de grupos con datos.**

- [2026-07-19] **feat(settings): FASE 1 de gestión de datos UX IMPLEMENTADA — quick wins de copy** (`5db1e0e5`, branch `2.0.5`; solo copy/l10n + presentación, CERO mecánica). Del estudio `MODO-NUBE-GESTION-DATOS-UX.md` §3.4 (decisión owner D3): modelo de **4 verbos reservados** (datos=Vaciar, sesión=Cerrar, cuenta=Eliminar, copias=Borrar). C1 Vaciar renombrado ("Vaciar mis datos"/"Vaciar definitivamente"/"¿Vaciar tus datos?") · C3 segunda confirmación (`confirmationDialog`→`alert`, contenedores DISTINTOS = anti-carrera same-anchor, molde de Eliminar cuenta) · C2 `wipeICloudWarning` por `storageMode` (variante "tu cuenta de Yala" en `.cloud`, ya no miente en Modo Nube) · subtítulos de una línea en filas (Exportar/Vaciar/Cerrar sesión/Eliminar cuenta) · icono Vaciar `arrow.counterclockwise` (`trash` reservado a cuenta) · C5 "Exportar datos" unificado con la fila ×16 · C6 "device"→"dispositivo" · desvío cruzado en Eliminar cuenta→"Vaciar mis datos". 7 keys l10n nuevas + 5 editadas ×16 locales (voseo es-AR; revisión nativa adversarial por idioma cazó typo-tilde fr `exportData`, comillas pt-BR/pt, signo ancho zh). **`DataWipeService`/`CloudSessionSignOut` INTACTOS.** Gates: build Yala+Yala Dev sin warnings nuevos · LocalizationParityTests 10/10 · validate-coverage OK. **Diferido a fases 2-4** (no en scope de Fase 1): hoja de alcance (componente 📱/☁️/👥), subsección "Tu cuenta" §3.2 (mueve UI + semántica FLAG-aware), retención solo-grupos, avisos de deudas, higiene faro/marcador post-delete. **PENDIENTE owner: push (commit local, ahead 1) + verificación visual en device.**

- [2026-07-18] **fix(bloque2): LOS 9 HALLAZGOS H-2026-07-18-* FIXEADOS — sesión autónoma, 9 commits atómicos `d8fe1ccf`→`099ffe25` (método por fix: investigador → plan validado → implementador → review adversarial Opus → gates build ×2 + suite completa parallel OFF + validate-coverage).** H-3 `d8fe1ccf` reset de cursor en la transición pendingApproval→active del PROPIO member (acumulador inout aplicado TRAS el merge de page.cursors en el mismo save — el avance de la propia página no lo pisa; re-pull inmediato) · H-4 `f1c79424` loop del canal: re-arranque foreground/post-sign-in vía `GroupsLoopRestartLogic` (SSOT con guard D8 de mount-mismatch → startIfEligible mid-session ya es seguro, doc-comment actualizado) + retry-once del 401 con refresh FORZADO (el auto-refresh del SDK solo dispara <30s de margen; multi-chunk re-usa el token; single-flight del SDK protege la rotación) + breadcrumbs `groupsLoopStopped/Restarted` · H-7 `4dd040d2` empty state de re-entrada del tab Grupos (GroupsEmptyStateLogic pura, flag OFF byte-idéntico, CTA por el intent de crear-grupo, verificado en SIM) · H-6 `15a52cb9` sign-out solo-grupos: retry interno con presupuesto GLOBAL 45s + hardCap decreciente de quiescencia, alert transitorio ("Un momento más") vs permanente, spinner+caption, cover "tus datos personales siguen en este dispositivo", breadcrumb `groups-only`→`icloud-groups-session` · H-2 `74d5e906` freeze: grupo moved fuera del picker (GroupExpenseEligibilityLogic sin default — 2º call-site Inbox cazado) + Guardar disabled reactivo con hint + alert de red SIEMPRE localizado (JAMÁS localizedDescription crudo — el review cazó dev-strings) · H-5 `5a9afd13` bump VIVO de dataVersion por-CICLO en pullUntilExhausted (la señal existía pero era DIFERIDA; canal CloudKit intacto a propósito) · H-8 `8f57a184` quiescencia infinita post-fresh-start: escape empty-store en `BootSaveGateLogic` (fast-path del restore INTACTO byte-idéntico; import colgado JAMÁS se fuerza; endurecido `!isSyncingAnyPhase` cubre `.setup` que isImportQuiescent no ve; cualquier `.import` event deshabilita el escape para siempre — review: NO reabre el crash-loop; contador `quiescenceDeferredBootCount` + canario `cloudBootSaveDeferredRepeatedly`) · canario promotedToAuto `298c0a31`: `.cloud` promueve DIRECTO por el TESTIGO DE MONTAJE `personalStoreMountedMode` (jamás la key persistida — kill-window del cutover), `detail=cloudModeDirect`; residual `importSettled=false` de `.icloud` solo-grupos store-vacío es LEGÍTIMO · H-9 `099ffe25` `GroupExpenseIconResolver` bridge-first → `subcategoryName` self-contained → genérico, compartido feed/detalle/Stats (el root NO era lookup por ID: era dependencia del TX bridge personal muerto tras el wipe; matching por nombre localizado frágil cross-idioma documentado — key canónica en SplitExpense = fix de raíz futuro). **Canario identity-gap INVESTIGADO Y BENIGNO** (deletes de filas jamás subidas — syncID sintético perezoso + churn del bridge; NO causa la divergencia tx_items×8 del ticket, que sigue aparte; propuesta pre-D9: separar la clase sintética o el gate "llora lobo"). Lecciones: la suite completa como gate por fix CAZÓ una regresión que ni el review adversarial ni los tests focalizados vieron (test de secundaria con premisa stale del mount-mismatch); el sim 9D0F6D32 del encargo no existe (usar 2F491E72). **PENDIENTE owner: re-verificación device en UNA sentada — guion `MODO-NUBE-BLOQUE2-REVERIFICACION-DEVICE.md` (el paso 0 SANA el device B solo con abrir la build) + 4 ratificaciones listadas ahí (cover en eliminar-cuenta, card Moved, canario identity-gap, Merkle persistido).**

- [2026-07-18] **qa(device): BLOQUE 2 del Bloque C (G6+G8) COMPLETO — FUNCIONALMENTE VERDE, 0 hallazgos de datos, 9 de robustez/UX (H-2026-07-18-1..9, registro en los guiones G6/G8 "Corrida 2026-07-18" + memoria fase 4).** ⚠️ **EL FLAG `groupsBackendCompiledDefault=true` QUEDÓ COMMITEADO TEMPORAL EN 2.0.5 (`2efd2929`, decisión owner) — REVERTIR antes de cualquier archive/release desde esta branch y antes de D9.** Corrida con Yala Dev por cable ambos devices (TestFlight IMPOSIBLE hoy: build prod = CloudBackendConfig placeholder + kill-switch 0% — el guion G6 quedó corregido; CloudKit corrió en Development, residual Production cubierto por parity+canario D9). VERDES: migración owner con meta histórica y FKs CloudKit-era EXACTAS · partición por-grupo en device · freeze · re-join con rebind del member_key legacy · **R10 en device fresco (LA PRUEBA REINA: 11 grupos desde cursor 0, balances exactos)** · borrado de copia congelada + guard · G8 e2e (fan-out ~20s bidireccional, killed/background según plataforma, convivencia CK limpia) · §16d 2 botones (cierra el paso 6 de Fase G del SIGNOUT) · red Merkle de grupos validada en vivo. SERIOS pre-D9 (fixes en sesiones aparte): H-3 hueco de cursor del re-join (RLS oculta contenido en pendingApproval y el cursor avanza → reset de cursor en la transición a active) · H-4 el loop del canal muere en silencio sin re-arranque (ni foreground ni re-sign-in; solo cold boot) · H-6/H-7 UX del sign-out solo-grupos · H-8 quiescencia infinita post-fresh-start (`firstImport` jamás llega → TODOS los saves gateados diferidos para siempre — B quedó semi-funcional) · H-9 refs cross-store de iconos. Falta del Bloque C: fixes + Bloque 3 (M1+G1-G3) + restos Fase G + canarios AE (`groupMerkleDivergence>0` ahí es ESPERADO, lo indujo el H-3).

- [2026-07-17] **qa(device): BLOQUE 1 del Bloque C (gate §12) VERDE — #36/#37 CERRADOS en device + fix cover re-verificado; 3 SERIOS nuevos en vuelo** (corrida guiada owner en device QA, cuenta `admin@yala-app.pe` con Hide My Email; registro en guiones I14/SIGNOUT-WELCOME "Corrida 2026-07-17"; commit `22164466` = fix alert fresh-start tint primario + bump coverage). Validado: kill-test A repetido (pre-espera + resume solo), suspensión = recuperación autónoma, `prefsDrainSentinelCleared`/`prefsCutoverDrained` con consent llegando en RE-migración, cover `c174a663` + auto-exit, SIGNOUT A/C/D, R9 REAL de Google (mismatch + cero claim; test users GCP ×3), 3 ciclos migración/reversa/re-migración con conteos exactos. **Abiertos pre-D9:** H-3 migración fuerza SIWA (chip/sesión paralela), **H-4 TX de época nube huérfana de relaciones tras la REVERSA** (ticket `Bugs/qa_cloud-tx-epoca-relaciones-huerfanas.md`; eco del pull y applier EXONERADOS por experimento — ventana acotada a la reversa), ~~H-5 adopt del Welcome sin re-drive~~ **CERRADO en código (`25b579c4`)**: detector puro `WelcomeAdoptAutoResume` en el poll de `WelcomeCloudSignInView` (4 ticks ociosos con `isWorking==false` → `resumeIfNeeded()`, mismo camino boot/rekick — el drive del runner es SÍNCRONO en sus entradas públicas, por eso ocioso+transicional ⇒ aparcada real; 3 autos sin avance → botón Retomar + hint `waitingImport`; terminales de fallo protegidos por doble capa: el poll retorna en `.error` y `MigrationBootDecision` da `.none`; auto-resume estructurado dentro del task del poll ⇒ respeta el cancel de onDisappear); re-verificación device = Fase 6 nueva del guion I14 (H-3 chooser toca la misma vista — esa sesión rebasea sobre este cambio). Pendiente Bloque C: Bloque 2 G6+G8 (chip listo; build flag ON + 2 devices + member B en "Prueba" antes de migrar), Bloque 3 M1+G1-G3, restos Fase G, barrido canarios en dashboard AE.

- [2026-07-17] **feat(metrics): TELEMETRYDECK ELIMINADO — telemetría propia mínima en 3 commits (owner: "ni lo uso, llegué al límite y no confío en sus datos")** (branch `2.0.5`, `67508ee5`/`13791215`/C3; método exploración → 3 ratificaciones owner vía AskUserQuestion → plan → /review-plan [NECESITA AJUSTES, 5 aplicados] → impl con 3 subagentes de purga). **Ratificaciones:** (1) canarios del gate de Modo Nube → backend propio (la condición "canarios en cero" del Bloque C ahora se lee del dashboard de Analytics Engine, NO de TelemetryDeck); (2) KPIs = SOLO usuarios activos/día + registros/día, en `POST /metrics` del yala-gateway → Workers Analytics Engine (datasets `yala_metrics_staging`/`yala_metrics`, retención 90d, DESPLEGADO ambos envs); (3) registro con doble variante local (onboarding full/groupsOnly/groupInvite) | cloud (claim `created`, one-shot `metrics.cloudRegistered.<userID>` — el re-claim del mismo líder colapsa a `created` y doble-contaría). **Piezas:** `Yala/Services/Metrics/` (MetricsService no-op sin `start()`; MetricsSpool cola persistida cap 50 kill-tolerante — los canarios no son fire-and-forget; MetricsPingLogic día UTC = bucketing AE; MetricsClient público sin auth, 4xx=drop [jamás atascar la cabeza del spool], 5xx/offline=retry) + los 32 wrappers typed conservan firma exacta + catálogo `MetricsCanary` para los ~30 `track(.x)` crudos. `install` = sha256(bucketSeed) truncado 16 hex — el seed de cohortes JAMÁS sale del device. **Semántica `metrics.*` = DEVICE-GLOBAL** (el sweep de sign-out es lista explícita y no las toca; `-uitest-reset` sí las limpia). **Purga:** SPM fuera (⚠️ gotcha: editar pbxproj por script debe respetar bloques ANIDADOS `requirement = {...};` — una llave huérfana rompió el proyecto y `plutil -lint` + `xcodebuild -list` lo cazan), ~190 call-sites de producto borrados (~65 archivos), `APIKeyService` borrado, `TELEMETRY_DECK_APP_ID` fuera de Info.plist, PrivacyInfo DeviceID purpose → Analytics. **Queries del gate** en `qa/cloud/README` § /metrics (SIEMPRE `sum(_sample_interval)`, jamás `count()`). Pendiente owner: D1 cerrar la cuenta TelemetryDeck cuando muera el último build viejo; D4 limpiar `TELEMETRY_DECK_APP_ID` de `Secrets.xcconfig` (gitignored); validar las queries AE en el dashboard con los primeros datapoints reales.

- [2026-07-15] **feat(groups-backend): G0 CERRADO + G1-G4 COMPLETOS — el canal Grupos→backend está funcionalmente completo y TODO DARK** (commits `ac579536`/`b86dbf1c`/`2aed07d9` [noche 1] + `e22a991d` G3 + `2a2cf471` G4 + `748ed295` claim-promoción + `68f5555d` perf pull [noche 2 + mañana]; flag `CloudSyncFlags.groupsBackendEnabled=false`). **⚠️ RIEL PARA SESIONES PARALELAS: staging (`fostjbbwstyuunmmefuk`) tiene VIVA la infra de grupos (8 tablas `split_*`/`group_*`, ~12 RPCs `create_group`/`join_group`/`apply_group_delta`/…, columna `profiles.personal_claimed_at`) — JAMÁS dropear objetos de staging sin validar contra esta entrada y el DDL contrato `supabase-groups-staging.ddl` (el incidente del 2026-07-14: una sesión de "limpieza de spikes" con contexto stale dropeó la infra G1 recién aplicada). Los objetos `spike_*` del Spike B YA NO EXISTEN (cleanup formal de G0 aplicado 2026-07-15) — pgcrypto la extensión SIGUE (G7 la usa).** Piezas: canal de sync aparte (GroupsSyncClient: drain con muro `groupEntityNames` disjunto del personal, push/pull/apply, pull-hasta-agotar con guard cursor-no-avanzó, dead-letter `upstream_400:<slug>` con RE-DRIVE al aprobar al member propio, loop de cadencia reutilizando SyncCadencePolicy, GroupsSyncBreadcrumb sin PII) + gateway `/groups/{push,pull,merkle,rpc/:fn}` (pull PARALELIZADO ~42s→2.5s) + cliente RPC tipado (GroupsMembershipClient/GroupBackendMembershipService con createGroup SERVER-FIRST) + separación `SplitMember.memberKey` LOCAL-only (born-backend ya no contamina `cloudKitUserRecordID`; guards en los backfills de `refreshCurrentUserFlags`) + `claim_account` PROMOCIONA filas ligeras de grupos a `created` (decisión owner Opción A — el solo-grupos que activa Yala completo ya tiene camino de migración). **HALLAZGO que corrigió el diseño §2**: las FKs del historial de grupos son `SplitMember.id.uuidString` (determinista por `(groupID, member_key)`), NO `cloudKitUserRecordID` — riesgo R10 (namespaces CloudKit vs backend en grupos migrados a device fresco) es DECISIÓN PENDIENTE DE G6. Goldens: grupos 16/16 + account 27 pass/2 skip (los 2 stateful pasaron a SKIP condicional limpio — corren tras seed). Gotcha sim: la contaminación StoreKit sandbox de ProUpsell ×2 REINCIDE — `simctl erase` es el remedio estándar (Lista Negra actualizada). **[2026-07-15/16, 3ª noche — commits `09a66cbd`/`d1395866`/`b101f2c8`/`53fe10d7`]: G4-INVITES/CONSENT ✅ (invite por token con `s` self-referential — el AASA `?s=*` exige la PRESENCIA de `s`, un link solo-g/t NO abre la app; handler con intent persistente, join RPC síncrono mata el bug de Pia por construcción; consent §8 + sign-in solo-grupos §16d que autentica y NADA más; detección de member backend por `userID==sub`, JAMÁS `isCurrentUser` que applyMember no setea) + ENDURECIMIENTO PRE-FLAGS ✅ (Merkle client-side con `GroupMerkleProjection` SEPARADA de la emisión — reusar el emission map habría divergido en CADA grupo: la emisión omite `created_at` por grant y `group_members` es pull-only; fixtures de PROYECCIÓN c1 generadas ejecutando el código real del gateway; espejo App Group + chunking 50 + teardown sign-out con guardia de generación + piggyback 5.6 con guard de secundaria y abstención por `canRunDomain()` + backoff RPC con one-shots creadores excluidos de TODO retry — un 502 post-COMMIT reintentado en create_group_invite = token huérfano) + CAS del `reverse_claim` y enforcement del freeze VERIFICADOS YA-CERRADOS pre-sesión (aplicados 2026-07-11: migración `i11_reverse_claim_cas` con md5 matching + goldens 19-22 verdes — los 2 pendientes del gate de flags personal quedan CERRADOS con evidencia). Log completo en el ticket.** **[2026-07-15/16, 4ª noche — commits `e3af04ab`/`a7de7cec`/`3e23acf1`/`7b5ff216`/`97e5c280`/`b63bbb90`]: G5 COMPLETO ✅** — cutover interno de escritura (SplitGroup.isBackendGroup LOCAL-only + partición POR-GRUPO **bidireccional**: guards CKSyncEngine Y filtro del drain backend — el /review-plan cazó que editar un grupo CloudKit con flag ON habría drenado a un group_id inexistente; routing create/invite/membership server-first por memberKey) + matriz de sign-out ×4 con fila SOLO-GRUPOS (push-all de grupos con drainOnce-antes-del-pre-check [SEV-2: outbox vacío ≠ History drenada] + gate de QUIESCENCIA a la entrada [HIGH: la purga salvaba al mainContext compartido con el mirror `.icloud` VIVO — la clase del crash-loop de restore] + PreferenceSyncService.remove(forKey:) nueva [sin limpiar iKV el consent resucitaba] + `.cloud` ampliado con push-all de grupos y marker includeGroups kill-safe) + grupos POR SESIÓN M1/D8 (YalaGroups-Secondary; outbox/cursores aislados GRATIS vía YalaSyncMeta-Secondary; guards/tab/piggyback relajados bajo flag; gates 3/5 y 4/5 retirados DETRÁS del flag con tipos vivos; **HIGH: purga de frontera exhaustiva de las superficies de join-intent en UserDefaults.standard — el intent de la invitada se habría ejecutado bajo el DUEÑO**) + ELIMINAR-CUENTA GDPR e2e (RPC delete_personal_account HARD DELETE aplicado en staging con método g3_02 [review PRE-aplicación + SEV-3 enumeración viva + WIRE one-shot verde sobre sub B + md5 registrado]; cliente con orden ANTI-ZOMBIE forget→teardowns→delete→cierre-por-modo; sentinel __deleted_user__→resolvedDisplayName en 13 superficies incl. GroupBalanceService que lo filtraba a Balances) + EXPORT ampliado con grupos (CSV separado, sin gate por flag — read-only; personal byte-idéntico testeado). Residual 'status=left' VALIDADO CERRADO (fila viva server-side, los demás members la aplican como campo, el que sale pierde membership → skip por remoto-vacío). Flaky de entorno arreglado de raíz (tormenta IO de containers on-disk IdRemap-* con víctima itinerante → cleanup no-op). Pendientes owner nuevos: ratificar HARD DELETE personal, SIWA revoke (5.1.1v, .p8 al Worker), auth.users (Admin API), g5_01 a PROD con gates SEV-1/2. Siguen del plan §11: **G6 migración grupos vivos (con R10 + adopción setea isBackendGroup atómico), G7 pgcrypto, G8 APNs.** **[2026-07-16, sesión diurna — commits `658643f0`/`0f6d5f42`/`c1ad72d4`/`dac0042d`]: G6 COMPLETO EN CÓDIGO ✅ + SCHEMA CLOUDKIT DESPLEGADO A PRODUCTION (owner)** — decisiones owner: R10=namespace-aware en applyMember (remap descartado), token EN el marcador, deploy coordinado (rama `g6-3-marker` + commit retenido hasta el deploy → el .ckdb jamás mintió). G6-1: RPC `migrate_group` (meta histórica + members legacy user_id NULL atómico; idempotente-suave; dup-member_key y casts malformados → yala_bad_input [anti retry-eterno]; squat insider aceptado) aplicado en staging (md5 `e7863927e5cb58398ae1e36b84f268f9`) + goldens 21/21 (E2E R10 server-side; settlements usan unidad `smoney`). G6-2: `isLegacyMemberKey` (UUID-parse) + rama R10 (id byte-idéntico al owner CloudKit-era ⇒ balances correctos en device fresco — R10 CERRADO) + adopción ATÓMICA de isBackendGroup (cierra la nota de G5-A) + seam legacyMemberKey. G6-3: field keys `movedToBackendAt`+`backendReInviteToken` en GroupMeta (ENCRYPTED el token; parity verde ambos .ckdb) + **GUARD SIMÉTRICO DE PULL** (G6 estrena grupo en zona CloudKit VIVA + isBackendGroup=true — sin el guard, members rezagados pisarían las ediciones backend del owner y el deleteZone nukearía al member re-joineado; excepción quirúrgica para el conflicto del propio marcador) + uploader kill-safe (candidatos por movedToBackendAt, JAMÁS !isBackendGroup; seed con LECTURA VIVA — edición concurrente gana por LWW; push transient aborta sin marcador) + freeze service-level + tarjeta/CTA re-join + C5 borrar-copia-congelada. La Mac se cayó a mitad de G6-3 — el implementador se REANUDÓ desde su transcript sin pérdida. **PENDIENTE owner: device-QA cross-device del guion [[MODO-NUBE-G6-GUION-DEVICE]] (gate §11; la prueba reina = balances R10 en device fresco). Quedan G7 pgcrypto (+ assert de logging settings §16e) y G8 APNs.** **[2026-07-16, 6ª sesión nocturna — commits `cdd2b849`/`fa7cb4cd`/`802e6a3e`]: G7 PGCRYPTO ✅ + G8 APNs ✅ — EL PLAN §11 (G0–G8) QUEDA COMPLETO EN CÓDIGO.** G7 (aplicado en staging): 8 columnas † a bytea con pgp_sym_* (las 2 extra split_shares.amount+split_settlements.note a RATIFICAR owner); llave-como-argumento (residente-en-DB descartada: aparece en dumps); migración SIN fuga (2 DDL sin llave + recrypt vía execute_sql — jamás en schema_migrations/logs; roundtrip 239/239 pre-cutover); readers `groups_pull_rows_*` SECURITY INVOKER con amounts como TEXT escala-4 (**hallazgo C1 del /review-plan: vía RPC el Merkle habría recibido número JS → rama double del canon → divergencia LATENTE que ningún gate cazaba**; el wire pasa de número a string — único cambio de shape, WireValueDecoder compatible, cliente Swift INTOCADO); `yala_try_decrypt` tolerante-POR-FILA (anti-DoS de bytea basura); column-UPDATE grants REGENERADOS (C5: atan por attnum — sin regenerar, push en noop SILENCIOSO); gate §16e AUTOMATIZADO (golden g7-logging-settings). Llave staging en `~/Secrets/yala-groups-enc/staging.key` + `gateway/.dev.vars` + secret del Worker; goldens/cross-member exigen `GROUPS_ENC_KEY` en env. G8-1 (aplicado): RPCs get_group_push_tokens (co-members ACTIVOS, caller excluido, pendingApproval fuera) + prune_push_token (guard de radio); ⚠️ modelo de amenaza REAL a RATIFICAR (co-members enumeran device tokens vía PostgREST — estructural con jamás-service_role; token inerte sin Auth Key); POST /push/register|unregister + FAN-OUT en waitUntil (dedup, cap 50, BadDeviceToken→prune, canario groupApnsSendFailed; c.executionCtx LANZA sin ctx → helpers de goldens con ctx no-op). G8-2: PushTokenRegistrar/Client (capture local-siempre, upload flag&&sesión; platform por build) + seam sign-out RELLENO (async, DOBLE gate flag&&sesión [byte-identidad: paths .cloud alcanzables con flag OFF y sesión viva] + timeout 4s anti-cuelgue-offline) + push key `yala` → syncNowFromPush(20s) (context STRONG, asignado también en piggyback; exactamente-una-vez; CK-primero intacto). Gates: gateway 183/2 skipped · cross-member 71/0 · suite 4531/418 ×3 · builds ×3 · validate-coverage OK. Guiones: [[MODO-NUBE-G8-GUION-DEVICE]] NUEVO + apéndice G7-descifrado al de G6. **Para el encendido queda SOLO el gate §12: device-QA G6+G8, ratificaciones, llave+APNS keys de prod (re-aplicar g7/g8 a prod — drift anotado), pendientes heredados, canarios en cero.** **[2026-07-16, 7ª sesión — GATE §12 BLOQUES A+B ✅ (commits `8ed3f47f`/`c2725354`/cierre): PROD PROMOVIDA — 13 migraciones (g1_01b→g8_02+g12_01, diff DERIVADO de list_migrations con SQL byte-exacto del historial [la terna del incidente se netea a g1_01b], sandwich G7 con llave PROPIA de prod, paridad md5 33/33 funciones, yala_push+proacl+estructural exactos, golden invertido 42501) + Worker `yala-gateway-production` DESPLEGADO POR PRIMERA VEZ (7 secrets incl. JWT_SIGNING_SECRET propio; OPENAI_API_KEY ya era la ROTADA — confirmación owner mismo día) + B1 SIWA revoke 5.1.1(v) REAL (canje en sign-in vía Worker, par (token, appleUserID) en Keychain con match+clear-first — 2 hazards cross-cuenta M1 cazados por reviews; /account/siwa/exchange|revoke; población-cero argumentada) + B2 `auth.users` MUERE con la cuenta (g12_01 en AMBOS envs; HARD DELETE y vía RATIFICADOS owner; supuesto load-bearing rolbypassrls=true assertado; WIRE verde con sub desechable; el re-claim ya no renace la cuenta). Ratificaciones owner ×4 vía AskUserQuestion. Nuevos baselines: gateway 196/2 · suite 4563/422. Chip del gate retirado. DEL GATE QUEDA SOLO EL BLOQUE C (owner): device-QA G6+G8 + canarios en cero → ENCENDIDO.]**

- [2026-07-17] **feat(cloud-sync): DIFERIDOS #34 CERRADO — `cloudModeEnabled` remote-config (kill-switch sin release), prerequisito del rollout PÚBLICO** (branch `2.0.5`; método exploración → 3 ratificaciones owner vía AskUserQuestion PRE-plan → plan → /review-plan [NECESITA AJUSTES, 5 aplicados] → impl → adversarial [APROBADO CON FIXES: 1 SERIO + 4 menores, 5/5 aplicados]). **Ratificaciones:** (1) kill = corta solo la ENTRADA (fila Almacenamiento no-migrados, cards sign-in nube del Welcome, entrada secundaria M1) — un `.cloud` "engaged" (persistido ∨ `uiState != .idle`) conserva runtime/outbox/fila/REVERSA, el runtime NO se gatea por el remoto a propósito; (2) percent 0-100 por flag + bucket FNV-1a estable por instalación (seed UUID persistido; golden de vectores fijos protege cohortes — JAMÁS `hashValue`, randomizado por proceso); (3) `groupsBackendEnabled`+`cloudOnboardingChoiceEnabled` en el shape + composición `compilado && remoto` del getter de grupos (remoto solo MATA, nunca enciende; setter=override source-compatible con `= true; defer { = false }`; ⚠️ nota teardown-vs-kill para la sesión de ENCENDIDO en el doc-comment del getter + qa/cloud/README §GET /config). **Worker:** `GET /config` PÚBLICO sin auth/attest/bindings (un kill-switch no puede caerse por dependencias); valores en wrangler.toml [vars] (patrón ENFORCE §j.2: flip = editar var + `wrangler deploy`); **DESPLEGADO staging (100×3) Y prod (0×3), curl verde ambos** (gotcha: la propagación tarda ~20-30s — un 404 inmediato post-deploy no es bug). **Cliente:** `CloudRemoteConfig`/`RemoteFlagDecisionLogic` (absentDefault PARAMÉTRICO — A1 del review: los tests corren siempre DEV_BUILD, la rama prod fail-closed entra por tabla; snapshot `cloudSync.remoteConfig.*` en `.standard` sobrevive sign-out por el carve-out `cloudSync.*`; last-cached-wins sin TTL, futuro=stale) + `StorageRowGateLogic` + fetch boot 14.56/entradas gateado `isConfigured && !uiTest` (prod HOY: cero tráfico y superficies ya ocultas — doble-dark byte-idéntico; DEV: staging sirve 100 + default-ausente ON ⇒ QA intacta). **SERIO del adversarial (lección replicable):** bajo `-uitest` el HOST no es unit test (`isRunningTests`=false) — todo getter que lea `.standard` necesita TAMBIÉN el mirror de `-uitest`, y `-uitest-reset` debe limpiar el snapshot/toggle (la clase exacta de keys pegajosas de `applyUITestHooksEarly`). Gates: gateway 203/2 (correr con `GROUPS_ENC_KEY` **y** `PUSH_ROLE_JWT` de `~/Secrets/yala-groups-enc/` — sin el JWT, push.fanout falla por ENV, no por el cambio) · builds ×2 · suite completa parallel OFF · validate-coverage OK · área nueva `cloud-remote-config`. **Encadenado a D9 (owner): subir percents de PROD por escalones §j.2 (gate de costo observe) + configurar CloudBackendConfig de prod (sigue placeholder).** Residual ratificado: reinstalación de usuario nube bajo kill no re-entra hasta re-encendido.

- [2026-07-17] **fix(cloud-sync): DIFERIDOS #33 CERRADO — el full-rescan ante token de History IN-DECODIFICABLE queda ACOTADO por `lastDrainedTxAt` (último item del paquete pre-beta)** (branch `2.0.5`; método exploración → plan → /review-plan [NECESITA AJUSTES, 5 aplicados — el clave: T10 con ancla-en-futuro era FALSO y destapó el residual REWIND] → impl → adversarial [APROBADO CON FIXES: 1 SERIO + 2 menores + 1 nota, 4/4 aplicados]). Antes, token que no decodifica (o cuyo fetch por token lanza — migración destructiva de schema) → re-escaneo COMPLETO de History; con `historyPurgeEnabled` encendido habría re-emitido el corpus entero con **HLC fresco** (el reloj PERSISTIDO ya avanzó → el dedup `(syncID,hlc,op)` NO absorbe; en multi-device además PISA por LWW writes ajenos más nuevos no pulleados — evaluado: el dedup NO neutraliza). **Fix:** `HistoryTokenFallbackLogic` (tabla pura `absent/valid/broken × ancla`) + `fetchHistoryResolvingToken`/`executeBrokenTokenBranch` en el motor — re-escaneo ACOTADO a `> lastDrainedTxAt − 60s` (la MISMA ancla del guard del Hallazgo 2, `799213f4`) + re-anclaje a la última tx de la ventana (motor incluido, precedencia sobre `advancedToken`); el guard del Hallazgo 2 se SALTA con token roto (su fetch sería idéntico — doble fetch inútil). **Semánticas:** (a) sin ancla (cursor pre-schema) → full-rescan CONSERVADO; (b) el fetch acotado TAMBIÉN lanza → DEGRADA al full-rescan MEDIDO, jamás abortar (abortar = stall = la clase local-ahead inconvergible); ventana VACÍA (History purgada bajo el ancla) → drain ocioso barato hasta que un write nuevo sane. Breadcrumbs con rama y counts (canario si el hazard ocurre igual): `historyTokenBrokenBoundedRescan(window:)`/`historyTokenBrokenFullRescan(reason: no-anchor|bounded-fetch-failed, txs:)`/`historyTokenBrokenReanchored` — `historyTokenExpired` retirado (sus 2 call-sites eran el fallback). **SERIO del adversarial (lección replicable):** los reanchor (el nuevo Y el gemelo preexistente del guard) apuntaban a la última tx de la ventana CRUDA calculada ANTES de traducir — si `clock.send` lanza a mitad (drift, alcanzable con reloj persistido adelantado por snapshot/remap), re-anclar saltaba txs externas jamás emitidas = pérdida silenciosa → flag `translationAborted` suprime AMBOS reanchor y cae al avance normal (última tx CONSUMIDA, el punto de retry del invariante de drift); T14 lo caza. **Residual nuevo documentado:** rewind de reloj >60s + token roto simultáneos → el fallback es load-bearing y los writes del gap se pierden de la captura hasta que el wall-clock re-pase el cutoff (misma improbabilidad que M1, citado en doc-comment). Gates: builds ×2 0 warnings · suite completa parallel OFF · validate-coverage OK · T9-T14 + `HistoryTokenFallbackLogicTests`. **Sin guion device:** el token corrupto real solo aparece en migraciones destructivas de schema — cobertura por tests + breadcrumbs en Console.

- [2026-07-17] **fix(cloud-sync): DIFERIDOS #36 + #37 CERRADOS — re-kick del resume de migración aparcado (H1) + limpieza del sentinel del drenaje iKV en la reversa (H3)** (branch `2.0.5`; método plan → /review-plan [NECESITA AJUSTES, 3 aplicados: A1 fast-path limpia el flag, A2 guard de reentrada en resume/pollLeader, A3 la MISMA clase de sentinel stale existía por la puerta del sign-out `.cloud` — verificado: `removeUserPreferenceKeys` excluye `cloudSync.*` por doc y nadie lo purgaba]). **#36**: el resume era one-shot (gate 120s del runner vencía EN SILENCIO; push transient a mitad de página tampoco se re-drivaba) → pre-espera de quiescencia en `CloudMigrationController.resume()`/`pollLeader()` (poll 2s, tope 300s, patrón `awaitPersonalImportForBootSave`; el gate del runner se CONSERVA como enforcer — jamás un save sin quiescencia) + flag observable `resumeWaitingForImport` con caption honesto en la card de Almacenamiento (`storage.progress.waitingImport` ×16) + re-kick en `handleBecameActive` tras los freeze-guards (pure-logic `MigrationForegroundRekick.shouldRekick` = `MigrationBootDecision.decide != .none && !isWorking` — terminales de fallo NO auto-re-kickean, contrato del boot). **#37**: `cloudSync.prefsCutoverDrained.<userID>` quemado en el primer cutover hacía skip del drenaje en RE-migraciones (consent jamás llegaba a `user_preferences`) → prefijo SSOT + `sentinelKeys(in:)` puro en `PrefsCutoverDrain`; lo retiran `.persistICloudMode` (cuarteto de cierre §h — efecto LOCAL sin dependencia de red/sesión, kill-safe por journal-then-execute) Y `performSignOutWipeIfArmed` (A3), TODOS los userIDs por prefijo. Breadcrumbs: `migrationResumeAwaitingImport`/`migrationResumeDeferredAwaitingImport`/`migrationForegroundRekick`/`reversePrefsDrainSentinelCleared`. Descartados con criterio: observer one-shot de first-import para >300s (3 redes ya lo acotan), pre-espera en startMigration/startReverse (fuera de la evidencia H1). **PENDIENTE owner: re-verificación device (kill-test A repetido + re-migración con consent) — fase nueva del guion I14.**

- [2026-07-16] **feat(cloud-auth): GOOGLE SIGN-IN EN V1 — decisión owner (revierte "fase web/Android") + sesión 1 (Core) COMPLETA con WIRE verde** (`27463cb9`; brief congelado `docs/modo-nube/briefs/BRIEF-GOOGLE-SIGNIN-V1.md` con 4 ratificaciones owner; método plan → /review-plan [5 ajustes] → implementador → review adversarial [5/5 fixes]). SDK GoogleSignIn-iOS 9.2.0 (nonce custom desde 9.0.0 ⇒ patrón SIWA, **skip_nonce_check PROHIBIDO en Supabase**) + `CloudAuthService.signInWithGoogle()` + `storedProvider()` + par `(googleUserID, sub)` en `GoogleUserPairStore` (clear-before-write; consumidor = revoke sesión 3). Config: GCP `yala-502622` (2 iOS Client IDs en `~/Secrets/yala-google/`; app en Prueba — "Publicar app" encadenado al encendido) + Supabase STAGING Google ON (prod encadenado al encendido). **HALLAZGO H4 (WIRE real): GoTrue LINKEA identidades con mismo email verificado al MISMO sub** — el sign-in Google del owner aterrizó en su user Apple (identities apple+google, `profiles.provider` conserva `'apple'`) ⇒ la red R9 de sesión 2 compara por SUB (faro), NUNCA por provider a secas (falso mismatch en linking legítimo); R9 real solo con Hide My Email. Gotchas: `Secrets.xcconfig` está MUERTO (config per-scheme = build settings del pbxproj, patrón `URL_SCHEME`); guard in-flight de sign-in debe ser BIDIRECCIONAL entre providers (par cruzado por valores); perfil capturado exige pre-clear CROSS-provider (la regla anti-sangrado es ciega entre providers). ~~Quedan: sesión 2 (UI/chooser/§16d/R9 por sub + case `.cancelled`) y sesión 3 (revoke `disconnect()` + seams sign-out).~~ **[2026-07-16, sesión 2 — UI + R9 ✅]** (método plan → /review-plan [6 ajustes] → implementador → adversarial [APROBADO CON FIXES: S1 guion device omitido + 3 menores, aplicados]): chooser gana 3ª card Google (gate IDÉNTICO a `.cloudSignIn` — byte-idéntico DARK/uitest; opt-in nuevo `-uitest-cloud-chooser` solo-DEBUG para el XCUITest) + `WelcomeCloudSignInView(provider:)` (Apple byte-idéntico; Google con subtitle propio, error real → `.error(retryable)` y cancel silencioso) + `GoogleSignInButton` custom brand-compliant (Views/Shared; asset `GoogleG` generado por CoreGraphics de los paths oficiales; variants light/dark con A11Y-DM) + §16d ambos botones apilados. **Guard R9 SUB-FIRST (H4): `ProviderMismatchLogic` pura — `exists=true ⇒ proceed SIEMPRE` (protege la entrada secundaria M1: el faro es del DUEÑO), hash-de-sub igual ⇒ proceed (linking legítimo), mismo provider ⇒ proceed (notFound honesto); mismatch SOLO exists=false + faro + provider distinto + hash ausente-o-distinto → canario `cloudSignInProviderMismatch` (primer call-site real) + signOut + NO claim + fase `.providerMismatch`.** Red post-claim degradada a breadcrumb-only (`claimProfileProviderDiffers` — H4: mismo sub ⇒ jamás alerta). `CloudAuthError` gana `.cancelled`/`.presentationUnavailable` (presenter ya no hace preconditionFailure). Falso positivo ACEPTADO y documentado: invitada SIN cuenta en device ajeno con provider distinto ve R9 en vez de notFound (el canario ya no significa solo "2ª cuenta evitada"). 8 keys l10n ×16 + `groups.signin.body` método-neutral. Gates: builds ×2 + suite 4595/424 + XCUITest chooser verde + validate-coverage OK. ~~Residual anotado (sesión 3): el runner de migración congela `storedProvider()` en su PRIMERA construcción — leerlo EN el claim.~~ **[2026-07-16, sesión 3 — revoke + seams ✅ — EL WORKSTREAM GOOGLE QUEDA COMPLETO EN CÓDIGO]** (método plan → /review-plan [3 ajustes A1-A3] → implementador → adversarial [APROBADO, 0 fixes]): `GoogleTokenRevocation` espejo de B1 con **MATCH DOBLE** — (1) `pair.sub == currentUserID` (par de ESTA cuenta) Y (2) `sdkUserID == pair.googleUserID` (hallazgo del plan: un signIn del SDK exitoso con exchange Supabase FALLIDO deja sesión SDK de B con par de A — sin el 2º match, `disconnect()` revocaría el grant del humano equivocado); carrera vs 4s con skip ≠ fallo (skips `no-pair|stale-pair|no-sdk-session|stale-sdk-session` solo breadcrumb; solo `.failed` dispara el canario `googleRevokeFailed`); restore dentro del tope; par se limpia SOLO en éxito. Paso **4b** en `AccountDeletionService` tras el 4a de SIWA (byte-idéntico), antes del cierre 4c. **I4 CERRADO**: `MigrationWorkExecutor.provider` String → closure `@MainActor () -> String` leído VIVO en los DOS lectores (claim body Y faro `writeBeacon` — un faro `apple` para cuenta `google` habría hecho que R9 mostrara el método equivocado); tests de liveness assertan el BODY post-mutación (lección d49d2e47). Verificado por grep y documentado: ni los 6 paths de sign-out ni la frontera M1 barren el par Google (paridad SIWA); el borrado exitoso sí lo limpia. Fallback REST descartado por dependencia (sin sesión SDK tampoco hay refresh token). Fase G del guion device gana el paso 7 (grant desaparece de myaccount.google.com — owner). Gates: builds ×2 + suite 4604/425 + validate-coverage OK. Quedan (encendido, Bloque C): device-QA Fase G completa · "Publicar app" GCP · Google ON en Supabase PROD · canarios `cloudSignInProviderMismatch`/`googleRevokeFailed` en cero.

- [2026-07-14] **fix(session): carrera de anchors del cover terminal de sign-out CERRADA (`c174a663`) + decisión owner UX auto-exit en background** (branch `2.0.5`; método: Plan Mode → /review-plan [5 ajustes] → impl → /code-review high [8 finders + 6 verificadores adversariales] → verificación sim). Bug device 2026-07-14: DOS anchors (cover en ProfileView sheet + red C1 root) ante `phase == .awaitingRelaunch` → UIKit tumbaba AMBAS cadenas → app USABLE con el wipe de boot armado. **Fix:** dueño único en root (ProfileView cede con `dismiss()`) + presentación VERIFICADA por el onAppear del contenido real (verify loop compartido `runRelaunchNetVerifyLoop`, decisión pura `RelaunchNetLogic` + tests de tabla, canario TelemetryDeck `relaunchNetExhausted`) + blocker de matriz por CONDICIÓN VIVA + **`exit(0)` en background con relaunch terminal pendiente (decisión owner: basta ir al inicio — UX + red final)** en **YalaApp**, NO ContentView (hallazgo /code-review: `\.scenePhase` de ContentView es POR-ESCENA — iPad multi-ventana habría matado el proceso con otra ventana visible; el de YalaApp es el AGREGADO + guard isRunningTests) + key `storage.relaunch.bodyAutoExit` ×16 (el `body` de migración/reversa NO auto-exita, intacto) + seam sim `_debugForceAwaitingRelaunch`. Cura el sign-out secundario M1 y endurece la red de ENTRADA. **PENDIENTE owner: re-verificación device (4 fases, guion SIGNOUT-WELCOME — el race solo reproduce ahí)**; gap S3 (boot wipe abortado → proceso usable con fase idle, sin red) = HALLAZGO 2 del guion, item aparte. **GOTCHAS entorno de HOY:** (a) XcodeBuildMCP `build_run_sim` NO honra la buildConfiguration del scheme — con "Yala Dev" compiló `Debug` (sin `DEV_BUILD`, bundle prod) → fijar `session_set_defaults({ configuration: "Debug-Dev" })`; (b) el `button home` del MCP no backgroundea la app en sim (cero eventos de escena) → `xcrun simctl launch <sim> com.apple.Preferences` sí entrega `.background` real; (c) el warning Sendable de `hasLocalDataNow` (ContentView:1076) es PREEXISTENTE de HEAD y solo aflora en compiles completos (DerivedData fresco) — sesión aparte lo está arreglando.

- [2026-07-13] **decisión GRUPOS-EN-V1 (Modo Nube) + feat(groups): paquete de endurecimiento 5/5 IMPLEMENTADO** (branch `2.0.5`, `81b0a894`→`da10ebfe`; método: 3 exploradores → decisión owner vía AskUserQuestion → spec → Plan Mode + /review-plan [3 ajustes, el clave: container-nil no-secundaria NO alerta — ventana transitoria con retry de PendingInviteStore] → 5 commits con gates). **DECISIÓN owner (doc `$VAULT/Backlog/modo-nube/MODO-NUBE-GRUPOS-V1-DECISION.md`):** el miedo "lanzar Sign-in con Grupos solo en CloudKit es peligroso" NO se sostiene como peligro de datos — verificado en código: bridge→motor cloud COMPLETO (saves autor-default capturados, sweep asigna syncID, columnas split_* con paridad; `GroupBridgePreference` es entidad del store PERSONAL) y grupos mixtos (.icloud/.cloud/secundaria) NO divergen (cero acoplamiento Grupos↔storageMode). Dirección (4): v1 con CloudKit + endurecimiento; **Grupos→backend PROGRAMADO 1er incremento post-v1, antes de web/Android** (DIFERIDOS #3 actualizado; híbrido "por aprobación" DESCARTADO — doble verdad + reconciliador bidireccional). **Los 5 commits:** (1/5) test e2e bridge→drain→outbox (`GroupBridgeCloudSyncIntegrationTests`, container ON-DISK — History no fiable in-memory; `bridgeExpense(shouldSave:false)` esquiva los side-effects de `saveIfNeeded` causa de la blacklist R8; columnas split_* EXACTAS decodificando fieldsJSON; cardinalidad Caso A = 2 TX + 1 draft verificada empírica) · (2/5) invite: SOLO secundaria alerta (permanente sin retry) + `.notAuthenticated` → copy "necesitas iCloud" (`GroupAcceptShareErrorLogic`) · (3/5) `GroupsICloudAvailabilityGate` §i.8(c)2 (exención uitest OBLIGATORIA — GroupsSmoke lanza sin fake-icloud; reactividad: `isAccountAvailable` es computed → leer `syncService.status` registra la dependencia; arg standalone `-fake-icloud` en **YalaApp.init** para QA agentic — BUG cazado en verificación sim: en `applyUITestHooksEarly` jamás corría sin `-uitest`; verificación sim 3 estados VERDE) · (4/5) boot-guard GAP 1 (`fetchFreshRecordName` bypass del cache + `GroupsIdentityBootGuardLogic` solo-limpia-con-2-valores-válidos-distintos + `performAccountSwitchCleanup()` extraído + canario `groupsIdentityBootMismatch`) · (5/5) gate de quiescencia del bridge remoto por `StorageModeSignalRouter` (`.cloud` → coordinator; `.icloud` byte-idéntico). 5 keys l10n × 16 (voseo es-AR). **GOTCHAS entorno de HOY (Lista Negra ×2, TESTING-STRATEGY):** (a) `ProUpsellServiceOneShotTests` ×2 fallan en sims con transacción StoreKit sandbox persistida (race del singleton: el listener concede Pro y el default-Free lo baja — la expansión de Swift Testing muestra el flip; sobrevive uninstall — vive FUERA del container; el scheme "Yala Dev" construye bundle SIN `.dev` ⇒ el guard default-Free no aplica al host de tests; sim `9D45B075` borrado); (b) `MigrationWorkExecutorTests`+`CloudSyncRuntimeTests` fallan 9 issues SOLO en el sim `6A840DF8` — exonerado por worktree pre-paquete MISMO sim suites AISLADAS + pasaron con el paquete en `9D45B075`; pista `CloudSyncPull merkle:decode-200` JSON corrupto de staging. GOTCHA worktree: `Secrets.xcconfig` es gitignored — copiarlo a mano o el build del worktree falla (y `grep` estrecho puede disfrazar BUILD FAILED de "tests fallan": verificar el xcresult tiene tests > 0 antes de dictaminar). **PENDIENTE owner: device-QA fases G1-G3 añadidas al guion M1** (invite secundaria, cambio de Apple ID, gate sin-iCloud). Ticket `qa_groups-endurecimiento-modo-nube-v1`.

- [2026-07-13] **feat(cloud-sync): M1 SESIÓN SECUNDARIA (multi-cuenta en device) — 8/8 incrementos, DARK** (branch `2.0.5`, `2cef7817`→`b0997dc2`; método: diseño 2 rondas con owner → spec → plan + /review-plan [4 fixes, el crítico: guard de mount-mismatch] → 8 commits con gates → verificación sim 4 fases VERDE). Tras el sign-out del dueño, otra cuenta nube monta un store personal SECUNDARIO por archivo (`YalaModel-Secondary`+`YalaSyncMeta-Secondary`, `cloudKitDatabase: .none` SIEMPRE — el filtrado por userID sobre store compartido es INVIABLE: el mirror de CloudKit no filtra y el replay de History cruza períodos mirror-OFF), 1 slot persistente hasta sign-out. Piezas clave: `SecondarySessionStore` (descriptor `cloudSync.secondarySession.userID` + arm + marker de purga) · getter `CloudSyncFlags.storageMode` descriptor-aware (efectivo `.cloud` en secundaria; la key PERSISTIDA conserva el modo del DUEÑO — 4 lectores explícitos a `StorageModePersistence.read()`: deriver UI migración, reverseEligibility ×2, panel) · **guard de mount-mismatch en `canRunDomain`** (`isActive && !secondaryStoreMounted` ⇒ bloqueado — en la ventana de entrada un re-arranque del runtime drenaría la History del DUEÑO a la cuenta entrante; canario `cloudSecondaryMountMismatchBlocked` + freeze de drains) · hooks pre-mount kill-safe en YalaApp (`performSecondaryWipeIfArmed` con S3-abort y flags reseteados EN EL BOOT jamás in-session; `performSecondaryEntryTasksIfNeeded` con purga E2E-M1 + cancel notifs + healing de flags; markers AL FINAL) · `PrefsSyncBehavior.localOnly` (sin él, la rama `.icloud` escribiría las prefs de la invitada al iCloud KV del dueño y la `.cloud` subiría los valores del dueño a la cuenta entrante; señales de wipe del iKV del dueño tampoco se procesan) · Grupos NO arranca + tab filtrado + StorageSettings oculta · entrada Welcome: guard `proceedSecondarySession` (flag OFF = tabla v1 exacta) + orden kill-safe claim→descriptor(COMMIT)→flags + blocker terminal `secondaryEntryRelaunch` + red durable molde C1 + D1 sin trial · faro suprimido (belt — vive en el iCloud KV del dueño) · panel DEBUG + toggle DEV (`cloudSync.debug.secondarySessionEnabled`, solo DEV_BUILD). Diseño+decisiones (8 owner) + log: `$VAULT/Backlog/modo-nube/MODO-NUBE-M1-DISENO-MULTICUENTA.md`. **PENDIENTE owner: device-QA 2 cuentas TestFlight (guion `MODO-NUBE-M1-GUION-DEVICE.md`) + encendido del flag.** GOTCHA entorno (2026-07-13): XCUITests fallan en esta Mac por el runtime local — exonerado M1 (fallan idéntico en worktree pre-M1 + tras `simctl erase`; repro manual correcta) → Lista Negra en TESTING-STRATEGY con plan CI; gates locales con `-only-testing:YalaTests`. GOTCHA sim: `simctl spawn defaults write` externo resucita keys por cache de cfprefsd — para QA de boot-hooks usar el panel DEBUG (writes in-process) o editar el plist con el sim APAGADO (PlistBuddy: escapar los puntos de la key o crea dicts anidados).

- [2026-07-13] **feat(session): "Cerrar sesión" universal + Welcome 2 niveles (gap H4 de I14) — sesión nocturna autónoma completa** (branch `2.0.5`; método: 4 exploradores → 8 decisiones del owner → plan + /review-plan [8 ajustes] → impl por fases con gates → review adversarial [2 CRÍTICOS + 6 SERIOS aplicados + 4 menores] → suite 4189/374 verde + builds 0 warnings ambas schemes; commit único). **Decisiones owner:** fila "Cerrar sesión" SIEMPRE visible (final de Seguridad y cuenta, `profile_security_signout`; privada Y nube — oculta solo en group-invite); privado (.icloud) = reset a Welcome SIN tocar datos (re-entrada: "Ya tengo cuenta → Restaurar iCloud" → directToApp); nube (.cloud) = push-all VERIFICADO por fetch (bloquear si falla, jamás descarte) → teardown → **wipe por ARCHIVOS en el próximo boot** (`signOutWipeArmed` + `SwiftDataConfiguration.performSignOutWipeIfArmed` pre-mount: borra YalaModel+YalaSyncMeta, JAMÁS filas — **el replay de la History al remontar el mirror propagaría los deletes a iCloud**, HALLAZGO 3 qa/cloud/README; YalaGroups y claim-store intactos; orden idempotente, arm se limpia AL FINAL) → relaunch asistido; **caso Pia (cuenta ajena sobre datos locales) BLOQUEADO en v1** (`CrossAccountEntryGuardLogic`; spike `$VAULT/Backlog/modo-nube/SPIKE-M1-guest-sessions.md` — solución real M1: aun con limpieza, el remount re-importa el iCloud del dueño y el orphan-reconcile del adopt corre pre-relaunch); born-cloud DIFERIDO (sub-chooser con bypass single-option). **Welcome:** "Ya tengo una cuenta" → sub-chooser (Restaurar iCloud | SIWA nativo ASAuthorizationAppleIDButton) — en prod DARK/uitest hace bypass a restore = flujo actual INTACTO; SIWA → consent (.adopt) → `GET /account/exists` (read-only — el claim con `created` CREA cuenta server-side, jamás claimear sin exists=true) → guard → `startAdoptWithExistingSession` (sin re-SIWA; resetea failedRollback primero, S4) → relaunch. Onboarding marcado TEMPRANO al arrancar el adopt (cierra el hazard kill-mid-adopt → seed sobre cuenta ajena). **Del review adversarial:** red DURABLE del cover de relaunch en ContentView (C1 — ProfileView es sheet dismissible; con wipe armado la app JAMÁS queda usable) + `welcomeCloudSignIn`/`signOutRelaunch` en la matriz `ShellReadinessState` (C2; signOutRelaunch solo cede ante wipingData; welcomeCloudSignIn NO es welcome-chain tearable — el adopt en vuelo retiene intents) + pausa 250ms anti-`.coalesced` en el push-all (S1: un ciclo en vuelo devolvía coalesced sincrónico → 20 iteraciones en µs → blocked espurio) + abort del wipe si el borrado del archivo BASE falla (S3: continuar remontaría mirror sobre el archivo .cloud → replay destruiría el backup; arm persiste y reintenta) + re-verify del outbox pre-arm con credenciales AÚN vivas (S2) + freeze de `AppBootstrapper.handleBecameActive` con wipe armado (S2: los drains de Apple Pay/Siri escribirían en un store condenado) + `hasLocalDataNow` closure viva para el guard (S5: el mirror re-importa en background durante el Welcome) + flowTask cancelable en onDisappear + skip re-SIWA con sesión viva. **Residuales documentados:** race exists→created (~0, requiere borrado server-side entre requests), writes History-only post-teardown, blocked-sin-sesión requiere relanzar, sidecars external-storage, iCloud congelado re-importa post-wipe (semántica reinstalación — el Hero lo detecta, esperado). 27 keys l10n × 16 locales (voseo es-AR aplicado). Breadcrumbs `CloudSignOut*` sin PII. Botón DEBUG "Arm sign-out wipe" en CloudSyncDebugView (única verificación sim del boot-cleanup — SIWA no corre en sim). Coverage: área nueva `session-sign-out` + bump welcome-flow-visual/cloud-migration-ui/settings-profile-general. **PENDIENTE owner: device-QA** — guion `$VAULT/Backlog/modo-nube/MODO-NUBE-SIGNOUT-WELCOME-GUION-DEVICE.md` (4 fases + kill-tests + offline-test). Plan `~/.claude/plans/logical-jingling-lighthouse.md`.

- [2026-07-11] **fix(routing): bug TestFlight "toolbar muerta + bandeja que no abre" CERRADO — 5 commits (7aab55a3→a7f23868), plan aprobado + /review-plan con 3 ajustes** (branch `2.0.5`; método: 3 exploraciones paralelas → auditoría exhaustiva de intents/deeplinks/modales → Plan Mode → /review-plan (NECESITA AJUSTES, 3 aplicados) → implementación por fases con gates en worktree aislado por sesión paralela activa). **Root cause del repro** (tap notif registro automatizado → pro gate → alert inbox → toolbar bloqueada): cover del `InboxAlertModal` con binding setter no-op + sin `onDismiss:` + backdrop `Color.black.opacity(0)` hit-testeable + `.presentationBackground(.clear)` → el dismiss del paywall (que NO era blocker de readiness) tumbó el cover sin correr su reset → capa invisible tragándose todos los taps + router congelado por `hasActiveInboxAlert`. **Los 5 commits:** (1/5) setter real + `onDismiss:` + `allowsHitTesting(isVisible)`; (2/5) matriz `ShellReadinessState` COMPLETA (+6 blockers: proTrialOffer/whatsNew/syncSettingsSheet/restoreOffer/inviteError/groupSyncError — la doble-presentación mismo-anchor descartaba la segunda con el intent ya consumido; efecto gratis: los 2 alerts de grupos ya no se tragan mutuamente; `ReadinessGateObservers` partido en 2 por presupuesto del type-checker); (3/5) one-shots quemados al PRESENTARSE (trialExpired: del drain al onAppear del callsite; milestone: del PRODUCTOR a MilestoneUpgradeSheet.onAppear; trialOffer: clear en drain, no al emitir; firmas del inbox alert: del productor al drain vía `pendingInboxSignaturesToBurn`/`commitPendingInboxAlertSignatures` — residual documentado: drop por gate puede re-alertar drafts pending); (4/5) **gate estructural Clase D**: `.mainTab`/`.panel` drenaban A CIEGAS bajo covers superiores (intent consumido + sheet seteado tapado = presentación perdida) → `RouterConsumerGateLogic` (peek-first + hold, AppRouter INTACTO) + `SessionState.shellModalBlocker` (escritor único: `updateContentViewReadiness`) + `isMainTabModalVisible` (también blocker cross-node `mainTabModal` en la matriz) + `PanelSheetState.hasActivePresentation` + re-drains por `onChange` de cada dimensión (liberar blocker no bumpea revision — molde ChatSheet); (5/5) BORRADOS PresentationCoordinator/CascadeLogic/Kinds + test (dormidos, cero call-sites — el gate 4/5 implementa su propósito). ProUpsellService: `init(defaults:)` inyectable. Tests nuevos: RouterConsumerGateLogicTests (13) + PanelSheetStateActivePresentationTests + ProUpsellServiceOneShotTests (4) + 3 escenarios en RoutingIntegrationTests + 10 casos de matriz; ContentViewReadinessLogicTests migrado al builder `make()`. Gates: 109 tests/6 suites del área + suite completa en worktree; builds ambas schemes. **PENDIENTE del owner: device-QA del repro real en TestFlight** (tap notif con trial expirado; escenario espejo: alert visible + cerrar → toolbar viva). Diferidos capturados: canario telemetría de holds >N s (D4, decidir si se quiere), hardening CoachMarkOverlay (misma clase de trampa, sub-segundo), guard `.planning`, XCUITest del escenario paywall+notif. Plan `~/.claude/plans/haz-un-plan-formal-shimmering-popcorn.md`.

- [2026-07-12] **fix(groups): invitación/unión robusta (`dea3e61b`, branch 2.0.5) — bugs de la corrida real 2026-07-11** (método: 3 exploradores → Plan Mode con 2 diseñadores paralelos → /review-plan [5 ajustes] → impl → suite 4150/367 verde + XCUI 5/5 + builds 0 warnings). (1) La solicitud del invitado JAMÁS llegaba al owner: `acceptShare` creaba el member solo con `SplitGroup` local presente — la ventana export-only (≥60s, timing NORMAL de un fresh) lo saltaba EN SILENCIO sin reintento → `PendingJoinStore` (intent TTL 7d) + `GroupJoinReconciler` (4 triggers, decisión pura `GroupJoinReconcileLogic`). (2) Notif espuria "X se unió" al invitado por members PREEXISTENTES en su primer import → autoexclusión por identidad + baseline `SplitGroup.initialMemberImportStartedAt` (set en applyGroupMeta NUEVO, clear en `didFetchRecordZoneChanges` antes no-op, 15min auto-sana). (3) "¡Todo listo!" sin member (fallback defensivo) → `GroupJoinIntentTracker` @Observable + `GroupInviteOnboardingLogic` (invariante: jamás éxito sin member) + banner en tab Grupos + 10 keys l10n ×16. Hardening: enqueue con engine nil = log + canario `cloudkitGroupEnqueueDroppedNoEngine`; telemetría `groupJoinIntent*` (Expired>0 = invitado fuera pese a aceptar). Regla nueva en Reglas inviolables (post-accept = intent persistente). Descartado: el enlace de semanas (CKShare URL estable). PENDIENTE: device QA cross-device TestFlight — guion `$VAULT/Bugs/qa_groups-join-intent-reconciler.md`; remediación de Pia sin release: reabrir el enlace. Diferido a Backlog: alerts huérfanos detrás de fullScreenCovers.

- [2026-07-11] **feat(cloud-sync): DIFERIDOS #29 CERRADO — IdentityRemap §b.4 (bloqueante del gate de flags)** (branch `2.0.5`; método plan → /review-plan → Opus impl → Opus adversarial; gates en WORKTREE AISLADO HEAD+changeset por sesión paralela activa). Regenerar un UUID de identidad cableado como sync_id (`Tag.id`/`Account.shortcutID`/`Subcategory.shortcutID`) ya emite al outbox EN LA MISMA transacción del reparador (§b.4 crítica #6): tombstone(old, reason `.remap`, dedupeado por oldID) + upsert FILA-COMPLETA(new) + **upserts FULL de TODAS las filas referenciantes — hallazgo central: las columnas `*_ref` se derivan de la RELACIÓN VIVA al emitir (`Emit.ref(m.account?.shortcutID)`) ⇒ la regeneración cambia su proyección SIN write, History jamás las captura → divergencia Merkle perpetua clase-FX; la re-emisión sintética por travesía inversa concreta por tipo la cierra** + re-key del testigo `SyncIdentity` preservando `ck*` (§b.5: el CKRecord congelado es EL MISMO — en reversa, tombstone(old) del backend no resuelve testigo → skip correcto) + `lastReboundAt` + limpieza unit clocks del oldID. **DECISIÓN: tombstone+upsert CLIENT-SIDE, CERO cambios gateway/RPC/wire** (PK `(user_id,sync_id)` disjunta, conmutativo, HLC-idempotente; §b.4 ya fijaba esa traducción — op first-class diferido a v2 multi-device). El drain sigue SKIPeando updates identity-only (canario D4 = red para sitios NO cableados; `identityRemapEmitted` delata el remap sano). Review adversarial (3 SERIOS + 3 MENORES, 6/6 aplicados): PREFLIGHT antes-de-mutar para fallos previsibles (motor ausente / migración-reversa en curso vía `BGTaskMigrationGate.decide`+`MigrationPhaseStore`) — **hallazgo empírico: `ModelContext.rollback()` NO revierte valores en memoria** (rollback queda solo como cinturón de imprevistos + `identityRemapAborted`); espejo App Group DIFERIDO a post-save (`mirrorRemapRows` — pre-save envenenaba el rehydrate si el save fallaba: tombstone de un id VIVO); colisión de newID pre-chequeada (regla `00439727`); la one-shot de AppBootstrapper SALTA la regen masiva en `.cloud` (reinstalación post-cutover haría `regenerateAllUUIDs` sobre el corpus pulleado; regenKey quemado = intencional). Gates: IdentityRemapEmissionTests 15 (contenido REAL de outbox assertado, lección `d49d2e47`) · suite 4017/0 fallos · e2e staging 10/10 · builds 0 warnings. PENDIENTE: coverage-index área cloud-sync + nota v2 en qa/cloud/README (archivos retenidos por la sesión paralela al commitear). Plan `~/.claude/plans/diferidos-29-identity-remap.md`.

- [2026-07-11] **feat(cloud-sync): I11 REVERSA §h COMPLETA (6 sub-incrementos) + CORRIDA DEVICE VERDE end-to-end — con I10, la migración nube↔CloudKit queda entera en código; siguiente: I14** (branch `2.0.5`; sesión autónoma 2026-07-10 + corrida device del owner 2026-07-11 con el guion `MODO-NUBE-I11-GUION-DEVICE.md`). **Los 6 sub-incrementos:** I11-1 `4bdcd376` máquina §h pura (10 fases reverseConfirm(origin)→…→icloudActive + 13 eventos + 6 efectos append-only; entrada desde `done` Y `notStarted` — los ADOPTADORES tienen journal notStarted; boundary del rollback = el MOUNT del mirror; mismatch del reverseVerify RE-DRENA — backend→local manda, jamás re-sube; cuarteto de cierre EN ORDEN [deleteCloudKitMarker, clearCloudBeacon, persistICloudMode PAR, completeReverseServer]; **markerReconciliation pasó a switch EXHAUSTIVO sin default — el default silencioso habría enrutado un device EN PLENA REVERSA a falso `secondaryDeviceCloudLogin`**) · I11-2 `d94549c9` reverseReconcile REAL (**HALLAZGO central: el token de History del mirror SOBREVIVE el período mirror-OFF [S2] → el remontaje REPLAYEA la ventana nube nativamente; `deletingZombies` real = barrido tombstones-del-backend vs filas VIVAS por syncID vía pull read-only SIN applyPage** — el apply normal borra testigos; anti-eco: deletes del sweep bajo `outboxSaveAuthor`; `reverseOriginRaw` schema v2 que `reverseFailedRollback` CONSERVA; review cazó un `try?` que saltaba el borrado del marcador PERMANENTEMENTE) · I11-3 `77cb3361` server-side staging (RPC `migration_progress` + 4 acciones `reverse_*`; takeover SOLO de migración abandonada con lease expirado; `reverse_abort` des-congela sin estampar `reverted_at`; goldens TS 122/122; **ASIMETRÍA documentada: `reverse_claim` es read-then-UPDATE vs el INSERT atómico de `claim_account` — fix CAS requerido ANTES de multi-device, gate de flags**) · I11-4 `4efd4a62` dedup auto-cura (healDuplicates → MERGE real `AccountTagMergeService`, patrón deduplicateSeedCategories — la spec decía "reusa regenerateDuplicateUUIDs", imprecisión: eso repara COLAPSO, esto es contenido-idéntico-id-distinto; **HAZARD multi-device documentado [ganadores distintos → ambos mueren] = por qué la cura es reversa-only**; system accounts EXCLUIDAS) · I11-5 `0170d38a` panel + guion (gate DURO `ReverseEligibility` pre-diálogo; DIÁLOGOS-PRIMERO; jamás botón de forzado) · I11-6 `e76c551a` retiro harnesses Spike S5/S6. **CORRIDA DEVICE (2026-07-11): VERDE** — staging `rip=false` + `reverted_at` + **`migrated_at` INTACTO (§h.4, señal del re-cutover futuro)**; marcador BORRADO de CloudKit (Console 0 records); device en `.icloud` estable; freeze→icloudActive ~4 min; R7-R12 COLAPSAN en una pasada (~8s) si la época nube no tuvo escrituras. **4 HALLAZGOS:** (1) **el escape hatch post-cutover deja residuo silencioso** (journal borrado PERO marcador CloudKit vivo + `migrated_at` → falso `secondaryDeviceCloudLogin` que bloquea re-migrar) → botón panel "Borrar marcador CloudKit (stale)" `f58c6666`; (2) **CRÍTICO pre-flags: fila ExchangeRate huérfana del canal** — el boot post-cutover escribió FX CON syncID pero su History tx ESCAPÓ a la captura del drain → invisible al push, visible al Merkle local → **reverseVerify mismatch PERPETUO (3 corridas a `reverseFailedRollback`; divergencia local-ahead es inconvergible por diseño — autoridad backend→local)**; palanca: purga de la caché FX (re-derivable, cada TX guarda su `exchange_rate`) + diagnóstico FX en dry-run `397519b8`; **CERRADO 2026-07-11 — root cause = no-comparabilidad cross-mount del `historyToken` (mount `.icloud`→`.cloud` sobre el mismo archivo; `$0.token>staleToken` excluye la tx del mount nuevo SIN lanzar → `pending=0`), NO `table(forClass:)`; fix: `SyncCursor.lastDrainedTxAt` + guard `recoverIfHistoryTokenIncomparable` en `performDrain` (revalidación continua por timestamp, re-ancla al mount actual) + breadcrumbs `historyTokenIncomparable`/`historyTokenRecovered`; `CloudSyncEngineTests` T0-T7; store sync-meta `.none` → sin deploy CloudKit; detalle en `qa/cloud/README`**; (3) el sweep de zombies no tiene coordenadas para tombstones ya aplicados (el apply borra fila+testigo) PERO CloudKit quedó limpio igual: **la red real de propagación de borrados de la época nube es la History del MIRROR al remontar, no el sweep** — **CERRADO 2026-07-11 como cubierto: matriz 7-celdas en `qa/cloud/README`, única celda sin red (tombstone pre-mount + History del mirror PURGADA) INALCANZABLE hoy (purga doble-DARK), delatada por `scanOrphanMetadata`, reparación diferida D4**; (4) `rebindsVerified=2` — **CERRADO 2026-07-11 BENIGNO: los 2 pares `InboxDraft` byte-idénticos pre-`00439727` (rebind-por-ancla en la era pre-guard); panel read-only "Testigos rebindeados" + checklist del owner en `qa/cloud/README`**. **Gotchas operativos:** re-migrar un user = reset service-side (profiles + 16 tablas) + **re-sembrar `sync_seq_counters` ALTO (10000) — jamás dejarlo arrancar en 1 con un device que ya tiene cursor**; NO usar escape hatch post-cutover salvo emergencia; Console.app no muestra logs retroactivos — streaming ANTES de reproducir. Gates: 3990-3994 tests/354 suites · e2e staging 7/7 asserts estrictos · builds ambas schemes 0 warnings. **I14 (siguiente): UI real + consent + claimAction + relaunch asistido + encendido de flags con su gate** (IdentityRemap #29, huérfano cross-device #30, CAS del reverse_claim, enforcement del freeze en /sync/push, bug FX + sweep). Memoria `project_modo_nube_fase4_progreso`.

- [2026-07-10] **feat(cloud-sync): I10-WIRING w1-w8 COMPLETO — la máquina de migración §g cableada end-to-end + CUTOVER DEVICE VERDE (`done`, corrida 2026-07-10)** (branch `2.0.5`; sesión nocturna autónoma + corrida device del owner; método por sub-incremento: plan → `/review-plan` → implementador Opus → reviewer adversarial Opus). **Los 8 sub-incrementos:** w1 `2e26216f` `MigrationState` @Model journal single-row sync-meta (patrón SyncCursor; fases Codable con fixtures wire APPEND-ONLY; pendingEffects journal-then-execute N1; contadores S9 independientes) · w2 `1661dfa6` `MigrationRunner` (transición = UN save ANTES de ejecutar, molde SpikeS6; **B1 del review: `sameDeviceReclaim` SIEMPRE false — derivarlo del intent local pre-POST promovía a un device PERDEDOR como 2º líder; el backend colapsa el re-claim del mismo líder a `created`**; `.disableMirrorAndRelaunch` resuelto por OBSERVACIÓN en resume, anti relaunch-loop) · w3 `b8600c45` `CKIdentityCapture` (Estrategia B COLUMNAS del S5: SQLite READ-ONLY de side-tables, tri-estado NULL≠sin-fila, zona por FK exacto) · w4 `6f82adc5` `MigrationSnapshotUploader` (fila-COMPLETA 16 entidades, keyset `{table, afterSyncID}` H5; **BUG cazado por test: `fastForwardHistoryBaseline` anclado a la tx del PROPIO motor era cross-store no-comparable → writes concurrentes post-baseline se PERDÍAN ~50% → fix: última tx NO-motor**; poison sin dead-letter = página jamás confirmada = migración atascada en transient perpetuo → `deadLetterPoison` compartido) · w5 `bff1a74d` executor+verify (**HALLAZGO load-bearing: `verifyIntegrity` exige `lastPullCycleCompleted` que JAMÁS se pone durante la migración → `pullAndApplyOnce` OBLIGATORIO antes de verificar o skippearía SIEMPRE**; faro §g.4 TEMPRANO; `/sync/merkle` ya traía counts por entidad → cero endpoints nuevos) · w6 `57fd65d8` CUTOVER §g.4 e2e staging + gate §i.9 FUNCIONAL (server: `claim_account` gana `p_migration` ATÓMICO en el INSERT + RPC `migration_progress(cutover/complete)` + lease takeover 60min; **2 SERIOS del review: (1) kill-window que ENCLAVABA la migración — el marcador exporta ASYNC; un kill involuntario relanzaba con mirror OFF → el marcador jamás exportaba → clavado en markerWritten para siempre → el montaje mirror-OFF exige el PAR `.cloud && mirrorOffArmed`, flag escrito SOLO tras `isMarkerExported()` — hueco de la PROPIA spec §g.4 que el gate de export hizo visible; (2) un born-cloud sobre lease vencida recibía `created` y sembraba ENCIMA de datos a medio migrar → guard `p_migration` en el takeover, golden 9-bis**) · w7 `07464171` panel DEBUG Migración (dry-run §g.5 + botones idempotentes + escape hatch; JAMÁS "forzar done") · w8 `8ed70d53` capa de RED del líder (barrido residual drain+push ANTES de `complete`, canario `cloudCutoverLeaderOrphanReconciled`; INVARIANTE: el líder solo responde por SUS PROPIOS writes — fila ajena en el CloudKit congelado = residual DIFERIDOS #30) + drenaje único iKV→backend LÍDER-only (un adoptador tardío con HLC fresco PISARÍA prefs más nuevas del backend). Gate: 3929 tests/353 suites · e2e staging 7/7 · goldens TS 114/114 · builds 0 warnings. **CORRIDA DEVICE: `done` VERDE end-to-end** — marcador verificado en CloudKit Console (serverSeqCut=8157), `migrated_at`+mip=false en staging, 1136 tx + 8/8 drafts con sync_ids distintos; dato empírico: ~1.1k filas suben en minutos (el lease de 60min solo preocupa en corpus 10k+ — heartbeat por página pendiente antes de flags). **La corrida cazó 5 BUGS reales invisibles en sim:** `05a61829` (el panel no exponía el evento de consent → clavado en `consent`) · `d49d2e47` (**`performClaim` SIN `migration:true` → cutover clavado en `not_in_progress`; el param existía en cliente+goldens pero nadie lo pasaba — el gap era exactamente un test que no assertaba el BODY enviado**, lección replicada en los tests de I11-3) · `6fef2c15` (el escape hatch DESHABILITADO por `isWorking` durante los ~20 min del upload — el botón de EMERGENCIA bloqueado justo cuando hace falta) · `00439727` (**el rebind por ancla de contenido daba el MISMO syncID a dos filas VIVAS byte-idénticas — el residual A3 "aceptado como raro" materializado en el primer corpus real (2 pares de InboxDraft) → el upsert colapsa 8→6 → divergencia Merkle PERMANENTE → failedRollback; regla nueva: rebind SOLO si ninguna fila viva porta ya ese syncID + auto-cura `identityCollisionHealed`**; y `.rollback` notWired quedaba journaled-pendiente perpetuo → no-op real). **Deploy del owner (2026-07-10, `c76b5c2a`):** snapshots-contrato CloudKit consolidados en `Cloudkit Schemas/` (raíz, 3 containers × 2 entornos; `CloudKitGroupsSchemaParityTests` re-apuntado) + `CD_CloudMigrationMarker` a Production; **HALLAZGO de paso: `CD_GroupBridgePreference` FALTABA en TODOS los entornos con write-site VIVO en prod (`BridgeModeResolver:96`) — la clase exacta del incidente `isOpeningBalance`, cerrado.** Gotchas de proceso: la suite completa DEBE correr bajo scheme "Yala Dev" (la "Yala" no tiene config de staging); `npm test` del gateway re-crea filas parciales de budgets que divergen en el e2e Swift → re-tombstonear (SQL en `qa/cloud/README`, `0ca248e8`). Memoria `project_modo_nube_fase4_progreso`.

- [2026-07-08] **feat(cloud-sync): Fase 4 Modo Nube — 16/16 entidades cableadas al motor (I12-A `1bdab526` + I12-B `cded8f60`) + máquina de migración §g pure-logic (I10-pre `d18acf9e`) + prefs por storageMode (I13 `28b7b905`)** (branch `2.0.5`, sesión autónoma; método por incremento: exploración → plan → `/review-plan` → implementador Opus → reviewer(s) adversarial(es) Opus → suite completa + e2e staging → commit; los 4 reviews adversariales dieron 0 bloqueantes, con fixes menores aplicados). **I12:** drain GENERALIZADO a identidad no-opcional — las 10 entidades nuevas usan su `id`/`shortcutID` existente como sync_id (manifest `sync_id_source`, §b.1; `@Attribute(.preserveValueOnDeletion)` en las 10 identidades, metadata de History sin campo CloudKit → sin deploy .ckdb; SIN cambios en hubs: identidad inherente). **GOTCHAS:** (1) `table(forClass:)` era el punto de descarte silencioso del push — cazado por `/review-plan`; (2) NotificationItem.reportConfig NO es storage 1:1 → applier RMW directo del blob `configurationData` order-independiente, con GUARDIA inline: si `typeRaw` se vuelve mutable, su `columnKeyPaths` debe arrastrar las 2 columnas report o el Merkle diverge; (3) el e2e conserva estricto `diverged==["tx_items"]` — filas legacy hand-crafted de staging se TOMBSTONEAN server-side (UPDATE deleted=true vía PostgREST), NUNCA se relaja el assert; (4) DIFERIDOS #29: regenerar `Tag.id`/`shortcutID` cableados sin evento IdentityRemap (§b.4) = divergencia silenciosa → canario `identityMutationObserved` interino; IMPLEMENTAR antes de encender flags. **Decisión owner (2026-07-09):** el tombstone del PROPIO padre de cascada clasifica `cascade` (no `user`) — audit-only aceptado. **I10-pre:** `MigrationStateMachine` nonisolated (38 goldens; S9 timeout-red≠mismatch con contadores independientes; cutover journaled con faro TEMPRANO y marcador ÚLTIMO; `waitingForLeader`). **Decisión owner (2026-07-09):** `markerReconciliation(done + marcador)` → `.none` confirmado. Notas para I10-wiring en `~/.claude/plans/i10-migration-state-machine-pure.md` (S2: la REVERSA debe borrar el marcador CloudKit; N1: `resume()` no re-emite efectos → wiring infiere del sub-estado). **I13:** server-side ya existía (I6); cliente = `PreferenceMergeLogic` (34 keys, extracción ORÁCULO-PRIMERO byte-idéntica de `applyRemoteValues` — código CALIENTE de prod verificado key-por-key por review) + `PrefsOutbox` (App Group, HLC monótono persistido; drift >5min → `clockFailed`, no emite stale) + `PrefsSyncClient` + paso 5.5 del runtime **gateado DURO por `storageMode == .cloud`** (S1 del review: sin el gate, un device de SPIKE `.icloud`+runtime-ON haría split-brain iKV/backend). DIFERIDOS #30 (drenaje KV→backend en cutover, residual S8). Memoria `project_modo_nube_fase4_progreso`.

> Historial completo de decisiones (67 entradas, 2026-04-28 → 2026-07-06) archivado en `$VAULT/planning/DECISIONS.md` — sección "Archivo cronológico". Última tanda: las 10 entradas de la fase 2.0.4 (2026-07-01 → 07-06, compactadas en 9), archivadas el 2026-07-11 al cerrar la fase.
