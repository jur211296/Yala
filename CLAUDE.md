# Yala (iOS)

Yala es una app iOS de finanzas personales. Objetivo: entender gastos, cuentas, presupuestos y reportes con claridad.

## Stack

- Swift, SwiftUI, SwiftData (.xcodeproj)
- **Target iOS 26+** — APIs nativas (Liquid Glass, ToolbarSpacer, etc.)
- Schemes: **Yala** (producción) | **Yala Dev** (con toggle Pro y `DEV_BUILD`) | Tests: YalaTests
- Simulador: **iPhone 17 Pro**
- 21 SwiftData models. ModelContainer via `SwiftDataConfiguration`. Divisas SSOT en `Yala/Utils/CurrencyUtils.swift` (`CurrencyCode`, 48 divisas).

## Docs (leer cuando sea relevante)

| Archivo | Propósito |
|---------|-----------|
| `$VAULT/planning/CODEBASE-MAP.md` | Tablas de Services / Calculators / ViewModels / Tests con paths |
| `$VAULT/planning/UI-PATTERNS.md` | Design System, gotchas de SwiftUI, formularios, glass |
| `$VAULT/planning/SWIFT-STYLE.md` | ViewModel pattern, idioms modernos, DS.Semantic / DS.Gradients, "añadir preferencia" |
| `$VAULT/planning/L10N.md` | 16 locales, workflow para añadir keys, tests CI |
| `$VAULT/planning/DEVICE-QA.md` | Setup Yala Dev + agent-device patterns |
| `$VAULT/planning/BRAND-VOICE.md` | Tono y estilo de marca |
| `$VAULT/planning/WORKFLOW.md` | Workflow detallado de skills |
| `$VAULT/planning/PROJECT.md` · `ROADMAP.md` · `STATE.md` | Producto, plan, progreso |
| `$VAULT/planning/DECISIONS.md` | Registro de decisiones arquitectura |
| `$VAULT/planning/QA-SCENARIOS.md` | Escenarios de prueba |
| `qa/README.md` | QA automatizado (agent-device + suites) |

`$VAULT` = `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/YalaWiki/` (Obsidian, sync vía iCloud).

Carpetas del vault: `Backlog/` · `Ideas/` · `Bugs/` · `Attachments/` · `planning/`. Skills: `/backlog`, `/spec`, `/idea`.

## General Rules

- Hacer SOLO los cambios explícitamente solicitados. No mover UI, refactorizar adyacente, ni añadir mejoras no pedidas.
- Antes de editar, listar archivos a modificar y qué cambia. Esperar aprobación si son más de 3 archivos.
- Confirmar que rutas referenciadas existen antes de proceder. Si no, preguntar al usuario.
- Evitar refactors grandes innecesarios. No introducir dependencias nuevas sin justificación.
- Mantener separación UI / lógica / SwiftData.

## Reglas inviolables

### Errores y unwraps
- NUNCA `try?` que silencia. Usar `do { try } catch { print("Service: Error: \(error)") }`.
- NUNCA force unwraps sin validación previa (`guard let x = ... else { return }`).
- Logs SIEMPRE dentro de `#if DEBUG` — nunca datos sensibles en producción.
- API keys: NUNCA hardcodear. Usar `Secrets.xcconfig` + `Info.plist` via `Bundle.main.object(forInfoDictionaryKey:)`.

### SwiftData
- SIEMPRE `@Relationship(inverse:)` en relaciones bidireccionales.
- Verificar `deleteRule` en cada relación.
- `@MainActor` en servicios que manipulan `ModelContext`.
- **CloudKit compat:** NUNCA `@Attribute(.unique)`, propiedades con default obligatorias, ni relaciones non-optional.
- **`#Predicate` GENÉRICO-PROTOCOLO crashea (`DataUtilities.swift:85`) — usa concreto por tipo:** NUNCA un fetch genérico `func f<T: SomeProto>(…) { FetchDescriptor<T>(predicate: #Predicate { $0.id == id }) }` donde `id` es un **requisito del protocolo** — `$0.id` resuelve al keypath del **witness del protocolo**, que SwiftData NO puede casar con el keypath concreto `\ConcreteType.id` del schema → `Fatal error: Couldn't find \X.<computed …> on X` al EJECUTAR el fetch (SIGTRAP, no atrapable). Síntoma: dos keypaths distintos para `id` en el mensaje. SIEMPRE `#Predicate<ConcreteType> { $0.id == id }` **concreto por tipo** (los concretos resuelven en CUALQUIER `ModelContext`; si necesitas DRY, un genérico que solo EJECUTE un `FetchDescriptor<T>` ya construido concretamente es seguro). Costó el crash de "generar enlace / forzar sync" en Grupos (`fetchByID<T: HasUUID>`/`deleteModel` → 5 helpers concretos `splitGroup/Expense/…(byID:in:)`, protocolo `HasUUID` ELIMINADO como footgun; commit `c74349fc`). El `ModelContext(container)` dedicado NO era la causa (los `#Predicate` concretos del mismo archivo nunca crashearon ahí).
- **Lazy M2M con CloudKit — CSV mirror:** cuando una `@Relationship` M2M `[Type]?` es crítica para correctness (cálculos, filtros, displays) y la app corre en cold start con sync activo, **duplica el filtro como CSV de UUIDs** en el mismo record. Patrón replicado en `Budget.subcategoryIDs/accountIDs/tagIDs` y `TransactionItem.tagIDs`. Helpers: `setFilters(...)`/`setTags(from:)` para dual-write; `resolvedXIDs(scheduleBackfill:)` para read CSV-first con M2M fallback y auto-cura async. La relación M2M queda solo para cascade `.nullify` automático. Razón: SwiftData/CloudKit puede entregar el record con relaciones lazy `nil` mientras espera que lleguen los Tag/Subcategory/Account records — el código pre-fix interpretaba `nil` como "sin filtros" → comportamiento catastrófico. Naming asimétrico aceptado: `Tag.id`, `Subcategory.shortcutID`, `Account.shortcutID`, `Budget.id`. Backfill eager en bootstrap **solo para Budget** (pocos records); TX usa lazy on hot path (miles de TX, eager sería caro).
- **CSV mirror — stale ≠ nil al regenerar un UUID de identidad (commit `899c1c25`):** `resolvedXIDs(scheduleBackfill:)` (y los `resolved*` de Budget) leen **CSV-first y SOLO caen a M2M si el CSV es `nil`**. Un CSV *stale-pero-presente* (apunta a un UUID que ya no existe — p.ej. tras regenerar un id de identidad) NO cae a M2M → `byIDLookup` no lo resuelve → display/filtro queda **huérfano permanente** (no auto-sana). ⇒ **regenerar cualquier UUID de identidad (`Tag.id`/`Account.shortcutID`/`Subcategory.shortcutID`) OBLIGA a reconstruir TODOS los mirrors que lo referencian con _nuke-on-nil_**: si la M2M forward viene `nil` (ventana lazy en paths sin quiescencia: force-sync/DEBUG), escribir `encode((m2m ?? []).map(\.id))` (= nukea el CSV → fuerza el fallback a M2M en la próxima lectura), NUNCA saltar (dejar el CSV stale orfanaría permanente). El colapso de estos UUIDs (CloudKit entrega el record sin el campo → default `UUID()` evaluado UNA vez y compartido) lo auto-cura `CategoryDeduplicationService.repairCollapsedIdentityUUIDs` (repetible, gateado por quiescencia, regenera solo colisionados vía `collidedUUIDItems`/`regenerateDuplicateUUIDs` + reconstruye mirrors); la migración one-shot **difiere** si el primer import de CloudKit no se asentó (`MigrationGateLogic.shouldDeferMigration`) para no regenerar sobre datos parciales y quedar bloqueada por su sentinel.
- **Sync de Grupos (CKSyncEngine) NO debe arrancar/`save()` sobre el `mainContext` compartido antes de que el primer import personal de CloudKit se asiente — crash-loop en restore de iCloud:** la app monta **un solo `ModelContainer` con dos stores en el mismo `mainContext`** — personal (`cloudKitDatabase: .private`, espejado por NSPersistentCloudKitContainer) y grupos (`.none`, CKSyncEngine manual). En un device **restaurado de iCloud**, el store personal se importa a medias mientras el sync de Grupos hace `modelContext.save()` sobre ese mismo contexto → SwiftData dispara un **`_assertionFailure` interno (`EXC_BREAKPOINT`/SIGTRAP)** al persistir el grafo a medio hidratar. **El `do/catch` NO lo atrapa** (es un trap que mata el proceso, no un `throw`) → crash-loop en cada cold launch porque CKSyncEngine re-entrega su cola persistida. ⇒ **Fix de raíz (2026-06-21, commit `6360d964`): el delegate del sync usa un `ModelContext` DEDICADO (`groupSyncContext = ModelContext(container)`, `autosaveEnabled=false`, confinado a `@MainActor`), NO el `mainContext`.** Sus `save()` persisten solo el store de grupos (`YalaGroups`, `.none`); como los `Split*` no usan `@Relationship` (vínculos por IDs planos) y el import personal mergea al `mainContext`/viewContext (no a contextos derivados), el grafo personal a medio importar **no se arrastra** → el assert del SAVE DEL SYNC desaparece. **(Actualización 2026-06-22: el contexto dedicado solo NO bastó — comparte el `NSPersistentStoreCoordinator` con el store personal, así que build 29 SIGUIÓ crasheando, pero en un `save()` del store PERSONAL durante el import [seed de notificaciones / invite-onboarding / reconciles de boot], NO en el del sync. Fix COMPLETO: gatear por QUIESCENCIA del import [`iCloudSyncService.isImportQuiescent`, adaptador runtime de `SubcategoryDedupGate.decide`] TODOS los `save()` automáticos del store personal — seed/dedup notificaciones, scheduled-payments, exchange-rate, provisional-tx, race-cleaner, dedups de grupo, y los boot Tasks freeze/reconcileTransferPairs/reconcileDisplayName/refreshCurrentUserFlags/sendDueReports/migrateToLiveBalance — vía guard sync por-save o `awaitPersonalImportForBootSave` [que ahora espera primer-import + quiescencia, NO solo el primer evento]; y promover el sync de grupos por QUIESCENCIA, no por el primer `importEvent` [`SplitSyncStartGate.resolveWaitByQuiescence`, exige `hasCompletedFirstImport && isQuiescent`]. `SaveBreadcrumb` [Console.app, fuera de `#if DEBUG`, sin PII] instrumenta cada save site → si reaparece, el último `SAVE <site>` lo nombra sin necesidad de dSYM. **Residual documentado**: aceptar un invite con tap EXACTO durante el import [`ensureCurrentUserMemberExists`/`updateCurrentUserDisplayName` vía `handleJoinTap`] NO está gateado — es user-tap, no el auto-loop reportado; gatearlo rompería la UX de unirse.)** ~48 usos del delegate + reads (`group(for:)`/`mostRecentGroup`) repuntados al `groupSyncContext`; los 3 métodos de `GroupService` invocados desde el delegate (`ensureCurrentUserMemberExists`/`performRemovedSelfCleanup`/`refreshCurrentUserFlags`) se parametrizan con `context:` (default `mainContext` para UI/boot). **Refresh de UI**: observa `ModelContext.didSave(groupSyncContext)` → `markRemoteChangePending()` (`dataVersion` → `loadData()` re-fetch); las vistas de Grupos no usan `@Query`. **El bridge personal** escribe `TransactionItem`/`InboxDraft` en `YalaModel` (el store que se importa) → NO aislable por contexto; se gatea por **QUIESCENCIA del import** (`SubcategoryDedupGate.decide`, por `isImporting` no `isSyncing` para no bloquear exports normales); al diferir marca `SplitExpense.bridgePending=true` (save seguro del store de grupos) + retry en sesión (`scheduleBridgeRetry`), y `retryPendingBridges` (boot) **también se gatea por quiescencia** (sin pérdida si se cierra la app — recupera los `bridgePending` el próximo launch). Settlements diferidos: sin flag persistido, cubiertos solo por el retry en-sesión (residual documentado). **Cross-context benigno**: pasar un `SplitGroup` del `groupSyncContext` a métodos del mainContext es seguro porque solo se LEEN `isOwner`/`cloudKitZoneID` (no se muta+guarda el objeto cross-context). **Fase 1 mantiene como RED redundante** todo el gate/export-only de abajo; **Fase 2 (limpieza del gate) solo tras validar en device vía TestFlight**. ⇒ **(red, Fase 1) `SplitSyncManager.initialize()` crea SIEMPRE los engines pero en modo "export-only" (`automaticallySync = false`)** hasta que el import se asiente (`SplitSyncStartGate.decideStart`; offline/import asentado → `autoSync=true` directo). **Por qué crear-siempre y no diferir la creación** (refactor 2026-06-20, regresión del primer gate): diferir dejaba `privateEngine = nil` → crear grupo / **invitar por enlace** / `enqueueSave` fallaban toda la ventana (`createZone` no-op silencioso → grupo sin zona en CloudKit, no invitable; `createShare` lanzaba `engineNotInitialized`). En export-only el engine **existe** (export funciona vía `sendChanges` manual, que `createShare` ya invoca) pero **NO fetchea solo**, y CADA `save()` del delegate se difiere vía `deferMainContextWork()` (gate `autoSyncActive`): los handlers de fetch no corren (sin auto-fetch; `acceptShare` también gatea su `fetchChanges`), y los de sent/conflict (export — ej. system fields) hacen early-return ANTES de mutar el mainContext (se reconcilian tras promover vía un `serverRecordChanged`, **sin pérdida**). Apple: `automaticallySync=false` no programa sync; uno llama `fetch/sendChanges`. Al asentar el import (observer `.iCloudFirstImportCompleted` / poll 15s / tope 300s) → **`enableAutoSync()` RECREA ambos engines con `automaticallySync = true`** (vuelve al modo automático probado: push/coalescing/retry). **Cuidados de la recreación**: transfiere los `pendingRecordZoneChanges/DatabaseChanges` en-memoria del engine viejo al nuevo (el `.stateUpdate` persiste async → `loadState` del disco puede no tener un encolado de último ms; transferir directo evita perderlo); actualiza `_privateEngineRef`/`_sharedEngineRef` y el delegate **descarta callbacks de engines viejos vía `isCurrentEngine`** (un `.stateUpdate` tardío del viejo se guardaría con nombre equivocado → corromper el otro store). **Recuperación de grupos rotos**: `recoverOwnedGroupZonesIfNeeded()` (en `startEngines`, read-only sobre mainContext) re-encola zona+records de grupos owner sin GroupMeta subido (`SplitSyncStartGate.needsZoneRecovery`: `isOwner && ckSystemFieldsData==nil`) — recupera grupos creados en una ventana export-only previa (incl. el "Jurpi" del owner). **D2**: `retryPendingBridges`/`migrateShareGroupZoneIDs` (boot) también esperan el import (`awaitPersonalImportForBootSave` → `forceFetchAndWait`) antes de su `save()` del mainContext personal (idempotentes: difieren al próximo launch si no asienta). Diferir NO pierde eventos (cola en CloudKit + `stateSerialization`). **Cero silencios** (decisión owner): todo `try?` del subsistema de Grupos → `do/catch` con log; user-actions (invitar/eliminar/liquidar) → alert localizado (`Groups.Errors.*`, copy clara en vez de "CKSyncEngine not initialized"). Logging de `SplitSync*` INTENCIONALMENTE fuera de `#if DEBUG` (excepción consciente: el bug solo reproduce en CloudKit **Production**, verificable solo vía TestFlight Release en Console.app; sin PII).

### State Management (SwiftUI)
- `@Observable` SIEMPRE con `@MainActor`. `@State` SIEMPRE `private`.
- NUNCA `Binding(get:set:)` en body — usar `@Binding` + `.onChange()`.
- NUNCA `@AppStorage` dentro de `@Observable` (no triggerea updates).
- Preferir `@Observable` + `@State`/`@Bindable` sobre `ObservableObject`/`@Published`/`@StateObject`.
- **Preferencias persistentes → `AppPreferences` inyectado via `@Environment`.** NUNCA `@AppStorage` directo en views nuevas.

### Gotchas críticos
- **`containerRelativeFrame(.horizontal)` en `ScrollView(.vertical)` con `.contentMargins`** → deadlock de layout, splash nunca dismissa, sin crash log. Usar `onGeometryChange`. Detalles en UI-PATTERNS.md.
- **`YalaFormatter` no auto-refresca prefs** — lee `UserDefaults` directo. Vista que lo use con `decimalPlaces` o `currencyDisplayFormat` debe inyectar `@Environment(AppPreferences.self)` y leer `let _ = appPreferences.X` en body para registrar dependencia.
- **Forms con `TextField`/`TextEditor`/`SecureField`** (sin `Form`): obligatorio `dismissKeyboardOnTap()` desde el primer commit. Detalles en SWIFT-STYLE.md.
- **Swift Charts `.annotation { }` NO propaga environment objects `@Observable`** → el contenido de una annotation de un mark (`BarMark`/`LineMark`/etc.) se hostea fuera del árbol de la vista; una sub-View que lea `@Environment(AppPreferences.self)` (ej. `AmountText`) NO lo resuelve y dispara `SIGTRAP` (`_assertionFailure` en `EnvironmentValues.subscript.getter`) al renderizar — sin crash log claro. Dentro de annotations usar `Text(...)` con el valor YA resuelto del callsite (`appPreferences.currency(...)` desde `self`) o un formatter estático (`YalaFormatter`), NUNCA una sub-View con `@Environment`. **`.chartOverlay { }` SÍ propaga** (ahí `AmountText` funciona, ej. `CashFlowWidget`). Causa del crash del chip Estadísticas en grupos (`8bb5ace8`).

### Localización (l10n)
- **iOS NO hace fallback per-key con idioma de SISTEMA.** El patrón "marker-only hereda vía fallback iOS" es FALSO: si la variante (`es-ES`/`es-AR`/`en-GB`/`pt-PT`) es el idioma del sistema, iOS resuelve `Bundle.main` contra esa `.lproj` y devuelve la **key cruda** si falta (afecta `ls()` Y los ~290 `NSLocalizedString` directos). El fallback manual de `ls()` solo corre con **override in-app** (`parentBundle != nil`). → **Toda key nueva DEBE quedar en las 4 variantes completas, no sparse.** Usa `qa/scripts/add-l10n-key.sh` (ya las materializa) y `LocalizationParityTests.variants_haveAllKeys_fromParent` lo enforcea. `es-419`/`en`/`pt-BR` son las referencias completas. Detalle en L10N.md.
- **Keys duplicadas en un mismo `.strings`: la ÚLTIMA declaración gana, silenciosamente** (ni el compilador ni la paridad lo detectan — `NSDictionary` colapsa al parsear). `LocalizationParityTests.noDuplicateKeys_inAnyLocale` lo enforcea leyendo los archivos FUENTE vía `#filePath` (app + widget target). Si alguna vez hay que dedupe: keys usadas para **matching por nombre persistido** (ej. `subcategory.balanceAdjustment` — seed + CSV import + InitialBalanceService) deben conservar el valor runtime actual o se duplican entidades de usuarios.
- **Idioma para prompts LLM: SIEMPRE `AppLocale.current.identifier`, NUNCA `Locale.current.identifier`.** `Locale.current` refleja la región de FORMATO (`AppleLocale`, ej. `es_PE`) y no cambia con el idioma de la UI (`AppleLanguages`) → app en inglés recibía respuestas del chat en español. `AppLocale.current` (→ `LanguageManager.resolved.locale`) es el SSOT: override in-app → `Locale.preferredLanguages` → `en`. `Locale.current.region` sí es la fuente correcta para PAÍS/modismos. Output LLM cacheado (ej. sugerencias de chat) debe incluir el idioma en la cache key.

### iOS 26 Liquid Glass (OBLIGATORIO)
- `ToolbarSpacer(.fixed, placement: .topBarTrailing)` — placement es OBLIGATORIO.
- `.glassEffect()` para chips, barras flotantes, elementos translúcidos.
- Si existe API iOS 26 que mejore integración con sistema, USARLA.

### Tests (OBLIGATORIO)
Detalles completos en `$VAULT/planning/TESTING-STRATEGY.md`. Reglas mínimas:
- `makeTestContext()` es seguro desde Fase 5 (2026-04-29) — usa UUID suffix + `isRunningTests` detecta Swift Testing. Aún así prefiere `@Model` directos sin contexto cuando la lógica lo permite (más rápido).
- NUNCA `UserDefaults.standard` directo en tests → `UserDefaults(suiteName: "test.\(UUID().uuidString)")!` (helper `makeIsolatedDefaults()`).
- NUNCA tocar singletons `.shared` sin `@Suite(.serialized)` + `defer { restore }` o `_testReset()`.
- NUNCA `Task.sleep(.seconds(N))` con N>0.5 — usar señales determinísticas. Excepción: `≤50ms` para forzar dealloc.
- NUNCA `Date()` / `Calendar.current` en lógica testeada — inyectar vía param opcional `now: Date = .now` (patrón canónico, ya en `FinancialScoreCalculator`/`BudgetAlertService`).
- NUNCA `@Test(.disabled(...))` sin entrada en Lista Negra (TESTING-STRATEGY.md) con owner + deadline.
- NUNCA declarar fix completo si un test falla. "Preexistente" no es excusa: arreglar o registrar en Lista Negra con plan.
- Ejecutar con `-parallel-testing-enabled YES` (recomendado, -30% tiempo). `NO` solo como fallback si aparece race en singleton no cubierto.

### XCUITest (regresión UI determinista) — `YalaUITests`
- Scheme **Yala Dev**. Lanzar con `XCUIApplication().launchForUITest(...)` (helper en `YalaUITests/Support/`).
- Launch args (`#if DEBUG`, `UITestHooks`): `-uitest` (modo + store local sin CloudKit), `-uitest-reset`, `-uitest-skip-onboarding`, `-uitest-pro`, `-uitest-seed <minimal|realista|pesado>`.
- **Seed `minimal` por default** (rápido); `realista`/`pesado` solo si el test necesita volumen (arranque más lento, riesgo watchdog).
- Esperar **`waitForUITestReady()`** (señal `uitest_ready`) antes de interactuar — NUNCA `sleep`.
- Targetear por `accessibilityIdentifier` (`feature_element` / `feature_row_<claveEstable>`), NUNCA texto localizado ni coordenadas.
- Al cubrir un área `deterministic`: poner `coverage: "xcuitest:<File#test>"` en `qa/coverage-index.json` y bajar `_meta.backlogBaseline`.

### Audit markers
- `// A11Y-DT:` justifica font size hardcodeado (Dynamic Type).
- `// A11Y-DM:` justifica color hardcodeado (Dark Mode).

### Design System (en cambios UI)
- SIEMPRE `DS.Spacing`, `DS.Radius`, `DS.Typography`, `DS.Semantic.*`, `DS.Gradients.*` — NUNCA hardcoded.
- SIEMPRE filas clicables con `Button` + `contentShape(Rectangle())`.
- Componentes estándar: `YalaPrimaryButton`, `YalaEmptyState`, etc.
- Tablas DS.Semantic / DS.Gradients en SWIFT-STYLE.md.

### Backgrounds de vista
- TODA View root, sheet, fullScreenCover NUEVA → `.yalaScreenBackground(_:ignoredEdges:)`.
- NUNCA aplicar `.background(theme.background)`, `.background(.thBackground)` ni default iOS sin background.
- Forms/Lists dentro de sheet → `.scrollContentBackground(.hidden)` MANUAL antes del modifier (el modifier no lo hace automático para preservar predictibilidad).
- **Regla SSOT (reestructura 2026-06-08)**: el fondo lo determina el **contenedor de presentación RAÍZ** del stack. Una vista navegada (push) HEREDA el fondo de su stack — dentro de un tab → `.panel`; dentro de un sheet → el del sheet. **Un `.sheet` NUNCA es `.panel`.**
- Variantes (`YalaBackgroundVariant`) — 3 tras eliminar `.compact`:
  - `.panel` (default) — PanelBackgroundView gradient temático. SOLO vistas COMPLETAS: tab roots, vistas navegadas (push), fullScreenCover normal.
  - `.subtle` — `theme.background` plano. CUALQUIER sheet desde el bottom (sin detents o `[.large]`); TODO el stack del sheet de Profile (los ~20 Settings + sub-navegación); y success/celebración.
  - `.transparent` — sin fondo (DESNUDO, muestra el fondo de sistema del sheet — decisión owner, NO glassSheet). Sheets con detent PARCIAL (`.medium`/`.height`/`.fraction`). **Idiom-aware**: si el detent viene de `DS.Adaptive.sheetDetents(...)`, usar `DS.Adaptive.usesLargeSheets ? .subtle : .transparent` (iPad fuerza `.large` → debe ser subtle). Dual-detent `[.medium,.large]` con `selection: $selectedDetent` → `isLargeDetent ? .subtle : .transparent`.
- `.compact` ELIMINADA. `AnimatedMeshBackground` + `MeshConfigResolver` borrados (dead code). `DS.Adaptive.usesLargeSheets` (= isPad||isiOSAppOnMac) expuesto para el fondo idiom-aware.
- Param `ignoredEdges: Edge.Set?`: si `nil`, default `.all`. Para vistas con `safeAreaInset` pasar edges específicos (`[.top]`, `[.bottom]`).
- OUT: WelcomeFlow/onboarding (`DS.Gradients.heroIndigoBlack` especializado), InboxAlertModal (custom modal Color.black backdrop), popovers (iOS nativo).
- `GlassSheetModifier.glassSheet()` se mantiene como helper de sheets que SÍ quieren material (`.presentationBackground(.ultraThinMaterial)` + drag indicator). `.transparent` NO usa material (es desnudo).

#### Patrones aceptados temporalmente (deuda incremental, migrar al tocar el archivo)

- **Pattern B**: `ZStack { PanelBackgroundView(); content }` manual. Migrado masivamente al modifier (2026-06-08). Residuales aceptados: `PanelView` (tab root complejo con overlays) + las 6 vistas de onboarding/welcome (fondo hero propio, OUT). Sanity: `grep -rn "PanelBackgroundView(" Yala/` solo muestra esas + `PanelBackgroundView.swift` (def) + `ViewModifiers.swift` (modifier).
- **Pattern Subtle**: `ZStack { theme.background.ignoresSafeArea(); ...overlays; content }` en success screens (TransactionSuccessView, SubscriptionSuccessView, InboxApproveSuccessView, InboxBulkApproveSuccessView). Reescribir el ZStack rompería overlays propios (ConfettiView, RadialGradient glow). Semánticamente ES `.subtle`.

### Documentation & copy
- Describir features desde la perspectiva del USUARIO, no técnica.
- NUNCA fabricar features — solo lo confirmado en scope.
- Copy nuevo: leer BRAND-VOICE.md.

## Workflow

Skills disponibles vienen en el system-reminder de cada sesión. Flujos canónicos:

```
Feature:    /clear → Plan Mode → /review-plan → implementar →
            /verify-ios → /test-smart → /device-qa → /commit-one → /clear
Bug fix:    implementar → /verify-ios → /device-qa → /commit-one
Complejo:   añade /code-review high + /refine antes de commit
```

`/commit-one` ya incluye `/swift-audit` + tests antes del commit. No es necesario ejecutarlos por separado salvo que quieras arreglar issues mid-implementación.

Aplica el flujo **Complejo** cuando el cambio involucre lógica densa donde un bug sería costoso: sync/race (CKShare, bridges, NotificationService), cálculos financieros (FinancialScoreCalculator, GroupBalanceService, BudgetAlertService), migraciones SwiftData, bridge SplitExpense ↔ TransactionItem. Para rebranding visual, l10n o polish UI no aporta.

**Regla QA (contrato anti-drift):** la SSOT de cobertura es `qa/coverage-index.json` (validar: `bash qa/validate-coverage.sh`). Al tocar código bajo `Yala/`, en el MISMO commit actualizar el área correspondiente del index + sus tests/escenarios asociados (`coverage`, `lastVerified`). Cobertura por clasificación: `deterministic` → XCUITest en `YalaUITests`; `agentic` → `/device-qa`; `manual` → documentada. El **ratchet** del validador BLOQUEA si el backlog determinista (áreas `deterministic` sin XCUITest) crece respecto a `_meta.backlogBaseline` — escribe el test o baja el baseline conscientemente. `QA-SCENARIOS.md` queda como narrativa humana; la verdad ejecutable es el index + los tests + CI.

## Testing

| Tipo de cambio | Comando |
|----------------|---------|
| Modelo / servicio | `/test-smart` (relevantes) |
| Solo UI (Views) | `/verify-ios` |
| Antes de commit | `/test-smart` siempre |
| Tras merge / refactor grande | `/test-ios` (todos) |

Después de implementar cualquier cambio, SIEMPRE ejecutar `/verify-ios` o `xcodebuild` antes de presentar como completado.

## Corrección de errores

- SIEMPRE buscar TODAS las instancias del mismo patrón antes de declarar fix completo.
- NO confiar ciegamente en "BUILD SUCCEEDED" — verificar todos los casos.
- Si hay errores tras build exitoso, limpiar cache: `xcodebuild clean`.

## Self-Maintenance

Cuando se modifican modelos / servicios / ViewModels:
- Actualizar tablas en `$VAULT/planning/CODEBASE-MAP.md`.
- Actualizar conteo de tests si se agregaron nuevos.
- Agregar gotchas descubiertos a "Reglas inviolables" de este archivo.

Para preferencias nuevas (`UserDefaults`): ver checklist en SWIFT-STYLE.md sección "añadir una preferencia nueva".

## Control de Ejecución

Después de implementar código:
1. Mostrar resumen de cambios **en lenguaje de usuario** (qué cambia para él / qué problema resuelve), no descripción técnica de archivos editados.
2. Ejecutar `/verify-ios` SIEMPRE para confirmar que el build pasa.
3. Sugerir siguiente paso.
4. DETENERSE y esperar instrucción del usuario.
5. NO ejecutar tests, device-qa o commits automáticamente — solo el build.

**Git:** ejecutar cada comando de lectura UNA SOLA VEZ, secuencialmente, nunca en paralelo. No matar shells con git en curso.

**Tags:** SIEMPRE semver con prefijo `v` → `v1.0.0`, `v1.1.0`. Nunca sin prefijo o sin 3 componentes.

## Decisiones Recientes (TTL: hasta cierre de fase)

[Formato: [FECHA] Decisión breve — se archiva en DECISIONS.md al cerrar fase. Máx 3-5 entradas activas.]

- [2026-06-22] **`#Predicate` concretos por tipo en el sync de Grupos — EL FIX REAL del crash al generar enlace / forzar sync (el revert `0e8b40e7` por sí solo NO bastó)** (commit `c74349fc`, 2 archivos +46/−77; build + tests + verificador adversarial verdes). **El device QA de build 30 reveló la causa raíz REAL**: NO el `ModelContext` dedicado sino el `#Predicate` **GENÉRICO-PROTOCOLO** de `fetchByID<T: HasUUID>`/`deleteModel` — `$0.id` resuelve al keypath del **witness de `HasUUID`**, que SwiftData no casa con el `\SplitGroup.id` concreto del schema → `SwiftData/DataUtilities.swift:85 Couldn't find \SplitGroup.<computed …> on SplitGroup` en `buildRecord` durante `sendChanges` (createShare). **Prueba**: los ~doce `#Predicate` CONCRETOS del mismo archivo (`handleConflict`, `group(for:)`, `applyGroupMeta`, …) corrían sobre el MISMO contexto (dedicado en build 29/30) y NUNCA crashearon — solo el genérico-protocolo. **Fix**: 5 helpers concretos `splitGroup/Expense/Member/Share/Settlement(byID:in:)` con `#Predicate<ConcreteType> { $0.id == id }` + `fetchFirst<T>(_ descriptor:)` que solo EJECUTA un `FetchDescriptor` ya concreto; eliminados `fetchByID`/`deleteModel` genéricos + el protocolo `HasUUID` + 5 conformidades + 3 tests (footgun: re-genericizar reintroduce el crash; comentario en el sitio advierte). Regla "todas las instancias": ningún otro `#Predicate` genérico-protocolo en `Yala/`. Gotcha reusable añadido a Reglas inviolables › SwiftData. Verificador adversarial: 5 modelos con `var id: UUID` stored (sin `@Attribute(.unique)`/computed), paridad, sin `try?`. **El revert `0e8b40e7` se mantiene** (mainContext + quiescencia es más simple; el contexto dedicado era redundante) pero NO era lo que arreglaba el crash — **CONFIRMADO EN DEVICE: build 31 (= quiescencia + revert) SIGUIÓ crasheando idéntico al generar enlace** → prueba empírica de que el `#Predicate` genérico-protocolo crashea en mainContext también; el contexto nunca fue la causa. Crash device-only (Production/restore) → **✅ RESUELTO en build 32** (= `ccbc97e9` quiescencia + `0e8b40e7` revert + `c74349fc` concretos): el owner generó el enlace en device SIN `DataUtilities.swift:85` (share sheet OK, compartido por WhatsApp; log confirma `promoted importSettled=true` + `SAVE/SAVED SplitSync.conflict` sobre mainContext sin crash). Saga de 5 intentos CERRADA. Residuales NO fatales a vigilar (CKSyncEngine reintenta): `cloudkit.zoneshare` Partial Failure 1ª vez en createShare + `CKError Code=2 partialFailure` en el export de grupos post-promoción → verificar sync end-to-end del invitado. Pendiente aparte (no es el crash): deploy del schema CloudKit personal (`cloudkit-production-FIXED.ckdb`, `CD_isSystem` + 25 campos).

- [2026-06-22] **Revert del `ModelContext` dedicado → `mainContext` (complementario — NO era el fix del crash; el real es `c74349fc`) + corrección del schema CloudKit personal congelado en Producción** (commit `0e8b40e7`, 5 archivos +76/−118; build + tests verdes; flujo Complejo: revert → verify-ios → test-smart → `/code-review high`). **Device QA de build 30 (`ccbc97e9`) confirmó**: el crash-loop de arranque SE ARREGLÓ (quiescencia OK), PERO generar-enlace Y forzar-sync SEGUÍAN crasheando con un bug DISTINTO introducido por el contexto dedicado de `6360d964`: `SwiftData/DataUtilities.swift:85 Couldn't find \SplitGroup.id` — un `#Predicate` que NO resuelve sobre un `ModelContext(container)` manual (`buildRecord:1674 → fetchByID`, durante `sendChanges`; ~40 `#Predicate` del subsistema corrían sobre él). En builds ≤28 corrían sobre `mainContext` y funcionaban. **Fix**: el delegate del sync vuelve al `mainContext` compartido (~50 usos repuntados). El contexto dedicado era REDUNDANTE — la protección NO la daba él sino el **gate de QUIESCENCIA** de `ccbc97e9` (export-only + `deferMainContextWork` difieren todo `save()` del delegate hasta que el import se asienta+aquieta), que se MANTIENE intacto. Revert = build ≤28 (probado bueno) + quiescencia = estrictamente mejor. **`markExpensesPendingBridge` ELIMINADO** (su `save()` corría garantizado durante un import no-quiescente → en mainContext crashearía): el bridge diferido se recupera por `scheduleBridgeRetry` en-sesión (residual aceptado y documentado: app-killed en la rara ventana de import incremental antes del retry; CKSyncEngine re-entrega). `retryPendingBridges` (boot) sigue vivo — lo alimenta `GroupExpenseService` (Caso A user-action). Observer `didSave` del contexto dedicado removido → refresh de UI = paridad EXACTA build ≤28 (3 `markRemoteChangePending`; las vistas de Grupos no usan `@Query`). **`/code-review high` (3 finders adversariales): 0 bugs confirmados** (todos los saves del delegate gateados por `deferMainContextWork`, `recoverOwnedGroupZonesIfNeeded` read-only, refresh = paridad ≤28), cazó 2 comentarios cross-context stale → corregidos. 0 referencias a `groupSyncContext` en `Yala/`. Crash device-only (Production/restore) → QA en TestFlight build 31. **Fix APARTE (schema CloudKit, NO es el crash)**: el export del espejo personal falla en Producción porque el container `iCloud.com.jurgenschmidt.yala` tiene el schema **congelado en ~1.0** — le faltan **26 campos** en 10 record types (`isSystem`/`isSystemAccount`/`shortcutID`/CSV-mirror `tagIDs`/`accountIDs`/`subcategoryIDs`/`split*ID`/`Tag.id`/`needOverride`/`includeSharedExpenses`/`manualAmount`/etc.). Causa: el owner desarrolla en `...yala.dev` (Yala Dev) → nunca corrió el scheme Yala-producción en Debug contra `...yala` → su Development quedó stale, y Producción es append-only (sin auto-create de schema). `CD_isSystem` era solo el primer tropiezo del export. Generé `cloudkit-production-FIXED.ckdb` (= schema de prod **idéntico + los 26 campos**, sin quitar nada, sin cruft de Grupos, tipos verificados contra los modelos — INT64 para Bool, STRING para UUID/String?) para **Import → Deploy** en el CloudKit Console (revisar el diff = exactamente 26 ADD antes de confirmar; append-only). El doble-sync de Grupos sigue desactivado (`.none`); los `CD_Split*` en el container personal son espejos inertes (se dejan). **Pendiente**: bump build 31 en la Mac Mini + device QA SOLO TestFlight (generar enlace empty-state Y ajustes → NO crash + URL; forzar sync → NO crash; arranque sin crash-loop; matriz de grupos) + deploy del schema en el Console. Plan: `~/.claude/plans/jazzy-whistling-noodle.md`. Detalle vivo en memoria `project_groups_sync_crash_saga.md`.

- [2026-06-22] **El `ModelContext` dedicado (`6360d964`) NO bastó — fix COMPLETO: gate de QUIESCENCIA del import en TODO `save()` del store personal + promoción del sync por quiescencia + breadcrumbs comprehensivos** (commit `ccbc97e9`, 17 archivos +361/−101; build + 170 tests verdes; flujo Complejo: Plan Mode → `/review-plan` → implementar → verify-ios → test-smart → `/code-review high` → refine). **Build 29 (con `6360d964`) SIGUIÓ crasheando** (owner subió 5 `.ips` idénticos a las 8pm — crash-loop, device inutilizable). Sin dSYM real (archive borrado en ambas Macs), simboliqué con un **build local del mismo commit en Release**: el stack fiable (continuación async de Swift Concurrency + **CloudKit** → Yala → `performAndWait` → `_assertionFailure`) + los frames de Yala (coherentes: `NotificationItem.createDefaults`/`seedDefaultNotificationsIfNeeded`) revelaron que el assert ahora cae en un `save()` del **store PERSONAL** durante el import (sembrado de notificaciones, invite-onboarding, reconciles de boot), **NO en el del sync**. El contexto dedicado **comparte el `NSPersistentStoreCoordinator`** con el store personal → aísla los saves de GRUPOS pero NO los del store personal; y NINGÚN container aísla el store personal de su propio import. **Insight**: el primer `importEvent` es señal PREMATURA en restores multi-lote (NSPCKContainer sigue importando). **Fix (elegido por owner: "muchos breadcrumbs para identificar cualquier cosa")**: (a) `iCloudSyncService.isImportQuiescent` (adaptador runtime de `SubcategoryDedupGate.decide`: `!isImporting` + ventana de quietud) gatea TODOS los `save()` automáticos del store personal — seed/dedup notificaciones, scheduled-payments, exchange-rate, provisional-tx, race-cleaner, dedups de grupo (mismo coordinator), + los boot Tasks freeze/reconcileTransferPairs/reconcileDisplayName/refreshCurrentUserFlags/sendDueReports/migrateToLiveBalance (vía `awaitPersonalImportForBootSave`, que ahora espera primer-import **+ quiescencia**, exigiendo `hasCompletedFirstImport` para no proceder antes del import); (b) el sync de grupos promueve a auto-sync por **QUIESCENCIA** (`SplitSyncStartGate.resolveWaitByQuiescence`: `hasCompletedFirstImport && isQuiescent`), no por el primer `importEvent`; (c) **`SaveBreadcrumb`** (Logger `SaveGate`, fuera de `#if DEBUG`, sin PII) instrumenta ~20 save sites con `willSave/didSave/deferred` → si reaparece un crash, el último `SAVE <site>` en Console.app nombra el culpable sin necesidad de dSYM. **`/code-review high` (8 ángulos) cazó 3 fixes de correctness aplicados**: (1) `isImportQuiescent` es `true` ANTES del primer import (`lastImportDate==nil`) → `awaitPersonalImportForBootSave` ahora exige `hasCompletedFirstImport && isImportQuiescent` (los guards sync per-save NO lo exigen — perpetuarían el skip en launches normales; aceptan diferir solo durante import ACTIVO); (2) **5 boot saves automáticos sin gatear que el plan no cubrió** (freeze/reconcileTransferPairs/reconcileDisplayName/refreshCurrentUserFlags/sendDueReports — posiblemente EL crasher real) → gateados; (3) `migrateToLiveBalance` podía marcar el flag con tasas stale si un lote llegaba a mitad del recalc → re-verifica quiescencia antes del flag. **Cleanups**: dead code `SplitSyncStartGate.resolveWait`/`startedOnIncompleteImport` + 6 tests huérfanos eliminados; guard redundante de `retryPendingBridges` removido (el helper ya garantiza quiescencia). **El `ModelContext` dedicado se queda** como defensa de la clase B (saves del delegate del sync). **Residual documentado**: aceptar un invite con tap EXACTO durante el import (`ensureCurrentUserMemberExists`/`updateCurrentUserDisplayName` vía `handleJoinTap`) NO está gateado — es user-tap (no el auto-loop), gatearlo rompería la UX de unirse; el breadcrumb `DEFER performSilentSetup` lo señala. **Altitud (decisión)**: el gate-por-save es la única vía para la clase A (no hay save-hook en SwiftData ni container que aísle el store personal de su import); los breadcrumbs cubren los sitios no encontrados. **Descartado por ahora D1** (ModelContainer separado para grupos) — no arregla la clase A; escalación solo si build 30 sigue crasheando en el delegate del sync. **Pendiente — device QA SOLO TestFlight (build 30, bump en la otra Mac)**: arranque sin crash-loop; Console.app `DEFER <site> — importing` durante el restore → `SAVE <site>` tras quiescencia, sin `_assertionFailure`; `export-only → promoted`; generar enlace OK; matriz de grupos OK. Si reaparece, el breadcrumb nombra el sitio → build 31 dirigido. Plan: `~/.claude/plans/jazzy-whistling-noodle.md`. **ACTUALIZACIÓN (device QA build 30, 2026-06-22 PM): el crash-loop de arranque SE ARREGLÓ** (breadcrumbs `DEFER … import not quiescent` + `export-only — awaiting QUIESCENCE`, arranca bien). **PERO generar-enlace SIGUE crasheando con un bug DISTINTO**: `SwiftData/DataUtilities.swift:85 Fatal error: Couldn't find \SplitGroup.id on SplitGroup` (dos keypaths distintos) = un **`#Predicate` que no resuelve sobre el `groupSyncContext` dedicado** (`buildRecord:1674 → fetchByID`), durante `sendChanges`. El contexto dedicado de `6360d964` introdujo este crash (~40 `#Predicate` del subsistema corren sobre él; en build ≤28 corrían sobre `mainContext` y funcionaban). **FIX DECIDIDO/PENDIENTE: revertir el `groupSyncContext` → `mainContext`** (la quiescencia ya hace redundante el contexto dedicado — ESE es el fix real del crash original y se mantiene); ajustar `markExpensesPendingBridge`. **Aparte (no es el crash)**: el export del espejo personal falla en Producción — `Cannot create or modify field 'CD_isSystem' in record 'CD_Subcategory' in production schema` (+170) → desplegar `isSystem` de Subcategory en CloudKit Dashboard. Detalle vivo en memoria `project_groups_sync_crash_saga.md`.

- [2026-06-21] **Fix de raíz del crash al generar el enlace de invitación — `ModelContext` DEDICADO para el sync de Grupos** (commit `6360d964`, 8 archivos; flujo Complejo: Plan Mode → `/review-plan` → verify-ios → tests → `/code-review high` → refine). **Bug reportado por owner** (build 28, 2 `.ips` idénticos): al generar el enlace (empty state O ajustes del grupo, con spinner "generando enlace") la app **crasheaba** — el MISMO `_assertionFailure` interno de SwiftData del crash-loop, ahora disparado por `createShare → sendChanges → handleSentRecordZoneChanges → storeSystemFields + save()` sobre el `mainContext` mientras el store personal seguía importándose en un restore. **Por qué el export-only (`4d2dd0ab`) NO bastó**: tanto él como el gate (`4ba8a8fb`) se apoyaban en el **primer `importEvent`**, señal **prematura** en restores multi-lote — el export-only promovía a auto-sync ahí y desactivaba su propio gate de saves → segundos después, generar el enlace crasheaba. Gatear por "¿terminó el import?" es frágil de raíz (NSPersistentCloudKitContainer no da señal confiable de "restore completó"). **Fix (elegido por owner sobre el export-only que falló)**: `groupSyncContext = ModelContext(container)` dedicado para el delegate del sync → sus `save()` persisten solo el store de grupos (`YalaGroups`, `.none`; `Split*` sin `@Relationship`) y NO arrastran el grafo personal (`YalaModel`) a medio importar → el crash desaparece **sin depender del timing**. ~48 usos del delegate + reads repuntados; 3 métodos de `GroupService` parametrizados con `context:`; refresh de UI vía observer `didSave(groupSyncContext) → markRemoteChangePending` (las vistas no usan `@Query`). **El bridge personal** (escribe `TransactionItem` en `YalaModel`, no aislable por contexto) se gatea por **QUIESCENCIA del import** (`SubcategoryDedupGate`, decisión owner — no por el primer evento). **`/code-review high` cazó 2 GRAVE**: (1) pérdida de datos — al diferir el bridge no marcaba pendiente → ahora marca `bridgePending=true` (save seguro del store de grupos) + `scheduleBridgeRetry` en sesión; `retryPendingBridges` (boot) **también gateado por quiescencia** para no crashear al recuperarlos; (2) bloqueo indefinido — gate por `isImporting` (no `isSyncing`) para no bloquear exports normales; + (3) comentarios cross-context (benigno-latente) en ContentView/GroupInviteOnboardingView. **Incremental**: **Fase 1 MANTIENE el gate/export-only previo como RED redundante**; **Fase 2 (limpieza del gate) SOLO tras validar Fase 1 en device**. **Tests**: +1 regresión pure-logic `isImporting_isTrueOnlyForImporting…` (fija el discriminante del fix #2); el crash es device-only (Production/restore). **Telemetría**: reutiliza la del fix previo. **Pendiente — device QA SOLO TestFlight** (Production/restore; bump en la otra Mac, instalar encima del App Store): generar enlace (empty state Y ajustes) → NO crash + URL; cambio de grupo de otro miembro aparece en UI sin relanzar; matriz de flujos de grupos OK; no reaparece el crash-loop. Plan: `~/.claude/plans/jazzy-whistling-noodle.md`.

- [2026-06-20] **Fix Grupos inutilizables durante la ventana del gate (regresión del crash-loop fix) + endurecer errores (cero silencios)** — refactor del arranque de CKSyncEngine. **Bug reportado por owner** (device restaurado, el mismo del crash-loop): al tocar "Invitar por enlace" salía *"CKSyncEngine not initialized. Ensure iCloud is available."* **Causa raíz**: el gate previo (`4ba8a8fb`) DIFERÍA la creación de los engines hasta el primer import → `privateEngine = nil` toda la ventana → crear grupo / invitar / `enqueueSave` fallaban (`createShare` lanzaba `engineNotInitialized`; **`createZone` no-op silencioso → grupo creado sin zona en CloudKit, no invitable** — el "Jurpi" del owner). Afectaba CUALQUIER cold launch con iCloud (`hasCompletedFirstImport` es flag en-memoria, arranca `false` cada launch; puede esperar hasta el tope de 300s). QA en sim no lo cazó (sin iCloud → arranca ya). **Insight**: el `save()` que crashea solo vive en los handlers del delegate; crear el engine y encolar NO lo disparan; `automaticallySync=false` (doc Apple) evita el auto-fetch. **Fix (Plan A — gate + recrear)**: `startEngines(autoSync:)` crea SIEMPRE ambos engines (export-only `automaticallySync=false` durante la ventana) + `enableAutoSync()` los RECREA con `automaticallySync=true` al asentar el import (transfiere pending en-memoria del viejo al nuevo; `isCurrentEngine` descarta callbacks de engines viejos) + `deferMainContextWork()` gatea los 8 `save()` del delegate (early-return antes de mutar) + `acceptShare` gatea su `fetchChanges` + `recoverOwnedGroupZonesIfNeeded()` re-encola (read-only) zonas owner sin GroupMeta (recupera "Jurpi"). **Descartado ModelContext dedicado** (no elimina el gate del bridge — escribe datos personales — y añade coordinación de 2 contextos + refactor de 44 sitios). **D2**: `retryPendingBridges`/`migrateShareGroupZoneIDs` (boot) ahora esperan el import antes de su `save()` (`awaitPersonalImportForBootSave`) — cierra el follow-up del fix anterior. **D7 cero silencios** (decisión owner): todo `try?` del subsistema de Grupos → `do/catch` con log; user-actions sin feedback en `GroupDetailViewModel` (createShareLink/deleteExpense/confirm/reject) → alert localizado; mensaje opaco "CKSyncEngine not initialized" → `Groups.Errors.syncPreparing`; nuevas keys `groups.errors.{syncPreparing,inviteFailed,actionFailed}` × 16 locales (voseo es-AR). **Flujo Complejo**: Plan Mode → `/review-plan` (cazó: guard de identidad debe cubrir `.stateUpdate`, gate antes de mutar no solo del save, race de export documentado) → build+tests verdes → `/code-review high` (4 finders) → 4 fixes aplicados (race de promoción `loadState` stale → transferir pending del viejo; leak de `error.localizedDescription` técnico de CKError en ambos createShareLink → `inviteFailed`; double-filter redundante en recovery; comentario telemetría `importSettled`) + cleanup `makeEngine` helper (dedup startEngines/enableAutoSync). **Tests**: 5 pure-logic nuevos en `SplitSyncStartGateTests` (`shouldDeferDelegateSave`, `needsZoneRecovery`); orquestación (recrear/observer/recovery) device-only. **Telemetría**: `cloudkitGroupSyncPromotedToAuto` (params `importSettled` — false = promovido por hard cap) + `cloudkitGroupZoneRecovered` (count). **Pendiente — device QA SOLO TestFlight** (Production/restore; build Debug no ve la data real): instalar encima del App Store → "Jurpi" recupera zona + invitar genera URL sin error ni crash; Console.app `engines created (manual mode…)` → `promoted to auto` → `zone recovered`; forzar fallo (modo avión al invitar) → alert claro. **Riesgo conocido a verificar** (no regresión — idéntico pre-fix): aceptar invite DURANTE la ventana export-only no crea el SplitMember local hasta promoción/re-emit de `PendingInviteStore`. Plan: `~/.claude/plans/jazzy-whistling-noodle.md`.

- [2026-06-20] **Fix crash-loop de Grupos en device restaurado de iCloud — `_assertionFailure` de SwiftData en el save del sync (gate del arranque de CKSyncEngine)** (4 archivos: `SplitSyncStartGate.swift` nuevo + `SplitSyncManager.swift` + `TelemetryService.swift` + `SplitSyncStartGateTests.swift`; build + tests verdes; flujo Complejo: Plan Mode → review-plan → verify-ios → test-smart → code-review high). **Bug crítico reportado por owner** (device restaurado de iCloud): abrió el tab Grupos, terminó el onboarding de Grupos, la app crasheó durante la alerta de notificaciones, y desde entonces **crashea en cada arranque** (inutilizable). **Causa raíz (2 `.ips` idénticos)**: `EXC_BREAKPOINT (SIGTRAP)` → `_assertionFailure` **interno de SwiftData** en `modelContext.save()` dentro de `SplitSyncManager.handleFetchedRecordZoneChanges` (`:679`), disparado por **CKSyncEngine** (sync manual de Grupos) al aplicar records restaurados. Mecanismo: **un solo `ModelContainer` con dos stores en el mismo `mainContext`** — personal (`.private`, NSPersistentCloudKitContainer, **importándose a medias** en el restore) + grupos (`.none`, CKSyncEngine); el save de grupos persiste el grafo personal a medio hidratar → assert. **Loop** porque CKSyncEngine re-entrega su cola persistida en cada cold launch. El `do/catch` del save NO atrapa el SIGTRAP. Verificado de primera mano que la ingestión (`CKRecordTranslator`) ya está saneada y los `Split*` no usan `@Relationship` → el trap es a nivel de contexto/store, no de un campo. **Fix (gate)**: `SplitSyncManager.initialize()` refactorizado en `setupContainerAndDelegate()` (siempre — mantiene `container`/`delegate` para `acceptShare`) + `startEngines()` (single-flight `enginesStarted`, `@MainActor`) + `observeFirstImportThenStart()` (observer `.iCloudFirstImportCompleted` camino rápido + poll cada 15s con tope duro 300s) + `cancelGate()`. Helper pure-logic `SplitSyncStartGate` (`decideStart`/`resolveWait`/`startedOnIncompleteImport`) espeja `MigrationGateLogic`. Diferir la creación de los engines apaga la cadena entera (sin delegate → sin `processEvent` → sin save → sin bridge) sin perder eventos. **`/review-plan` cazó 2 ajustes pre-impl**: fallback por estado del import (no timeout fijo que cortaría restores lentos → observer + tope absoluto) + breadcrumb discriminante Teoría A (grafo personal) vs B (store de grupos). **`/code-review high` (3 finders)**: 2 fixes aplicados (breadcrumb gateado a `!hasCompletedFirstImport` → cero costo en hot path; comentario de excepción del logging en Release); refutados con evidencia (data race `enginesStarted` → la clase es `@MainActor`, serializa; leak del observer → el hardCap garantiza cleanup; TOCTOU de la notif → check+registro síncronos sin `await`, el poll re-evalúa el flag cada 15s; `try? Task.sleep` → patrón canónico de cancelación). **Hallazgo de altitud (decisión owner: shippear + follow-up)**: `retryPendingBridges` (paso 16.5) y `migrateShareGroupZoneIDs` (17.5) también hacen `save()` del `mainContext` sin gate y tocan el grafo personal — riesgo residual del mismo mecanismo, **NO observado** (los `.ips` solo muestran SplitSync; el gate recupera el device del owner). NO se amplió el scope (tocar el orden del bootstrap sin evidencia es arriesgado); el logging confirmará en TestFlight si reaparece en otro save. **Tests**: 10 pure-logic `SplitSyncStartGateTests`; la orquestación (observer/poll/single-flight) es device-only. **Telemetría**: `cloudkitGroupSyncGateHardCap` (params: isSyncing) — debe ser ~0 tras el rollout. **Logging de diagnóstico de `SplitSync*` fuera de `#if DEBUG`** (excepción consciente, aprobada en el plan; sin PII; necesario porque el bug solo reproduce en Production). **Pendiente — device QA SOLO vía TestFlight** (build nuevo, decisión owner): instalar encima del App Store build 25 → arranca sin crash-loop; en Console.app: `gate … deferred …` → `engines created` tras el import (no el `.warning` de hardcap, no el crash). Esto recupera el device del owner Y verifica sobre datos reales (Production). Plan: `~/.claude/plans/flickering-wibbling-taco.md`.

- [2026-06-19] **Fix etiquetas / UUIDs de identidad colapsados tras restore lento de iCloud — commit `899c1c25`** (6 archivos, build + tests verdes; flujo Complejo: Plan Mode → review-plan → verify-ios → test-smart → code-review high → refine; SIN bump, owner sube build aparte). **Bug reportado por owner** (su device: borró TestFlight 2.0, bajó App Store 2.0, restauró de iCloud): 3 etiquetas distintas (Alianz/Despedida/Devolución) se veían como UNA — repetidas en el filtro y Perfil›Etiquetas, mal asignadas en registros. **Causa raíz**: `Tag.id` (y por el mismo mecanismo `Account.shortcutID`/`Subcategory.shortcutID`) colapsado al MISMO UUID porque CloudKit entregó los records **sin el campo persistido** (creados por builds tempranas de 2.0) → SwiftData evalúa el default `var id: UUID = UUID()` UNA vez y lo comparte. `ForEach(id: \.id)` repite la 1ª fila; `Tag.byIDLookup` (`uniquingKeysWith`) colapsa el catálogo a 1 entrada. La migración one-shot `migrateShortcutIDsAndRebuildCSVMirrors` que debía repararlo **corría con datos incompletos en el restore lento** (descartaba el resultado de `forceFetchAndWait(15s)`) y quedaba bloqueada por su doble candado (`appEntityShortcutIDsRegeneratedV3`) → los records que llegaban colapsados después nunca sanaban. La build del App Store (25) SÍ tiene el código del fix; era el GATING lo que lo derrotaba. Commit que introdujo el candado: `9f17bf76`. **Fix (aditivo)**: (a) `CategoryDeduplicationService.repairCollapsedIdentityUUIDs` — pase **repetible** (no one-shot), gateado por quiescencia (`SubcategoryDedupGate`; corre en bootstrap + hook de cambios remotos + force-sync), detecta colisiones (`collidedUUIDItems`, regenera SOLO los colisionados vía `regenerateDuplicateUUIDs` → preserva ids únicos cross-device) y reconstruye los CSV mirrors afectados desde la M2M con **nuke-on-nil** (sweep con guard de intersección sobre los ids viejos snapshotados pre-regen → completo, sin sync storm; ver gotcha "CSV stale ≠ nil" en Reglas inviolables › SwiftData). (b) `MigrationGateLogic.shouldDeferMigration` — el one-shot DIFIERE (retry next launch) si esperó el import y no se asentó. (c) Botón DEBUG de reparación manual en `iCloudSyncSettingsView`. **`/code-review high` cazó 2 fixes aplicados**: el "skip-on-nil" original orfanaría permanente → cambiado a nuke-on-nil; + guards de eficiencia (`!old*.isEmpty` + `#Predicate { tagIDs != nil }`). Refutados con evidencia: falsos `@MainActor` (el enum ya es `@MainActor`), "offline nunca migra" (offline ⇒ `.runNow`, no difiere). **Tests**: 9 pure-logic `IdentityUUIDCollisionLogicTests`; el repair a nivel servicio es R8 → device QA. **Telemetría**: `cloudkitDuplicateDetected(model:"Tag"/"Account"/"Subcategory", keySuffix:"id-collision")` — debe caer tras el rollout (juez final). **Pendiente — device QA SOLO vía TestFlight** (build 26): la data real del restore vive en el entorno **Production** de CloudKit; un build Debug desde Xcode usa **Development** (`aps-environment` difiere Yala-Debug vs Yala-Release, sin `icloud-container-environment` que lo fije) → NO ve la data real, y mezclar entornos sobre el store local compartido es arriesgado. Recuperación: instalar build 26 encima del de App Store → auto-repair al arrancar (o Ajustes › iCloud › Forzar sincronización; el botón DEBUG no está en Release). **Diferidos**: D5 read-path heal para divergencia cross-device por repair concurrente (gated por telemetría); dedup del re-encode CSV triplicado (cruza scope a `EntityDeletionService` — rompería atomicidad). Plan: `~/.claude/plans/joyful-prancing-alpaca.md`.

- [2026-06-14] **Fix doble canal de sync CloudKit en Grupos — `groupsConfiguration` de producción faltaba `cloudKitDatabase: .none`** (1 archivo, `SwiftDataConfiguration.swift:222`, build verde; commit SIN tests — cambio de config, no de lógica). Investigación arquitectónica confirmó un **doble canal de sincronización** de los 5 modelos de grupos (SplitGroup/Member/Expense/Share/Settlement): (a) **CKSyncEngine manual** en el container dedicado `iCloud.com.jurgenschmidt.yala.groups` (record types `GroupMeta`/`Split*` sin prefijo, private+shared DB, zonas custom `SplitGroup-`, CKShare) — el intencional; (b) **espejo automático de SwiftData** (`NSPersistentCloudKitContainer`) escribiendo `CD_Split*` en la private DB del container **personal** `iCloud.com.jurgenschmidt.yala` — NO intencional. **Causa raíz**: el `ModelContainer` único (`YalaApp.swift:56-60`) monta `personalConfiguration` (`.private`) + `groupsConfiguration`; la rama de producción de `groupsConfiguration` omitía `cloudKitDatabase`, cuyo **default es `.automatic`** (confirmado en doc oficial Apple: la firma del init muestra `cloudKitDatabase: ... = .automatic`, y `.automatic` = "managed CloudKit sync using the **primary** ubiquity container from the app's entitlements") → adjuntaba el espejo al container PRIMARIO del entitlement (el personal, primero del array). Las ramas test/UITest YA usaban `.none`; producción era la única excepción. El comentario del equipo "local only — CKSyncEngine maneja sync" (`SwiftDataConfiguration.swift:106`/`:212`) y "Separated... to prevent NSCloudKitMirroringDelegate interference" (`:25-26`) revelaban la INTENCIÓN (sin espejo para grupos); el `.none` faltante dejó `.automatic` activo, solo redirigido al container equivocado. **Evidencia triple**: código + doc Apple + Dashboard (los `CD_Split*` existen en `…yala` Dev+Prod). **Impacto**: duplicación de datos de grupo en la private DB personal del usuario, doble cuota/tráfico CloudKit, y un **tercer escritor concurrente** sobre el store local `YalaGroups` que compite con `applyGroupMeta` (fetch-by-`cloudKitZoneID`-then-insert; sin `@Attribute(.unique)` porque CloudKit lo prohíbe) → **sospechoso de primer orden de los grupos duplicados** que combate `SplitGroupDeduplicationService` (dedup por `cloudKitZoneID`, keeper = `createdAt` más antiguo). **Sin pérdida de funcionalidad con el fix**: el sync mismo-usuario cross-device ya lo cubre el `privateEngine` del CKSyncEngine sobre el privateCloudDatabase de `.groups` (`SplitSyncManager.swift:116-124`); ningún código lee `CD_Split*` ni el container personal para grupos (grep vacío); Share ext/Widgets/`QuickExpenseIntent` solo montan `personalConfiguration`. **Fix**: añadir `cloudKitDatabase: .none` (alinea producción con test/UITest) + comentario en el sitio para evitar regresión. **No probadamente la ÚNICA causa** de los duplicados (las races `applyGroupMeta` vs creación-local viven en el Canal B por sí solo y no se pudieron reproducir en 2 devices aquí); es contribuyente confirmado y redundante. **Pendiente (device/Dashboard, no testeable en esta sesión)**: (1) **upgrade-over install** — abrir el store de grupos ya espejado con `.none` sin error de migración (dirección espejo→sin-espejo es la segura; sin pérdida de datos locales — mismo `databaseName`/URL); (2) **limpieza `CD_Split*`** — Dev: "Reset Development Environment"; **Prod: las DEFINICIONES de record type ya NO se pueden borrar** (schema CloudKit de producción es append-only — puerta de una sola dirección; quedan inocuas), sí los datos de la private DB propia; (3) **2 devices mismo Apple ID** — crear grupo en A → aparece en B sin filas duplicadas; (4) **telemetría** — `cloudkitDuplicateDetected(model:"SplitGroup")` debe bajar tras el rollout (juez final). Investigación: sesión inline (sin plan file).

- [2026-06-10] **Sesión de cierre pre-release 2.0 — residuales del audit cerrados; go/no-go gateado SOLO al checklist físico** (commits `48da824e` l10n + `6fc6959f` chore + `cd2dcf3a` qa + docs). (1) **Dedupe de las 31 keys duplicadas** del bloque legacy en 13 locales con decisión POR KEY verificando callsites: `subcategory.balanceAdjustment` conservó el valor runtime (matching por nombre persistido en 8 callsites — cambiarlo duplicaría subcats); 4 overrides por error objetivo (`common.none`→"Ninguna" género del callsite, fr `select` recupera acento, `empty.noSubcategories` forma larga canónica, de `deleteAllData` primera persona porque la du-form violaba "Sie de"); el resto = valor visible hoy. `widget.size/preferences` + accessors muertos eliminados; `InfoPlist.strings` en-GB creado. Test nuevo `noDuplicateKeys_inAnyLocale` (fuente vía `#filePath` — sobre Bundle sería falso verde). (2) Fallback `"Usuario"` → `L10n.Profile.defaultName` en GroupSettingsView Y `GroupService.currentUserDisplayName` (los otros `"Usuario"` son sentinels de comparación, NO localizables). (3) **Gotcha harness cazado y resuelto**: al publicar el contenido 2.0 en `WhatsNewConfig`, TODO arranque `-uitest` post-onboarding mostraba el sheet de novedades sobre la UI → los 6 XCUITests de inbox/tx/panel fallaban en su primer tap. `applyUITestHooksEarly` ahora marca `lastSeenAppVersion` (DEBUG-only); regla implícita: contenido nuevo de WhatsNew ya no puede romper XCUITests. Re-run 6/6 verdes → lastVerified de 3 áreas + whats-new-sheet (render 2.0 verificado en sim) + migration-csv-mirror (smoke: cold launch seed pesado 11.6k TXs, chips de tags completos al primer render vía CSV-first; la race real cross-device queda gateada). Galería de widgets es-ES PASS (pendiente Tanda 3, evidencia en Attachments). cashflow-waterfall NO actualizado (wizard con sheets anidados inoperable vía agent-device; fix del tooltip ya QA'd en Tanda 1 → manual ~5 min en checklist). (4) **D-C documentada en DECISIONS.md**: OpenAI key en Info.plist + PrivacyInfo sin FinancialInfo = diferido consciente post-2.0 (proxy backend con App Attest como épico; consent in-app cubre disclosure; rate-limit interino). (5) **Entregable `$VAULT/planning/RELEASE-QA-2.0.md`**: gate G1-G5 BLOQUEA release (SUB-DEDUP-01..12 el más crítico, MIG-V2-FLASH cross-device, bridge personal F-S2-*, iCloud F-ICL-*, Routing F12 con pasos formalizados) + deseables D1-D5, cada item con pasos/devices/criterio/lastVerified target. **Pendiente: SOLO el checklist físico (2 devices misma Apple ID, data real) + decisión de subir build 20.**

- [2026-06-08] **Fix Apple Pay "No se pudo ejecutar el atajo" — `ApplePayTransactionIntent` deja de requerir UI**. Cierra un bug reportado: al pagar con Apple Pay (Wallet automation, **pantalla bloqueada**) iOS Shortcuts mostraba "No se pudo ejecutar el atajo" **aunque el draft se guardaba bien**. **Causa raíz**: el `perform()` devolvía `some IntentResult & ProvidesDialog` y todos sus `return .result(dialog:)` pedían a iOS **presentar un diálogo** — imposible con la pantalla bloqueada → el atajo se marca fallido post-`save()`. El commit previo `53a8dda3` quitó el `ShowsSnippetView` y movió la notif a `Task.detached`, pero **dejó el `ProvidesDialog`** → el bug sobrevivió (atacó el síntoma equivocado; el dialog era el culpable, no el `await` de la notif). **Verificado (regla "todas las instancias")**: ApplePay es el ÚNICO intent con `openAppWhenRun=false` + `ProvidesDialog` en automatización background; `SiriNaturalEntryIntent` también es `false` pero se invoca manual por Siri en foreground/desbloqueado → su `ProvidesDialog & ShowsSnippetView` es correcto y NO se toca; el resto (`QuickExpense/Voice/Image`, `OpenVoiceEntry/OpenImageEntry`) son `openAppWhenRun=true`. **Fix**: return type → `some IntentResult`; los 6 returns pasan TODOS a `.result()` (sin dialog, **sin `throw`** — un throw también dispararía el error bloqueado); feedback 100% por **notificación local** (sí llega bloqueada). Happy path: la notif de éxito pasa de `Task.detached` a **`await` estructurado** (decisión consciente que revierte el `Task.detached` de `53a8dda3` — `sendNotification` solo consulta permisos + `notificationCenter.add()` ≈ ms, muy por debajo del budget; `await` garantiza el envío antes de que el proceso del intent termine, donde el task huérfano podía no completar). Los 5 paths de fallo → helper `notifyFailure()` (notif de error genérica, `deepLink: nil`) + telemetría `error` ∈ `no_amount`/`database`/`no_account`/`save_failed`. Limpieza: eliminados `formattedAmount`/`noteDisplay`/`notifTitle` (solo alimentaban el dialog). **L10n**: 2 keys nuevas `shortcut.notification.errorTitle`/`errorBody` × 16 locales (traducción real por idioma, vía `add-l10n-key.sh` + script Python; me alineé con la familia `shortcut.notification.*` — es-AR tuteo neutro como el hermano "Entra a la app") + accessor en `L10n.swift`; eliminadas 2 huérfanas `shortcut.applePay.error.noAmount`/`success` (solo las usaba ApplePay; las compartidas `shortcut.error.database/noAccount/save` se MANTIENEN para SiriNatural). **UX (decisión owner)**: notif de error en fallos (vs silencioso/throw) — el usuario detecta un atajo mal configurado. **`/review-plan` cazó 4 ajustes** pre-impl: `deepLink: nil` en error (no "inbox", no hay draft que abrir), orden L10n-antes-de-código (compila), nota opaque return type (todos `.result()` consistentes), eliminación pareja de huérfanas con grep de verificación. **Sin tests unitarios** (cambio de return type + canal de feedback, no de lógica; `parseAmountAndCurrency` no se toca). Cobertura ya clasificada `manual`/device-only (área `apple-pay-automation`, F-WCS-03) — `lastVerified` NO se actualiza hasta device QA. QA-SCENARIOS.md Sección 23 actualizada (narrativa + paso 9 pantalla bloqueada + paso 10-bis notif de error). Plan: `~/.claude/plans/planfiicalo-bien-y-elige-rippling-bentley.md`. **Pendiente**: device QA físico con pantalla BLOQUEADA (irreproducible en simulador) — pago Apple Pay real → no "No se pudo ejecutar" + draft + notif éxito; atajo mal configurado → notif de error; regresión SiriNatural mantiene snippet. Tras OK → actualizar `lastVerified`.

- [2026-06-08] **Fix invite-drop: persistencia del invite de grupo pendiente — follow-up de B4-04 (`92abfdcc`)**. Cierra un bug pre-existente cazado durante B4-04: `.presentGroupInviteOnboarding`/`.presentGroupReconnect` son `isTransient` (default) → `AppRouter.resetTransients()` los dropea en `.background` y no hay productor que los re-emita → un usuario que acepta un invite y backgroundea/mata la app antes de presentar queda como miembro del grupo (CKShare ya aceptado en Branch A) pero NUNCA ve `GroupInviteOnboardingView`. **Reachability alta** en fresh install (welcome abierto → intent en cola segundos/minutos = ventana de background larga). **Solución (persistencia, NO se toca `isTransient`)**: nuevo `PendingInviteStore` (UserDefaults JSON, patrón `PendingLeaveShareTracker`; guarda `shareURL` + `BrandedMetadata` Codable + TTL 24h, single entry last-write-wins) que re-emite vía el `acceptShareFromURL` existente — que ya re-resuelve el `CKShare.Metadata` fresco desde la URL (el opaco nunca se serializa; salda deuda documentada en `DeferredIntentBufferLogic:12-14`). **SSOT de persistencia en `CKShareEntryHandler.handle`** (`metadata.share.url`) → cubre AMBOS entry points: universal link Y `userDidAcceptCloudKitShareWith` nativo. Re-emit en cold launch (bootstrap paso 19) + foreground (`ContentView` `.active`). **Guards**: `isProcessingInvite` (concurrencia), pure `shouldReEmitInvite(storeHasEntry,isProcessing,queueHasInviteIntent,isPresenting)` — `isPresenting` evita re-presentación espuria con el cover abierto (clear es **on-complete**, no on-present); `guard isInitialized` (no procesar pre-bootstrap). **Clear** on-complete (onComplete invite + onDismiss reconnect) + `resetAll` (wipe) + error **permanente** (red transitoria → retry silencioso vía `isRecoverableInviteFetchError`, no pierde el invite). **Idempotencia verificada**: `onboardingMode=.groupInvite` se setea en `performSilentSetup` (dentro del view); si el view nunca se presentó, `inviteRouteDecision` re-da `.acceptAndShowInviteOnboarding`. **Telemetría** privacy-first: `inviteReEmittedFromStore` + `invitePendingExpired`. **Flujo Complejo**: `/review-plan` cazó 3 (path nativo no cubierto → SSOT en handle; guard `isPresenting`; error red vs permanente); `/code-review high` cazó 2 (guard `isInitialized` faltante en re-emit foreground; `BrandedMetadata` aplanada en 4 campos × 3 sitios → hecha `Codable` directa, −25 LOC). **Tests**: 7 pure-logic `PendingInviteStoreTests` + 8 en `AppBootstrapperTests` (`shouldReEmitInvite` matriz + `isRecoverableInviteFetchError`); 114 verdes. Smoke device-QA OK (arranque + background→foreground sin crash; re-emit con store vacío = no-op). Plan: `~/.claude/plans/sorted-weaving-quill.md`. **Pendiente**: device QA físico PIN-01..10 (CKShare real: background/kill durante invite, path nativo, retry de red) — el simulador no acepta shares reales. **Follow-up documentado**: el reconnect-join (`handleReconnectJoin`) no persiste para reintento si el accept async falla tras el clear-on-dismiss (no es regresión — comportamiento idéntico al pre-fix; mejora futura ortogonal).

- [2026-06-04] **Fix subcategorías duplicadas por re-sync de CloudKit + prevención — commit `f278d323`** (1 commit, 14 archivos, +626/-40). Cierra bug reportado en TestFlight: subcategorías seed duplicadas (×2/×3) dentro de categorías ×1 (single device, update encima). **Causa raíz** (confirmada por código): (a) `CategoryDeduplicationService` solo deduplicaba *Categorías* duplicadas — nunca subcats dentro de una categoría única; (b) el dedup era **código muerto en el arranque** — `ContentView.scheduleDeduplication()` jamás se invocaba (solo corría en force-sync manual); (c) **detonante**: la migración V3 (`7e3d18c2`, épico CSV-mirror) regeneró el `shortcutID` de TODAS las subcats + `save()` masivo → re-export a CloudKit que reconcilió el record local dirty con el mirror entrante del mismo objeto lógico (distinto `recordName`) → materializó copias. Las categorías no se regeneraron → no se duplicaron (firma exacta). **Riesgo producción**: usuarios 1.0→2.0 correrán esa migración por primera vez → mismo re-sync → mismo riesgo. **Fix**: 3 helpers pure-logic nuevos en `Yala/App/Logic/` — `SubcategoryDedupLogic` (identidad `categoría+sortOrder+iconName+name`, solo `isDefaultSeed && !isAnySystem`; keeper determinista `sortOrder→name→shortcutID` + reparenteo por **unión** = cero pérdida cross-device), `SubcategoryDedupGate` (gating por quiescencia: no corre con `isSyncing`, quietWindow 8s, throttle 60s; `GateDecision` con retry diferido para no perder el trailing-edge de 3s), `AccountTagDuplicateCountLogic` (detección read-only de copias Account/Tag — mismo detonante, solo telemetría). `CategoryDeduplicationService` extendido: `deduplicateSeedSubcategories` + `runAllDeduplication` (categorías→subcats) + `runDedupIfQuiescent` (gate + retry + reporte once-per-session); reusa `reparentInverseRelationships` (→internal) + resync CSV de budgets. **Cableado** en 3 puntos: arranque post-migración (paso 8.5, idempotente — corre aunque el sentinel V3 ya esté set), `observeRemoteStoreChanges(context:)` trailing-edge gateado por **feature flag** `subcatDedupRemoteHookDisabled` (toggle DEBUG en `iCloudSyncSettingsView`), y force-sync manual. Elimina `scheduleDeduplication` muerto. **Idempotente sin sentinel** (reintenta siempre). Telemetría `cloudkitDuplicateDetected(model:"Subcategory"/"Account"/"Tag")` = diagnóstico read-only del owner sin fresh install. **Decisiones**: cleanup automático (protege a todos al actualizar) + Account/Tag solo detección (confirmar con datos antes de tocar entidades del usuario potencialmente homónimas). Build se mantiene en **18** (el bump inicial a 19 se revirtió — la build 18 nunca llegó a TestFlight, es el número correcto para esta entrega). **`/review-plan` cazó 3 problemas pre-impl** (gap timing del hook sin retry, mecanismo del feature flag, Account/Tag fuera de scope). **`/code-review` high** (3 finders + verificación): 1 fix correctness (retry Task: `guard !Task.isCancelled` + reset garantizado); resto refutado (patrón existente / síncrono sin re-entrada / riesgos residuales documentados). **`/refine`**: report Account/Tag once-per-session (eficiencia). **Tests**: 20 pure-logic verdes local (8 SubcategoryDedupLogic + 6 Gate + 6 AccountTag) + 5 integration de regresión en `CategoryDeduplicationServiceTests` (bucket R8 — válidos en CI, crashean local). Build `TEST BUILD SUCCEEDED`. coverage-index `categories-subcategories-crud` + 12 escenarios `SUB-DEDUP-01..12` en QA-SCENARIOS.md. Plan: `~/.claude/plans/de-acuerdo-planifiquemoslo-bien-indexed-thompson.md`. **Pendiente**: device QA (SUB-DEDUP-01..12) con update encima sobre estado duplicado + verificar telemetría TelemetryDeck (`model=Subcategory` count; `model=Account`/`Tag` para decidir follow-up de dedup de esas entidades). Follow-up: M-A (reducir re-sync masivo de V3 de raíz), reparenteo de subcats huérfanas.

- [2026-06-03] **Auditoría sistémica de patrones UI (workflow `ui-audit`) + remediación de mayor impacto + dimensiones color, a11y y componentes completas — 13 commits, épico CERRADO (solo resta deuda Pattern B aceptada)**. Nuevo workflow reutilizable `.claude/workflows/ui-audit.js` (4 fases: descubrir inventario → escanear fan-out por chunks de ≤8 vistas → verificar adversarialmente refutando falsos positivos → sintetizar reporte). Audita las 303 vistas SwiftUI contra el DS en 8 dimensiones (tokens, tipografía, botones, backgrounds, glass-cards, color, componentes, a11y). 122 agentes, **544 señales → ~319 ubicaciones únicas**. Reporte commiteado en `AUDIT-UI-patterns.md` (raíz) con tabla de estado + pendientes; **re-ejecutable acotado con `args.onlyAreas: ["Settings","Panel"]`**. **Causa raíz cerrada**: `$VAULT/planning/UI-PATTERNS.md` tenía nomenclatura MUERTA (`Color.yalaCard`/`yalaBackground`/`yalaPrimaryText`, `Color.priorityNature`, `YalaTextButton` — 0 usos reales) → actualizado a la real (`theme.*`/`.thCard`/`.thBackground`, colores de marca `electricIndigo/hotPink/neonCyan/priorityNeed/essentialNeed/priorityNeedNew/optionalNeed`, `YalaPrimaryButton/YalaSecondaryButton/YalaToolbarButton/YalaSaveButton`, `.solidCard()/.selectableCard()/.panelCard()/.yalaScreenBackground()`, `.yalaIconBadge*`). **Nuevo token** `DS.Typography.title3 = Font.title3.weight(.semibold)`. **7 commits remediación (~140 hallazgos alta, build-verde, cambios visuales validados con device-QA en iPhone 17 Pro / Yala Dev)**: `78f3724a` 65 literales→tokens DS (46 vistas), `ef685afd` token title3 +5 migraciones, `16e90f1d` 16 accessibilityLabel (incl. envío Chat), `3b04baf9` 12 CTAs→YalaPrimaryButton, `1bd25d9e` 16 .onTapGesture→Button+contentShape, `c4651c21` 15 cards→solidCard/selectableCard, `a1649c28` 13 sheets→yalaScreenBackground(.panel) +8 scrollContentBackground(.hidden). **+2 commits color (post-compact)**: `ba4a36e0` 10 colores semánticos de estado→`DS.Semantic.*` (alias 1:1, cero cambio visual), `3509355f` 16 markers `// A11Y-DM` (paletas decorativas intencionales) + 3 grises de chart→`theme.secondaryText` (device-QA) + 1 token swap. **+2 commits a11y**: `2e4d93c7` 8 labels VoiceOver (BudgetCharts/AccountForm/CategorySelector/CategoriesPie/PanelHero/GroupingReorder) + keys nuevas `accessibility.expand/collapse` (16 locales), `2709e391` 9 áreas táctiles ≥44pt (HIG; filas de día con expansión vertical para no desbordar; M/A selector device-QA OK). **+1 commit componentes**: `64cb28c0` 2 empty states→`YalaEmptyState` (Tags+Records, −37 LOC, preserva filter-aware msg + botón Limpiar filtros); los 8 icon-badges NO migrados — `.yalaIconBadge*` es dead code (0 usos) + renderiza RoundedRectangle vs `Circle` de la convención real (`RecordRowView`), migrar sería regresión → el reporte erró. Tooling+reporte en `ec63c446`, docs en `6fce16b0`. **Patrón ejecución**: sub-agentes paralelos (Agent tool) con mapeo file:line+token exacto + criterio para SALTAR falsos positivos; por sub-lote `git add -u` + sanity diff (conteos +/−, 0 anti-patrones) + build (`xcodebuild`/`build_run_sim` MCP) antes de cada commit. Identifiers XCUITest preservados (`new_transaction_save`, `inbox_success_accept`). **Casos saltados (el reporte ERRÓ)**: `BiometricLockOverlay` (fondo opaco de seguridad), `RecordsStandaloneView:315` barra flotante (debe ser glassEffect, no solidCard), `PeriodSelectorComponents` (.thCard deliberado), `TagFormView:91` (no era el CTA de guardar — ya usa YalaSaveButton), 6 `.fontWeight` que eran overrides intencionales (bold en heroes/KPIs, NO redundantes). **device-QA OK**: `registerButton` (YalaPrimaryButton full-width/capsule/disabled) + `UpgradePromptSheet` (gradient .panel legible). Owner aceptó "commitear ya, confío en el patrón" tras validar muestras (auto-push `2.0`). **color — CERRADA** (`ba4a36e0`+`3509355f`): triage de 54 hallazgos → 10 fixes zero-change (semánticos→`DS.Semantic.*`, alias 1:1) + 16 markers A11Y-DM (decorativos intencionales: paywall/What's New/edición masiva/acciones/Siri; `financeGreen` se mantiene = tono de marca) + 4 fixes (3 grises de chart→`theme.secondaryText` device-QA'd + `.red.opacity(0.7)`→`errorForeground.opacity(0.7)`) + 30 decorativos documentados (decisión owner: mantener variedad, NO aplanar a marca) + 6 falsos positivos/stale (`GlobalSearchView:415` ya migrado). **a11y — CERRADA** (`2e4d93c7`+`2709e391`): verificación reveló que `16e90f1d` ya resolvía 16 hallazgos (reporte stale). Restante: 8 labels VoiceOver (todos con keys existentes salvo `expand`/`collapse` nuevas) + 9 áreas táctiles ≥44pt (filas de 7 chips usan expansión vertical 36→44h; resto absorbido por Spacer/flex; M/A selector 32→44 device-QA limpio). **componentes — CERRADA** (`64cb28c0`): 2 empty states→`YalaEmptyState`; 8 icon-badges NO migrados (reporte erró — `yalaIconBadge` dead+RoundedRect vs `Circle` real). **LAS 8 DIMENSIONES DEL AUDIT CERRADAS o diferidas conscientemente.** Resta solo deuda baja: Pattern B (~70 `ZStack{PanelBackgroundView()}`) y media/baja sueltos. (`.yalaIconBadge*` dead code — **ELIMINADO 2026-06-04**, ~50 LOC: `YalaIconBadgeModifier` + 3 funcs + previews; build verde. Convención viva = `Circle()` hand-rolled con tokens `DS.Icon.badge*`.) **NO tocar**: Pattern B (~70 baja `ZStack{PanelBackgroundView()}`, deuda aceptada), media/baja sueltos. Working tree limpio (solo `.ddp/` + `AUDIT-2.0-bugs.md` untracked, ajenos).

- [2026-05-26] **Refactor robusto del subsistema de routing — 12 fases F0-F11** (épico completo F0-F12, F12 bump pendiente). Cierra bug TestFlight reportado por owner: notif tap a transacción automatizada → Inbox sheet → user aprueba → alert "automatizaciones" aparece tardío (fullScreenCover atascado detrás del sheet). Auditoría sistémica (3 Explore agents + 20 tests AppRouter preservados) reveló fragilidades estructurales: dedup semántico cross-intent ausente, readiness gates incompletos (3 de 12 dimensiones), CKShare dual entry, dueling monetization, `.remoteOnboardingCompleted` transient, `.profile` consumer dead code, sin buffer pre-bootstrap general. **Arquitectura nueva**: `RouterEntryGate` (facade @MainActor singleton — todos los productores submit ahí, aplica supersession + buffering + enqueue) → `AppRouter` (motor existente + 3 helpers additive: `drop(where:)`, `contains(where:)`, `queueSnapshot`) → `PresentationCoordinator` (orquestador @Observable, expone state observable consumido por shell vía @State como espejo — decisión owner para minimizar riesgo regresión visual) → `DeferredIntentBuffer` (UserDefaults JSON, TTL 24h, schema versioning, decode tolerante a corruption). **7 pure-logic helpers nuevos** en `Yala/App/Logic/`: `ContentViewReadinessLogic` (matriz 15 flags), `IntentSupersessionLogic` (reglas declarativas extensibles), `PresentationCascadeLogic` (mapea RouterIntent → CascadeDecision), `DeferredIntentBufferLogic` (codable layer), `NotificationGateLogic` (defer vs enqueue), `MonetizationDedupLogic` (trial expired blocks trial offer), `PanelAvailabilityLogic`. **~110 tests pure-logic** + 4 nuevos AppRouter (drop/contains) + ~8 integration end-to-end `@Suite(.serialized)` cubriendo el bug del owner + dueling monetization + remote wipe priority + buffer round-trip + single-intent drain regression. **Productores migrados**: ~55 callsites de `AppRouter.shared.enqueue` → `RouterEntryGate.shared.submit` vía sed mecánico (16 archivos). **`CKShareEntryHandler`** consolida YalaAppDelegate + AppBootstrapper.acceptShareFromURL (eliminó ~30 LOC duplicadas). **Signals iCloud promovidos a RouterIntent** (`.remoteOnboardingCompleted` nuevo, `.iCloudMismatch` + `.remoteWipe` ya existían): 3 observers `NotificationCenter.default.publisher` eliminados de ContentView; productores (`PreferenceSyncService:314,330`, `AppBootstrapper:334`) llaman al gate directo. **`.profile` consumer eliminado** del enum ConsumerID + case `.profileNavigate` del RouterIntent (F0 confirmó 0 productores en producción — dead code). **`.planning` consumer preservado** — pre-flight reveló que NO es dry-run (BudgetsListView + ScheduledPaymentsView usan peek-first pattern legítimo). **Readiness gates ampliados** de 3 → 15 dimensiones vía `ReadinessGateObservers` ViewModifier (encapsula 12 onChange para mantener body de ContentView dentro del type-checker budget). **Telemetría privacy-first** 3 events nuevos: `routingIntentSuperseded(droppedID, by)`, `routingIntentDeferred(intentID, reason)`, `routingReadinessBlocked(blocker)` con throttle a blockers user-visible (skip splash/lock). **Resolución del bug del owner via triple defensa**: (1) F9 supersession rules — `.navigate(.inbox)` y `.presentInboxSheet` supersedean `.showInboxAlert` pendiente al enqueueear; (2) F8 readiness gate — `hasActiveInboxAlert` bloquea drain mientras modal visible; (3) F9 NotificationService persist — tap durante lock/onboarding va al buffer, drena post-unlock. Resultado: el alert "automatizaciones" jamás aparecerá tras dismiss del sheet — o superseded, o gateado. **`PendingAIInput: Codable`** habilitado (era Equatable/Hashable; cambio trivial). Build verde end-to-end en cada fase (F1-F11 secuenciales, ~16 archivos modificados + 13 nuevos). **F12 bump build a TestFlight pendiente** — owner subirá tras validar manual del bug en simulador / TestFlight. **Decisiones owner consolidadas**: (a) coordinator orquesta lógica pero @State del shell sigue siendo binding UI source (espejo sincronizado), no migración masiva — minimiza riesgo regresión visual; (b) signals iCloud → RouterIntent (unifica modelo + permite supersession cross-type); (c) refactor onboarding chain queda fuera de scope (épico propio); (d) no se crea ARCHITECTURE.md separado (DECISIONS.md + CODEBASE-MAP.md cubren). **`/review-plan` cazó 16 problemas pre-implementación** (autoclosure no reactivo, snapshot ambiguo, defer keyword reservado, etc.) — todos corregidos. **20 tests existentes de AppRouter intactos** (single-intent drain regression protegido). Pendiente: F12 device QA + bump build + commit final agrupado.

- [2026-05-26] **Refactor V3 unificado del CSV-mirror SSOT — 7 commits atómicos**. Cierra el "flash vacío" de chips de tags en cualquier cold launch (no solo durante migración) atacando 4 frentes simultáneos. **Commit 1**: extender CSV mirror a `FavoritePayment` + `ScheduledPayment` (paridad con TX/Draft/Budget) — nuevos campos `tagIDs: String?` + extensions `+TagsCSVMirror.swift` (replica patrón canónico TransactionItem), auto-mirror en init body, migrar 3 writers (`FavoriteEditorViewModel:71`, `DataWipeService:113`, `ScheduledPaymentEditorViewModel:125`) a `setTags(from:)`, extender `EntityDeletionService.deleteTag` para re-encodear CSV de 4 models afectados (TX/Draft/Favorite/Sched) además del Budget existente con nueva telemetría `tagDeleted.duration_bucket`. 6+6 tests pure-logic (`FavoritePaymentTagsCSVTests` + `ScheduledPaymentTagsCSVTests`). **Commit 2**: migración V2 → V3 unificada gateada al hook `iCloudFirstImportCompleted`. Nuevos `MigrationGateLogic` (pure-logic enum WaitDecision con 4 cases test) + `MigrationBackfillLogic.planForBudget(...)` (per-relation decisión `.writeFromM2M` vs `.nukeStale` + `sawRace`). Reemplazo de las 2 funciones `persistAppEntityShortcutIDsIfNeeded` + `backfillBudgetFilterCSVsIfNeeded` (líneas 681-827 AppBootstrapper) por una sola `migrateShortcutIDsAndRebuildCSVMirrors(context:waitedForSync:waitDuration:)` con **single save atómico** — fusión regen UUIDs + re-encode CSV de 5 models (Budget/TX/Draft/Favorite/Sched) en una transacción. **Sentinel bump V2 → V3** (`appEntityShortcutIDsMigratedV3`) fuerza re-corrida en 100% devices instalados; legacy cleanup de 3 keys obsoletas (`appEntityShortcutIDsMigratedV2`, `Yala_BudgetFilterCSV_v2`, `Yala_BudgetFilterCSV_attempts_v2`) en AppPreferences + DataWipeService + AppBootstrapper. **Sin maxAttempts** — retry infinito hasta `!sawRace` (gate al hook filtra la mayoría; bounded por convergencia device, no contador artificial). Task `@MainActor` en paso 8.5 invoca `forceFetchAndWait(timeout: 15)` solo cuando `isAccountAvailable && !hasCompletedFirstImport`. Telemetría enriquecida con `waitedForSync`/`waitDuration_bucket`/`sawRace` + counts por modelo + nueva case `tagCatalogRebuilt`. 4+5 tests (`MigrationGateLogicTests` + `MigrationBackfillLogicTests`). **Commit 3**: catalog injection `@Environment(\.tagCatalog)` global. Nuevos `TagCatalogEnvironment.swift` (EnvironmentKey<[UUID: Tag]> con `defaultValue: [:]`), `TagCatalogProvider.swift` (ViewModifier con `@Query<Tag>` única + dispara `tagCatalogRebuilt` on count change), `TagDisplayResolver.swift` (4 overloads TX/Draft/Favorite/Sched + helper `resolve(ids:catalog:limit:)` pure-logic). Aplicado `.modifier(TagCatalogProvider())` en `ContentView` antes de MainTabView. Migración de 3 Rows display (`RecordRowView:79+199+212-213`, `InboxDraftRowView:102+168+180-181`, `FavoriteRowView:39+119+131-132`) — chips resueltos vía catalog inyectado. 6 tests `TagDisplayResolverTests`. **Commit 4**: `FilterCriteria.populateTagUUIDs(from: [Tag])` helper en FilterService — mapea `[Tag]` → `Set<UUID>` en `selectedTagUUIDs` para tag matching CSV-first. Migración exhaustiva de **17 callsites** de FilterCriteria (más que los 11 del plan inicial — audit reveló 6 adicionales en CategoriesTabView): `PanelViewModel:1455` (canonical builder) + `StatisticsViewModel:465+485` (Sankey/Trend builders, ahora reciben `allTags: [Tag]` param propagado desde `calculateTrendData`/`calculateSankeyData` + callers `DetailContainerView:563` y `CategoriesTabView:635`) + `FinancialReportViewModel:136` + `RecordsViewModel:193` + `TrendsTabView:1270+1410+1493+1587+1638` (5 sitios) + `CategoriesTabView:1198+1221+1290+1358+1438+1474` (6 sitios). + 5 logic readers (`GlobalSearchView:91+104` con env catalog + nuevo helper `matchesTagName`, `PanelViewModel:1379`, `ScheduledPaymentsViewModel:348` con fetch local de tags + try/catch, `BudgetsViewModel:537-539` telemetría diagnóstica). 5 tests `FilterCriteriaPopulateTagUUIDsTests`. **Commit 5**: form pre-population. 6 callsites migrados a `TagResolver.fetch(ids:in:)` con `do/catch`: `RecordsViewModel.getSelectedTransactionTags(context:)` (firma nueva, callsite `BulkEditSheet.BulkTagEditorSheet:477` con `@Environment(\.modelContext)`), `NewTransactionView:1408+1652`, `InboxDraftEditSheet:759`, `FavoriteEditorView:395`, `ScheduledPaymentEditorView:1061`, `ScheduledPaymentDraftService.createDraft(from:context:)` (firma extendida + 3 callers actualizados). **Commit 6**: export/LLM/autocomplete. `TransactionsExportService.applyInMemoryFilters/makeCSVData/makeExportData` aceptan `tagCatalog: [UUID: Tag] = [:]` (default vacío para back-compat) y resuelven UUIDs vía catalog desde `Tag.byIDLookup(allTags)` fetch en cada entry point. `FullFinancialContextBuilder.buildTagsTop10` reusa el `allTags` ya inyectado para construir catalog + resolver. `ChatSuggestionsLLMService:247` y `DescriptionAutocomplete:163` migran al mismo patrón. **Commit 7**: eliminación legacy fallback paths. `TagSpendingCalculator` rama `useCSVPath=false` (líneas 62-66) borrada — `allTags` ahora **required** (sin default). `PivotTableCalculator` "Fallback legacy M2M direct" (líneas 212-224) borrado — `allTags` required en `group`/`buildTree`/`groupKeys`. `FilterService.matchesCriteria` rama legacy `selectedTags` sin `selectedTagUUIDs` eliminada — `selectedTagUUIDs` es la única SSOT. **Bump build TestFlight** `CURRENT_PROJECT_VERSION 13 → 14` (20 occurrences). **Commit 8**: 12 escenarios `MIG-V2-FLASH-01..12` en `$VAULT/planning/QA-SCENARIOS.md` cubriendo cuenta poblada / offline / local-only / 10k+ TXs / cross-device / FavoritePayment+ScheduledPayment chips / Search tag.name / Edit form pre-population / Inbox draft / Tag filter en Stats/Trends/Categories / V2→V3 bump device-poblado. **Decisiones arquitectónicas clave**: (a) `@Environment(\.tagCatalog)` global vs @Query per-Row (consistente con patrón AppPreferences/YalaTheme; un solo @Query<Tag> en root). (b) Sentinel V3 bump (cost ~3s cold launch siguiente al deploy, 1× por device, aceptable según directiva owner "lo más robusto sin diferir"). (c) Single save atómico fusionando regen + re-encode (vs status quo 2 saves en pasos secuenciales). (d) Retry infinito sin counter (gate al hook filtra mayoría; bounded por device convergence). (e) `setTags(from:)` mantiene asignación M2M `self.tags = tags` (el sync storm documentado es por iterar masivamente, no por escrituras puntuales en flow de usuario — `FilterControlBar`, Editor sheets). (f) Audit reveló **6 callsites adicionales de FilterCriteria** no listados en plan inicial (CategoriesTabView con 6 vs 2 estimados) → migrados todos en Commit 4 sin diferir. (g) Mantener `FilterCriteria.selectedTags` (Set<PersistentIdentifier>) para UI state de chips selection — no deprecation prematura. **Build SUCCEEDED tras cada commit**. ~31 tests pure-logic nuevos en `YalaTests/` sin context (regla R8). Plan: `~/.claude/plans/quiero-que-me-des-wild-coral.md`. Pendiente: device QA con 12 escenarios MIG-V2-FLASH-XX en simulador iPhone 17 Pro con cuenta poblada (5k+ TXs, 30+ tags, 10+ budgets) + cross-device (físico A + simulador B con misma Apple ID) + verificar telemetría TelemetryDeck post-deploy (`appEntityShortcutIDsRegenerated.sawRace=false` >95%, `waitDuration_bucket` distribución `lt_1s`/`1_3s` mayoría, `tagCatalogRebuilt` <10/sesión p95, `tagDeletionDuration_bucket` mayoría `lt_500ms`).

- [2026-05-26] **Fix migración shortcutID/Tag.id colapsado V2** — commit atómico `7e3d18c2`. Cierra bug crítico aparecido en producción tras épico CSV mirror: presupuestos con todas las subcategorías (1416% gastado), TagsSettingsListView con primer Tag repetido N veces, chips de etiquetas colapsados al mismo nombre. **Causa raíz**: la migración `persistAppEntityShortcutIDsIfNeeded` V1 (commit `2a1e1771` 26-abr, ampliada en CSV mirror épico para Tag.id) hacía `entity.id = entity.id` (idempotent self-assignment) que es no-op en SwiftData — no marca dirty, el default `UUID()` evaluado al deserializar nunca llegaba a CKRecord, todos los records sin field persistido colapsaban al mismo UUID. Filter `.contains($0.shortcutID)` matcheaba TODAS las subcats; `ForEach(id: \.id)` colapsaba rows; cálculo de presupuesto sumaba todas las TXs. **Approach V2**: (a) `Tag.id` siempre regenerar via nuevo helper `regenerateAllUUIDs` — campo nuevo del épico, jamás persistido en V1, la heurística por duplicados falla con 1-entity (count==1). (b) `Account.shortcutID` / `Subcategory.shortcutID` heurística por duplicados via `regenerateDuplicateUUIDs` — preserva UUIDs únicos sincronizados desde otros devices (Atajos cross-device). (c) Nuke CSV mirrors corruptos en TODOS los modelos (Budget + TransactionItem + InboxDraft) — backfill posterior los rehidrata desde M2M. (d) Backfill TX/Draft escribe `tagIDs` directo (`CSVMirrorCodec.encode(...)`) sin reasignar M2M — evita marcar dirty cada TX (sync storm en cuentas con miles de TXs). (e) Per-relation skip en Budget loop (no AND-gate completo): si solo 1 de 3 M2M es nil, procesa los otros 2. (f) `sawRace` tracks los 3 loops (Budget + TX + Draft) — sentinel se setea solo cuando todo procesado o tras maxAttempts=5. (g) Sentinel `appEntityShortcutIDsMigratedV2` + removeObject de sentinels backfill SOLO en success path (atomicidad si save throwea). (h) Helper `@MainActor` para prevenir mutación @Model off-main en callers futuros. (i) `DataWipeService` limpia las 3 V2 sentinels para que migración + backfill re-ejecuten tras wipe + reseed. (j) Telemetría `appEntityShortcutIDsRegenerated` siempre fire (no gateada por `regen > 0`) — visibility en 1-entity-case. V1 keys eliminadas (dead). **Code-review max-effort + refine integrados pre-commit**: 36 candidatos en 5 angles, 10 fixes aplicados sin diferir. **Tests**: 9 pure-logic en `AppEntityShortcutIDRegenerationTests` (6 heurística + 3 force-all). Build verde end-to-end. **Trade-off conocido**: Atajos de Siri con shortcutID guardado pre-fix pierden auto-resolución de cuenta/subcat (UX hiccup puntual, fallback by name en `QuickExpenseIntent` queda como sprint dedicado). Plan: `~/.claude/plans/enchanted-gathering-alpaca.md`. Pendiente: device QA con 8 escenarios MIG-V2-01..08 cold launch sobre estado roto + cross-device + cuenta con 1 entity (force-all kicks in) + verificar telemetría TelemetryDeck.

- [2026-05-25] **CSV mirror épico — Budget filters + TransactionItem.tags + InboxDraft.tags resilientes a CloudKit lazy hydration**. Commit atómico `4605121f` (41 archivos, +1306/-184). **Bug crítico cerrado**: presupuestos filtrados por subcategoría mostraban consumido = TODO el periodo tras cold start o sync incremental. Causa: `@Relationship` M2M `[Type]?` aparece `nil`/`[]` durante ventana lazy de CloudKit antes que lleguen los records relacionados; cálculo interpretaba `nil` como "sin filtros" → suma catastrófica. Bug ortogonal latente paralelo: `tx.tags = currentTags` en bulk add con M2M lazy nil clobbeaba tags previos silenciosamente. **Approach**: dual M2M + CSV mirror (SSOT lectura, eager en el record). 4 campos nuevos `Budget.subcategoryIDs/accountIDs/tagIDs: String?` + `TransactionItem.tagIDs: String?` + `InboxDraft.tagIDs: String?` (CSV de UUIDs). M2M se mantiene solo para cascade `.nullify` automático. **Helpers compartidos**: `CSVMirrorCodec` (encode/decode UUIDs ↔ CSV — usado por 3 mirrors + tests), `TagResolver.fetch(ids:in:)` (FetchDescriptor by id — 6 callsites: TransactionService×2, RecordsViewModel×2, DraftService×2), `Tag.byIDLookup(_:)` (Dictionary con `uniquingKeysWith` para tolerar duplicates CloudKit — 2 callsites). **Writers** via helpers únicos `setFilters(...)`/`setTags(from:)` con dual-write. Auto-mirror en init body: `self.tagIDs = tagIDs ?? CSVMirrorCodec.encode(tags.map(\.id))` cierra el path donde CREATE callsites (NewTransactionViewModel, ChatAssistantViewModel, CSVImport) creaban TX con M2M poblada pero CSV nil. **Readers** via `resolvedXIDs(scheduleBackfill: true)` con M2M fallback + auto-cura async race-safe: el `Task @MainActor` verifica `currentM2M == snapshotIDs` antes de escribir (evita resurrección de tags borrados por writer concurrente). **Backfill eager** en `AppBootstrapper.backfillBudgetFilterCSVsIfNeeded` con sentinel `AppPreferences.Keys.budgetFilterCSVMirrorV1` + retry counter (max 5) tolerante a race CloudKit (M2M nil = sync en curso, skip + retry; M2M [] = sin filtros, OK procesar). **Tag.id UUID nuevo** estable migrado en `persistAppEntityShortcutIDsIfNeeded` paralelo a `Account.shortcutID`/`Subcategory.shortcutID` (evita regeneración per cold launch que rompería CSV references). **15 fixes del code-review max-effort aplicados**: (1) Tag.id migration, (2) Dictionary `uniquingKeysWith` (anti-crash CloudKit dedup race), (3) InboxDraft CSV mirror (DraftService.approveDraft ya no pierde tags), (4) write-write race con snapshot check, (5) PivotTableCalculator con `allTags` param, (6) FilterService con `selectedTagUUIDs` CSV path, (7+14) auto-mirror en init body de 3 modelos, (8) bulkAddTags/Remove con `try` propagado (no clobber silent), (9) SessionState do/catch (no `try?` que silencia), (10) CategoryDeduplicationService resync CSV POST-save (no inline durante reparent — captura UUIDs que iban a ser borrados), (11) EntityDeletionService re-encodea CSV tras cascade nullify, (12) 2 callsites legacy displayProperties migrados a computeDisplayProperties (1 TODO movido a Backlog/insights-iconlookup-precomputed.md), (13) telemetría event params `budgetID/periodType/hadM2MNotEmpty`, (15) DataWipeService usa setFilters (SSOT). **Refines /refine post-fixes**: comments narrando ticket reformulados (11 sitios — eliminar "Cierra el bug", "Sprint X", "FU-02 cleanup"; reformular como design intent), `Yala_BudgetFilterCSV_*` keys → `AppPreferences.Keys.budgetFilterCSVMirror*`, `pruneBudgetCSVMirror` 3 overloads → 1 helper `reencodeBudgetCSVMirrors(in:)` con do/catch en lugar de `try?`. **Tests**: 5 archivos test (3 nuevos): `BudgetFilterCSVTests` (13 pure-logic codec), `BudgetFilterRegressionTests` (5 context-based — flake R8 simulator local, válidos en CI), `TagTests` extendido con `id_*` (3), helpers `TestHelpers.makeTestBudget` con params CSV opcionales. Build verde end-to-end, 55 pure-logic verdes (`/test-smart`). Telemetría: `AnalyticsEvent.budgetFiltersAppearEmpty` con dedup por sesión. **8 escenarios QA documentados** BG-CSV-01..06 + TX-CSV-07 (bulk add tag con M2M lazy preserva previos) + TX-CSV-08 (Tab Tags spending correcto cold start). **Out of scope diferido**: InsightsCalculator `iconLookup` precomputado (no es @MainActor — refactor en sprint dedicado, ticket en `Backlog/insights-calculator-iconlookup-precomputed.md`); display/search/IA readers de `tx.tags` (GlobalSearchView, autocomplete, export, Chat, FullFinancialContextBuilder) siguen leyendo M2M directo — correctness OK, solo UX stale puntual durante race. Plan: `~/.claude/plans/quiero-abordar-todos-los-prancy-jellyfish.md`. Pendiente: device QA con 8 escenarios + verificar `budgetFiltersAppearEmpty` event en TelemetryDeck post-deploy (<1% expected; si más, regresión latente).

- [2026-05-25] **Transfer pair integrity épico — 6 commits atómicos `4cd4ec6f → ae7168da`**. Cierra Bug B reportado (transferPairID regenerándose en cada save) + 12 huecos identificados por code-review max-effort + cleanups del refine. **Commits**: (1) `4cd4ec6f` `TransferPartnerLookup` helper centralizado con 2 contratos: `partner` (silently picks one en collisions) + `partnerSkipIfAmbiguous` (returns nil si 3+ TXs sharing pairID, evita widening desync en bulk operations). (2) `99e104a2` Helper hardening `resolveTransferPairID(candidates:)` con fallback symmetric out → in (no más trampa `?? ""` que perdía pairID válido en in), UUID canonicalization uppercase (case-mismatch en groupings case-sensitive rompía pairs legacy), validation + trim. `didSet` en `transactionType` limpia state SOLO al SALIR de `.transfer` (`guard oldValue == .transfer`) — preserva `exchangeRate` en TXs foreign-currency cuando user togglea `.expense ↔ .income`. 13 tests pure-logic nuevos. (3) `1c431ee7` `prefillFromContext` robustness: defensive sign assignment cubre caso degenerado (ambos amounts ≥ 0) sin crash; partner missing NO-destructive (defiere al reconciler boot-time — clear durante sync race haría que CKSyncEngine resuelva conflicto contra partner remoto recién subido, destruyendo pair legítimo); reemplaza fetch inline (18 LOC) con `TransferPartnerLookup.partner`; `try?` reemplazado por `do/catch` (regla CLAUDE.md). (4) `26112401` `TransferPairReconcileService` boot-time paso 16.4.8 Task-wrapped (no bloquea cold launch). Detecta orphans (1-of-1 con pairID → clear), collision (3+ shared pairID → telemetry only, NO auto-repair), malformed (no-UUID/whitespace → clear). Pure-logic `computePairingPlan` empareja N=2 balanced (1 neg + 1 pos misma fecha/abs(amount)/currency); skip N>2 ambiguous. UUID canonicalization antes de grouping detecta correctamente pairs con case-mismatch. Predicate con nil guard explícito (CloudKit-backed safe). 2 events telemetría privacy-first (`cloudkitTransferOrphanRepaired` + `cloudkitTransferCollisionDetected`) vía `enum TransferReconcileContext`. 13 tests pure-logic. (5) `f84218ce` CSV/XLSX 4 paths invocan `computePairingPlan` sobre TXs `createdAt >= importStart` (filtra a SOLO TXs recién importadas, evita falsos positivos con orphans históricos — el reconciler boot-time los maneja). SSOT pure-logic compartido. (6) `ae7168da` Bulk operations en `RecordsViewModel` + `TransactionService` paralelos: `bulkUpdateSubcategory` BLOQUEA si selección contiene transfers (subcat sistema obligatoria); `bulkUpdateAmount` propaga con signo opuesto preservado, BLOQUEA cross-currency transfers (cambiar amount destruiría exchange rate); `bulkUpdateNote` propaga SOLO si partner ya estaba sincronizado con tx (preserva divergencias intencionales), `empty→nil` consistente; `bulkAddTags` MERGE con tags propios del partner (no clobber — bug de la primera implementación); `bulkRemoveTags` propaga. Usa `partnerSkipIfAmbiguous` para skipear collisions. `BulkEditSheet` surface `bulkUpdateError` vía `.alert`; 5 `apply*` funcs gate `appliedChanges.insert` + `onComplete` cuando hay error (evita UX deception "success + alert" simultáneo). `TransactionServiceError` 2 cases nuevos. L10n 2 keys (`cannotEditTransferSubcategory` + `cannotEditTransferAmountCrossCurrency`) × 16 locales con traducciones reales (voseo es-AR, Sie de, vouvoiement fr, です/ます ja, 你 zh-Hans, tu/você pt-PT/pt-BR). 3 tests pure-logic. **Decisiones técnicas**: (a) **B-NEW-2 (post-save reorder) REVERTIDO** — análisis profundo: mover asignación de `editingTransferPair` post-`context.save()` puede empeorar el caso degenerado (SwiftData NO rollbackea pending inserts en throw; retry con flag=nil entraría al CREATE branch creando duplicates en lugar de UPDATE los pending). Mantener pre-save assignment (idempotente en retry). (b) **`private` vs `internal`**: `@testable import` solo abre `internal`. Mantenido como `internal nonisolated static` — surface mínima. (c) **F5 NO-destructive**: defiere al reconciler boot-time en lugar de clear durante prefill (evita CloudKit sync race). (d) **F15 cross-currency orphans pairing** deferred — el refactor para soportar matching cross-currency vía `amountInPreferredCurrency` tolerance es follow-up no trivial. (e) **TransactionService duplicate fix sin consolidar** — ~150 LOC duplicados entre RVM y TS. Decisión consciente del plan (scope acotado). Comments `(Keep in sync)` eliminados durante /refine; deuda técnica documentada. **Tests**: 29 pure-logic nuevos (10 helper + 3 didSet + 13 reconciler + 3 service) = **162 tests verdes, 0 fails**. Build SUCCEEDED post-cada-commit. **Documentación**: bug ticket `transaction-disappear-and-transfer-ghost.md` → `qa_transaction-disappear-and-transfer-ghost.md` con sección Implementación. `Backlog/debounce-transactions-imported-from-sync-observer.md` creado (post-M6 TODO movido del código). Plan: `~/.claude/plans/necesito-que-investigues-y-radiant-sparrow.md`. Pendiente: device QA con 17 escenarios T-01..T-17 + escenarios nuevos del code-review (toggle .expense ↔ .income con foreign-currency preserva rate; cold launch con orphans seeded; CSV import opposite-sign; bulk-edit cross-currency amount alerta; bulk-add tag respeta partner tags).

- [2026-05-22] **FU-02-cleanup — TX/drafts huérfanos + propagation fix + leave/remove cleanup**. 1 commit atómico tras FU-02 inicial. Cierra el TODO diferido del FU-02 + 4 bugs adicionales descubiertos en pre-flight + 1 bug oculto P0. **Cambios**: (1) `freezeForSoftDelete` en `GroupTransactionBridge` libera TX cuenta real (nilea `splitExpenseID/splitSettlementID/splitGroupZoneID` — preserva monto/fecha/cuenta/subcat), mantiene TX virtual sistema intacta, convierte drafts `groupExpense/groupSettlement` a `sourceType=.manual` preservando 12 cached fields. Pure-logic `classifyForSoftDelete + computeFreezePlan + shouldBridgeExpense`. (2) **Propagation fix**: `isHiddenForAll` + `isArchived` añadidos a `CKConstants.GroupMetaField` + `CKRecordTranslator` (applyGroupFields/group(from:)/update con `readBool default-safe`). `softDelete` y `setArchived` ahora llaman `SplitSyncManager.enqueueSave(group)` post-save para subir el record (antes solo escribían CKShare custom key, que solo se lee al retap link). Convergen dos canales: record sync + CKShare custom key. (3) **Bug oculto P0 leaveGroup**: `unbridgeExpenses` destructive `context.delete(tx)` eliminado entirely. `leaveGroup` ahora usa `freezeForSoftDelete` (decisión #1 owner — preservar rastro financiero). `deleteGroup` DEBUG-only también migra. (4) **`removeMember` cross-device cleanup**: observer en `applyMember` detecta flip current user `.active → .removed` vía pure-logic `SoftDeleteObserverLogic.shouldTriggerRemovedSelfCleanup`. Encola `pendingRemovedSelfZoneNames`, procesa post-save con nuevo `GroupService.performRemovedSelfCleanup(zoneName:)` (flow simétrico a leaveGroup). (5) **Race serverRecordChanged fix**: `handleConflict` captura pre-state, si server retorna stale false revertiendo local true, re-aplica + re-enqueueSave + re-`propagateBoolCustomKey`. `propagateBoolCustomKey` cambia de `private` a `internal static` para uso desde SplitSyncManager. (6) **`leaveShare` network fail retry persistente**: nuevo `PendingLeaveShareTracker` (UserDefaults JSON-encoded Set<PendingLeaveShareEntry>) + `SplitZoneManager.leaveShareByZone(zoneName:ownerName:)` standalone (no requiere SplitGroup vivo). AppBootstrapper paso 16.7 retry boot-time. (7) **Bridge guard hidden group** en `bridgeExpense`: early return si `group.isHiddenForAll==true` — invitado fresh-install no bridgea expenses de grupo soft-deleted. (8) **Boot-time safety net** F6 paso 16.4.5 fetch SplitGroups con `isHiddenForAll==true` + SplitMembers `isCurrentUser==true && status==.removed`. (9) **Cleanup oportunista**: `case manual` añadido a `DraftSourceType` (icon `square.and.pencil`). `MemberField.isCurrentUser` huérfano eliminado de CloudKitConstants. Línea `member.isCurrentUser = false` no-op en `removeMember` borrada (device-local, reescrito por `refreshCurrentUserFlags`). 3 comments engañosos corregidos (`setArchived`, `softDelete`, `acceptShareFromURL`) — ahora reflejan que sync sí lleva el field. (10) **Bug latente documentado** (NO fix en este commit, FU-04 si telemetría revela frecuencia): fresh-install + admin removió previamente → newMember insertado con `isCurrentUser=false`, observer F5b no se dispara. `refreshCurrentUserFlags` setea isCurrentUser=true posteriormente pero el VM filter `activeGroups: !isArchived && !isHiddenForAll` no chequea status, grupo queda visible hasta retap link. **Tests**: 16 pure-logic nuevos en `GroupTransactionBridgeSoftDeleteTests` (regla R8, sin context). Cubren classify + computeFreezePlan (con 12 fields preservation enumerados) + shouldBridgeExpense + shouldTriggerRemovedSelfCleanup + PendingLeaveShareTracker JSON round-trip. **CloudKit Production schema deploy**: `isArchived` + `isHiddenForAll` se auto-promueven via primer write desde build deployed (patrón previo `membersCanInvite`). **11 escenarios QA** B3-29..B3-39. **Cierra silenciosamente** B3-26/B3-31/B3-32/B3-35 (pendientes QA del FU-02 original). Plan: `~/.claude/plans/el-punto-5-incluyamoslo-dazzling-hoare.md`. Pendiente: device QA con 11 escenarios + CloudKit Dashboard schema verify post-merge.

- [2026-05-22] **FU-02 — Soft-delete grupo (owner-only, irreversible, gateado por balance=0)**. 3 commits atómicos `ee9df686` (F1 modelo+service) + `9081be6f` (F2 UI) + `3914b8c7` (F3 routing) cierran el TODO FU-02 diferido del épico Groups Pulido Final tras B-07 (archive con warning). **Motivación**: `GroupService.deleteGroup` deshabilitado desde el épico inicial porque el cascade real (delete CKZone + cascadeDeleteGroupData + unbridgeAll) disparaba loops infinitos en logs sync. Owner quería un "delete UX" verdadero invisible para todos sin tocar CKZone (que era el problema). **Patrón canónico reusado**: `propagateArchivedToShare` (A4 v3.2 C5) replicado 1:1 como `propagateHiddenForAllToShare` — escribe `CKShareCustomKey.isHiddenForAll` al CKShare zone-wide, leído pre-accept por invitados que retap el link. **Decisiones del owner consolidadas via AskUserQuestion**: (1) Irreversible desde la app — recovery solo via CloudKit Dashboard (procedimiento documentado en `apuntes-grupos.md` FU-02 sub-sección "Recovery procedure": 3 pasos setear flag=false en CKShare + en SplitGroup record + forzar resync). (2) Solo owner (`group.isOwner == true`) — admin no-owner ve archive pero no delete. (3) UX invitados silencioso al sync — sin notif, sin banner; el SplitGroup remoto trae `isHiddenForAll=true` y desaparece del listado automáticamente. (4) Gating estricto `|netBalance| > 0.01` en cualquier (memberID × currencyCode) bloquea — cross-currency cubierto por construcción. (5) Defensa en profundidad en service — `softDelete` recalcula balances vía `GroupBalanceService.calculateBalances` independiente de la UI (robusto contra callsites futuros). (6) Tests solo pure-logic — evita flake R8 documentado de `makeTestContext()`. **F1 cambios**: `SplitGroup.isHiddenForAll: Bool = false` (lightweight migration nativa, sin `@Attribute(.unique)`, sin relationships); `CKShareCustomKey.isHiddenForAll`; `GroupService.softDelete(_:)` con fetch interno expenses/shares/members/settlements + `GroupBalanceService.calculateBalances` + throw `.outstandingBalance` si !=0; static `propagateHiddenForAllToShare(zoneID:isHidden:) async`; `GroupServiceError.outstandingBalance` reintroducido (fue eliminado en Subset 3 del épico cuando se removió restricción de leave/remove); filter `!$0.isHiddenForAll` en `GroupsViewModel.activeGroups`/`archivedGroups` (**a nivel VM, NO en service** — preserva semántica de `fetchAllGroups`/`fetchActiveGroups` para `updateCurrentUserDisplayName` línea 535 y `refreshCurrentUserFlags` línea 578 que iteran sobre TODOS los grupos del current user); `TelemetryService.groupSoftDeleted` separado de `groupDeleted` (que sigue reservado al path destructivo deshabilitado, preserva semántica de telemetría). **F2 cambios**: sección `deleteGroupSection` en `GroupSettingsView` gated `if group.isOwner` debajo de `dangerZoneSection` (2 secciones independientes con propio `.thCard` + stroke — NO apiladas, consistente con `leaveGroupSection`). Botón `.disabled(anyMemberHasOutstandingBalance || isDeleting)` + caption inline rojo "Liquida deudas pendientes antes de eliminar" cuando bloqueado. `confirmationDialog` destructive irreversible "¿Eliminar este grupo? · El grupo desaparecerá para todos los miembros. Esta acción es irreversible." → `performSoftDelete` async → `DS.Haptic.warning() + dismiss()`. **Reuso L10n key names huérfanos** del path destructivo deshabilitado (`deleteGroup/deleteGroupConfirm/deleteGroupFinalConfirm/deleteGroupDisabledHint`) reescribiendo los strings con semántica soft-delete via script Python idempotente con regex que preserva el resto del archivo intacto — 48 strings reescritos en 12 locales + es-AR voseo override `Liquidá` (es-ES/es-AR mayoría/en-GB/pt-PT heredan via fallback iOS). **F3 cambios**: `ReconnectMode.deletedForAll` case nuevo + `inviteRouteDecision` recibe `isHiddenForAll: Bool = false` con **branch primero** (orden override `isHiddenForAll > isArchived > memberStatus > onboarding` documentado en docstring); `acceptShareFromURL` (AppBootstrapper) + `userDidAcceptCloudKitShareWith` (YalaAppDelegate) lecturas simétricas `metadata.share[CKShareCustomKey.isHiddenForAll]`; `GroupReconnectView` agrega case `.deletedForAll` en 3 switches (title/subtitle/ctaLabel) con `deletedForAllTitle(groupName)` placeholder `%@` paridad con `archivedTitle`/`alreadyMemberTitle` + branch CTA `.archived || .deletedForAll → onDismiss()`; `ContentView.handleReconnectJoin` defensa-en-profundidad case `(.archived, .deletedForAll): return` — el CTA nunca llega aquí pero el switch debe ser exhaustivo. L10n: 3 keys nuevas `Groups.Reconnect.deletedForAllTitle/Body/Cta` × 12 locales completos via script Python idempotente (marker `// MARK: FU-02-reconnect`). **Race tolerance**: si invitado retap link ANTES de que `propagateHiddenForAllToShare` complete (~3s típico), key ausente → `== 1` es false → fallback safe a modo previo (`standardReconnect`/`alreadyMember`/etc); sync posterior del SplitGroup trae `isHiddenForAll=true` y desaparece del listado al refresh. **Tests pure-logic** 4 nuevos en `AppBootstrapperTests`: `inviteRouteDecision_hiddenForAllOverrides{Archived,ActiveMember,PendingApproval,FreshUser}` (17/17 verdes con suite completa). **Hallazgo durante /review-plan**: filter inicial en `fetchAllGroups`/`fetchActiveGroups` predicates a nivel service cambiaba la semántica de 2 callsites no-UI cross-cutting (`updateCurrentUserDisplayName` + `refreshCurrentUserFlags`) — movido a nivel VM por evitar regresiones silenciosas. R7 documentado: `SplitMember`s en grupos hidden siguen procesando `enqueueSave` triviales sin impacto visible. **Hallazgo durante implementación**: añadir `case deletedForAll` al enum `ReconnectMode` rompía 2 switches no exhaustivos (GroupReconnectView 3 switches + ContentView.handleReconnectJoin) — el enum case se movió de F1 a F3 para mantener atomicidad por commit. Plan: `~/.claude/plans/graceful-stargazing-charm.md`. Pendiente: device QA escenarios B3-23..B3-28 + B21-10 (cross-device Físico-A owner + Físico-B invitado retap link para validar `.deletedForAll` mode + sync silencioso + race CKShare custom key).

- [2026-05-20] **Onboarding informativo del tab Grupos (3 steps) + diferir seed sistema**. Feature nueva pre-deploy de Grupos. Se presenta sheet con `NavigationStack + PanelBackgroundView()` al primer tap del tab Grupos, gateado por `GroupsOnboardingLogic.shouldShow(hasShownOnboarding:onboardingMode:hasPendingGroupDeeplink:)` (AND-gating de 3 blockers). **Step 1 Hero animado** (`GroupsOnboardingDemo`) replica patrón técnico de `YalaAIChatPreview`: enum `Stage` con 4 fases (`.cardInitial` 1s → `.notifVisible` 1.5s spring `.move(.top)` → `.balanceChanged` 1.5s `.contentTransition(.numericText())` "Saldo: S/ 0" → "Debes S/ 25" con color shift gris→`.essentialNeed` amber → `.holding` 1s → crossfade 350ms reset). Loop `Task<Void, Never>` cancelable en `.onDisappear`. VoiceOver activo salta a `.balanceChanged` final sin loop. Reduce Motion solo fades. Componentes mock read-only `OnboardingDemoGroupCard` (con enum `BalanceState .initial/.debt` + balance siempre positivo `abs(value)` — la narrativa "debes" la da el label, no el signo) y `OnboardingDemoNotificationBubble` (estilo iOS push, Yala logo 32pt + cuerpo). Nombres "Sofía"/"Amigos" localizados por locale (Sarah/Friends, Sophie/Freunde, さくら/友達, 苏菲/朋友, etc.) con **placeholders positional** `%1$@/%2$@/%3$@` en `step1DemoNotifTemplate` y 4 placeholders en `step1A11yLabelTemplate` — ja/zh-Hans reorden grupo antes del verbo. **Step 2 Capabilities** 3 cards (`person.3.fill` hotPink Crea / `creditcard.fill` theme.accent Registra / `arrow.left.arrow.right.circle.fill` essentialNeed Liquida) con helper `capabilityCard` replicado literal de YalaAIOnboardingView. **Step 3 Privacy** header gradient Circle 96pt `lock.shield.fill` + 2 bullets (iCloud por miembro + data personal no sincronizada) + **CTA único "Ir a Grupos"** que solo cierra el sheet (NO abre `GroupFormView` — el user queda en empty state que ya tiene CTA "Crear grupo" propio). Toolbar X step 1 (`.close` — NO persiste flag, reaparece) / chevron back steps 2-3 / "Saltar" trailing steps 1-2 SOLO (persiste flag, sin Skip en step 3 que tiene CTA cubriendo). `GroupsOnboardingSheetModifier` simplificado vs YalaAI: **1 solo binding** `isPresented` sin encadenamiento `pendingOpen*`/`showSheet*` porque no abre otro sheet. `onPersistFlag` invocado en `.complete` y `.skip` con **orden invertido — seed antes que flag** para resiliencia (si seed falla, flag no persiste, onboarding reaparece). **Defer seed sistema** del bootstrap eager (paso 6.0 eliminado de `AppBootstrapper:128-131`) a 5 callsites lazy idempotentes confirmados en F0 grep (no 3 como decía el plan): nuevo onboarding `onPersistFlag`, `GroupInviteOnboardingView.performSilentSetup:222`, `OnboardingView:1781`, `SubcategorySelectorSheet:45`, `GroupBridgeSystemEntities.systemSubcategory:243` (defensive re-seed lazy). Idempotencia garantizada por flag `seedSystemGroupCategoriesExecuted` + double-check por nombre en DB. **R-2 contingencia identificada**: si `.contentTransition(.numericText())` glitchea con strings que cambian de longitud (etiqueta "Saldo:" → "Debes" + monto), fallback es separar en 2 `Text` en HStack. Decisión en device QA. **4 events telemetría** `groupsOnboarding{Shown/StepViewed/Completed/Skipped}` con params String. **Flag** `hasShownGroupsOnboarding` per-device (synced=false), patrón análogo a `hasShownYalaAIOnboarding`. **L10n**: 18 keys × 16 locales con script Python idempotente (`/tmp/yala-groups-onboarding-l10n.py`, marker `// MARK: GroupsOnboarding-keys`). Voseo es-AR overrides solo en 6 keys (resto hereda); en-GB/es-419/es-ES/pt-PT heredan por fallback iOS (solo marker como sentinel). **Tests**: 6 pure-logic `GroupsOnboardingLogicTests` cubren todos los blockers + AND-gating combinado + `.completed` mode permite (regresión review). Sin `makeTestContext()` ni UI, evita flake R8. **`/review-plan` cazó 8 problemas críticos pre-implementación**: modifier ambiguo con `pendingOpenCreateGroup` + onChange invertido (refactor → 1 binding sin encadenamiento), placeholders `%@ %@ %@` no positional (zh-Hans/ja necesitan reorder), balance "S/ 0 → -S/ 25" feo cross-locale (refactor → 2 keys label + abs(value)), a11y label con monto en letras vs formateado (escapado over-engineering localizando "cincuenta"/"veinticinco"), CTA opens form vs solo close (decisión user: solo close, queda en empty state), `onPersistFlag` orden seed+flag (invertir para resiliencia), iPad sidebar sin escenario QA específico (añadido GR-OB-13), F0 greps adicionales `OnboardingMode.groupInvite` + `YalaLogo` asset. **Bug fixes durante implementación**: (1) parameters dict de TelemetryService es `[String: String]` no `[String: Int]` → cast `String(rawValue)`. (2) `appPreferences.currencyCode` no existe → `appPreferences.defaultCurrencyCode.rawValue` (CurrencyCode enum). (3) `Color.thPrimaryText` retorna `ThemeColor` ShapeStyle, no `Color` → no se puede asignar a `var amountColor: Color`; refactor a `@ViewBuilder amountText(_:)` con condicional inline. **Aprendizajes**: el patrón `seedSystemGroupCategoriesIfNeeded` ya tiene 4 sitios de defensa (no solo el bootstrap) — eliminar el del bootstrap es seguro porque cualquier path que invoque el bridge tiene red de seguridad lazy. SourceKit cross-file diagnostics ruido masivo durante edits en archivos nuevos (resuelven al compilar — verified pattern documentado). Plan: `~/.claude/plans/witty-napping-ritchie.md`. Pendiente: device QA con 13 escenarios GR-OB-01..GR-OB-13 (incluyendo iPhone 17 Pro + iPad Pro sidebar landscape/portrait, VoiceOver, Reduce Motion, monedas USD/EUR/PEN, fresh install verificando `seedSystemGroupCategoriesExecuted=false` antes del primer tap del tab).

- [2026-05-17] **Panel polish — cierre del sprint con AmountText API simplificada + greeting + FAB glass** (commit `e6998797`). 3 ajustes coordinados tras feedback iterativo del owner sobre las 4 iteraciones previas del sprint (44a2030a/9d3bbc87/54b6f47c/8974106d). **AmountText refactor profundo**: eliminado `enum Size` (6 cases hardcoded en pt: hero 44 / kpi 28 / headline / body / subheadline / caption) + `symbolFontStyle` helper. Nueva firma `font: Font + secondaryFont: Font` explícitos — cada callsite controla tipografía con `Font` nativo SwiftUI (familiar, sin enum custom). Decision rationale: el `Size` enum imponía tamaños hardcoded inventados (44pt hero) que rompían "tamaños estándar como estaban pre-sprint"; el owner quería SOLO la regla "symbol/decimal un nivel menos en tamaño y color" sin que el componente decidiera tamaños propios. Nuevos tokens DS `DS.Typography.heroAmount` (44pt semibold) + `heroAmountSecondary` (20pt regular) reutilizables para heroes destacados, con marker A11Y-DT justificando tamaño fijo (escalado vía `minimumScaleFactor 0.7`). **Greeting Hero Panel**: `headline` (17pt) → `title2` (22pt). El headline competía mal con el KPI 44pt; el owner descartó volver al `title` original (28pt, "competía") y al `subheadline` (15pt, "tímido") — title2 es el intermedio elegido. **FAB glass interactive recuperado** sin perder tap target: eliminé `FABPressStyle` manual (10 LoC, scale 0.92 + opacity 0.85 + spring), `.glassEffect(.regular.interactive().tint(...))` dentro del label restaura animación nativa Liquid Glass de press. Root cause del bug "AI FAB no responde el 100%" descubierto: el `ZStack` envolvente del AI FAB (Image sparkles centrado ~20pt + lock badge condicional con `.offset(14,14)`) tenía áreas vacías dentro del frame `56×56` que SwiftUI NO consideraba tappables aunque el `.glassEffect` las cubriera visualmente. Fix: `.contentShape(Circle())` tras `glassEffect` fuerza hit area al círculo visual completo. El Tx FAB tenía menos flake porque su label es Image directo (no ZStack) — el contentShape unifica el patrón en ambos. El commit previo `54b6f47c` había diagnosticado erróneamente "interactive consume el tap" — la realidad era el tap target ambiguo del ZStack. **Migración masiva**: 88 callsites — heroes Panel + Stats (4 tabs) + Planning monthlyTotal + success screens (TransactionSuccess/InboxApproveSuccess) usan tokens DS hero; 39 `Text(currency(...))` sueltos previamente sin migrar (Reports CashFlow + Groups + Settings + Planning + Transactions/SaveAsFavorite) migrados a AmountText con jerarquía symbol/decimal subordinada; resto vuelve a fonts estándar pre-sprint (DS.Typography.headline/subheadline/body/caption según el contexto). Migración técnica: sed inteligente para los 88 callsites + agent Explore en paralelo para limpiar los modifiers externos (.font/.foregroundStyle/.lineLimit/.minimumScaleFactor/.monospacedDigit) que duplicaban o entraban en conflicto con los internos de AmountText — convertidos a params del init. **Cleanup post-/simplify** (3 agents review en paralelo): A11Y-DT marker añadido en heroAmount tokens + ScheduledPaymentsWidget 28pt KPI inline (único callsite mid-tier escapando los tokens, intencional); `tint: .color(Color.primary)` omitido en NeedTrendWidget tooltip (default `.primary` cubre); `attributedAmount` defaults alineados a Font literals (`.subheadline`/`.caption`) para evitar warning main actor-isolated; `tint: .color(.thPrimaryText/Secondary)` (5 callsites — ThemeColor es ShapeStyle no Color, incompatible con enum Tint) cambiados a `.primary`/`.secondary` hierarchical. **Skipped del simplify** consciente con razón documentada: (a) preset factory enum (`AmountText.hero(...)`, `.row(...)`, etc.) que el reuse-agent y quality-agent ambos recomendaron para ~47 callsites con pares ultra-repetidos — re-introduciría una capa de abstracción que JUSTO eliminamos del refactor, violaría directiva owner "sin complicar"; (b) tokens monospaced para 19 callsites CashFlow — beneficio bajo, no era el problema reportado; (c) refactor de TrendsTabView VM para exponer Double en lugar de String pre-formateado en 3 callsites — fuera de scope; (d) format args en una línea (sed-style post-migración) vs split — automatable con swiftformat en otra pasada. **Tests**: 10/10 `AmountTextLogicTests.splitFormatted_*` siguen verdes; eliminados 3 tests de `symbolFontStyle_*` obsoletos (función removida). Build SUCCEEDED end-to-end. 47 archivos / +346/-317 LOC. **Aprendizajes**: (a) los 4 commits previos del sprint Panel polish iteraron sobre el FAB sin atacar el root cause real (ZStack hit area) — el `FABPressStyle` manual era un workaround del síntoma, no del problema; el `.contentShape(Circle())` + `interactive()` es la solución mínima que recupera ambos (animación nativa + tap confiable). (b) Cuando un refactor introduce un enum de sizes hardcoded, hay riesgo alto de divergir del lenguaje de tamaños existente (DS.Typography ya tiene una jerarquía); mejor que el componente reciba el font externamente. (c) El sed greedy `Text\(appPreferences\.currency\((.*)\)\)` → `AmountText(value: \1)` funciona bien para los 39 simples — los compuestos con interpolación quedan filtrados naturalmente porque el regex no matchea `Text("...\(currency(...))...")`. (d) El review escéptico con 3 agents en paralelo cazó 7 issues accionables pero también propuso 4 false-positives que requirieron juicio (preset factory, tokens monospaced, etc.) — el filtro "no complicar" del owner fue la guía. **Pendientes**: (a) device QA del FAB sistemático en simulador iPhone 17 Pro (owner ya validó cualitativamente "ya funciona bien"); (b) 13 callsites compuestos sin migrar (`Text` con interpolación o `L10n.X.format(currency(...))`) — la regla symbol/decimal no aplica a strings concatenados; `AmountText.attributedAmount` cubre el caso si se nota la inconsistencia; (c) `swiftformat` opcional para uniformar split args; (d) considerar promover el 28pt/14pt de ScheduledPaymentsWidget a un token DS `kpiAmount` si aparece un 2do usuario mid-tier; (e) bump build TestFlight si owner quiere shipear este polish con los heroes ya merged (último bump fue build 9 en `bc860b68`).

- [2026-05-13] **Stats polish — Tab Tendencias panel-aligned (versión final post-iteraciones)**. Tercera tab cerrada del épico `stats-polish-panel-alignment` tras Records (`f3ea9ccd`) e Insights (`c1d2154c`). **Estructura final**: Hero (TrendsPeriodMenu standalone centrado + `largeTitle` del metric activo period-specific via `heroKPIValue(for: summary)` = `summary.netBalance/totalIncome/totalExpense` — **NO** running balance global como retornaba `currentKPIValue` — fix del bug "no hace caso al período" + `.contentTransition(.numericText())` mitigación salto visual cuando metric cambia; chips income/expense con `VariationChip` inline; subtítulo motivacional NUEVO específico de tendencias `heroVariationSubtitle(for:)` con 4 ramas según `summary.balanceVariation`: ONSET (`nil` → "Tu primer período con Yala"), STABLE (`< 5%` → "Tu balance se mantiene estable"), VarUp/VarDown con `previousPeriodLabel` stripped del "vs " hardcoded por `PreviousPeriodHelper.formatComparisonText` — strip helper `strippedComparisonLabel(_:)` reusado por `findingText`). → `filterBarPanel` (TX-3 panelStyle:true) → `chartsLayout` wrapper preserva iPad wide side-by-side (HStack `isWide && hasComparison`) o VStack stacked → **Trend Chart Section** (title "Tendencias" + InfoHint + metric selector Circle 32pt **AFUERA** del card; header **interno** con `trendsViewModel.selectedMetric.displayName` "Saldo/Ingresos/Gastos" `subheadlineEmphasized` + `currentKPIValue .headline` + "vs S/X" inline + VariationChip `.medium` top-right + comparisonPeriodText caption — patrón TrendsCarouselWidget de PanelView; chart 170pt) → **Comparativa Section** (nueva, separada del antiguo TabView carousel; title "Comparativa" + M/A selector AFUERA; header interno simétrico con `periodComparisonTitle` "vs período/año anterior" promovido a `subheadlineEmphasized .thPrimaryText`; chart 170pt fix bug `PeriodComparisonChartView` hardcoded `frame(height:220)` → `frame(height: chartHeight)` para que respete prop) → `cashFlowWidget` (title + 3-opciones selector Circle AFUERA, widget interno preserva `.solidCard(radius: widgetRadius)` — NO se envuelve doble) → `weekdayChartSection` (title afuera "Promedio por día de la semana" + card con header KPI `weeklyAverage + perWeekSuffix` izq + día pico `topWeekday.weekdayLongName.capitalized` derecho con monto promedio — estilo Panel-Small; chart 140pt) → **Trend Insight Card protagonista AL FINAL** (movido por decisión owner — antes estaba arriba post-filterBar pero "ocupaba demasiado espacio hasta ver las gráficas core"). Gate `txCount >= 5` via `insightsViewModel.insightData?.periodSummary.transactionCount`. **Free**: rule-based via `TrendInsightLogic.finding(metric:currentTotal:previousTotal:)` con 4 cases (ONSET / VARIATION up/down / STABLE) → 9 strings L10n según metric (varUp/DownExpense, varUp/DownIncome, varUp/DownBalance, stableExpense/Income/Balance, onset) + CTA chip "Refinar con IA · Pro" → UpgradePromptSheet. **Pro**: reusa flow `InsightsViewModel.triggerAIGeneration()` con `aiInsights.heroText` placeholder (V1 limitation documentada — análisis Insights general, no específico tendencias). Header `proInsightHeader` shared (sparkles + "Análisis del período") entre pre-AI/post-AI/loading/error. Footer balanceado post-AI: tag identitario izq "✨ Análisis con IA" + chip "↻ Regenerar" der (capsule accent.opacity(0.12)). **TrendInsightLogic pure-logic helper NUEVO** `Yala/App/Logic/TrendInsightLogic.swift` (~50 LOC, sin SwiftUI/L10n imports — localización vive en callsite `findingText`). **Tests**: 7 Swift Testing en `YalaTests/TrendInsightLogicTests.swift` (onset / stable / zeroPrev / 5% threshold / variations 10%/15% / negative balance). **Decisión protagonismo al final** validó la propuesta original del owner contra los 3 caminos discutidos (A ship arriba + QA, B mover al final con V1 genérico, C eliminar y V2 completo) — eligió B mientras V2 espera. **V2 ticket creado**: `Backlog/trends-insight-card-v2-bullets.md` (resumen narrativo 4 bullets uno por gráfica con TrendsAIService dedicado + `historicalTotals[N]` data layer + Rule 2 TREND SOSTENIDO skipped V1). **Brand voice §2.4 aplicado**: "Cuidado" / "Cuidado con la marcha" → "Ánimo, paso a paso" / "Ajustemos juntos" (propositivo, plural inclusivo, nunca regañar). **Eliminaciones**: `trendChartsCarousel` TabView + `recentRecordsSection` + `emptyRecordsState` + param `onNavigateToRecords` (callsite DetailContainerView limpio); struct `CompactRecordRow` preservada por uso en CategoriesTabView. **Cleanup `/simplify` post-impl** (3 agents en paralelo): orphan L10n keys `motivationalOne/motivationalMany` (0 callsites — reemplazadas por `hero*`) eliminadas + 23 strings × 12 locales; fusión `trendEmptyState ≡ comparisonEmptyState` → `chartEmptyState`; helper `strippedComparisonLabel(_:)` dedup 2 callsites; dedup `convertMetricToTrendType` ≡ `mapMetricToTrendType` → mantengo `map`; empty `// MARK: - Chip Text Helpers` eliminado; comments ticket-narrating (F4-F8, TX-*, R-T-*, B6) limpiados manteniendo WHY donde aplica. **Cross-file deuda técnica documentada** post-épico: `markdownAttributed` + `aiLoadingPlaceholder` + `aiErrorCard` shared con InsightsTabView (extracción a `Yala/App/Views/Shared/` cuando cierre el épico — rompe atomicidad ahora); `PreviousPeriodHelper.formatComparisonText` aloca DateFormatter por llamada (afecta Panel + Stats, fix en sprint dedicado). **L10n total**: 20 keys × 16 locales (~272 strings net post-cleanup) — `stats.trends.{comparisonTitle, insightTitleFree %@, insightTitlePro, refineWithAI, generateAI, regenerate, aiFootnote, heroOnset, heroStable, heroVarUp %d %@, heroVarDown %d %@}` + sub-enum `Insight.{onset %@, varUp/Down Expense/Income/Balance %d %@, stable Expense/Income/Balance}` (9 keys). Voseo es-AR overrides solo `heroVarUp` ("Seguí así") + `varDownExpense` ("Mantené el ritmo"). 4 locales minimal (es-AR/es-ES/en-GB/pt-PT) heredan via fallback. **Tooltips fine-tune (cross-file Panel + Stats)**: `TrendChartView` anchor 15% → 25% → **35%** + `todayMarker` Hoy label con PointMark invisible Y@12% del top (antes annotation directa del RuleMark sin Y específica); `PeriodComparisonChartView` tooltip anchor `upperBound` → `upperBound - 25% range`; `CashFlowWidget` `position(y: plotFrame.midY)` (antes `y: minY + 20` con offset `-50` → flotaba off-canvas arriba). **Decisiones owner consolidadas iterativamente**: (a) metric selector hero → secciones (después devuelto a fuera del card al lado del title), (b) largeTitle hero refleja `selectedMetric` (no solo balance), (c) NO pill Detalle/Observaciones (Trend Insight Card único contenedor inteligencia), (d) Circle TX-7 V1 mantenido, (e) subtítulo motivacional unidad "movimientos" → REEMPLAZADO por balance variation específico de tendencias, (f) card Tendencias header interno + KPI estilo PanelView, (g) altura gráficas 220 → 170 + weekday 200 → 140, (h) Insight Card movido al FINAL como cierre narrativo (no hero), (i) brand voice §2.4 — eliminar "Cuidado" en todos los locales. **Tests**: 7 nuevos TrendInsightLogicTests + 12 reusados (RecordsMotivationalLogic + FilterControlBarLogic) — todos verdes. **20 archivos** modificados (+1003/-593 LOC). **Aprendizajes**: feedback iterativo intenso del owner con device QA visual cada 2-3 cambios — 8 iteraciones distintas (hero header interno → afuera, metric selector adentro → afuera al lado title, KPI sizes unificadas, hover tooltips fine-tune tres rondas, subtitle "movimientos" → balance variation específico, weekday "desnuda" → estilo Panel-Small, insight al final, brand voice). El bug "balance hero no hace caso al período" se cazó tarde (varias iteraciones) porque `currentKPIValue` reusaba running balance final del chart (global) mientras los chips income/expense sí eran period-specific — solo evidente cuando el owner cambió de período y notó que el largeTitle no se movía. `PeriodComparisonChartView.chartHeight` hardcodeado `220` era bug latente pre-existente que ignoraba el prop — fix de 1 línea con beneficio cross-feature. Pendiente: device QA con 33 escenarios T-01..T-33 + iPad simulator T-20 + commit docs separado para Obsidian ticket marked done + learnings G1 + QA-SCENARIOS.md. Siguiente: polish tab Distribución (reusará patrón `chartEmptyState` + `strippedComparisonLabel` + headers internos KPI estilo Panel).

- [2026-05-13] **Stats polish — Tab Insights panel-aligned (commit `c1d2154c`, versión final post 4 iteraciones)**. Segunda tab cerrada del épico `stats-polish-panel-alignment` tras Records (`f3ea9ccd`). **4 iteraciones G/H/I/J** con feedback inmediato del owner en cada paso. **Estructura final**: Hero (TrendsPeriodMenu centrado standalone + balance largeTitle + chips income/expense con `VariationChip` inline + subtítulo motivacional reusando `RecordsMotivationalLogic`) → `filterBarPanel` (TX-3 gated) → **Salud financiera** (header externo via `YalaSectionHeader(L10n.Panel.Health.cardTitle)` + `FinancialScoreView(hideHeader: true)` — flag retrocompat añadido para que la card pierda título/subtítulo internos y el callsite los dibuje fuera, consistente con resto de section headers; Panel sigue `hideHeader: false` default) → `proHeroSection` Pro (card panelCard tappable con sparkles + título + subtítulo + chevron, preserva `coachMarkAnchor("proAiSummary")`) o `aiInsightsTeaser` Free → `contentModePill` (Detalle/Observaciones, solo `>=5 tx`, labels reusan `L10n.Stats.Insights.modeDetail` + `L10n.Insights.intelligentInsights` — 0 keys nuevas para labels, default `.detail`, reset defensivo a `.detail` cuando `<5 tx`) → branch `detailModeContent` (header HStack `[Text("Tus cifras") .title + Text(period.displayName) .caption .secondary]` + Spacer + VStack right-aligned `[ComparisonModeSelector + Text(previousPeriodLabel) .captionSmall .tertiary]` que balancea las 2 líneas a la izquierda con las 2 a la derecha → `dailyAverageCard` full-width (icon 32pt accent + VStack largeTitle + Group { VariationChip + `dailyAvgIn(period)` | placeholder `Text(" ").opacity(0)` reserva altura constante evita salto vertical } + Spacer + `.panelCard()`) → `LazyVGrid(.flexible() × 2)` con 4 `QuickStatCell` (top cat / top sub / mayor gasto / día caro promedio) → `subscriptionsCard` condicional `subscriptionsTotal > 0` → `commitmentsSection` colapsable + `needSection` colapsable enriquecida con 3 `needBucketRow` (Circle 32pt icon `house.fill`/`star.fill`/`sparkles` en colores `Color.essentialNeed`/`priorityNeedNew`/`optionalNeed` + label + `≈ S/X/día` promedio + monto absoluto + `%`) + tip rule-based contextual reusa `L10n.Insights.ruleOptionalHigh` cuando `optionalPercent > 40`) o branch `observationsModeContent` (proObservations cards Pro / `freeObservationsSection` Free con `prefix(2)` reglas + `lockedFunFact`). **Patrón `+/-` glass del Panel** (`Image(systemName: isExpanded ? "minus" : "plus").glassEffect(.regular.interactive(), in: Circle())` con `DS.Panel.headerAccessorySize`) para Compromisos + Necesidad — helper privado `collapsibleHeader(title:subtitle:isExpanded:action:)` evita duplicación. **Decisiones clave consolidadas en iter J final** tras 3 iteraciones descartadas: (a) **título genérico "Tus cifras" + subtitle dinámico** `period.displayName` resuelve "no hace caso al período" (anterior "Tu mes en cifras" no aplicaba a `.thisYear` / `.last7Days` / `.custom`). Aplicable cross-tab para próximos polish (Trends/Categories). (b) **M/A migra del hero a la sección Detalle** — el hero queda solo con periodMenu standalone; M/A circles + "vs anterior" viven en el header de Detalle donde controlan los VariationChip del bento + chips del hero. (c) **Free flow simplificado** sin card rule-based hero inline (eliminado en iter J) — usuario Free ve solo aiInsightsTeaser debajo de Score; las 2 reglas en Observaciones cubren info que daba el insight hero. (d) **`InsightsCalculator` Rules 5/6 universales NUEVAS** (sin tonos, "menos personalizado"): Rule 5 Daily average snapshot dispara siempre que `quickStats.dailyAverage > 0` con `YalaFormatterStatic.currency(value:currencyCode:)` off-MainActor; Rule 6 Savings ratio dispara `if totalIncome > 0` con positive (`netBalance > 0` → "Ahorras un X%") o negative (`netBalance < 0` → "Tus gastos superan tus ingresos"). Garantizan 1-2 observaciones en `.allTime`/`.custom` donde Rule 2 (expense trend) no dispara por falta de `previousPeriod`. **`QuickStatCell` migra a `.panelCard(small: true)`** (TX-4). **`FilterControlBar` pasa `panelStyle: true, inlinePeriodSelector: false`** (TX-3). **VStack body root spacing `.md`** (TX-6). **9 keys L10n × 16 locales** (~144 strings) con voseo es-AR en `motivationalMany`/`needDailyFormat`/`ruleDailyAverage`/`ruleSavingsPositive` (`ruleSavingsNegative` invariable): `stats.insights.{motivationalOne, motivationalMany %d, detailHeader ("Tus cifras"), aiCardTitle, aiCardSubtitle, dailyAvgIn %@, needDailyFormat %@, modeDetail}` + `insights.{ruleDailyAverage %@, ruleSavingsPositive %d, ruleSavingsNegative}`. Aplicado vía 3 scripts Python idempotentes con anchors (`stats.records.motivationalMany %d` + `stats.insights.dailyAvgIn %@` + `insights.tipOptionalHigh`). 4 locales con override parcial (es-AR/es-ES/en-GB/pt-PT) heredan via fallback iOS para keys sin anchor; voseo es-AR añadido manual para 2 reglas universales que usan verbos 2da persona. **Cleanup**: `monthHighlights` key (string "Tu mes en cifras") eliminada del L10n.swift y 16 .strings tras rename a `detailHeader` ("Tus cifras"). **Tests**: `RecordsMotivationalLogicTests` (5) + `FilterControlBarLogicTests` (7) reusados sin modificar — siguen verdes. 0 tests nuevos para `InsightsCalculator` Rules 5/6 (consistente con patrón existing — Rules 1-4 tampoco testeadas, no existe `InsightsCalculatorTests.swift`). Build verde end-to-end en 4 iteraciones. **21 archivos** modificados (+859/-303 LOC). **Aprendizajes**: 4 iteraciones de diseño con feedback inmediato del owner. Iter G (pill en header del bento) descartado por espacio. Iter H (title "Tu mes en cifras" + M/A en Detalle) descartado por título no genérico. Iter I (InsightCard inline + title fijo) descartado por insight duplicado con observations. Iter J final consolida: pill standalone + title "Tus cifras" + subtitle period + sin insight inline Free + reglas universales calculator. `/simplify` (3 agents en paralelo) confirmó código limpio post iter J — candidatos a refactor (`GenericSegmentedPill` shared con `TransactionTypeSegmentedView`, `CollapsibleSectionHeader` shared con `PanelPanoramaSection`) requerían tocar archivos fuera del scope del commit Insights single-commit → skipped por atomicidad. **Riesgos pendientes QA**: R-I-1 VariationChip inline overflow Dynamic Type XXL (mitigado `lineLimit(1) + minimumScaleFactor(0.7)`), R-I-2 iPad wide (`LazyVGrid.flexible() × 2` fixed), R-I-4 coach mark `proAiSummary` sobre nuevo card ~80pt vs ~50pt botón previo (sin offsets hardcoded en `ProTourSteps.insightsSteps` — solo id/title/message), Risk-D doble-padding al migrar `solidCard → panelCard` (mitigado eliminando `.padding(.lg)/.padding(.md)` previos por callsite), Risk-E salto vertical `dailyAverageCard` cuando variation alterna nil ↔ valor (mitigado con placeholder `Text(" ").opacity(0)`). **24 escenarios QA** I-01..I-24 documentados (matriz Free/Pro × Detalle/Observaciones × <5/≥5/0 tx × showVariations × Dynamic Type XXL × VoiceOver × Reduce Motion). Pendiente: device QA simulador iPhone 17 Pro + Pro Tour Phase 3 manual check. Siguiente: polish tab Tendencias (reusará TX-2 simplificado + TX-3 panelStyle + TX-14 empty preserva hero + patrón genérico "Tus cifras"/period del Insights).

- [2026-05-13] **Stats polish — Tab Registros (Stats) + RecordsStandaloneView panel-aligned (commit `f3ea9ccd`, versión final post-device QA)**. Primera tab cerrada del épico `stats-polish-panel-alignment`. **Decisiones revisadas tras device QA** que cancelan/refinan el plan original del ticket: (1) **TX-1 CANCELADO** — el scroll-driven inline title se descartó tras feedback del owner ("el title como estaba antes le daba más jerarquía y limpieza"). Se mantiene `.navigationTitle("Estadísticas") + .large` original en `StatisticsView`, y `.navigationTitle("Registros")` original en `RecordsStandaloneView`. NO se implementa `showInlineTitle`, `onScrollGeometryChange`, `inlineTitleVisible` bindings ni el ViewModifier `scrollDrivenVisibility` (extraído y luego eliminado por YAGNI tras revert). (2) **TX-2 SIMPLIFICADO** — el hero pierde chip identidad ("Tus registros"), icono `list.bullet.rectangle`, label "Saldo · {Período}" y top row entero. Resultado final: `TrendsPeriodMenu` **centrado arriba** standalone (sin label al lado), balance `largeTitle` debajo (gating original `!expensesOnlyMode && filteredCount > 0`), chips income/expense tappables, subtítulo motivacional. Empty state preserva el hero — period menu siempre accesible. (3) **TX-3 REFINADO** — flag retrocompat `panelStyle: Bool = false` + `inlinePeriodSelector: Bool = true` en `FilterControlBar`. Cuando `panelStyle=true`: render dentro de `PanelSection(compact: true)` + `GlassEffectContainer` + `glassEffectID` per chip, **sin** `.contentMargins(.horizontal, .md, ...)` (los chips arrancan alineados al borde izquierdo del PanelSection, mismo origin que el título "Filtros aplicados"). Convenience init `where TrailingContent == EmptyView` actualizado con los 2 nuevos params. (4) **TX-6 REVISADO** — spacing root del VStack body `.lg` (16pt) → **`.md` (12pt)** (no `.lg` como decía originalmente). Menos aire entre hero/filterBar/lista. (5) **TX-13 NUEVO** — `PanelSection` con flag `compact: Bool = false`: cuando `true`, spacing interno `header→content` y `content→footer` bajan de `.lg` a `.sm`, y el font del title baja de `DS.Typography.title` (Font.title2.semibold) a `DS.Typography.subheadlineEmphasized` (Font.subheadline.semibold). Default false preserva 100% el comportamiento del Panel original. Convenience init `where Footer == EmptyView` actualizado. (6) **TX-14 NUEVO** — empty state preserva el hero (cazado en review escéptico pre-implementación). El `heroSummary` siempre se monta; solo el balance `largeTitle` se oculta cuando `filteredCount == 0`. Chips income/expense visibles siempre. El `filterBarPanel` también se monta si `activeFilterCount > 0` (permite quitar chips uno a uno aún en empty state). **Pure-logic helpers** sin dependencias L10n (tests Swift Testing sin context, sin singletons): `RecordsMotivationalLogic.subtitle(forCount:) -> RecordsHeroSubtitle?` con 5 tests (count 0/1/2/N/negativo) + `FilterControlBarLogic.shouldRenderPanelSection/shouldShowInlinePeriodSelector` con 7 tests (legacy×3 / panel×4). 12 tests verdes, sin regresión en 48 tests preexistentes de Records. **L10n recortada** vs plan original — solo 2 keys × 16 locales = 32 strings (eliminadas `heroChip` y `balanceLabel` por TX-1/TX-2 revisados): `stats.records.motivationalOne` ("1 registro · Buen comienzo") + `stats.records.motivationalMany %d` ("%d registros · Sigue así" / "Seguí así" en es-AR). Traducciones reales por idioma (Sie de, vouvoiement fr, です/ます ja, 你 zh-Hans, tu/você split pt-PT/pt-BR, en/en-GB). Script Python idempotente con marker. **Cleanup post-épico documentado inline** en `FilterControlBar.swift` (eliminar `panelStyle`/`inlinePeriodSelector` flags + branch legacy + convenience init con esos 2 params + helper `FilterControlBarLogic` + tests cuando las 3 tabs restantes migren) y en `PanelSection.swift` (el flag `compact` queda como API permanente, no requiere cleanup — útil para futuras secciones secundarias). **Tickets de polish hermanos actualizados** en el vault: `inbox-polish-panel-alignment.md`, `planning-polish-panel-alignment.md`, `reports-polish-panel-alignment.md`, `transactions-forms-polish-panel-alignment.md` reciben las revisiones TX-1 (cancelado) + TX-6 (a .md) + TX-13/TX-14 (nuevos). Cada uno mantiene sus particularidades (Inbox/Reports preservan chip identidad en su mini-hero porque sirve para señalizar contexto sin tabs visibles arriba; Planning/Forms heredan sin chip). **Stats ticket** actualizado con secciones Insights/Trends/Distribución reflejando heroes simplificados + recortes L10n. **18 escenarios QA** R-01..R-18 documentados (R-17 paridad StandaloneView + R-18 empty state UX nuevos). **28 archivos** modificados (+599/-234 LOC). **Aprendizajes**: 5 iteraciones de diseño con device QA antes de llegar al patrón final — owner descartó (a) chip identidad redundante, (b) scroll-driven title fragmentado por tab, (c) period selector en toolbar trailing (descentrado por iOS asymmetric layout), (d) titleRow custom sticky vs scrolleable, (e) `.principal` placement no centrado por items trailing. La iteración final ("`.navigationTitle/.large` original + period selector inline en hero") preservó la jerarquía visual deseada con cambios mínimos al chrome existente. `/review-plan` cazó 4 problemas pre-implementación original (regresión UX empty state, convenience init sin nuevos params, enum con L10n dependency, ViewModifier reusable). `/simplify` (3 agents en paralelo) iterativo cazó: (a) `onScrollGeometryChange` siempre montado, (b) ViewModifier `scrollDrivenVisibility` reusable (que luego se eliminó por YAGNI tras revert), (c) comments narrando refactor. Pendiente: device QA con 18 escenarios R-* en simulador iPhone 17 Pro (Dynamic Type XXL, VoiceOver `accessibilityElement(.contain)+.isHeader`, Reduce Motion). Siguiente: polish tab Insights (reusará `panelStyle` flag + `PanelSection(compact:)` + `RecordsMotivationalLogic` patrón + `FilterControlBarLogic`).

 Primera tab del épico `stats-polish-panel-alignment`. Monta la **infra transversal TX-1/TX-2/TX-3** que reusarán las próximas 3 tabs (Insights, Tendencias, Distribución). **TX-1 title navbar collapsable scroll-driven** vive como `@State showInlineTitle: Bool` en `DetailContainerView` + `RecordsStandaloneView`, con `Binding<Bool>?` opcional propagado a `RecordsTabView` (zero-cost si nil). Histéresis defensiva: `showAt=4pt` (subido desde el `1pt` original del spec por riesgo R-B oscilación con bounce) + `hideAt=0pt`. Reset al cambiar tab. Durante la transición, Insights/Trends/Categories muestran su `tab.title` estático inline; cuando migren cada una reportará su scroll y el computed `currentNavigationTitle` se reduce a `showInlineTitle ? selectedTab.title : ""`. **TX-2 hero edge-to-edge** reemplaza `summaryRow`: chip identidad `list.bullet.rectangle` + "Tus registros" + `TrendsPeriodMenu` inline; label `Saldo · {período}` con `·` separator neutro cross-locale; balance `largeTitle` con `minimumScaleFactor 0.7` + `lineLimit 1`; chips income/expense tappables (preserva la interacción del summaryRow original); subtítulo motivacional. **Decisión empty state crítica** (cazada en review escéptico): el hero **siempre se monta** incluso con `groupedRecords.isEmpty` — esto preserva acceso al period menu (regresión UX si se ocultaba, ya que el `controlBar` original siempre era visible). Solo el balance `largeTitle` + label se ocultan cuando no hay registros. **TX-3 FilterControlBar refactor con flag retrocompat** `panelStyle: Bool = false` + `inlinePeriodSelector: Bool = true`: cuando `panelStyle=true` renderiza chips dentro de `PanelSection(title: L10n.Panel.FilterBar.sectionTitle)` + `GlassEffectContainer` (idéntico a `PanelFilterControlBar`). Convenience init `where TrailingContent == EmptyView` actualizado con los 2 nuevos params (default retrocompat). 4 callsites legacy (Insights/Trends/Categories/FinancialReport) siguen funcionando idéntico. **Polish replicado tal cual en RecordsStandaloneView** (visible cuando Records se navega desde "Más") — comparte `RecordsTabView` como child, hereda automáticamente; solo cambia el title del container. **ViewModifier `.scrollDrivenVisibility(_:showAt:hideAt:)`** extraído al final del flujo (review `/simplify`) en `ViewModifiers.swift` — encapsula `onScrollGeometryChange + threshold + withAnimation easing` y cuando el binding es nil NO monta el listener (perf real: evita closure cada frame en callsites sin title scroll-driven). **Pure-logic helpers** sin dependencias L10n: `RecordsMotivationalLogic.subtitle(forCount:) -> RecordsHeroSubtitle?` (5 tests Swift Testing) + `FilterControlBarLogic.shouldRenderPanelSection/shouldShowInlinePeriodSelector` (7 tests). 12 tests verdes, sin regresión en 48 tests preexistentes de Records. **L10n**: 4 keys × 16 locales con traducciones reales (voseo es-AR "Seguí así", Sie de, vouvoiement fr, です/ます ja, 你 zh-Hans, tu/você split pt-PT/pt-BR, en/en-GB). 64 strings vía script Python idempotente. **Cleanup post-épico** documentado inline: cuando las 3 tabs restantes migren, eliminar `panelStyle`/`inlinePeriodSelector`, branch legacy, convenience init con esos 2 params, helper `FilterControlBarLogic` + tests, y reducir el switch de `currentNavigationTitle` a 1 línea. **Decisiones del owner** consolidadas: approach con flag retrocompat (no componente separado) + cleanup obligatorio post-épico; threshold defensivo `showAt=4pt`; tests para todo lo útil sin escatimar; polish idéntico a RecordsStandaloneView. **R-J/R-K identificados en review escéptico**: glass IDs estables (mapping documentado por chip type — strings inline como `chip.account`, `chip.tag.{tagID.hashValue}`, `chip.currency.{rawValue}`; coherente con `PanelFilterControlBar`), `contentOffset.y` con `safeAreaInset(.top)` de chips de tabs sticky requiere QA explícito en ambos contextos (tab + StandaloneView). **18 escenarios QA** R-01..R-18 documentados en ticket Backlog (R-17 paridad StandaloneView + R-18 empty state UX nuevos). **27 archivos** modificados (+754/-230 LOC). **Aprendizajes**: `/review-plan` cazó 4 problemas pre-implementación (regresión UX empty state ocultando period menu; convenience init sin los nuevos params hubiera roto F3.5; enum con computed L10n hubiera mezclado layers; ViewModifier extraído ahora evita duplicación con 5 callers futuros). `/simplify` (3 agents en paralelo) cazó el critical perf de `onScrollGeometryChange` siempre montado + extracción del modifier + comments narrando refactor. Pendiente: device QA con 18 escenarios R-* en simulador iPhone 17 Pro (Dynamic Type XXL, VoiceOver `accessibilityElement(.contain)+.isHeader`, Reduce Motion). Siguiente: polish tab Insights (reusará TX-1/TX-2/TX-3 + `.scrollDrivenVisibility`).

- [2026-05-11] **Cards `solidCard`/`selectableCard` glass elegante — paywall + onboarding (commit `5fda2d6f`)**. Refactor visual + extracción de modifier shared. Nuevo `selectableCard(isSelected:radius:activeColor:inactiveColor:)` en `ViewModifiers.swift` centraliza el patrón "card con stroke condicional 1pt/2pt" duplicado en 3 vistas. **SubscriptionView + ProTrialOfferSheet (paywalls)**: featuresSection → `.solidCard(radius: DS.Radius.xl)`; planCard/currentPlanCard → `.selectableCard()`. Esquinas `.continuous` (squircle Apple). Strokes theme-aware (`theme.accent` / `theme.cardBorder`) — antes hardcoded `Color.brandPrimary` / `Color.primary.opacity(0.05)`. Radio button del plan selector: `Color.brandPrimary` → `theme.accent`. Foregrounds `.primary`/`.secondary` → `.thPrimaryText`/`.thSecondaryText` (restore button, manageInAppStore, retry/maybeLater). Links legal footer con `.tint(theme.accent)` (antes azul iOS default). Icon backgrounds del `featureRow` con `style: .continuous`. **YalaAIOnboardingView.optionCard** y **OnboardingView.binaryCard** → `.selectableCard()` (binaryCard preserva `styleAccentColor`/`cardStroke` bifurcado por `backgroundStyle` heroFlow/themedPanel vía params opcionales `activeColor`/`inactiveColor`). **Decisión user**: `theme.accent` en lugar de `Color.brandPrimary` fija — en tema default electricIndigo coincide visualmente, en los 5 temas Pro el ring del plan toma el accent del tema. Gradients de marca (`DS.Gradients.subscription` savingsBadge + gradient hero indigo) NO se temifican (identitarios). **Out of scope deliberado**: Hero hardcoded `frame(height: 60)`/`frame(height: 320)` en SubscriptionView — design constants locales sin DS tokens equivalentes; refactor a `.safeAreaInset` mejor patrón pero con riesgo visual fuera del scope solidCard. Token `.thTertiaryText` para los 132 callsites de `.foregroundStyle(.tertiary)` — sprint DS dedicado (decidir color × 9 temas + migración masiva); `.tertiary` funciona correctamente en backgrounds gris estándar sin bug visual real. **Aprendizajes**: investigación previa (Explore agent) mapeó tokens DS (`solidCard`/`panelCard`/`SolidCardModifier`, `theme.cardBorder`, `theme.accent`), patrones de cards seleccionables en 5 vistas, y confirmó que apilar overlay extra sobre `.solidCard()` daría double-border indeseado (1pt base + 2pt accent overlap) → patrón híbrido manual para `planCard` antes del modifier shared. `/simplify` (3 agentes review en paralelo) cazó 4 fixes accionables (foregrounds `.primary` → `.thPrimaryText` x2 + `.tint(theme.accent)` legal links + `RoundedRectangle` icon background con `style: .continuous`) + identificó la duplicación del patrón "card con stroke condicional" en 4 vistas como candidato a abstracción shared. **Net**: 5 archivos, +64/−58 LOC + 1 abstracción shared para 4 vistas. **Pendiente**: device QA visual paywall (SubscriptionView + ProTrialOfferSheet) en tema default + 1-2 temas Pro (cyan/glass) verificando ring del plan seleccionado, links legal con tint correcto, foregrounds theme-aware.

- [2026-05-11] **Onboarding restyle iter5 — padding horizontal consistente en titles (commit `c490c63c`)**. Sprint trivial single-commit que añade `.padding(.horizontal, DS.Spacing.xl)` a los titles de los 7 steps que faltaban (Step 1 `welcomeTitle`, Step 2 `purposeTitle`, Step 3 `accountsTitle`, Step 4 `accountTypeTitle`, Step 5 `currencyNameTitle*` ternary, Step 6 `balanceTitle`, Step 7 `categoriesTitle`). Step 8 `confirmationMotivation` ya lo tenía desde iter1. **Causa raíz**: device QA reveló que el title de Step 4 ("Te ayudamos a crear tu primera cuenta") llegaba muy justo a ambos bordes mientras el subtitle tenía padding `.xl`. Inconsistencia visual heredada del flow original — los subtitles tenían padding pero los titles no. Iter5 alinea los 7 titles al mismo padding del subtitle para respiración consistente. **Edge case confirmado iter4**: el bug "sheet en 0" en variant `.creditCard` mode "detailed" (line+available) NO está cubierto por el prefill defensivo de iter4 (que solo prepuebla `directSpendingText`). Owner confirmó que es edge case raro y queda como está. **Build verde + 24/24 tests existentes verdes**. Sin tests nuevos. **L10n**: 0 keys. **Onboarding restyle cerrado tras iter5** — owner confirmó "quedaría ya el onboarding" tras este fix. 29 escenarios QA documentados (OB-VS-29 nuevo verifica padding consistente). Plan: conversación inline. Pendiente: device QA full con 29 escenarios OB-VS-01..29 + 8 DL-* + 42 acumulados épico Groups Pulido Final.

- [2026-05-11] **Onboarding restyle iter4 — refinamientos Step 6 + persistencia calculator (commit `0c95a206`)**. Sprint single-commit con 5 ajustes precisos sobre iter3, todos en Step 6. (1) **Signo negativo añade prefix `-`** en `.manual`: cuando segmented control = negativo, aparece "-" largeTitle antes del symbol body, mostrando "- S/ 1234" (más explícito que solo el segmented). (2) **Símbolo simétrico en `.guided`**: `guidedBalancePreview` ya no usa `appPreferences.currency()` (que retornaba symbol+amount mismo tamaño). Ahora separa symbol body `secondaryTextStyle` + amount largeTitle.semibold `primaryTextStyle` (vía `appPreferences.number(abs(signed), forceFullPrecision: true)`) — formato visualmente idéntico al manual. Si negativo, prefix `-` largeTitle. `accessibilityElement(.ignore) + accessibilityLabel(a11yLabel)` con `appPreferences.currency(signed, ...)` preserva la lectura VoiceOver completa. (3) **Botón "Editar" `primaryTextStyle`** (era `Color.hotPink` que se veía mal sobre el preview). Más conservador, no compite con el icon halo de la card. (4) **Tap card 2 con valor previo NO auto-abre sheet**: callback ahora `balanceMode = .guided; if initialBalanceText.isEmpty { openBalanceGuide() }`. Si ya hay monto (de manual o calc anterior), espera tap "Editar". Separa intent de selección vs intent de modificación. (5) **Prefill defensivo del calculator**: nuevo helper `openBalanceGuide()` que llama `prefillCalculatorFromInitialBalance()` antes de presentar el sheet — mapea `initialBalanceText` al campo apropiado de `calcFieldState` según `selectedAccountType` (variant `.simple` → `simpleAmountText`, `.creditCard` → `directSpendingText`, `.general` → `bankAccountsText`) solo si los campos están vacíos (`AmountInputHelper.formatWithGrouping` para el formato). Esto cubre el bug reportado: "calcular monto → escribir a mano → volver a calcular → sheet aparece en 0". El `fieldState` `@Observable` reference debería persistir entre presentaciones del sheet (verificado en código), pero el prefill defensivo garantiza UX correcta sin depender de la causa raíz exacta del bug que mi análisis estático no identificó. Tap "Editar" del preview también usa `openBalanceGuide()`. **Build verde + 24/24 tests existentes verdes**. Sin tests nuevos (no hay pure-logic — helper UI). **L10n**: 0 keys nuevas (reuso completo). **Decisiones del owner**: signo prefix visible (UX explícita), symbol simétrico (consistencia visual con manual), botón sin hotPink, tap card sin valor abre / con valor espera "Editar", prefill defensivo universal (cubre `.simple` + `.creditCard` + `.general` variants). 28 escenarios QA documentados (OB-VS-06 + OB-VS-28 actualizados con marker _(iter4 update)_). Plan: conversación inline. Pendiente: device QA con 28 escenarios OB-VS-01..28.

- [2026-05-11] **Onboarding restyle iter3 — refinamiento Step 3 + Step 6 (commit `e42488bc`)**. Sprint single-commit con 2 cambios precisos sobre iter2. **Step 3 reorden**: cards intercambiadas — card 1 ahora "Varias cuentas" (multiple, iconTint `.priorityNeedNew`) preseleccionada por default + card 2 "Una sola cuenta" (single, iconTint `.hotPink`). **Default global**: `selectedUsageMode` cambia de `.dayToDay` → `.fullControl` — el user llega a Step 3 con multi-cuentas marcada. Callback Step 2 card "control" cuando viene de expensesOnly: setea `.fullControl` (era `.dayToDay`) para coherencia. Step 4 (.accountType) ya no se skipea por default — el user explícitamente debe elegir "Una sola cuenta" en Step 3 para skipearlo. Cero tests dependen del default (verificado grep). **Step 6 redesign**: eliminado completamente `balanceLivePreview` arriba — cada modo muestra el saldo en su propia sección debajo de la card seleccionada, sin duplicación visual. En `.manual` el form editable expande TextField con el monto. En `.guided && hasValue` expande nuevo computed `guidedBalancePreview` con `OnboardingFieldSection` (mismo wrapper que el manual): label "Saldo inicial" + monto formateado en `largeTitle.semibold` (vía `appPreferences.currency()` con thousands separator + decimales) + **botón "Editar"** inline (icon `square.and.pencil` + label en `Color.hotPink`) que reabre el sheet del calculator. Tap card 2 directo también reabre el sheet. En `.guided && empty` sigue el hint amber de iter2. Tap card 1 desde `.guided` con valor: cambia a `.manual` y el monto previo del calc se carga en el TextField. **L10n**: reuso de keys existentes (`Onboarding.accountBalanceLabel` "Saldo inicial" + `Action.edit` "Editar") — **0 keys nuevas**. **Decisión owner consolidada**: livePreview arriba era redundante porque ambos modos ya muestran el saldo en su card propia (manual via TextField, guided via guidedBalancePreview). Estructura más limpia. **Tests existentes verdes** (24/24). Sin tests nuevos. 28 escenarios QA documentados (OB-VS-03 actualizado _(iter3 update — REORDEN)_, OB-VS-06 actualizado _(iter3 update — sin livePreview arriba)_, **OB-VS-28 nuevo** preview read-only + botón Editar). Plan: conversación inline sin doc separado. Pendiente: device QA con 28 escenarios OB-VS-01..28.

- [2026-05-11] **Onboarding restyle iter2 — feedback adicional sobre iter1 (commit `48367147`)**. Sprint single-commit con 2 ajustes precisos. **Step 1**: eliminado completamente el saludo interactivo "Hola, %@. Yala está listo para ti" (computed `welcomeGreetingText` + callsite en `nameStep`). Decisión owner: sobrecargaba sin sumar. Keys `Onboarding.greetingBubble` + `Onboarding.greetingBubbleEmpty` eliminadas en 16 locales + accessors en `L10n.swift`. Step 1 ahora va directo de subtitle → form del nombre. **Step 6 rediseño**: el CTA card prominente de iter1 no comunicaba claramente que era tappeable. Reemplazado por **2 binaryCards estilo Step 2/3/4**: card 1 "Conozco mi saldo" (icon `pencil` halo `.priorityNeed`, subtitle "Lo escribes a mano") **preseleccionada por default** + card 2 "Calculemos tu saldo juntos" (icon `arrow.triangle.2.circlepath` halo `.hotPink`, subtitle "Te guiamos paso a paso"). Tap card 1 expande el form manual abajo (sign segmented + TextField gigante con currency symbol — diseño intacto desde iter0). Tap card 2 abre sheet del calculator. **`balanceLivePreview` siempre arriba** (independiente del modo, feedback constante). **Tras sheet cancelado sin elegir**: card 2 sigue seleccionada (stroke 2pt `.hotPink`) y aparece **hint amber** debajo con icon `exclamationmark.triangle.fill` `.essentialNeed` + caption "Aún no completaste el cálculo. Toca para retomar." (voseo es-AR "Tocá"). Re-tap card 2 reabre el sheet. **Color amber NO rojo** (decisión: brand voice cálido, alineado con "atención pendiente" no "algo roto"). State management: nuevo enum `BalanceMode { manual, guided }` privado + `@State balanceMode: BalanceMode = .manual`. Nuevos helpers: `manualBalanceForm` (form expandido — extraído del balanceStep original sin tocar diseño) + `balanceIncompleteHint` (hint amber con `fixedSize(horizontal: false, vertical: true)` para wrap correcto). Reuso del `binaryCard` existing — no nueva abstracción. **Eliminado** computed `balanceGuideCTA` de iter1. **L10n net iter2**: 3 keys nuevas (`balanceManualOption` "Conozco mi saldo", `balanceManualHint` "Lo escribes a mano" — es-AR "Lo escribís a mano", `balanceGuidedIncompleteHint` "Aún no completaste el cálculo. Toca para retomar.") + 2 eliminadas (`greetingBubble`, `greetingBubbleEmpty`) × 16 locales = +16 strings net (vs +80 de iter1). Aplicado vía script Python idempotente. Reuso de keys existentes para card 2: `accountBalanceLearnMore` (title) + `balanceGuideHint` (subtitle). **Tests existentes verdes** (24/24): 7 OnboardingTelemetryEventBuilder + 10 OnboardingPrefillResolver + 7 FullModeActivationLogic. Sin tests nuevos (no hay nueva pure-logic — `BalanceMode` enum + estado SwiftUI, todo UI). 27 escenarios QA documentados (OB-VS-01 actualizado _(iter2 update — saludo eliminado)_, OB-VS-06 actualizado _(iter2 update — REDISEÑO)_, **OB-VS-27 nuevo** sheet cancelado → hint amber + retap reabre). **Decisiones consolidadas en sesión**: (1) `.manual` preseleccionada por default — reduce un tap, mayoría usa input manual. (2) Tras sheet cancelado `.guided` sigue marcada con hint (no auto-revert a `.manual`) — preserva intent del user. (3) Re-tap card reabre sheet sin CTA extra "Recalcular" — la card ya es interactiva por contrato. (4) `balanceLivePreview` siempre arriba en cualquier modo. (5) Color hint amber `essentialNeed` (no rojo error) — brand voice cálido. Plan: conversación inline sin doc separado (alcance reducido vs iter1). Pendiente: device QA con 27 escenarios OB-VS-01..27 (incluyendo SE + AX5 + Reduce Motion + VoiceOver + 5 temas themedPanel).

- [2026-05-10] **Onboarding restyle iter1 — QA visual feedback aplicado (commit `6f0040a5`)**. Sprint single-commit que aplica feedback de QA visual del owner sobre `3f4cdf95`. **4 reglas universales R1-R4** en heroFlow: R1 elimina `titleAccentGradient` (titles primary), R2 hero icons primary (Steps 2-7), R3 paleta de iconos de cards restringida a `.hotPink/.priorityNeed/.priorityNeedNew/.essentialNeed` (sin indigo, sin cyan), R4 progressIndicator activo+inactivo bifurcado (`.white` + `.white.opacity(0.2)` heroFlow / `theme.accent` + `theme.secondaryText.opacity(0.2)` themedPanel — antes inactivo era invisible sobre indigo). **Step 1**: eliminada struct `WelcomeBubblePreview` → Text inline simple sin contenedor con `.smooth(0.45)` keyed en text. Copy nuevo "Hola, %@. Yala está listo para ti" en 16 locales (voseo es-AR "para vos", vouvoiement fr, Sie de, です/ます ja, 你 zh-Hans). **Step 6**: drop auto-launch del calculator + CTA card prominente (icon halo + chip "Recomendado" amber). **Reusa `accountBalanceLearnMore`** existente (NO eliminar — tiene 2 callsites externos en `BalanceCalculatorSheet.swift:164` y `AccountFormView.swift:65` que el plan original no contemplaba; hallazgo del review escéptico). 1 key nueva `balanceGuideHint`. **Step 7**: grid categorías 52pt → 40pt (reusa `categoryIconSize` existing). Eliminada propiedad huérfana `notifIconSize`. **Step 8 rediseño tipo Apple Settings**: eliminado `readyHeader` (Circle 96pt + sparkles 44pt). Title `largeTitle.bold`, subtitle `body`. Items en lista compacta con **Rectangle 1pt bifurcado** (heroFlow `Color.white.opacity(0.1)` / themedPanel `Color(.separator)` — `Divider()` default invisible sobre indigo). Privacy en grid 2x2 mini-cards individuales. 4 keys L10n nuevas (versiones cortas): `privacyLocalShort/NoTrackingShort/IcloudShort/NoSharingShort`. **Hallazgos del review escéptico** (todos incorporados): callsites externos `accountBalanceLearnMore`, R4 inactivo también bifurcado, `iconTint` default `.electricIndigo` viola R3 → `.hotPink` defensivo, `Divider()` invisible sobre indigo → Rectangle bifurcado, `.padding(.top, DS.Spacing.xl)` migra al VStack tras eliminar readyHeader. **L10n net**: 1 modify + 5 new + 0 delete × 16 locales = +80 strings. Aplicado vía script Python idempotente. **Tests existentes verdes** (24/24): 7 OnboardingTelemetryEventBuilder + 10 OnboardingPrefillResolver + 7 FullModeActivationLogic. Sin tests nuevos (no hay nueva pure-logic — todo UI/style/copy). `/simplify`: 2 fixes (eliminada referencia "tras QA iter1" del comment de `styleAccentColor` por regla CLAUDE.md no narrar ticket; constante nombrada `iconColumnWidth` en `rowDivider`). 26 escenarios QA documentados (OB-VS-01..08 actualizados con `_(iter1 update)_` + OB-VS-26 nuevo R4 capsules bifurcadas). **Aprendizajes**: `/review-plan` cazó 5 problemas críticos pre-implementación; el grep defensivo `accountBalanceLearnMore` reveló los 2 callsites externos. SourceKit cross-file diagnostics ruido durante edits (resuelven al compilar). Plan: `~/.claude/plans/ajusta-el-plan-considerandot-sharded-boot.md`. Pendiente: device QA con 26 escenarios OB-VS-01..26 (incluyendo SE + AX5 + Reduce Motion + VoiceOver + 5 temas themedPanel).

- [2026-05-09] **Bug `review-deeplinks` — centralizar selectMainTab para tabs ocultos en Más** (commit `bde61bb2`). 6 fases atómicas. **Causa raíz**: `TabView` nativo iOS 18+ con `selection` binding requiere que `Tab(value:)` esté montado en `ForEach(visibleTabs)` del mismo tick — asignar `selectedMainTab = X` cuando el tab está oculto en "Más" deja el binding en el vacío. El patrón correcto (`temporaryTab` + `Task.sleep(50ms)` + `selectedMainTab`) estaba solo en `.recordsStandalone` y `.groups`; otros 16 callsites lo violaban (chevrones del Panel, hero, Score, deeplinks externos, MorePlaceholderView). **Solución**: helper único `SessionState.selectMainTab(_:)` con cancellation de `pendingTabSelectionTask` + GC-08 guard centralizado (eliminado guard duplicado en `handleMainTabIntent`). Helper pure-logic `MainTabSelectionLogic.decide(requested:config:)` para testabilidad. Extension inversa `AppTab.asConfigurable` (nil para `.more`/`.search`). `TabBarConfiguration.loadFromStandardDefaults()` deduplica lectura `UserDefaults`. **Cancellation crítico**: dos deeplinks rápidos consecutivos podrían dejar `temporaryTab` limpio antes que el Task posterior llegara al `selectedMainTab = X`, regenerando el bug — el cancel previene esto. **Migración**: 16 callsites (4 funciones síncronas SessionState + 10 cases handleMainTabIntent + MorePlaceholderView.hiddenTabRow + GroupsContainerView.openPanel + FullModeActivationView + PanelViewModel.navigateToStatistics). **Tests**: 9 pure-logic en `MainTabSelectionLogicTests.swift` (visibleTab/hiddenTab/panel/more/search/groupInvite x2/emptyConfig/hiddenPlanning) — todos verdes. **Decisiones clave**: helper centraliza el invariante "monta-luego-selecciona" en un sitio que es imposible saltarse (vs Approach B "todo via AppRouter" que convertía nav síncrona en async, y Approach C "fix puntual" que dejaba bug latente en deeplinks externos); helper pure-logic separado vs lógica inline (alineado con `RemoteWipeSignalDecider`/`OnboardingPrefillResolver`/`FullModeActivationLogic`); guard GC-08 en SessionState no en helper (helper queda libre de conocer `OnboardingMode`); mantener semántica de overwrite de `temporaryTab` ya ocupado (consistente con patrón anterior — follow-up si UX confusa). **NO migrar** `SessionState.resetToDefaults:370` (transient durante wipe). **Aprendizajes**: `/review-plan` cazó 5 problemas pre-implementación (cancellation faltante en diseño inicial, race con `didSet` de selectedMainTab cuando otra mutación concurrente limpia `temporaryTab`, callsites adicionales en `MorePlaceholderView`+`GroupsContainerView`+`FullModeActivationView` no listados en plan inicial, verificación tras F4 antes de tocar handleMainTabIntent, escenario iPad sidebar DL-08). `/simplify` cazó 1 oportunidad real: helper `loadFromStandardDefaults` en TabBarConfiguration (deduplica triple-llamada UserDefaults+fromJSON). Sanity check `grep -rn "selectedMainTab\s*="` confirmó 0 callsites residuales fuera del helper. SourceKit cross-file diagnostics ruidosos (mismo target — resuelven al compilar; ignorables). 8 archivos / +236/-50 LOC. Plan: `~/.claude/plans/proxede-a-planificar-parsed-wall.md`. Pendiente: device QA con 8 escenarios DL-01..DL-08 (incluye iPad sidebar landscape).

- [2026-05-09] **Onboarding completo restyling — Welcome Hero/Chooser + Yala AI**. Sprint único 9 commits F1-F9 que llevan el lenguaje visual del `WelcomeFlowContainer` (heroIndigoBlack + logo 128pt + cards translúcidas Liquid Glass) y del `YalaAIOnboardingView` (PanelBackgroundView + capability cards + readyHeader gradient + toolbar leading X/chevron + progress en `.principal` + CTA único `YalaPrimaryButton`) al `OnboardingView` completo (8 steps fresh-install) **Y** al `FullModeActivationView` (wrapper para users mode `groupsOnly` activando full). **Bifurcación contextual** vía nuevo wrapper `OnboardingFlowScreen<Content>` + enum `OnboardingBackgroundStyle` (`.heroFlow` post-Welcome dark indigo continuo + paleta blanca rígida; `.themedPanel` FullModeActivation `PanelBackgroundView()` adapta tema con texto themed). 6 computed style helpers privados en OnboardingView (`primaryTextStyle`/`secondaryTextStyle`/`mutedTextStyle`/`cardStroke`/`styleAccentColor`/`titleAccentGradient`) bifurcan según el style. **Cambios estructurales**: (1) `binaryCard` reescrito sin checkmark/circle, icono con halo (RoundedRectangle 44×44 con `iconColor.opacity(0.18)` + Image gradient 20pt semibold), stroke 2pt accent en seleccionada (vs 1pt cardStroke). 6 callsites actualizadas con iconColor diferenciado por step (purpose: accent/essentialNeed; accounts: neonCyan/priorityNeedNew; accountType: 4 tints; categories yes/no: priorityNeed/secondary). Step 4 ForEach inline consolidado a binaryCard via helper `iconTint(for: AccountType)`. (2) Nuevo `OnboardingFieldSection<Content>` private struct reemplaza `SectionBox` solo en Steps 5/6 (label arriba `secondaryTextStyle` + content `.solidCard()` background-aware). `SectionBox` global no se toca. (3) Step 1 nameStep dibuja YalaLogo 128pt con `colorMultiply(.white)` solo en `.heroFlow` consumiendo `logoTopSpacing` del wrapper (mismo nivel visual que WelcomeFlowContainer). En `.themedPanel` mantiene app icon 120pt como fallback (FullMode skipea Step 1 vía prefilled, defensivo). Title con `titleAccentGradient` (`.hotPink → .white` heroFlow / `.hotPink → theme.accent` themedPanel). Nuevo `WelcomeBubblePreview` subview (saludo en vivo `"Hola, %@ 👋 — voy preparando tu Yala…"` + placeholder `"¿Cómo te llamas?"`) con `.smooth(0.45, extraBounce: 0.15)` keyed en text + Reduce Motion respetado. (4) Step 6 nuevo `balanceLivePreview` computed: `largeTitle.semibold` con monto formateado en vivo + `.contentTransition(.numericText())` keyed en `initialBalanceText`/`balanceIsPositive`. Empty state muestra `"S/ 0"` en `mutedTextStyle`. (5) Step 8 readyHeader inline (Circle 96pt gradient `.hotPink → styleAccentColor` + sparkles 44pt en gradient match) reemplaza el `confirmationMotivation` largeTitle previo — el motivacional pasa a subtitle `title2.semibold`. Items con `.thCard` + stroke + iconColor variado (styleAccentColor/hotPink/essentialNeed/priorityNeed). Privacidad como card translúcida con header `privacyTitle` (reusa key existing) + 4 bullets. CTA Step 8 cambia a `L10n.Onboarding.startUsingYala` ("Empezar a usar Yala", más identitario que `finish`). (6) **Toolbar bifurcado**: `OnboardingView` envuelve body en `NavigationStack` propio + `toolbarBackgroundVisibility(.hidden)`. `topToolbar` con `leadingToolbarButton` switch en `(backgroundStyle, currentStep)`: heroFlow Step 1 → X cancel solo si `onCancelFromStep1!=nil` / heroFlow Steps 2-8 → chevron back llama `goBack()` / themedPanel todos los steps → X cancel total via `onCancel`. Progress capsules en `.principal`. **Eliminado obligatoriamente** el `.welcomeBackButton(tint: .secondary, ...)` modifier en F1 — coexistía con el toolbar como overlay, hubiera causado dos chevrons en Step 1 (regresión OB-VS-23). (7) **Bottom CTA bifurcado**: `floatingNavigationButtons` solo renderiza fade gradient + back capsule en `.themedPanel` (PanelBackground claro necesita separación). En `.heroFlow` solo CTA único `YalaPrimaryButton` (back vive en toolbar). Helper `goBack()` extraído como función privada (instrumentado con telemetría). `advance()` extraído de la closure inline del primary button con `trackPickIfApplicable(forStep:)` interno. (8) **`FullModeActivationView` refactor**: drop NavigationStack envolvente + drop toolbar Cancel — pasa `.themedPanel` + `mode: .fullActivation` + `onCancel: { onComplete() }` al OnboardingView. (9) **Bugfix preexistente double-fire** `.onboardingCompleted`: hoy disparaba 2 veces en FullMode (uno desde `OnboardingView.completeOnboarding` con `expensesOnly`/`usedSeedCategories`, otro desde `FullModeActivationView.completeFullActivation` con `mode: fullActivation`). Fix: `OnboardingView.completeOnboarding` agrega param `mode` derivado del `init`; `FullModeActivation.completeFullActivation` deja de disparar `.onboardingCompleted` (mantiene solo `.fullModeActivationCompleted`). Resultado: 1 fire único por flow con `mode` correcto (verificable en OB-VS-24). **Telemetría granular** (9 cases nuevos en `AnalyticsEvent`): `onboardingStarted/StepViewed/PurposePicked/AccountsPicked/AccountTypePicked/CurrencyPicked/CategoriesPicked/BackTapped/Cancelled`. Pure-logic helper `OnboardingTelemetryEventBuilder` con 4 funciones (`paramsForStarted/StepViewed/Cancelled/BackTapped`) en `Yala/App/Logic/` + 7 tests Swift Testing verdes. **Disparo**: `started`+`stepViewed(.name)` en `.task` raíz; `stepViewed(next)` y picks (state final efectivo, no por handler tap) en `advance()`; `backTapped`+`stepViewed(prev)` en `goBack()`; `currencyPicked` en `accountCurrencyPickerSheet onDismiss` solo si `currencyAtPickerOpen != accountCurrency`; `cancelled` en callbacks toolbar X ANTES del callback original. Step enum gana `trackingName: String` computed (lowercased "name"/"purpose"/etc.) desacoplado del Int rawValue. **L10n**: 3 keys nuevas (`greetingBubble`+`greetingBubbleEmpty` Step 1, `startUsingYala` Step 8 CTA) × 16 locales = 48 strings con traducciones reales (voseo es-AR "Empezá", Sie de "wie heißt du", vouvoiement fr "Comment vous appelez-vous", です/ます ja "Yalaを始める", 你 zh-Hans "你叫什么名字", tu pt-PT "te chamas", você pt-BR "seu nome"). Helper Python `/tmp/yala-l10n-insert.py` insertó idempotente en 14 locales (10 con anchors INSERT, 4 sin anchors APPEND con marker). 25 escenarios QA documentados (OB-VS-01..25) cubriendo per-step × per-style + Dynamic Type AX5 + Reduce Motion + VoiceOver + 5 temas en .themedPanel + regresiones críticas (OB-VS-23 un chevron, OB-VS-24 telemetría única) + riesgo NavigationStack chrome (OB-VS-25 logo position). **Aprendizajes**: `/review-plan` cazó 6 problemas + 3 riesgos antes de implementar — eliminación obligatoria del `.welcomeBackButton` modifier; bugfix double-fire `.onboardingCompleted` aprovechable; logo `logoTopSpacing` solo se EXPONE (responsabilidad del Step 1 content, no del wrapper); `toolbarBackgroundVisibility(.hidden)` para chrome NavigationStack; instrumentar picks en `advance()` no en handlers (1 evento por step efectivo, no por tap); helper `OnboardingFieldSection` privado en lugar de tocar `SectionBox` global. F2 visual SE para `confirmationMotivation` largeTitle → subtitle (3 keys cortas paralelas `confirmReadyExpenses/DayToDay/FullControl` documentadas como contingentes en F7 si QA reporta overflow). `.numericText()` con currency string puede tener jitter al cambiar signo (validar OB-VS-06). Pendiente: device QA con 25 escenarios + decisión final sobre keys cortas Step 8 según overflow visual.

- [2026-05-09] **M6 — Cuenta personal real en Caso A del bridge SplitExpense ↔ TransactionItem**. 9 fases F1-F8 cierran épico Groups Pulido Final (items diferidos del Sprint Pulido 3). Cambio arquitectónico: Caso A (yo soy payer) ahora crea TX cuenta real `-totalAmount` con subcat manual del gasto + TX virtual `+lentAmount` sistema "Préstamo a grupos". Resuelve paradoja M5 donde mi BCP no veía la plata que salió (panel personal mentía). **Contrato editorial**: "lo que tú escribes queda como tú lo escribiste" — bridge **preserve+update** Caso A NUNCA pisa campos personales (cuenta/note/subcat/tags) después de creación inicial; solo actualiza monto/fecha/moneda/splitTotalAmount/splitType en eventos remotos. **Sincronía 100% personal cross-device**: TX cuenta real va por CKSync privado del user (no shared zone), preserve+update preserva cuenta local cuando llega SplitExpense remoto del mismo user. **Race autodelete**: nuevo notification `transactionsImportedFromSync` (cada import successful, no solo first) + `GroupBridgeRaceCleaner.cleanupPendingDraftsWithMatchingTX` borra drafts pendientes Caso A cuando ya existe TX real para mismo `splitExpenseID`. **Items entregados**: F1 InboxDraft + 3 fields opcionales (`originReasonKey`/`originActorName`/`originGroupName` snapshot al crear, lightweight migration safe). F2 bridge refactor `bridgeExpense` (sig: `accountForCurrentUser: Account?` + `isRemoteSync: Bool`) — preserve+update Caso A si TX real existe Y currency compatible (NUNCA pisa account/subcat/note/tags); incompat currency → delete + draft con hint impersonal "currencyChanged"; primera vez con cuenta proveída → crea TX real; sync remoto sin cuenta → draft con hint "remoteCreate" actor=payerName snapshot; helper `createDraftCaseA` interno + flag `skipDraftCleanup` para race con DraftService. F3 GroupExpenseService.createExpense/updateExpense propaga `accountForCurrentUser` end-to-end. F4 form chip "Cuenta" condicional (solo Caso A `.full/.completed`) + VM `selectedAccount`/`isReady`/`isCaseA`/`isAccountRequired`/`canSave` extender + `resetAccountIfIncompatible()` onChange currency + `isGroupInviteOverride` para tests + 12 tests pure-logic verdes. F5 race cleaner + sync hook (`iCloudSyncService.applyRemoteChanges` post-save dispara notification cada import) + `GroupBridgeRaceCleaner` static (`cleanupPendingDraftsWithMatchingTX` runtime + `computeCleanupPlan` pure-logic) + observer en `AppBootstrapper` step 14.5 + 6 tests verdes. F6a NewTransactionView edit parcial (computed `isBridgedCasoA`, refactor `Group { }.disabled` → granular: centralContent siempre disabled si bridged, bottomChips/registerButton habilitados Caso A) + `currencyFilter` en AccountSelectorSheet edit Caso A + delete/duplicate `.disabled(isBridgedCasoA)` (regenera draft, UX confusa) + banner copy contextual `editPartialBanner`. F6b nuevo `GroupExpenseAccountFinalizationSheet` (~190 LoC, replica Settlement Caso D pattern) + `DraftService.approveGroupExpenseAccountDraft` (10 pasos atómicos: guards, fetch SplitExpense+SplitGroup, crea TX cuenta real con splitExpenseID, set skipDraftCleanup=true, invoca bridge para regenerar virtual lent, defer reset, cache+delete draft+save) + InboxView routing por `targetTransactionID==nil && needsUserInput.contains("account")`. F7 GroupCardView D3 estilo Splitwise puro (max 3 rows + chip "+N más" CTA detail, sort by amount desc, perspective `.iOwe`/`.theyOweMe` con counterpartyName resuelto) + helper pure-logic `GroupsViewModel.computeCurrentUserDebts` + 8 tests + cleanup NO defaults universal (eliminadas funciones `defaultSettlementAccount/setDefaultSettlementAccount` de `GroupPersonalPreferences`, callsites en `SettlementFormView.resolvePreselectedAccount`, `GroupSettlementDraftFinalizationSheet`, bridge `bridgeSettlement` Caso C/D — `removeAll` mantenido para limpiar legacy). F8 L10n 6 keys × 11 locales completos (es-AR voseo "Podés"/"Elegí", es/es-419, de Sie "Sie können", fr vouvoiement "Vous pouvez", it, ja です/ます, zh-Hans 你, pt tu form Portugal, pt-BR você, nl, pl) + es-ES/en-GB/pt-PT heredan via fallback iOS + 14 escenarios QA M6-01..M6-14 incluyendo race cleaner, cross-device sync, edit parcial, currency change. **Decisiones clave**: subcat NO sincroniza (Opción I conservadora — categorías son privadas CloudKit private DB; en M6 incluso solo el payer no propaga, simetría perfecta), hint copy con actor snapshot para `remoteCreate` y impersonal para `currencyChanged` (sync remoto no expone "modifiedBy" → más honesto), `splitTotalAmount` semántica diverge en TX virtual lent (`amount=lent, splitTotalAmount=total` — ningún consumer asume equivalencia, validado pre-F2 con grep), no migrar M5 (owner archivará grupos manualmente), delete TX cuenta real bloqueado en NewTransactionView (regenera como draft, UX confusa). **Aprendizajes**: `/review-plan` cazó 8 problemas pre-implementación (F1 incompleto solo 1 field → ampliado a 3, DraftService branch sin documentar orden → 10 pasos numerados con defer, actor name semántico vs payer name → impersonal para currencyChanged, delete gating no documentado → M6-13 escenario, F2 path "sin cuenta" sin guard → assertionFailure DEBUG, splitTotalAmount semántica → grep pre-F2 confirmado seguro, `currentUserMemberID` nil edge case → `isReady` gate, F8 copy currencyChanged debe ser impersonal). Refactor a `computeCurrentUserDebts` static helper hecho post-F7 inicial al detectar tests requerían pure-logic (instanciación de `private(set) var` no posible). **Race cuidado**: `skipDraftCleanup` flag + `defer` en DraftService garantiza reset si bridge falla mid-flight; draft permanece pendiente para retry (idempotency natural). 31 tests pure-logic nuevos (5 InboxDraftM6Fields + 12 GroupExpenseViewModelM6 + 6 GroupBridgeRaceCleaner + 8 GroupsViewModelDebtsM6) — todos verdes. Tests de `GroupPersonalPreferences` reescritos a 2 cases sobreviviendo (removeAll cleanup legacy). Build verde end-to-end en cada fase. Pendiente: `/device-qa` con 14 escenarios M6-01..M6-14 + verificación cross-device sync personal (M6-04, M6-07, M6-14 críticos — necesitan 2 devices misma Apple ID). TODO post-launch: añadir debounce 1s al observer `transactionsImportedFromSync` para mitigar invocaciones múltiples en cold launch. Closes items diferidos Sprint Pulido 3 (cambio arquitectónico, #34 selector moneda, D3 doble fila multi-moneda).

- [2026-05-08] **Yala AI Onboarding (4-step tutorial first-use post-consent)**. 7 fases F0-F7. Flag `hasShownYalaAIOnboarding` (per-device, no synced) + 5 cases telemetría (`yalaAIOnboardingShown/Completed/Skipped/Dismissed/TonePicked`) + L10n.YalaAI.Onboarding (28 keys × 16 locales = 448 strings, voseo es-AR + Sie de + vouvoiement fr + です/ます ja + 你 zh-Hans + tu/você pt-PT/pt-BR) + pure-logic `YalaAIOnboardingLogic.nextScreen(consent×onboardingShown)` con 4 tests verdes evita drift entre 3 launchers (PanelView/RecordsStandaloneView/DetailContainerView). Animación dummy del chat en Step 1 con loop infinito + delay inicial 800ms (deja leer título) + pausa total con VoiceOver + solo fades con Reduce Motion + cancel safety en `.onDisappear`. Reusa componentes reales (`ChatMessageBubble`, `ChatLoadingIndicator`, `ChatMessage` struct mockeable). `ChatDraftPreviewCard` nuevo (read-only) en lugar de `ChatTransactionDraftCard` (que requiere SwiftData `@Query`). Step 2 destaca "Registra gastos al instante" con badge "Nuevo" en `Color.essentialNeed` (amber, universal en los 9 temas — evita clash con `theme.accent==.hotPink` en tema rosa Pro). Step 3 con `InsightTone`/`InsightFocus` selectors persistencia inmediata al tap (consistente con `AIPersonalizationSheet`). Step 4 con back chevron permite volver a step 3. **Encadenamiento sheets vía `onDismiss:` callback nativo iOS** (no `Task.sleep(350ms)` frágil) — `pendingOpenChatAfterOnboarding` flag setea solo en path `onComplete`, callback dispara `showChatSheet=true` post-animación de cierre nativa. **Background `PanelBackgroundView()`** (no `.thBackground`) — coherente con `ChatTopicsSheet` y aprovecha gradient atenuado tipo Welcome Hero (post-commit `32e3b355`) en los 4 temas glass/translucent. **Comportamiento de cierre**: complete (CTA Step 4) abre chat + setea flag; skip (topRight steps 1-3) cierra + setea flag; X (topLeft) cierra SIN setear flag (vuelve a aparecer). 20 escenarios QA documentados (AI-OB-01..AI-OB-20). **Aprendizajes**: `/review-plan` cazó 4 problemas críticos antes de implementar (sheet collision con Task.sleep frágil → onDismiss nativo, badge clash en rosa → essentialNeed amber, telemetría faltante en inventario, animation distrayendo título → delay 800ms) + 3 mejoras opcionales aplicadas (pure-logic helper con tests, a11y labels explícitos, escenario QA AI-OB-19 documentando UX side-effect del Saltar tras cambio de tono). Auditoría temas confirmó 9 themes (4 free + 5 PRO) y guió decisión `PanelBackgroundView()` post-commit `32e3b355`. Ticket: `Backlog/onboarding-yala-ai.md` (status: implemented-pending-qa). Pendiente: Device QA con 20 escenarios AI-OB-* en simulador (5 temas mínimo).

- [2026-05-08] **Sprint Pulido 3 — cierre items diferidos** (6 commits `f547cd85..7267d552`). Cierra 4 bloques pendientes del épico Groups Pulido Final post Sprint Pulido 2. (1) **C0 #16-bis copy notif** (`f547cd85`): title con emoji `👋 {nombre} quiere unirse a {grupo}` + body separado "Aprueba o rechaza desde Yala." (1 key actualizada + 1 nueva × 16 locales). Voseo es-AR "Aprobá o rechazá", impersonal de "In Yala annehmen oder ablehnen", vouvoiement fr "Approuvez ou refusez", です/ます ja, 你 zh-Hans. (2) **C1 #36 cleanup masivo** (`f3d66817`, +32 −1332 LoC): eliminado todo el path `autoCreate=false` del bridge — `SplitGroup.autoCreateTransaction` campo + 7 funciones `GroupPersonalPreferences` (preserva `defaultSettlementAccount` per-currency) + `GroupTransactionBridge.resolveAccount`/`importGroupHistory*`/`updateTransaction`/`updateDraft`/`unbridgeAllForGroup`/`makeBridgedDraft`/`matchSubcategoryFromList`/`case noAccountsAvailable` + `GroupSettingsView.mySettingsSection` completa + 3 archivos sheets (`BridgeActivationSheet`, `BridgeDeactivationSheet`, `BridgeRadioOption`) + tests legacy + 11 keys L10n × 16 locales = 176 strings + catch en `GroupExpenseService` (F0e). **Sin migración SwiftData** — schema plano confía en lightweight migration nativa (campo Bool con default). **Decisión "no legacy users"** del owner habilitó cleanup en lugar de gating. (3) **C2 #29+#30+#31 visuales feed** (`c85a5a28`): nuevo `GroupExpenseAmountResolver` (pure-logic 3 cases) + `GroupExpenseAmountView` componente + prefetch `txBridgeMap` en VM con filter logic correcto (TX1 con subcat=nil PREFERIDA sobre TX2 sistema "Préstamo a grupos") + `mySharesByExpense` + `currentMemberID`. Row del feed ahora muestra: icono+color de subcat per-user (fallback `splitTypeBadge` si no hay TX), copy "Pagaste S/X" / "{name} pagó S/X", monto perspectiva personal con caption "Prestaste"/"Te prestaron"/"No participaste". 5 keys L10n × 12 locales (4 heredan) + 1 eliminada (`paidBy` huérfana). 3 tests pure-logic verdes. (4) **C3 #32+#33 visuales form** (`88242ab3`): chip "División" cambia a teal (2 tokens DS nuevos `splitMethod*` para mejor contraste dark, NO infoBackground azul) + tamaño label. Placeholder "Agregar nota" → `Color(.tertiaryLabel)` (más claro). (5) **C4 #15 wipe vs grupos B1** (`c911a0f3`): alert del wipe ahora aclara "Esto no incluye tus grupos. Para salir, ve a Grupos, salda lo pendiente y abandónalos." (decisión owner: NO crear fila "Salir de todos los grupos", user va manual). 1 key × 16 locales (3 heredan, es-AR voseo "andá/saldá/abandonalos"). (6) **C5 routing inteligente invite link** (`7267d552`): `ReconnectMode` enum (7 cases) + custom CKShare key `isArchived` propagada por `GroupService.setArchived` + `inviteRouteDecision` extendida con `isArchived` + `currentMemberStatus` + `SplitSyncManager.currentMemberStatus(zoneName:)` helper + `GroupReconnectView` UI condicional por mode. **6 escenarios extra**: archived (CTA "Entendido", no acepta) / alreadyMember (nav groupDetail) / pendingDuplicate (nav groups lista NO detalle) / rejectedRetry+leftRetry+removedRetry (acceptShare + ensureCurrentUserMemberExists con reactivate). **Race archived mitigado**: fallback safe — key ausente → `.standardReconnect`; sync posterior trae estado al SplitGroup local. **`.left` vs `.removed` BIFURCADOS** (copys distintos "Saliste" vs "Te removieron"). 17 keys L10n × 13 locales (3 heredan) con voseo es-AR. 7 tests `inviteRouteDecision_*` nuevos en `AppBootstrapperTests` (13/13 verdes). **F0 pre-flight cazó**: SwiftData lightweight nativo OK, `bridgeExpense` M5 path ya cubre `.groupInvite` (importGroupHistoryAsDrafts redundante post A0-Bridge V2.0), `CKRecordNameZoneWideShare` existe en SDK, `paidBy` confirmada huérfana, `noAccountsAvailable` catch en `GroupExpenseService`. **Aprendizajes**: `/review-plan` cazó 11 problemas + 3 graves (SwiftData migration verificada con F0; `importGroupHistoryAsDrafts` impact resuelto leyendo bridgeExpense; `txBridgeMap` filter `?? false` siempre `false` para subcat nil → corregido con `guard let sub`); `onJoin` mantenido como `() -> Void` (no over-engineering con `(ReconnectMode) -> Void`); tokens DS `splitMethod*` (alineado con `splitMethodChip` que los consume, no `splitDivision*`). Diferido a sesión M6 aparte: cambio arquitectónico bridge Caso A con cuenta personal real seleccionable + #34 selector moneda editable + D3 doble fila multi-moneda en GroupCardView (estilo Splitwise). Pendiente: device QA full con 15 escenarios SP3-* (instruir testers borrar app + reinstalar antes por SwiftData schema change).

- [2026-05-08] **Sprint Pulido 2 — 9 bugs + simplify cleanup post QA A3-04**. 8 commits `a8722338..cfc16ff9`. **Items cerrados**: (1) **#16 instrumentación** (`a8722338` chore): 6 prints DEBUG en SplitSyncManager.applyRemoteRecord (status+isPending+isAdmin), GroupNotificationService.processRemoteChanges (isActive+summary+rateLimited+scheduling), NotificationService.sendNotification (authStatus+alert/center/lockScreen settings + add() result). Decisión: separar diagnose de fix — el commit C0 instrumenta, el fix root cause (C1) está DIFERIDO al post device QA con Console.app capturando logs `[#16-debug]`. Hipótesis ranked en `Ideas/apuntes-grupos.md`: rate limit comiendo el primer pending, isAuthorized=false con permisos OK, isActive flag no persistido, isAdmin race de cache. (2) **#23 + #24** (`9e9e05eb` fix): eliminar "Regenerar enlace" completo (state + dialog + UI block + 3 keys × 13 locales = 41 strings — A3 reemplazó la necesidad) + renombrar "Agregar a mis registros" → "Llevar también a mis registros" alineado a BRAND-VOICE.md (12 locales con traducciones reales). **Decisión voseo es-AR**: el copy es infinitivo + impersonal → NO requiere voseo gramatical → NO añadir entrada en es-AR (hereda vía iOS fallback). Coherente con patrón actual (es-AR tiene 306/4167 strings — solo verbos en 2da persona conjugados). (3) **#22 + #20-bis + #17-bis** (`417a70b0` fix): invite banner identidad con `BrandedMetadata` propagada end-to-end (AppBootstrapper → ContentView → GroupInviteOnboardingView, reusa key `welcomeWithGroup` existente — 0 keys nuevas) + botones aprobar/rechazar inline +height (xxs→sm) sin dim row para pending + counts horizontal `X · Y` con dot separator. (4) **#26 + #27** (`ea0f4812` feat): `GroupCardDisplayMode` enum + helper pure-logic `GroupCardDisplayLogic` + 6 tests + chip warning amber "Esperando aprobación" (tap disabled) / chip rojo "Solicitud rechazada" (tap → alert padre "¿Salir del grupo?") + `SplitMember.isVisible` + filter en GroupSettingsView miembros. **Decisión #25 droppeado**: con #26 bloqueando entrada al GroupDetailView para pending/rejected, gear gating es innecesario. Alert pattern: en padre `GroupsContainerView` con callback desde card (single-modal global vs N alerts montados). (5) **#35** (`3f7d6f88` fix): chips "gelatina" + pull-to-refresh espurio en form gasto. Causa raíz: `.refreshable` del `GroupDetailView` padre se propaga al sheet del form en iOS 26 (NewTransactionView con estructura idéntica pero sin padre refreshable NO reproduce). Fix: `.refreshable {}` no-op absorbe gesto. (6) **#28** (`a6b4501e` feat): header del GroupDetailView con `ToolbarItem(.principal)` + `GroupIconBadge` (28pt) + nombre. Cambio `.large` → `.inline`. (7) **/simplify cleanup** (`cfc16ff9` refactor): tupla anónima → `InviteLinkService.BrandedMetadata` struct Equatable+Sendable + nonisolated `.empty`. Componentes shared `StatusChip` (chip pill icon+text+capsule) + `GroupIconBadge` (Circle+icon configurable) en `Yala/App/Views/Shared/`. Helper VM `groupCardRow` (callsites duplicados de GroupCardView en filteredGroups + archivedGroups). `notificationSettings()` consolidado a 1 fetch (era double-fetch en DEBUG). Comentarios narrando ticket eliminados — solo se mantienen los que explican WHY no obvio. **L10n total**: 4 keys nuevas × 12 locales (#26 chips + alert) + 12 locales × 2 keys renombradas (#24) + 41 strings eliminadas (#23) = ~+34 net. **Tests**: 6 pure-logic GroupCardDisplayLogic 100% verdes. **Aprendizajes**: `/review-plan` cazó 7 problemas pre-implementación (callsite metadata invite, voseo es-AR no aplica a infinitivos, alert padre vs card, helper isVisible compuesto, double-fetch consolidación, comentarios narrando ticket, struct dedicado para tupla pública). Reusar `welcomeWithGroup` existente evitó 12 strings nuevas. **Diferidos**: fix root cause #16 (post device QA) + bloque feed/detalle gasto (#29-#34 + #36 — requiere decisión producto sobre 5 escenarios moneda+cuenta) + #15 wipe vs grupos (decisión arquitectónica). Detalles completos en `Ideas/apuntes-grupos.md`.

- [2026-05-08] **Sprint Bug #21 + #14 — Subcategorías vacías + FullModeActivation rediseñado**. 4 commits (C0+C1+C2+C3) cierran A3-05 reportado + bug latente #14 de apuntes-grupos. **Decisión arquitectónica**: el bug NO era filtro mal puesto — era conflicto entre 3 invariantes (A0-Bridge `isSystem` excluido de pickers manuales + modo `.groupInvite` minimal + form que pide subcat). **Approach J elegido** sobre H (ocultar chip) y D (seed automático): empty state con CTA "Crear categorías" → seed bajo demanda. Storage 0 hasta que user opta. Reversible. (1) **C0 cleanup**: eliminada `createGeneralAccountIfNeeded` de `GroupInviteOnboardingView.performSilentSetup` — la cuenta "General" era vestigio del diseño previo a A0-Bridge V2.0; bridge usa cuenta sistema "Grupos [moneda]" y `SettlementFormView` ya gatea `accountSection` por `!isGroupInviteMode`. (2) **C1 fix**: `SubcategorySelectorSheet` empty state usa `YalaEmptyState.actionTitle/action` (API ya existente) — icon `tag.fill`, copy "Aún no tienes categorías" + CTA que ejecuta `seedCategoriesIfNeeded` + `seedSystemGroupCategoriesIfNeeded` + `viewModel.loadData()`. 3 keys × 16 locales (es-ES/pt-PT heredan via `ls()` fallback). Voseo es-AR ("tenés"/"creá"/"vas a poder gestionar"). (3) **C2 rediseño**: `FullModeActivationView` reescrito de 290 LoC custom 2-step (account + notification) a 125 LoC wrapper de `OnboardingView` con `prefilledData: ICloudAccountSummary?` (rama B existente). Skipea `.name` + `.currencyName` + `.categories` (si Approach J ya sembró). Steps activos: purpose → accounts → accountType → balance → categories (condicional) → confirmation. **Decisión D2**: eliminado `requestNotificationPermission()` — permission iOS lazy en eventos explícitos posteriores. NavigationStack envolvente con toolbar Cancel. `cleanupResidualGeneralAccount` paranoid safeguard para QA legacy. `prefilledSummary` cacheado en `@State` (evita recompute en re-renders). Helper pure-logic en `Yala/App/Logic/FullModeActivationLogic.swift` (`buildSummary` + `shouldDeleteResidualGeneralAccount`) — 9 tests sin context (evita flake R8). (4) **C3 polish**: `GroupStatsView` muestra placeholder "Asigna categorías a tus gastos..." con icon `chart.pie` + `solidCard` cuando `categoryBreakdown` solo tiene "Sin categoría" (computed `allUncategorized` en VM). Evita pie chart de 1 segmento confuso. 1 key × 16 locales. **Total L10n**: 4 keys × 16 = 64 strings. **Tests**: 19 verdes (10 SubcategorySelectorViewModel + 9 FullModeActivationLogic, 100% pure-logic). **Aprendizajes**: `/review-plan` cazó 4 problemas pre-implementación (L10n.swift accessors omitidos, keys con sub-dots vs camelCase flat convención, prepareSummary async innecesario, micro-orden por commit). `/simplify` cazó 3 fixes (hardcoded `"defaultCurrencyCode"` → `AppPreferences.Keys.defaultCurrencyCode`, `"onboardingMode"` → `OnboardingMode.userDefaultsKey`, computed `prefilledSummary` → `@State` cacheado). Tests con `makeTestContext()` descartados temprano — verificación end-to-end via Device QA A3-05-bis + C2-01 + C2-02 + C3-01 (QA-SCENARIOS.md). Pendiente: `/device-qa` acumulado.

- [2026-04-28] Traducciones reales keys-por-keys de los 4 locales nuevos (nl `b3f16aba`, pl `d6b3748e`, zh-Hans `d6697ec5`, ja `8891408f`). Cierra épico l10n M14 — reemplaza rule-based v0. Approach validado: ~17 Edits grandes (50-150 keys), `grep -c` por Edit, sesión ~60 min. Decisiones específicas por locale (forma です/ます ja, pronombre `你` zh, voseo es-AR, glosarios financieros, counter words contextuales, placeholder reorder con notación positional para idiomas SOV). Pendiente: device QA visual en simulador para los 4 locales. Siguiente: M16 (ASC metadata localizada).
- [2026-04-28] 3 fixes runtime logs post-QA chat-registrar-transacciones (`773fe651` + `35bd11d1` + `aade3042`). (1) Sheet collision chat→NTV — gate del consumer `.panel` mientras `showChatSheet=true` vía markUnready/markReady del AppRouter. (2) ProfileImageStorage — guard `fileExists` antes de `Data(contentsOf:)` (caso normal vs error real). (3) UserSegmentService race — `currentSegment` se leía como `.dormant` durante cold start (recalculate antes del primer importEvent). Fix con NotificationCenter (patrón establecido) + 2 flags separadas (`hasCompletedFirstImport` en iCloudSyncService + `hasRecalculatedAfterFirstImport` en UserSegmentService) + helper `markRecalculatedAfterFirstImport()`. AppBootstrapper:555 gate-ea con la flag — caía a "require interaction" si segmento no confiable. Tests: 2 nuevos (`@Suite(.serialized)` en UserSegmentService). Pendiente: device QA con cold launch + cuenta poblada >100 tx (verificar log `(pre-import)` en primer scan).
- [2026-05-04] Subset 2 del épico Groups Pulido Final (`8945a30b`) — A10 + A9 + A13 + A12. (1) **A10**: `GroupTransactionBridge.resolveAccount` cambia de `-> Account?` a `throws -> Account` con `noAccountsAvailable`. Propagación selectiva: creación bubble-up al alert; batch import + updates `try?` (resilient). Pattern matching catch directo en GroupExpenseService. Logger.error reemplaza print debug-only. 1 key l10n × 13 locales. (2) **A9**: eliminadas 43 líneas de heurísticas (nameMatch único + admin único / oldest joinedAt) en `ensureCurrentUserMemberExists`. Identidad solo desde `cloudKitUserRecordID`. Path defensivo legacy backfill intacto. Riesgo duplicación legacy mitigado por `deterministicUUID(zoneID, recordName)`. (3) **A13**: nuevo `GroupService.updateCurrentUserDisplayName(_:)` con cache de grupos por zoneID + filtro `displayName != trimmed` (idempotente). Llamada en `GroupInviteOnboardingView.performSilentSetup` paso 8.5 + boot-time reconciliation `AppBootstrapper.reconcileCurrentUserDisplayNameIfNeeded` (paso 16.6) cubre kill-app entre acceptShare y onboarding-complete. Self-healing sin flag. (4) **A12**: función pura `AppBootstrapper.inviteRouteDecision(hasCompletedOnboarding:onboardingMode:) -> InviteRouteDecision` reutilizada por `acceptShareFromURL` y `YalaAppDelegate` — simetría perfecta entre branded link y CKShare nativo. Verificado: `ContentView.onJoin` ya invoca `acceptShare(metadata:)`. Tests: +8 (resolveAccount + inviteRouteDecision con 4 estados). Cleanup `/simplify`: hardcoded `"userName"`/`"hasCompletedOnboarding"` → `AppPreferences.Keys.*`. Pendiente: device QA full al final del épico (acumulado, no por subset). Siguiente: Subset 3 (A6 race bridgeExpense + A7 dedup balance + A8 leave/remove con deuda).
- [2026-05-05] **A0-Bridge** del épico Groups Pulido Final — rediseño completo del bridge `SplitExpense` ↔ `TransactionItem` (modelo M5 simplificado). 16 commits F1-F16, 14 fases técnicas + L10n + QA docs. **Decisiones clave**: (1) TX expenses van TODAS a cuenta virtual `Grupos [moneda]` (auto-creada por `ensureSystemAccount`) — desacopla bridge de conciliación bancaria. (2) Settlements sí tocan cuenta real (Caso C/D) pero solo `.full`/`.completed`; `.groupInvite` solo virtual con upsell. (3) **5 subcategorías sistema** en 2 categorías (`Grupos` expense + `Cobros de grupos` income) con naming contable claro: Préstamo a grupos, Cobro de préstamo, Pago de liquidación, Liquidación enviada, Liquidación recibida. (4) **TX-puntero** para drafts groupExpense: TX1 se crea con subcat=nil + draft con `targetTransactionID` → finalize UPDATEa subcat (saldo virtual cuadra desde t=0). (5) **Idempotency vía delete+recreate** en bridge — robusto a edits de payer/monto y sync race. (6) **Drafts groupExpense/groupSettlement no eliminables** — solo desde el grupo origen. (7) **3 toggles AppPreferences** synced: includeGroupTransactionsInFeed, includeGroupsInPanelTotal, includeGroupTransactionsInStats. (8) **Auto-archive** cuenta virtual cuando vacía + recreación lazy on demand. **Diferidos a iteración posterior**: BridgeActivationSheet/DeactivationSheet UI (lógica core completa via `importGroupHistory` async + `unbridgeAllForGroup`); GroupExpenseDraftFinalizationSheet/GroupSettlementDraftFinalizationSheet visualmente distintivos (drafts funcionan en Inbox normal con icons distintivos `person.2.fill`/`arrow.left.arrow.right.circle.fill`); GroupDraftsPendingBanner del Panel; bloqueo edición `EditTransactionView` de TX bridgeadas (protección operativa via pickers que excluyen sistema + bridge idempotency). **Tests A0-Bridge**: 61 tests pure-logic (F1=24, F2=9, F3=8, F4=12, F15=8). Tests integration con `makeTestContext()` en blacklist R8 — validación funcional via Device QA F16 (21 escenarios documentados en `$VAULT/planning/QA-SCENARIOS.md`). **L10n**: 37 keys × 13 locales con traducciones reales (voseo es-AR, です/ます ja, 你 zh-Hans, vouvoiement fr, Sie de). **Sin migración**: grupos no estaban en producción; deploy aplica desde cero (instruir testers borrar app + reinstalar). Pendiente: completar UI sheets diferidas + device QA full.
- [2026-05-05] **A0-Bridge V2.0** del épico Groups Pulido Final — completa los 4 items P0+P1 pendientes que quedaron diferidos del A0-Bridge inicial (commits `414dc75b` F1, `f2e08e6d` F2, `eb59c9b7` F3, `179e621b` F4, `ab786490` F5). 5 commits F1-F5 + tests/verificación F6. **Items entregados**: (1) **P0-1 Settlement form Caso C proactivo** — `SettlementFormView` con AccountSelector filtrado por currency, preselect desde `defaultSettlementAccount`, `createSettlement`+`confirmSettlement` con `accountForCurrentUser` para disparar bridge inmediato (2 TX al instante). `.groupInvite` oculta selector. `AccountSelectorSheet` extendido con parámetro opcional `currencyFilter` (retrocompat). (2) **P0-2 Sheets activación/desactivación tardía** — `BridgeActivationSheet` con opciones "Empezar desde ahora" / "Importar todo el historial" (corre `importGroupHistory` async con `ProcessingProgressView` determinate); `BridgeDeactivationSheet` con "Eliminarlas" / "Mantenerlas como histórico". Toggle `personalAutoCreate` intercepta cambios cuando hay data pre-existente. **Anti-loop guard D8** via flag `isInternalToggleRevert` (revert visual del binding sin re-disparar `onChange`). **Alert error** propaga `error.localizedDescription` al user (en lugar de solo log DEBUG). (3) **P1-3 Bloqueo edición TX bridgeadas** — `NewTransactionView` detecta `splitExpenseID/splitSettlementID` y entra en read-only: oculta `transactionTypeSelector`, `ContextualGuideBanner` y toolbar trailing favorites; `disabled + opacity 0.5` en form; banner CTA contextual (`assignFromInbox` / `editFromGroup` / `editSettlementInGroup`) navega a Inbox o `groupDetail(zoneID)` (fallback `groups`). (4) **P1-4 Sheets finalización drafts especializados** — `GroupExpenseDraftFinalizationSheet` (read-only header + selector subcat, path TX-puntero existente), `GroupSettlementDraftFinalizationSheet` (read-only header + AccountSelector con preselect + persiste `defaultSettlementAccount`). `InboxView` switch por `draft.sourceType` con fallback a `InboxDraftEditSheet` para sources personales. **Decisión D7 crítica**: `approveDraft` rama `groupSettlement` crea TX manual INDEPENDIENTE — **NO setea** `splitSettlementID`/`splitGroupZoneID`. Razón: el bridge hace delete+recreate sobre TX con esos IDs al editar el settlement; mantener los IDs haría que una edición posterior borre la TX manual del user. La TX virtual (lado bridge) sigue ligada al settlement y se regenera. **Currency** se resuelve desde origen (`SplitExpense`/`SplitSettlement` por ID) en `.onAppear` — `cachedCurrencyCode` solo se popula en `approveDraft`. **L10n**: 11 keys × 16 locales con traducciones reales (voseo es-AR "Asigná"/"finalizá", です/ます ja, 你/账户 zh-Hans, vouvoiement fr, Sie de, "Posteingang" de para Inbox, "Caixa"/"Skrzynkę" pt/pl, "Finalise" en-GB). **Aprendizajes**: `/review-plan` cazó 7 problemas reales antes de implementar (loop infinito en onChange, `approveDraft` borrando TX manual, currency no cacheado, alert error faltante, read-only incompleto en toolbar/typeSelector). `@Query` reemplazado por fetch directo en `.onAppear` para evitar re-fetch. Tests A0-Bridge integration siguen verdes; flake R8 preexistente confirmado en `resolveAccount_throwsNoAccountsWhenEmpty` (pasa aislado, falla en suite paralela — race con `makeTestContext()` documentado). Pendiente: `/device-qa` full con 21 escenarios F16 acumulados (borrar app + reinstalar antes). Bloquea: A3 (`pendingApproval`) + A4 (Welcome Chooser) — desbloqueados ahora.
- [2026-05-05] **A3 — pendingApproval flow (opción B)** del épico Groups Pulido Final. 6 fases F1-F6 (modelo + servicios + notificación + UI admin + UI invitado + L10n + tests/docs). **Items entregados**: (1) **F1 modelo**: `SplitMemberStatus` extiende con `.pendingApproval` y `.rejected` (rawValue String CloudKit-compat); helpers `isPendingApproval`, `isRejected`, `canWrite`. `GroupService` añade `pendingMembers(in:)`, `approveMember(_:in:)`, `rejectMember(_:in:)` (todos `requireCurrentUserAdmin`). `ensureCurrentUserMemberExists` modificado: invitados nuevos entran como `.pendingApproval` (owner siempre `.active`); existing `.rejected`/`.removed` re-pone en `.pendingApproval`; **case explícito para `.pendingApproval` no-op** evita bug donde re-tap link convertía pending a active. `validateCurrentUserCanWrite` en `GroupExpenseService` lanza nuevo `pendingApproval`. `bridgeExpense`/`bridgeSettlement` endurecen guard a `currentMember.isActive` — pending/rejected no triguean bridge. (2) **F2 notificación admin**: `RemoteChangeSet.newPendingMembers` + `buildPendingMemberNotification` con prioridad sobre newMember. `SplitSyncManager` bifurca al clasificar `splitMember` records leyendo `record["status"]` directo del CKRecord, con **pre-cache `isCurrentUserAdminOfGroup` por zoneID** dentro del batch — solo admins reciben notif. (3) **F3 UI admin**: `pendingApprovalSection` en `GroupSettingsView` (gated `isCurrentUserAdmin && !pendingApprovalMembers.isEmpty`) con avatar + nombre + 2 botones Aprobar/Rechazar y confirmation dialogs. `GroupDetailViewModel.pendingApprovalMembers` computed. (4) **F4 UI invitado**: `PendingApprovalBanner` (pending + rejected states) en `GroupDetailView` con `DS.Semantic.warningBackground` / `errorBackground`. **FAB ya bloqueado** por `canCurrentUserParticipate` existing — sin tocar. **Refresh wiring** ya cubierto por `.onChange(of: sessionState.dataVersion)` existing. Step 3 onboarding "Solicitud enviada" en `GroupInviteOnboardingView` con detección async vía `mostRecentGroup()` + `ensureCurrentUserMemberExists`. (5) **F5 L10n**: 14 keys × 16 locales con traducciones reales (voseo es-AR, です/ます ja, 你/管理员 zh-Hans, vouvoiement fr, Sie de, "ligação" pt-PT, "Anfrage" de) + 1 plural `pendingRequestsCount` × 12 stringsdict (4 reglas pl, sin distinción singular ja/zh). **stringsdict pattern**: keys plain (sin `%d` literal), `%#@count@` resuelve plural. (6) **F6 tests**: +8 (4 SplitModels: rawValue stable, pending/rejected not active, garbage rawValue fallback; 2 GroupService errorDescription; 2 CKRecordTranslator round-trip pending/rejected). **Decisiones clave**: `rejected` case nuevo (no reusa `removed`) — mejor UX copy + telemetría. Re-pone en pending tras rechazo/removed (sin límite reintentos V2.0). Owner siempre `.active`. Last-write-wins en aprobación concurrente. Sin unbridge en reject (pending nunca creó gastos). Deeplink reusa `groups/{UUID}` (un tap más a Settings). Trade-off bridge: removed/rejected dejan de bridgear updates remotos (caso edge, aceptado). **F0 pre-flight cazó 5 hallazgos**: FAB ya cubierto por `canCurrentUserParticipate`, refresh wiring ya existe en GroupDetailView:153, `GroupInviteOnboardingView` no recibe `group` (usa `mostRecentGroup()`), stringsdict keys plain confirmado, bug `else { active }` en branch reactivateInactive. **Aprendizajes**: `/review-plan` cazó el bug del branch reactivateInactive antes de implementar (caso explícito `.pendingApproval` no-op crítico). `SectionBox` no acepta `subtitle` (count va inline como label). `DS.Typography.callout` y `title3` no existen — usar `label` y `title2`. `L10n.Common.continue` no existe — usar `Action.continueAction`. Pendiente: device QA acumulado (21 escenarios F16 A0-Bridge + 9 escenarios A3-01..A3-09 nuevos). Siguiente: A4 (Landing + AASA + Welcome Chooser).
- [2026-05-06] **A4 — Landing + AASA + Welcome Chooser pre-onboarding** del épico Groups Pulido Final. ÚLTIMO P0 de Fase A — cierra Fase A. 7 fases F0-F7. **Items entregados**: (1) **F1 Web**: `Web/src/pages/invite.astro` multilenguaje SSR via `detectLanguage(Accept-Language)` reutilizando `Web/src/i18n/translations.ts` (6 idiomas: es/en/de/fr/it/pt); 9 keys × 6 idiomas. `Vary: Accept-Language` header. `<html lang>` dinámico + `og:locale`. `PUBLIC_DEEP_LINK_SCHEME` env var (yaladev preview / yala production). `Web/.env.example` documentado. AASA sin cambios. (2) **F2 iOS infra**: `iCloudSyncService.forceFetchAndWait(timeout: 15)` async observa `Notification.Name.iCloudFirstImportCompleted` (existing) con `withCheckedContinuation` + `NSLock` single-resume; `ICloudAccountSummary: Equatable` struct + `@MainActor extension ModelContext.iCloudAccountSummary(appPreferences:)` excluye infra A0-Bridge (cuentas sistema, TX bridgeadas, subcat sistema); `AppPreferences.hasShownWelcomeChooser` per-device. Tests: 3 `forceFetchAndWait_*` + 11 `ICloudAccountSummary` (flake R8 conocido en suite paralela, pasan individuales). (3) **F3 Welcome Chooser + Rama A**: `WelcomeChooserView.swift` (~150 líneas) full-screen 3 cards (sparkles/icloud.and.arrow.down/person.2.badge.plus). Inserción en `ContentView.checkInitialSyncState` reemplazando el `showOnboarding=true` (NO antes — preserva el toast mágico para returning users con data en iCloud). Race CKShare: gate `.fullScreenCover(isPresented: Binding { showWelcomeChooser && !showGroupInviteOnboarding })` auto-cierra el chooser. Invariante: `hasShownWelcomeChooser` se setea SOLO tras tap consciente. Telemetría event `welcomeChooserBranchSelected`. (4) **F4 Rama B**: `WelcomeRestoreView.swift` (~280 líneas) state machine 5 estados + reintento + confirmation dialog "empezar de cero". Cancelación task en `.onDisappear` (`searchTask?.cancel()`) + `guard !Task.isCancelled` antes de cada mutación de state — previene resume sobre vista no presentada. `OnboardingView.init(prefilledData: ICloudAccountSummary?)` API híbrida (`Step` permanece privado): `prefilledSkippedSteps` UNION `usageModeSkippedSteps`; `.task` popula `userName`, `selectedCurrency` desde summary ANTES del fallback UserDefaults. Reset zombi: `prefilledOnboardingData = nil` en `onChange(of: hasCompletedOnboarding)` + `hasShownWelcomeChooser = false` en `performLocalWipeForRemoteSync` (path `skipOnboarding=false`) — chooser se vuelve a mostrar tras wipe. (5) **F5 Rama C**: `InviteRecoveryView.swift` (~150 líneas) con `UIPasteboard.hasURLs` auto-detect (evita toast clipboard hasta confirmar URL) + validación inline live + reusa `InviteLinkService.isInviteLink` + `AppBootstrapper.handleInviteLink` existing. `InviteLinkService.alternateHosts`: array con 4 hosts canónicos + alternos (yala-app.pe, yala-pe.com, yala-app.com.pe + variantes www) — paste manual NO sigue redirects HTTP. `extractShareURL` y `isInviteLink` aceptan los alternos. 11 tests `InviteLinkServiceTests` (canónicos, alternos, custom schemes yala/yaladev, foreign rejected, decode con alternate). (6) **F6 L10n**: 35 keys × 16 locales con traducciones reales por locale (voseo es-AR "Pegá"/"empezá"/"contános", です/ます ja, vouvoiement fr "Vous", Sie de, 你 zh-Hans cercano, "ligação" pt-PT, "tu" forms pt-PT). Plurals via `String(format: ls(...))` con `%d` literal (sin `.stringsdict` — adjetivo invariante en todos los idiomas). (7) **F7**: cierre épico (CLAUDE.md + STATE.md + QA-SCENARIOS.md A4-01..A4-12 + backlog [x]). **Decisiones clave**: (a) Welcome Chooser inserta al final de `checkInitialSyncState`, NO antes — evita degradar el toast mágico de returning users. (b) Rama B usa `forceFetchAndWait(15s)` adicional sobre el wait existente (8s) — control explícito al user "estoy seguro que tengo cuenta, espera más". (c) `OnboardingView.init(prefilledData:)` híbrida en lugar de `Set<Step>` externo — encapsula skip-logic, cero acoplamiento. (d) Web SSR con `Accept-Language` + `Vary` header en lugar de client-side i18n — sin FOUC, SEO multi-idioma correcto, single source of truth con `translations.ts`. (e) `hasShownWelcomeChooser` per-device (no synced) + reset en wipe path. **F0 cazó hallazgos**: `skippedSteps` solo 2 referencias (no 6 como estimado), `TelemetryService` enum simple sin schema migration, vercel.json hosts confirmados (4 alternos exactos). **Aprendizajes**: `/review-plan` cazó 6 problemas reales antes de implementar (cancelación task `.onDisappear`, reset zombi prefilled tras wipe, dominios alternos paste manual, verificación post-deploy Vary header, telemetría event check, suite race con singleton). Build verde y tests pasan en cada fase. Pendiente: device QA acumulado (21 escenarios F16 A0-Bridge + 9 A3-01..A3-09 + 12 A4-01..A4-12 = 42 escenarios; borrar app + reinstalar antes). Siguiente: Fase B (P1+P2 backlog).
- [2026-05-05] Subset 3 del épico Groups Pulido Final (`dbce20a6`) — A6 + A7 + A8. Cierra Fase A salvo A3/A4 (no son código Swift). (1) **A6**: NSLock descartado tras `/review-plan` — habría introducido regresión (descartar second-call legítimo de update). `@MainActor` + sync function + idempotency check via fetch existing TX/Draft ya garantizan atomicidad por construcción. Comment documenta el invariante en `bridgeExpense` línea 80. Sin test E2E (runner crashea con globals del bridge — patrón documentado Subset 1). (2) **A7**: dedup `expenses` por `id` con `Dictionary(grouping:by:\.id).values.compactMap(\.first)` en `calculateBalances` y `rawDebts` (cubre `calculateDebts` + `globalSummary` por propagación). +4 tests cubren las 3 funciones + regresión de IDs distintos. (3) **A8**: eliminada restricción "balance == 0"; `removeMember` y `leaveGroup` ya no lanzan `outstandingBalance`. UI: warning copy en confirmation dialog (mensaje dinámico), helper `hasNonZeroBalance(for:)` extraído por `/simplify` (reuse). Limpieza simétrica: `case GroupServiceError.outstandingBalance` + branch + helpers privados `memberHasOutstandingBalance`/`memberBalances` + key `leaveGroupDisabledHint` × 13 locales. 2 keys nuevas con traducciones reales por locale (voseo es-AR, です/ます ja, 你 zh-Hans, vouvoiement fr). Aprendizajes: `/review-plan` cazó la regresión del NSLock antes de implementar; greps previos zanjan ambigüedades de "qué eliminar"; `/simplify` post-implementación detecta duplicación de computeds. Pendiente: device QA full al final del épico. Siguiente: A3 (decisión `.readOnly` vs iOS 26 access requests) + A4 (landing page web + AASA).
- [2026-05-08] **A4 v3.2 — Sprint 2.1 polish refinado CERRADO en 8 commits** post-QA del Sprint 2 (commit `0e638931`). Cerrado en cadena: `a2cefd42` (base Sprint 2.1) + `44858f59` (logo centrado + back cards eliminadas) + `e718ede9` (logo 128pt) + `211579cc` (Chooser mirror VStack) + `b261b8c1` (Hero minLength 80) + `63931f91` (`WelcomeFlowLayout.logoTopSpacing` adaptativo proporcional 10%/min 60pt vía GeometryReader) + `3e8b22f0` (`/simplify`: extraído `WelcomeFlowScreen<Content>` wrapper SSOT — GeometryReader + background gradient + frame del content) + `f6444eb9` (Chooser invite card icon `essentialNeed` amber, evita cyan duplicado vs `neonCyan`). **Sub-iteraciones post-QA visual**: (a) Logo Hero centrado verticalmente entre top safe area y cards (Spacer flotante); back cards eliminadas (case ±2) — solo front + 1 side card a cada lado, `offsetSideX` 140→200pt para que NO se crucen con la front (resuelve cruce visual de translucencia). (b) Logo duplicado a 128pt en Hero + Chooser; Chooser refactorizado de `ScrollView { VStack }` a `VStack` con `Spacers` flexibles (misma estructura que Hero) → logo a la misma altura visual exacta en ambas pantallas, transición Hero→Chooser sin "salto" de logo. **TODO pendiente** (anotado en `apuntes-grupos.md`): rebranding del OnboardingView (steps 1-8 post-Chooser) para igualar background indigo→negro del flow Welcome y adaptar elementos al theme liquid glass — sigue con `.thBackground` claro, requiere sesión completa de polish (~4-6h). 8 ítems originales del Sprint 2.1: (1) **Cards Hero reducidas ~mitad**: 200×130/160pt (de 280×200/240). Iconos 26pt con gradient brand sobre frame 56×56 con tinte 0.18. (2) **Cards laterales sobresaliendo + page indicator dots**: ZStack con todas las 8 cards renderizadas con posiciones derivadas signed mod (`(idx - currentCardIndex + n) % n` + signed wrap a `[-2, +2]`). Posiciones: front (1.0, 0, 0), ±1 (0.82, ±140, ±5°, opacity 0.55), ±2 (0.7, ±185, ±8°, opacity 0.22). 8 capsules debajo del stack (3pt × 6pt no-activo, 3pt × 18pt activo blanco), tap navega + reset timer rotationTask. (3) **Cards translúcidas estilo Liquid Glass del Panel** (NO Material genérico ni accentColor sólido): `.background(.thCard)` + stroke `Color.white.opacity(0.1)` + `DS.Panel.widgetRadius` (20pt). Diferenciación brand SOLO en el icono (gradient + tinte 0.18 del accentColor). (4) **Carousel transition rediseñado**: animation `.smooth(duration: 0.45, extraBounce: 0.15)` (más elegante que spring 0.6/0.75 previo); SIN `.id(currentCardIndex)` + transition asymmetric (rompía sincro back/middle); SIN `.clipped()` para no cortar shadows en bordes. Las 8 cards se mueven juntas porque sus propiedades visuales (offset/scale/rotation/opacity) cambian con `currentCardIndex` dentro del `withAnimation`. (5) **Copy subtitle nuevo**: "Tú captura. Yala se encarga." × 16 locales con traducciones reales (voseo es-AR "Vos capturá", you en/en-GB, du Sie de, vouvoiement fr, te tu it, あなたは撮るだけ ja, 你拍下来 zh, jij vastlegt nl, ty rejestrujesz pl, você captura pt/pt-BR, tu capturas pt-PT). (6) **Logo Hero 64pt** (de 36pt) para alinear con Chooser. (7) **Chooser cards con `.solidCard(padding: 0, radius: DS.Radius.xl)`** reemplaza `.ultraThinMaterial` que se veía grisáceo sobre dark — usa `.thCard` del theme `liquidGlass` + stroke `theme.cardBorder` (mismo look que widgets del Panel). (8) **Hero+Chooser unificados en 1 fullScreenCover** (`WelcomeFlowContainer.swift` nuevo): enum `WelcomeFlowStep { .hero, .chooser }`, state interno + transitions `.opacity` con animation `.smooth(duration: 0.5, extraBounce: 0.1)`. Alert "Detectamos tu cuenta" vive DENTRO del container (no en ContentView). Resuelve el "frame azul vacío" entre dismiss del Hero y present del Chooser que aparecía con dos covers separados — el background gradient persiste continuo. ContentView state: eliminados `showWelcomeHero`, `showWelcomeChooser`, `showDetectedDataAlert`, `detectedICloudSummary`; agregados `showWelcomeFlow: Bool` + `welcomeFlowInitialStep: WelcomeFlowStep`. Cover separado del Chooser en ContentView eliminado. `presentNextOnboardingScreen()` y InviteRecovery `.onBack` cableados al nuevo modelo (`welcomeFlowInitialStep = .chooser` cuando InviteRecovery hace back). **Decisiones clave**: (a) Approach ZStack con 8 cards renderizadas a la vez (en lugar de matchedGeometryEffect o `.id`+transition asymmetric) — más simple, sin race entre back/middle/front, performance OK porque cards con opacity 0 se omiten en draw pass. (b) `.solidCard()` modifier (existente, usado por widgets del Panel) en lugar de `.glassEffect()` iOS 26 nativo — mantiene look consistente con el resto de la app, sin riesgo de Material genérico. (c) Cross-fade `.opacity` simple entre Hero y Chooser (sin matchedGeometryEffect ni `.move`) — el background compartido hace que la transición se sienta continua sin necesidad de tricks geométricos. (d) Page indicator capsules en lugar de Circles — más moderno, `frame(width: 18 vs 6)` da un "dot largo" visual claro del activo. (e) `WelcomeFlowContainer.initialStep` parameter inmutable (no binding) — el ContentView setea `welcomeFlowInitialStep` ANTES de poner `showWelcomeFlow = true`, y el container respeta su valor inicial. **Aprendizajes**: matchedGeometryEffect descartado tras review (riesgo glitches con array que rota); `.id(currentCardIndex)` + transition asymmetric descartado (rompe sincro de back/middle al cambiar índices). Build verde, 10 tests pure-logic OnboardingPrefillResolver siguen pasando. Pendiente: device QA con escenarios A4-26 (cover unificado sin frame azul) + A4-27 (page indicator + cards translúcidas + tap navigation) + re-test A4-23 con dims nuevas.
- [2026-05-07] **A4 v3.2 — Sprint 2 polish visual/UX** post-QA del épico Groups Pulido Final. 4 hallazgos consolidados (#5, #8, #11, #9c) en una sesión unificada. **Items entregados**: (1) **#9c fix bug userName override**: nuevo `Yala/App/Logic/OnboardingPrefillResolver.swift` con 2 funciones puras (`resolveUserName`, `resolveCurrencyCode`) — si `prefilledData != nil` (rama B) devuelve SOLO summary aunque sea nil; si `prefilledData == nil` (rama A) lee defaults con guards (`!isEmpty`, `!= "Usuario"`). 10 tests pure-logic. `OnboardingView.task` refactorizado para invocar el resolver — ya no pisa prefilled con UserDefaults residual. (2) **#8 copy unificado "Empezar desde cero" × 16 locales**: 4 españoles cambian todas las "configuración"/"de cero" → "desde cero" (`startFresh`, `startFreshConfirm.title`, `startFreshConfirm.confirm`, `notFoundBody`, `detectedData.startFresh`). 12 locales no-español alinean `welcome.restore.startFresh` con su `welcome.detectedData.startFresh` correspondiente (ej. en "Start setup" → "Start fresh", de "Einrichtung starten" → "Neu beginnen", fr "Commencer la configuration" → "Repartir de zéro"). Out of scope: `icloud.remoteWipe.confirm` (contexto wipe), `account.deleteBalanceError` (uso matemático). (3) **#5 Welcome Hero rediseño completo**: cards 200/240pt (mitad de altura previa), fondo sólido con difuminado claro/oscuro suave (no translúcido), paleta calmada rotando indigo/pink/teal/amber/purple sin neonCyan saturado, stack lateral pronunciado (offsets 32/-32, rotations 7°/-9°, scales 0.88/0.78), background `DS.Gradients.heroIndigoBlack` (token nuevo) gradient indigo→negro vertical, **8 cards** con Grupos vector viral + Asistente IA + Presupuestos como nuevos diferenciadores (eliminadas voice/multiAccount/currencies, fusionadas en assistant/multiAndCurrencies, reformuladas capture/import/icloud/more). **Carousel horizontal Estrategia 1**: front `.id(currentCardIndex)` + `.transition(.asymmetric(insertion: .move(edge: .trailing).combined(with: .opacity), removal: .move(edge: .leading).combined(with: .opacity)))` + spring 0.55/0.78. Back/middle interpolan implícitamente con `withAnimation(.spring)`. Title sobre fondo dark: `.white` para "Tus finanzas personales," + gradient `.hotPink → .white` para titleAccent (contraste >4.5:1 garantizado, evita gradient indigo→pink que daría contraste insuficiente sobre indigo). Logo Yala con `.colorMultiply(.white)`. (4) **#5 background extendido al Chooser** (decisión user "Hero + Chooser, NO Onboarding"): mismo `heroIndigoBlack`, cards re-estilizadas con `.ultraThinMaterial` (glass sobre dark), stroke `Color.white.opacity(0.12)`, text `.white` / `.white.opacity(0.7/0.4)`, icon backgrounds `iconTint.opacity(0.25)`, iconTints brand (.hotPink/.neonCyan/.priorityNeed). (5) **#5 chevron back desde Chooser → Hero** (F6a): `.overlay(alignment: .topLeading)` con Button 44pt touch target — NO NavigationStack (evita navigation bar overhead). Callback `onBack` cableado en `WelcomeFlowModifier` para `showWelcomeChooser=false; showWelcomeHero=true` SIN setear `hasShownWelcomeChooser` (sigue false hasta tap consciente). (6) **#5 chevron back desde Onboarding step 1 → Hero** (F6b): nuevo callback opcional `onCancelFromStep1` en `OnboardingView.init` (preserva API existente). Overlay topLeading gateado por `currentStep == .name && onCancelFromStep1 != nil`. ContentView: `showOnboarding=false; hasShownWelcomeChooser=false; prefilledOnboardingData=nil; presentNextOnboardingScreen()` — vuelve al Hero. State del onboarding se descarta silenciosamente (fresh install). (7) **#11 RestoreView `.found` rediseño**: header `sparkles` con `foregroundStyle(LinearGradient(colors: DS.Gradients.heroIntense, ...))` reemplaza `icloud.and.arrow.down.fill` plano azul. Counts pasan de un único box con filas a **layout adaptativo según `visibleCountItems.count`**: 1 → card centrado max-width 280pt; 2-3 → HStack columnas iguales; 4 → `LazyVGrid` 2×2. Cada card: VStack con icon brand (theme.accent) + número grande (`DS.Typography.amount`) + label caption, fondo `.thCard` + `dsCardShadow()` + `DS.Radius.lg`. Refactor reemplaza `summaryRow` por struct `CountItem` + `countCard` + `countCards(for:)`. **Decisiones clave**: (a) Hero+Chooser fondo unificado con fade default entre fullScreenCovers — mismo gradient hace transición imperceptible. (b) Carousel **Estrategia 1** elegida: front `.id` + transition asymmetric, back/middle interpolan implícitamente. Estrategia 2 (TabView paginated) descartada (page indicator overhead). (c) Cards fusionadas (voz+IA, multi+divisas) reducen 7→8 cards manteniendo conciso pero más completo. (d) Copy chevron back reusa `L10n.Welcome.Invite.back` ("Volver" × 16 locales). (e) Voseo es.lproj limpio (audit confirma tuteo correcto). **F0 cazó**: brand colors viven en `Yala/App/Views/Shared/UIHelpers.swift:228-248`; SF Symbol `target` ya usado en onboarding purposeStep:303; 6 keys del flow Welcome con "de cero"/"configuración" descubiertas vs 5 del plan inicial (notFoundBody adicional). **Aprendizajes**: `/review-plan` cazó 4 problemas reales antes (carousel incompleto, NavigationStack overhead, copy `.title` faltante, keys L10n inconsistencia) + 5 riesgos (brand colors, contraste a11y, SF Symbol, re-fetch iCloud, logo asset). Fixes preventivos: gradient titleAccent `.hotPink → .white` (mitiga contraste), `.ultraThinMaterial` correcto sobre dark. Build verde end-to-end (`/verify-ios`). Pendiente: device QA con simulador (A4-22..A4-25 nuevos + A4-01/A4-02/A4-06 re-test post-polish), reanudar 39 escenarios A0-Bridge/A3/A4 pendientes. **Out of scope diferido**: TODO #10 auditoría sistémica `Dictionary(uniqueKeysWithValues:)` + telemetría.
- [2026-05-07] **A4 v3.2 — Sprint 1 Fix P0 #2 + #6** post-QA del épico Groups Pulido Final (commits `435c67d8` + `9a818f28`). (1) **#2** (P0, diferido desde 2026-05-06): banner `dormantGroupsAsMain` "Tienes gastos compartidos pendientes" aparecía en users post-onboarding sin grupos. Causa: `NudgeTriggerEvaluator.shouldShow(.dormantGroupsAsMain, ...)` retornaba `true` incondicionalmente — las otras 3 nudges dormant ya gating por contexto, esta era la única excepción. Fix: gateado por `groupMemberCount > 0`. Test `dormantGroupsAsMain_alwaysTrue` reemplazado por 2 cases (with/without groups). (2) **#6** (P0 regresivo de A4 v3.1): tras reinstall fresh con misma Apple ID, Hero aparecía ~1s y se reemplazaba automáticamente por PanelView "Hola, [name residual]" + alert "Datos no disponibles". Causa raíz real (NO la hipótesis original que asumía KV-Store sincronizando `hasCompletedOnboarding` — esa flag ya era `synced: false` en `AppPreferences:353`): `performLocalWipeForRemoteSync(skipOnboarding: true)` en `ContentView:553` autopromovía la flag al recibir `.remoteWipeDetected` con `onboardingAlreadyDone=true` desde KV-Store del Apple ID. Los timestamps escritos por `signalOnboardingCompleted`/`signalWipeInitiated` en instalaciones previas sobreviven al uninstall — el KV-Store es per-Apple-ID, no per-app. Fix A: nuevo `RemoteWipeSignalDecider` (pure-logic, `Yala/App/Logic/RemoteWipeSignalDecider.swift`) decide si procesar signal según `hasCompletedOnboarding` + `isWipingData` + presencia timestamp. Devices fresh-install (`hasCompletedOnboarding=false`) ignoran el signal y marcan AMBOS timestamps (wipe + onboarding) localmente para idempotencia y silenciar también el "Caso B" `.remoteOnboardingCompleted` de `checkForRemoteWipeSignal`. `handleRemoteOnboardingCompleted` recibe guard simétrico `hasCompletedOnboarding` + elimina asignación espuria de la flag (no debe completar onboarding por signal cuando user mid-onboarding sin estado local). 6 tests pure-logic del decider cubren matriz fresh/returning/wiping. **Fix B explorado (gate `hasExistingData` en línea 87 del ZStack) DESCARTADO**: `/review-plan` cazó la regresión grave — desmontaba MainTabView ante cualquier borrado legítimo de cuentas/categorías. Defensa-en-profundidad especulativa puede romper más de lo que arregla. Sin regresión cross-device wipe (devices con estado local siguen procesando signal). **Aprendizajes**: hipótesis del user puede ser parcialmente errónea — investigación profunda en código antes de aceptar diagnóstico al pie de la letra. `/review-plan` cazó el problema del Fix B antes de implementar. Pure-logic decider habilita tests sin SwiftData ni UI (evita flake R8 conocido). Pendiente QA con 3 devices (Físico-A, Sim, Físico-B) — pre-condición CRÍTICA: KV-Store contaminado por install previa para reproducir bug #6 (sin contaminación → "pasa" sin validar). Sprint 2 (#5 polish visual Hero, 9 puntos) y Sprint 3 (#8 copy "Empezar configuración" en `WelcomeRestoreView`) en sesiones separadas tras validar QA con A4-01..A4-21 limpio. Reanudar QA del épico desde A4-01 con los 3 devices. 21 escenarios A0-Bridge + 9 A3 quedaron ⏳ pendientes (no afectados por el bug #6).
- [2026-05-07] **Sprint extra — TODO #10 cleanup CloudKit duplicates** (commits `fe897dd3` F1+F2, `a4b70fe4` F3+F4+F5, `20ac4223` F6 tests, + commit final post-`/simplify`). Cierra el TODO #10 que quedó diferido en A4 v3.2 Sprint 2 polish — el fix `f07a26be` resolvió el crash inmediato pero era defensivo, no curativo. **Items entregados**: (1) **Auditoría sistémica de 14 sitios `Dictionary(uniqueKeysWithValues:)`** vía 3 agents Explore en paralelo: **0 P0 reales**; `AppBootstrapper:321` reclasificado a P3 (UUID local round-trip CloudKit, sin race posible — el TODO original lo asumía P1 erróneamente); 4 sitios P2 (claves String editables = sort-by-name) endurecidos con `uniquingKeysWith: { first, _ in first }` (`AccountsSettingsListViewModel:48`, `TagsSettingsListViewModel:51`, `TrendsTabView:38`, `PanelViewModel:939`). Resto (PersistentIdentifier, UUID local memoria, enum cases) sin cambios — riesgo cero confirmado. (2) **Telemetría `cloudkitDuplicateDetected`**: nuevo case enum `AnalyticsEvent` + enum `DuplicateDetectionContext: String` (`.bootCleanup`, `.runtimeFetch`, `.syncApply`, `.uniquingFallback`) + helper `TelemetryService.cloudkitDuplicateDetected(model:count:context:keySuffix:)` (extraído por `/simplify` — 4 callsites de ~7 líneas a 1 línea cada uno). **Composite key local incluye zoneID** para no perder cuenta de zones distintas — la `key` es solo dedup local en `trackedOnceKeys: Set<String>`, NO se envía al backend (privacy-first). Payload: `model`/`count`/`context.rawValue`. (3) **Cleanup proactivo `SplitGroupDeduplicationService`** (`Yala/Services/Groups/`, patrón `CategoryDeduplicationService` / `NotificationService.deduplicateNotifications`): pure-logic `computeDedupPlan(groups:) -> DedupPlan` testeable sin context (R8 evita `makeTestContext()` flake) + service wrapper `deduplicateSplitGroups(in:) -> Int` que fetch → plan → `context.delete(extras)` → save + telemetría por zone duplicada. **Keeper = oldest `createdAt`**. Members/Expenses/Settlements **NO se tocan** — vinculados por `groupZoneID` String, no por `@Relationship` (F0 confirmó ausencia). Integrado como **paso 16.4 sync** en `AppBootstrapper` (entre init Group services y retryPendingBridges/reconcileDisplayName Tasks) — sync porque consumers downstream Tasks pueden tropezar con duplicados si corre async. 6 tests pure-logic verdes. (4) **Telemetría runtime defensiva**: `GroupService.updateCurrentUserDisplayName` y `refreshCurrentUserFlags` comparan `dict.count < array.count` post-`uniquingKeysWith` → dispara `.uniquingFallback` (sin crashear). `SplitSyncManager.group(for:)` cambia a fetch ordenado por `createdAt asc` (canónico determinístico) + dispara `.runtimeFetch` cuando results.count > 1 (red de seguridad para race post-boot no cubierto por el cleanup boot-time). `groupName(for:)` sin sortBy (sin callsites — `/simplify` removió defensa innecesaria). **Causa raíz del race confirmada**: `SplitGroup` se crea por DOS paths con `id` distintos pero `cloudKitZoneID` idéntico — (a) CloudKit sync via `SplitSyncManager.applyGroupMeta` (id desde `record.recordID`), (b) acceptShare local (id fresh `UUID()`). Race fetch-then-insert en `applyGroupMeta:957-963` sin transacción atómica. Mitigación cleanup proactivo + defensa runtime cubre el caso en práctica. **Cascade-delete CloudKit aceptado conscientemente**: `context.delete` propaga a otros devices via SwiftData CKSyncEngine; si telemetría muestra recurrencia post-deploy iterar a heurística más fina (ej. `isOwner` flag o sync state). **Out of scope** (segunda iteración): cleanup post-fetch en `applyRemoteRecord`, transacción atómica fetch+insert, dedup de SplitMember/SplitExpense/SplitSettlement (sus `id` provienen del recordID round-trip, sin race). **Aprendizajes**: 3 agents Explore en paralelo (audit `Dictionary`, telemetría infra, CloudKit dedup) cazaron numéricamente el alcance antes de planificar — sin esto, el TODO original habría guiado a un over-fix de `AppBootstrapper:321` (P3, no P1). `/review-plan` cazó 5 problemas pre-implementación: composite key sin zoneID perdía info de N zones, cascade-delete CloudKit no documentado, race post-boot no cubierto (red de seguridad runtime telemetry), falta update CODEBASE-MAP.md, F0 verificación pre-flight de `SplitGroup.swift`. `/simplify` cazó 5 oportunidades: extraer helper telemetría (4→1 línea cada callsite), enum quita stringly-typed code, eliminar refs `Sprint extra TODO #10` de comments (regla CLAUDE.md no narrar tarea), remover sortBy defensivo en `groupName(for:)` sin callsites, agregar docstring R8 al test helper. Build verde end-to-end. Pendiente: device QA cold launch verificar log "SplitGroupDedup: removed N duplicates" + telemetría TelemetryDeck dashboard si recurrencia detectada.
- [2026-05-06] **A4 v3.1 — Welcome Hero + Chooser fix unificado** (cierra bugs #1 P1 + #4 P0 de `apuntes-grupos.md` en una sesión). Insight arquitectónico: el **Welcome Hero pre-Chooser** es el lugar natural para fetch background de iCloud — el user lee 7 cards animadas mientras decidimos si tiene data, y el alert "Detectamos tu cuenta" ocurre ANTES del Chooser, no post-onboarding. **Esto elimina 3 paths de autopromote silente** sin necesidad de wipe+reseed flow ni helpers complejos. **Items entregados**: (1) **F1 (1 commit atomic)**: `WelcomeHeroView.swift` (~330 líneas) — stack de 3 cards visibles (front/middle/back con offset/rotation/scale + spring animation 3.5s) sobre 7 cards data (capture · voice · import · multi-account · 48 divisas · iCloud · "y mucho más"). Title con accent gradient indigo→pink. Background theme.background + glow asimétrico pink→indigo desde abajo. iCloud fetch task corre invisible (`forceFetchAndWait(timeout: 10)` + `iCloudAccountSummary`). `handleEmpezar` SIN recursión (single-shot guard `hasTappedEmpezar` + dispatch único tras timeout 3s extra). Pause card rotation con VoiceOver activo (`@Environment(\.accessibilityVoiceOverEnabled)`). Refactor `ContentView.checkInitialSyncState`: eliminar autopromotes (líneas ~602-668), eliminar `iCloudSyncWaitingView` + `isWaitingForSync` state + `showICloudDataFound` alert. Extraer `runReturningUserPostChecks()` (SSOT post-onboarding checks) y `consumeDetectedSummary(_:)` (lógica WelcomeRestoreView callbacks SIN re-fetch). `presentNextOnboardingScreen` cambia `showWelcomeChooser` por `showWelcomeHero` cuando `!hasShownWelcomeChooser`. **`WelcomeFlowModifier`**: extraído como ViewModifier (patrón `GroupInviteModifier`) — Hero cover + alert "Detectamos tu cuenta" + onChange reset state cuando llega CKShare. Necesario para evitar typecheck timeout en body grande. **Extension SSOT** `ICloudAccountSummary.isFullyPrefilled` en `iCloudSyncService.swift` reusada por `WelcomeRestoreView` (1 línea cambio) + `WelcomeFlowModifier`. (2) **L10n**: 23 keys × 16 locales (5 hero meta + 14 cards × 2 + 4 detected data alert) con traducciones reales por locale: voseo es-AR ("Apuntá", "Decilo", "Registrá", "Viajá", "Cargá"), tuteo es estándar, Sie de, vouvoiement fr, です/ます ja, 你 zh-Hans, "tu" forms pt-PT, en/en-GB diferencia "organizes" / "organises". Python script appendea 368 strings × 16 archivos. (3) **F2 tests**: +7 pure-logic en `ICloudAccountSummaryTests.swift` (5 `isFullyPrefilled` casos + 2 `hasAnyData` casos puros). 100% passing. Tests integration `makeTestContext()` siguen con flake R8 conocido (no regresión, no míos). (4) **F3 QA-SCENARIOS.md**: 18 escenarios A4 (12 originales + 6 nuevos): A4-01/03/11 modificados, A4-13 (alert detected data + 2 CTAs), A4-17 (slow network), A4-18 (rotation 1min), A4-19 (VoiceOver pause), A4-20 (Dynamic Type XXL), A4-21 (race CKShare). (5) **F4 cierre**: `apuntes-grupos.md` #1 + #4 marcados resueltos. **Decisiones clave**: (a) Hero approach elimina necesidad de v2 alert post-onboarding + wipe+reseed flow + `resetFirstImportFlag` + sleep 2s + revalidación summary multi-device race — todo eso desaparece porque la decisión es PRE-onboarding, no POST. (b) **7 cards inicial** (no 11) — reducido tras `/review-plan` para shipping inicial; las 4 restantes (siri, widgets, alerts, export) en iteración posterior si ROI lo justifica. (c) Sin destructive role en alert primary — no se borra nada hasta que user tap card concreta del Chooser. (d) Returning users (`hasCompletedOnboarding=true` cached) → MainTabView directo, sin Hero, sin cambios. (e) Group invites bypassean Hero via universal link + reset state en `onChange(showGroupInviteOnboarding)`. (f) `Image("YalaLogo")` reusado del SplashScreenView/WelcomeChooserView (theme-agnostic). (g) `PBXFileSystemSynchronizedRootGroup` en `.xcodeproj` significa auto-discovery de archivos nuevos (sin tocar pbxproj). **Aprendizajes**: `/review-plan` (2 rondas) cazó 7 problemas + 4 riesgos antes de implementar (handleEmpezar recursivo bug, F0 verificación DS tokens, race CKShare en alert, 11→7 cards, post-checks no extraídos, mixto String(localized:)/L10n.X.Y, F1+F2 ordering, theme-aware logo, transition splash→Hero, a11y VoiceOver/DT, Timer→Task-based loop). F0 grep verificaciones cazaron mappings reales: `DS.Semantic` solo status colors (no marca → usar `Color.electricIndigo/hotPink/neonCyan/priorityNeed/essentialNeed` del proyecto), `DS.Gradients.heroNeutral/heroIntense` ya tienen pink→indigo, `YalaPrimaryButton(isLoading:)` API existing. Build typecheck timeout en body forzó extracción a ViewModifier (patrón GroupInviteModifier). #2 P0 (banner saldos pendientes) diferido fuera de scope (decisión user). Pendiente: device QA en físico B (cuenta poblada) + simulador A (cuenta limpia) — A4-13 + A4-21 son críticos.
