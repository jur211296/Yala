name: QA

on:
  push:
  pull_request:

jobs:
  # Barato y rápido: valida el SSOT de cobertura + el ratchet anti-drift.
  # Corre en Linux (solo necesita python3). Bloquea merge si el index regresa.
  coverage-index:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validar coverage-index (ratchet anti-drift)
        run: python3 qa/validate-coverage.py

  # Suite de tests. Runner macos-26 (Tahoe): Xcode `latest-stable` (26.x) + sim
  # iPhone 17 Pro. Build UNA vez + test-without-building.
  #
  # ESTRATEGIA (2026-06-04 — pure-logic BLOQUEANTE; context-based + UI advisory):
  #  (1) HOST CLOUDKIT CRASH (ARREGLADO, b25fc8c2): el host Yala.app arrancaba con CloudKit
  #      en sims SIN cuenta iCloud (CI) → NSCloudKitMirroringDelegate loop → proceso de test
  #      inestable (0 tests + restart loop) aunque las aserciones pasaran. Fix: YALA_TEST_MODE
  #      (isRunningTests determinista en @main) + cloudKitDatabase .none en configs de test +
  #      gate de bootstrap/sync. Eliminó la cascada del host.
  #  (2) SwiftData IN-MEMORY INSERT-TRAP: las 7 clases que crean un ModelContext in-memory
  #      hacen EXC_BREAKPOINT flaky de SwiftData en CI (víctima ALEATORIA por run; -retry no
  #      converge). Aisladas en su propio paso ADVISORY hasta migrarlas a pure-logic.
  #  (3) "Aislamiento de singletons" (hipótesis previa) DESCARTADA: el run per-suite-a-solas
  #      probó que los ~10 fallos del subset eran DETERMINISTAS (cada suite falla idéntico
  #      corriendo sola, sin contaminación cross-suite). Eran tests STALE vs cambios
  #      INTENCIONALES de producto (regla Insights eliminada, Sankey tap-filter por pool,
  #      seed Panel diferido, iCloud env sin cuenta). Arreglados en f1575df7.
  # El paso pure-logic (skip-7) es BLOQUEANTE: subset estable y determinista (verificado
  # local + CI). Las 7 context-based + YalaUITests siguen ADVISORY. Gate duro = coverage-index
  # + pure-logic. Detalle en TESTING-STRATEGY.md.
  # TODO(@jur, 2026-07-15): migrar las 7 context-based a pure-logic / target separado +
  # investigar el insert-trap SwiftData in-memory → volverlas bloqueantes también.
  tests:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Seleccionar Xcode (latest-stable 26.x)
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      # Secrets.xcconfig está gitignored (API keys); sin él el build falla. El template
      # (placeholders) basta en CI: los tests no llaman APIs reales (LLM/exchange-rate
      # son agentic, fuera de CI).
      - name: Crear Secrets.xcconfig (placeholder para CI)
        run: cp Secrets.xcconfig.template Secrets.xcconfig
      - name: Build for testing
        run: |
          xcodebuild build-for-testing \
            -scheme "Yala Dev" \
            -project Yala.xcodeproj \
            -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
            -derivedDataPath .ddp \
            -quiet
      # YalaTests SALVO las 7 clases que crean un ModelContext in-memory (insert-trap flaky
      # de SwiftData en CI — paso advisory aparte). BLOQUEANTE: el subset pure-logic es
      # determinista y estable (verificado local + CI; los fallos previos eran tests stale
      # vs cambios intencionales de producto, arreglados en f1575df7 — NO aislamiento).
      # `-retry-tests-on-failure` se mantiene como red para flakes transitorios puntuales
      # (p.ej. tests async con timeout). Ver TESTING-STRATEGY.md.
      - name: Unit tests (YalaTests pure-logic) — BLOQUEANTE
        run: |
          xcodebuild test-without-building \
            -scheme "Yala Dev" \
            -project Yala.xcodeproj \
            -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
            -only-testing:YalaTests \
            -skip-testing:YalaTests/BudgetFilterRegressionTests \
            -skip-testing:YalaTests/CategoryDeduplicationServiceTests \
            -skip-testing:YalaTests/ChatAssistantViewModelTests \
            -skip-testing:YalaTests/CurrencyConverterCacheTests \
            -skip-testing:YalaTests/ICloudAccountSummaryTests \
            -skip-testing:YalaTests/InitialBalanceServiceTests \
            -skip-testing:YalaTests/UserSegmentServiceTests \
            -derivedDataPath .ddp \
            -parallel-testing-enabled NO \
            -retry-tests-on-failure \
            -quiet
      # ADVISORY: las 7 clases context-based (crean ModelContext in-memory). Corren para
      # visibilidad (cuando pasan confirman no-regresión); no bloquean por el insert-trap
      # flaky de SwiftData en el runner de CI. Plan de migración en TESTING-STRATEGY.md.
      - name: Unit tests (YalaTests context-based) — advisory (SwiftData insert-trap flaky; ver Lista Negra)
        continue-on-error: true
        run: |
          xcodebuild test-without-building \
            -scheme "Yala Dev" \
            -project Yala.xcodeproj \
            -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
            -only-testing:YalaTests/BudgetFilterRegressionTests \
            -only-testing:YalaTests/CategoryDeduplicationServiceTests \
            -only-testing:YalaTests/ChatAssistantViewModelTests \
            -only-testing:YalaTests/CurrencyConverterCacheTests \
            -only-testing:YalaTests/ICloudAccountSummaryTests \
            -only-testing:YalaTests/InitialBalanceServiceTests \
            -only-testing:YalaTests/UserSegmentServiceTests \
            -derivedDataPath .ddp \
            -parallel-testing-enabled NO \
            -retry-tests-on-failure \
            -quiet
      - name: UI tests (YalaUITests) — advisory (flaky en runner frío, ver Lista Negra)
        continue-on-error: true
        run: |
          xcodebuild test-without-building \
            -scheme "Yala Dev" \
            -project Yala.xcodeproj \
            -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
            -only-testing:YalaUITests \
            -derivedDataPath .ddp \
            -parallel-testing-enabled NO \
            -retry-tests-on-failure \
            -quiet
