---
phase: 07-beta-preparation
plan: 06
type: execute
---

<objective>
Asegurar estabilidad pre-release: error handling robusto y validaciones completas.

Purpose: App estable que no crashea y previene datos inválidos.
Output: Error handling consistente, validaciones implementadas.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-phase.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PHASE7-BETAPREP-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revisar error handling en operaciones SwiftData</name>
  <files>Neto/App/ViewModels/*.swift, Neto/App/Logic/**/*.swift</files>
  <action>
    1. Buscar operaciones de save/delete/fetch:
       - modelContext.save()
       - modelContext.delete()
       - FetchDescriptor
    2. Verificar que cada operación tiene manejo de errores:
       - try/catch apropiado
       - No force unwrap (!) en resultados
       - Logging de errores para debugging
    3. Buscar force unwraps peligrosos:
       - grep -rn "!" Neto/App/ | grep -v "//\|!=\|!="
    4. Reemplazar force unwraps con:
       - Optional binding (if let, guard let)
       - Nil coalescing (??)
       - Valores default donde tenga sentido
    5. Asegurar que errores no crashean la app
  </action>
  <verify>grep de force unwraps peligrosos devuelve mínimos (solo donde es seguro)</verify>
  <done>Error handling robusto en operaciones de datos</done>
</task>

<task type="auto">
  <name>Task 2: Implementar validaciones de entrada de datos</name>
  <files>Neto/App/Views/Transactions/NewTransactionView.swift, Neto/App/Views/Budgets/*.swift, Neto/App/Views/Scheduled/*.swift</files>
  <action>
    1. Revisar formularios de creación/edición:
       - NewTransactionView: amount != 0, account != nil, subcategory != nil
       - BudgetFormView: amount > 0, categorías seleccionadas
       - ScheduledPaymentFormView: campos requeridos completos
    2. Verificar que botones de guardar están deshabilitados si validación falla
    3. Verificar que se muestran mensajes de error claros
    4. Asegurar que no es posible guardar datos inválidos
    5. Añadir validaciones faltantes si las hay
  </action>
  <verify>No es posible guardar entidades con datos requeridos faltantes</verify>
  <done>Validaciones completas en todos los formularios</done>
</task>

<task type="auto">
  <name>Task 3: Verificar comportamiento offline</name>
  <files>Neto/NetoApp.swift, archivos relacionados con CloudKit</files>
  <action>
    1. Verificar configuración de SwiftData:
       - ModelContainer configurado correctamente
       - CloudKit sync habilitado pero no requerido
    2. Verificar que la app funciona sin conexión:
       - Datos locales accesibles
       - Creación/edición funciona
       - No hay crashes por falta de red
    3. Si hay código que asume conectividad, añadir checks:
       - Network reachability check donde sea necesario
       - Graceful degradation
    4. Documentar comportamiento offline
  </action>
  <verify>App funciona completamente sin conexión a internet</verify>
  <done>Comportamiento offline verificado y documentado</done>
</task>

</tasks>

<verification>
Antes de declarar plan completo:
- [ ] No hay force unwraps peligrosos
- [ ] Todas las operaciones de datos tienen error handling
- [ ] Formularios validan datos requeridos
- [ ] App funciona offline
</verification>

<success_criteria>
- Error handling robusto
- Validaciones completas
- Comportamiento offline funcional
</success_criteria>

<output>
Crear `.planning/phases/07-beta-preparation/07-06-SUMMARY.md` con:
- Force unwraps removidos/justificados
- Validaciones añadidas
- Comportamiento offline documentado
</output>
