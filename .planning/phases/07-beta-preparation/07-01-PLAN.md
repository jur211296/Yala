---
phase: 07-beta-preparation
plan: 01
type: execute
---

<objective>
Limpiar código: eliminar TODOs, código muerto, y asegurar consistencia en naming conventions.

Purpose: Código limpio y mantenible antes de release público.
Output: Build sin warnings, código revisado y consistente.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-phase.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PHASE7-BETAPREP-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar y resolver TODOs/FIXMEs</name>
  <files>Todos los archivos Swift en Neto/</files>
  <action>
    1. Ejecutar: grep -rn "TODO\|FIXME" Neto/
    2. Para cada resultado:
       - Si es relevante y pendiente: resolver o crear issue en ISSUES.md
       - Si está obsoleto: eliminar
       - Si es informativo: convertir a comentario normal sin TODO/FIXME
    3. Documentar decisiones en el summary
  </action>
  <verify>grep -rn "TODO\|FIXME" Neto/ devuelve 0 resultados o solo items documentados en ISSUES.md</verify>
  <done>No hay TODOs/FIXMEs sin resolver o documentar</done>
</task>

<task type="auto">
  <name>Task 2: Eliminar código muerto y comentado</name>
  <files>Todos los archivos Swift en Neto/</files>
  <action>
    1. Buscar funciones/vistas no referenciadas (usar Xcode "Find Call Hierarchy" o grep)
    2. Buscar bloques de código comentado (// grandes bloques)
    3. Eliminar código que no se usa
    4. Eliminar bloques comentados (el historial está en git)
    5. Limpiar imports no usados (revisar warnings de Xcode)
  </action>
  <verify>xcodebuild -scheme Neto -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | grep -i "unused\|dead code" devuelve vacío</verify>
  <done>No hay código muerto ni bloques comentados significativos</done>
</task>

<task type="auto">
  <name>Task 3: Revisar naming conventions y warnings</name>
  <files>Archivos con naming inconsistente</files>
  <action>
    1. Build completo y revisar TODOS los warnings
    2. Resolver cada warning (no ignorar)
    3. Revisar enums: nombres y casos deben reflejar uso actual
    4. Revisar ViewModels: métodos con nombres claros de acción
    5. Revisar que no haya nombres legacy que ya no aplican
    6. Asegurar consistencia: PascalCase clases, camelCase funciones/props
  </action>
  <verify>xcodebuild -scheme Neto -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | grep -c "warning:" devuelve 0</verify>
  <done>Build con 0 warnings, naming consistente en todo el proyecto</done>
</task>

</tasks>

<verification>
Antes de declarar plan completo:
- [ ] `grep -rn "TODO\|FIXME" Neto/` sin resultados pendientes
- [ ] Build sin warnings
- [ ] No hay código comentado en bloques grandes
</verification>

<success_criteria>
- Todas las tareas completadas
- Build limpio sin warnings
- Código revisado y consistente
</success_criteria>

<output>
Crear `.planning/phases/07-beta-preparation/07-01-SUMMARY.md` con:
- TODOs encontrados y resolución de cada uno
- Código eliminado (lista de archivos/funciones)
- Warnings resueltos
- Cambios de naming realizados
</output>
