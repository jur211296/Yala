---
phase: 07-beta-preparation
plan: 02
type: execute
---

<objective>
Auditar localizaciones y añadir monedas para beta testers internacionales.

Purpose: App lista para usuarios en diferentes regiones con sus monedas.
Output: 0 hardcodes, 7 monedas soportadas (PEN, USD, EUR, MXN, COP, BRL, GBP).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-phase.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PHASE7-BETAPREP-SPEC.md
@Neto/Utils/CurrencyUtils.swift
@Neto/Utils/L10n.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar strings hardcodeados</name>
  <files>Neto/App/Views/**/*.swift</files>
  <action>
    1. Buscar strings literales en vistas:
       - grep -rn 'Text("' Neto/App/Views/ | grep -v 'String(localized'
       - grep -rn '\.navigationTitle("' Neto/App/Views/
       - grep -rn 'Label("' Neto/App/Views/ | grep -v 'String(localized'
    2. Para cada hardcode encontrado:
       - Crear key en L10n.swift
       - Añadir traducción en los 6 archivos .strings
       - Reemplazar hardcode por L10n.key
    3. Verificar formato de números y fechas usan Locale
  </action>
  <verify>Los comandos grep anteriores devuelven 0 resultados de hardcodes</verify>
  <done>No hay strings hardcodeados en vistas</done>
</task>

<task type="auto">
  <name>Task 2: Añadir 4 monedas nuevas (MXN, COP, BRL, GBP)</name>
  <files>Neto/Utils/CurrencyUtils.swift, Neto/Utils/L10n.swift, Neto/Resources/*.lproj/Localizable.strings</files>
  <action>
    1. En CurrencyUtils.swift:
       - Añadir casos al enum CurrencyCode: mxn, cop, brl, gbp
       - Añadir normalización en normalizeCurrencyCode() para cada moneda
       - Añadir tasas fallback en rateToPEN(): MXN~0.22, COP~0.0009, BRL~0.75, GBP~4.70
       - Añadir info en currencyInfo() con nombre localizado y bandera
    2. En L10n.swift:
       - Añadir keys: Currency.mxn, Currency.cop, Currency.brl, Currency.gbp
    3. En cada Localizable.strings (es, en, fr, de, it, pt):
       - Añadir: "currency.mxn" = "Peso mexicano" (y equivalentes)
       - Añadir: "currency.cop" = "Peso colombiano"
       - Añadir: "currency.brl" = "Real brasileño"
       - Añadir: "currency.gbp" = "Libra esterlina"
  </action>
  <verify>Build exitoso y CurrencyCode.allCases.count == 7</verify>
  <done>7 monedas disponibles en selector de moneda</done>
</task>

<task type="auto">
  <name>Task 3: Verificar keys en todos los idiomas</name>
  <files>Neto/Resources/*.lproj/Localizable.strings</files>
  <action>
    1. Extraer todas las keys del archivo base (es):
       grep -o '"[^"]*"' Neto/Resources/es.lproj/Localizable.strings | head -1 | sort > /tmp/keys_es.txt
    2. Para cada idioma (en, fr, de, it, pt):
       - Extraer keys
       - Comparar con base
       - Añadir keys faltantes
    3. Verificar que no hay keys huérfanas (en .strings pero no usadas en código)
  </action>
  <verify>diff de keys entre idiomas devuelve vacío</verify>
  <done>Todos los idiomas tienen las mismas keys</done>
</task>

</tasks>

<verification>
Antes de declarar plan completo:
- [ ] grep de hardcodes devuelve 0
- [ ] CurrencyCode tiene 7 casos
- [ ] Todos los .strings tienen las mismas keys
- [ ] Build exitoso
</verification>

<success_criteria>
- 0 strings hardcodeados en vistas
- 7 monedas soportadas
- Localizaciones sincronizadas
</success_criteria>

<output>
Crear `.planning/phases/07-beta-preparation/07-02-SUMMARY.md` con:
- Hardcodes encontrados y resueltos
- Monedas añadidas con sus tasas
- Keys añadidas por idioma
</output>
