node_modules/
.wrangler/
.dev.vars
dist/
*.log
.DS_Store

# Copied at build/test time from ../capability_manifest.json (repo-root SSOT of the sync
# capability-set). Bundled into the Worker; regenerated by `npm run sync:manifest`.
capability_manifest.json
