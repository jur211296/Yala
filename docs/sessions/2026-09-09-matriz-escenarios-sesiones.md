---
updated: 2026-09-09
tags: [sesiones, onboarding, cierres, matriz, verificacion-de-spec]
estado: referencia del ADR 2026-09-09 «Sesiones — dos ejes»; se archiva cuando `shell-derives-from-two-session-axes` esté en 2.1
---

# Matriz de escenarios del modelo de sesiones — ¿qué ticket cubre cada celda?

> Pedida por Jürgen el 2026-09-09 tras ver en su móvil «Aquí ya hay datos guardados» al intentar crear
> un grupo: «escenarios como ese son los que me preocupan». Cada fila es un **estado del móvil × una
> acción**, lo que el ADR espera, y el ticket que lo cubre. Las filas marcadas **HUECO** no estaban
> cubiertas al escribir los 13 tickets y se añadieron a su ticket el mismo día (columna «añadido en»).
> Lo que dice «hoy» está medido en `3a94604e`.

## Estados del móvil

| # | Estado | Cómo se llega |
|---|---|---|
| A | Instalación fresca: sin store, sin sesión | instalar |
| B | Store con espejo de iCloud y datos importados, onboarding NO completado (Welcome visible) | A → «privado» o «Restaurar» → reabrir → volver atrás / matar la app |
| C | Sesión privada activa, sin grupos | onboarding completado en iCloud |
| D | Sesión privada + cuenta de grupos asociada («equipo») | C → asociar |
| E | Sesión en la nube completa | born-cloud, o migración desde C |
| F | Sesión en la nube solo grupos (sin privada) | «Vengo por un grupo» |
| G | Tras «Cerrar sesión»: neutro duradero, iCloud con datos | C/D/E/F → cerrar sesión |
| H | Tras «Vaciar datos» | cualquiera → vaciar |
| I | Restaurando de iCloud (datos bajando) | «Ya tengo cuenta → iCloud» → Continuar |
| J | Kill-switch remoto de nube (`cloudModeRolloutPercent = 0`) | decisión de Jürgen en el gateway |
| K | iCloud no disponible en el dispositivo (sin cuenta / desactivado) | ajustes del teléfono |
| L | Faro puesto (este Apple ID ya tuvo cuenta en la nube) | cualquier alta nube previa con este Apple ID |
| M | Sin red | — |
| N | Segundo dispositivo del mismo Apple ID | — |

## Filas

| Estado × acción | Lo que el ADR espera | Cubre | Hueco / añadido en |
|---|---|---|---|
| A · Primera vez → privado, iCloud con datos | validar → alert doble → borrar+reinicio+onboarding limpio / cancelar→chooser | `welcome-private-fresh-start-skips-icloud-check` | **en `2.1` 2026-09-10**, verificada por unit (`WelcomePrivateICloudGateTests`, 5 suites / 32 casos, 3 mutantes). El recorrido con CloudKit real espera **device-QA** (guion en `tickets/qa/`). El aviso tiene **tres** salidas, no dos: borrar (2.º gesto), **restaurar** y cancelar |
| A · Primera vez → privado, iCloud vacío | onboarding directo (reinicio si hace falta) | ídem | **en `2.1` 2026-09-10**, verificada por unit (`WelcomePrivateICloudGateTests`, 5 suites / 32 casos, 3 mutantes). El recorrido con CloudKit real espera **device-QA** (guion en `tickets/qa/`) |
| A · Primera vez → privado, **K** (sin iCloud) | no se puede validar: informar y seguir en local (`.localNoMirror`), como hace «Restaurar» con `.iCloudDisabled` | ídem | **HUECO** → añadido · **en `2.1` 2026-09-10**, verificada por unit (`WelcomePrivateICloudGateTests`, 5 suites / 32 casos, 3 mutantes). El recorrido con CloudKit real espera **device-QA** (guion en `tickets/qa/`). Deja un **testigo** (`cloudSync.privateChoseWithoutICloud`): la validación no se pierde, se aplaza |
| A · Primera vez → privado, matar la app entre «borrar» y el reinicio | el borrado y el destino son kill-safe (armar + boot), nunca onboarding sobre datos a medio borrar | ídem | **HUECO** → añadido · **en `2.1` 2026-09-10**, verificada por unit (`WelcomePrivateICloudGateTests`, 5 suites / 32 casos, 3 mutantes). El recorrido con CloudKit real espera **device-QA** (guion en `tickets/qa/`). El arm arma **también** el neutro durable: sin eso el arranque siguiente montaría `.iCloudMirror` e importaría el corpus que se estaba borrando |
| **K → C** · activar iCloud DESPUÉS de un onboarding hecho sin él | el espejo se adjunta y trae el corpus viejo: **mismo aviso, con las mismas cifras**, y dos salidas — «déjalo así» (conviven, ahora avisado) o borrar de iCloud con 2.º gesto y onboarding limpio | `welcome-private-fresh-start-skips-icloud-check` | **HUECO** encontrado el 2026-09-09 → cubierto el 2026-09-10. La tercera salida del aviso de A («restaurar») **no existe aquí y no es una salida perdida**: el espejo ya los está trayendo, así que «son míos» y «déjalo así» son el mismo desenlace. **«Cancelar» = déjalo así, decisión de Jürgen del 2026-09-10** |
| A · Primera vez → nube (Apple/Google) | consentimiento → [I]: nueva → crear `complete` → [P]; completa → adopta; solo grupos → entra y ofrece activar | `cloud-sign-in-discovers-account-kind` + born-cloud existente | **[I] en `2.1` 2026-09-10**, verificada por unit (`CloudIdentityRoutingLogicTests` + `CloudIdentityDiscoveryTests`); el recorrido espera device-QA contra staging **y el deploy del Worker** — hoy `kind` no se sirve. La **oferta** de «Activar Yala completo» **en `2.1` 2026-09-11** (ticket 8): al quedar montada la sesión solo-grupos se abre la activación, con su chooser; verificada por scan de cableado (`FullModeActivationWiringTests`) |
| A · Primera vez con **L** (faro) | encamina a entrar con esa cuenta + «Crear otra cuenta» visible | `beacon-routes-only-never-blocks` | **en `2.1` 2026-09-10**, verificada por unit (`WelcomeNewBranchRouteTests`) + XCUITest (`WelcomeChooserUITests.testNewBranch_withBeacon_…`, faro fingido solo en lectura): encamina, y «Crear otra cuenta» abre el chooser ENTERO, privado incluido (decisión de Jürgen). El intro nombra el método del faro («creada con Apple») y, si el faro no lo sabe, no afirma ninguno. El recorrido con faro real espera device-QA (guion en `tickets/qa/`) |
| A · Ya tengo cuenta → iCloud | reinicio → buscar → found: continuar / desde cero; notFound; cloudPaused (J); iCloudDisabled (K) | existente + `reentry-*` | — |
| A · Ya tengo cuenta → Apple/Google | [I]: completa → adopta; solo grupos → entra F; no existe → botón a «Primera vez → nube» | `cloud-sign-in-discovers-account-kind` | **[I] en `2.1` 2026-09-10**, verificada por unit (`CloudIdentityRoutingLogicTests` + `CloudIdentityDiscoveryTests`); el recorrido espera device-QA contra staging **y el deploy del Worker** — hoy `kind` no se sirve. El botón del `.notFound` lleva al **consentimiento** con el proveedor ya elegido |
| A · Ya tengo cuenta → Google con **L** de Apple, cuenta inexistente | informa «se creó con Apple» + dos salidas (entrar con Apple / crear con Google) | `beacon-routes-only-never-blocks` | **en `2.1` 2026-09-10**, verificada por unit (`ProviderMismatchLogicTests`: `.mismatch` lleva sus dos salidas, con un invariante sobre toda la tabla) + scan del emparejamiento salida↔verbo (`WelcomeSignInVerbTests`). «Entrar con Apple» no vuelve a pedir el consentimiento (misma ruta ya consentida); «Crear con Google» pasa por el del alta. La pantalla con sign-in real espera device-QA |
| A · Vengo por un grupo → crear | canal on → [I] → nueva: [G]; solo grupos: entra; completa: entra completa + Grupos. Store sin espejo en TODOS los arranques | `groups-only-second-launch-mounts-icloud-mirror` + `cloud-sign-in-discovers-account-kind` | **la mitad [I] en `2.1` 2026-09-10**, verificada por unit. Con relanzamiento, quien venía a CREAR aterriza en el Panel: el intent del organizador no es durable (ticket propio) |
| A · Vengo por un grupo → invitación (link o pegar) | igual que crear, con «unirme» forzado al terminar | ídem + `GroupBackendInviteEntryLogic` existente | invitación nombrada explícitamente → añadido en el ticket del mount |
| **B** · Vengo por un grupo (crear o invitación) | **sin bloqueo**: vuelta al neutro (borra local, iCloud intacto, relanza) y sigue | `groups-only-second-launch-mounts-icloud-mirror` | era el hueco de la captura de Jürgen → añadido |
| **B** · Primera vez → privado | = A: validar contra iCloud (no contra el store) → alert | `welcome-private-fresh-start-skips-icloud-check` | **en `2.1` 2026-09-10**, verificada por unit (`WelcomePrivateICloudGateTests`, 5 suites / 32 casos, 3 mutantes). El recorrido con CloudKit real espera **device-QA** (guion en `tickets/qa/`) |
| **B** · Ya tengo cuenta → iCloud | restaurar (los datos ya están bajando: `found`) | existente | — |
| **I** · Vengo por un grupo | cancelar la señal de restore, vuelta al neutro, seguir | ticket del mount | **HUECO** → añadido |
| C · Grupos (pestaña) → asociar cuenta | [I]: nueva/solo grupos → [G] → asociada; **completa → bloqueo** con 2 salidas | `cloud-sign-in-discovers-account-kind` + `groups-account-association-in-storage-row` | **el bloqueo en `2.1` 2026-09-10** (sin escribir nada, verificado por unit). Su 2ª salida es TEXTO y no botón: no hay intent que navegue a la fila de Ajustes (ticket propio) |
| C · llega una **invitación** (link) | misma regla que asociar: [I] → asociar → unirme; completa → bloqueo | `cloud-sign-in-discovers-account-kind` | **HUECO** (solo cubría el Welcome) → añadido · **cerrado en `2.1` 2026-09-10**: la invitación sobrevive al bloqueo en `PendingJoinStore` (TTL 7 d) y se retoma con otra cuenta |
| C · Ajustes → «migrar a la nube» | [I]: nueva → cutover existente (E); solo grupos (mi asociada) → **promover ESA cuenta** + cutover; completa → bloqueo | `cloud-sign-in-discovers-account-kind` + `groups-account-association-in-storage-row` + backend | **HUECO** → añadido a los tres · **2026-09-10: sus 3 celdas están en la tabla [I] con test, pero el CABLEADO no** — esa puerta no consulta `exists` y su sign-in ocurre dentro de `startMigration`; «cuál es mi asociada» lo persiste el ticket 10. Ticket propio · **2026-09-16: cableada** (`settings-migrate-to-cloud-adopts-silently-instead-of-migrating`, en `qa`): la puerta pregunta antes del claim —al tocar con la sesión de grupos, tras firmar sin ella— y el claim ya no adopta con «Migrar». Tres cambios de Jürgen sobre la celda: solo grupos **sin ninguna** asociada también migra; nueva con **otra** asociada bloquea; la cuenta que volvió a iCloud para en el claim con su aviso. Verificada por unit (`StorageMigrationIdentityGateLogicTests`, `MigrationRunnerTests` §10c, `MigrationIdentityGateWiringTests`) + XCUITest (`StorageMigrationIdentityBlockUITests`); falta device-QA |
| C · Cerrar sesión | **espera al último export a CloudKit** → borra local → neutro duradero → Welcome. Sin red: «un momento más», nunca borra sin subir | `session-exits-one-verb-per-session` | **HUECO grave** (medido: nadie espera al export hoy; `.privateReset` no borraba y por eso no lo necesitaba) → añadido · **en `2.1` 2026-09-11 (paso 9)**, verificada por unit (`PrivateSignOutExportGateLogicTests`: canal de copia, filtro, veredicto y bucle con reloj inyectado · `PersonalExportPendingCounterTests`: historial real on-disk · `PrivateSignOutWiringTests`: orden, recuento pegado al arm, nunca filas) + XCUITest (`SessionExitsPerCellUITests`, celda C). Borra por ARCHIVOS pre-mount (el mismo boot-wipe de la nube) y relanza. Agotada la espera: aviso con el número y salida avisada (decisión de Jürgen). Sin iCloud: hoja «no hay copia» + segundo gesto, sin espera. El testigo con CloudKit real espera **device-QA** (guion en `tickets/qa/`, empieza por medir sus tres supuestos) |
| C · Vaciar datos | local + iCloud; grupos no aplica; → onboarding | ídem | **en `2.1` 2026-09-11 (paso 9)**: la hoja nombra el iPad, el Mac y todo dispositivo del Apple ID (decisión de Jürgen); aterriza en el onboarding personal sin pasar por el Welcome. Tras la review adversarial, la señal a los otros dispositivos del Apple ID solo sale de una sesión PRIVADA (`wipeSignalsAppleIDDevices`); desde la nube o solo-grupos vaciaba el iPad privado del dueño. Verificada por unit; iCloud → `notFound` espera device-QA |
| C · cambia el Apple ID del teléfono | la sesión privada termina (es del Apple ID): cerrar sesión. Hoy `AppBootstrapper.checkForICloudMismatch` avisa; alinear | `shell-derives-from-two-session-axes` | **HUECO** → añadido |
| D · Cerrar sesión | pushAll grupos verificado → export CloudKit → cerrar nube → borrar local → Welcome | `session-exits-one-verb-per-session` | export CloudKit añadido · **en `2.1` 2026-09-11 (paso 9)**, verificada por unit + XCUITest (celda D, hoja `signout_private_with_groups`). Grupos sin salida de emergencia («nunca descarta»); una segunda subida tras la espera recoge lo escrito mientras; con el kill remoto y filas pendientes el cierre espera. Device-QA en `tickets/qa/` |
| D · Desasociar | grupos se van; **se PREGUNTA** qué pasa con las filas puenteadas (decisión de Jürgen del 9-sep, deroga el «enlace dormido») | `groups-account-association-in-storage-row` | **en `2.1` 2026-09-11 (paso 10)**, verificada por unit (`GroupsAssociationLogicTests` · `GroupsAccountAssociationStoreTests` · `GroupsAssociationRegistrarTests` · `GroupsAssociationDetachSweepTests` · `GroupsDetachedBridgeLedgerTests`) + XCUITest (`GroupsAssociationRowUITests`, tres celdas). El recorrido con cuenta real espera **device-QA** (guion en `tickets/qa/`). **El enlace dormido NO se conserva y es deliberado**: con el puntero puesto y sin filas de grupo, el movimiento queda ATRAPADO (ni editable ni borrable) y ningún barrido lo repara. Conservar libera los punteros y registra el gasto en un libro sellado por cuenta |
| D · Re-asociar la misma / otra cuenta | misma: **cero duplicados**; otra: no toca | ídem | **en `2.1` 2026-09-11 (paso 10)**, verificada por unit (`GroupsAssociationLogicTests` · `GroupsAccountAssociationStoreTests` · `GroupsAssociationRegistrarTests` · `GroupsAssociationDetachSweepTests` · `GroupsDetachedBridgeLedgerTests`) + XCUITest (`GroupsAssociationRowUITests`, tres celdas). El recorrido con cuenta real espera **device-QA** (guion en `tickets/qa/`). Lo que la misma cuenta recupera es la ausencia de duplicado, no el ENLACE: `TransactionItem` no tiene identidad serializable con la que anclarlo y el campo que la daría cuesta un deploy de schema. Ticket propio: `groups-reassociation-does-not-restore-the-bridge-link` |
| D · Vaciar datos | personal local + iCloud; **grupos y asociación intactos** → onboarding [P] → sigue D | `session-exits-one-verb-per-session` | **HUECO** (no decía qué pasa con la asociación) → añadido · **en `2.1` 2026-09-11 (paso 9)**: la sesión de grupos y los grupos no se tocan; aterriza en el onboarding personal. Retirada «Seguir con mis grupos» |
| D · Eliminar mi cuenta (la de grupos) | en «Tu cuenta de Yala» de la cuenta asociada: desasociar + borrar en backend; personal intacto | ídem + asociación | **HUECO** → añadido · **en `2.1` 2026-09-11 (paso 9)**: sale de la lista principal y vive en «Tu cuenta de Yala» (dos toques); el cierre local conserva lo personal. La asociación como estado propio es del ticket 10 |
| D · Ajustes → «migrar a la nube» | promover la asociada a `complete` + cutover → E | ver fila C·migrar | añadido · cableada con la fila C·migrar (2026-09-16) |
| D · **N** (segundo móvil) → Ya tengo cuenta → iCloud | restaura personal y **ofrece entrar con la cuenta de grupos asociada** (la asociación viaja por iCloud-KV) | `groups-account-association-in-storage-row` | **HUECO** (la asociación se persistía solo local) → añadido · **en `2.1` 2026-09-11 (paso 10)**, verificada por unit (`GroupsAssociationLogicTests` · `GroupsAccountAssociationStoreTests` · `GroupsAssociationRegistrarTests` · `GroupsAssociationDetachSweepTests` · `GroupsDetachedBridgeLedgerTests`) + XCUITest (`GroupsAssociationRowUITests`, tres celdas). El recorrido con cuenta real espera **device-QA** (guion en `tickets/qa/`). La fila ofrece entrar con esa cuenta y el empty state de Grupos dice «vuelve a tu cuenta» en vez de «crea una». **El desasociar deja tombstone**: sin él, el dispositivo que no lo pidió reponía la asociación en su arranque siguiente |
| E · Cerrar sesión | = hoy (`cloudSecureSignOut`) | existente | — |
| E · «Volver a iCloud» (reverse cutover existente) | lo personal vuelve a iCloud (C); la cuenta pasa a `groups_only` y queda **asociada** si tiene grupos (D). Única degradación permitida | `backend-account-kind-complete-or-groups-only` (decía «no hay degradación») | **HUECO / contradicción** → añadido · **la mitad del backend, verificada 2026-09-10** por `g15_01` (`reverse_complete` escribe `kind='groups_only'`; el cliente refresca en `handleReverseCutoverCompleted`). ✅ **RECORRIBLE desde el 2026-09-10**: Jürgen abrió la reversa a born-cloud y `g15_02` cambió el guard de `reverse_claim` de `migrated_at` a `kind='complete'` (aplicado en los dos entornos), con el gate del cliente `ReverseEligibility` exigiendo mapa CloudKit **solo a quien migró**. Queda el device-QA de CloudKit real: que el mirror exporte por primera vez un corpus born-cloud **no está medido** en este repo (`WelcomeMirrorRelaunchLogic`) |
| E · Eliminar mi cuenta | en «Tu cuenta de Yala» (5.1.1 v) | `session-exits-one-verb-per-session` | **en `2.1` 2026-09-11 (paso 9)**: fuera de la lista principal; `DeleteAccountDialogUITests` entra por «Tu cuenta de Yala». Y se abrió también al solo-grupos (F), que no la tenía |
| E · Vaciar datos | contenido de la cuenta; grupos no | ídem | **en `2.1` 2026-09-11 (paso 9)**: aterriza en el onboarding personal; ya no avisa por el iCloud KV del Apple ID (la cuenta propaga sus borrados por el motor) |
| F · Activar Yala completo → privado | chooser → validar iCloud → alert / **o restaurar** → [P] → D | `full-mode-activation-must-ask-where-personal-data-lives` | «restaurar» como tercera salida: ratificada por Jürgen el 2026-09-09 · **en `2.1` 2026-09-11**, verificada por unit (`FullModeActivationFlowLogicTests`: pantallas, orden del cierre, reanudación y cableado) + XCUITest (`FullModeActivationChooserUITests`: el chooser antes que cualquier onboarding; privado → onboarding). El borrado es SOLO de la zona de iCloud: lo local de un solo-grupos nunca vino de iCloud. La sonda, el relanzamiento y restaurar con CloudKit real esperan **device-QA** (guion en `tickets/qa/`). Tras la review adversarial: la convergencia de los gastos de grupo tras restaurar, verificada contra el bridge real (`GroupsBridgeRestoreConvergenceTests`: la transacción real restaurada sobrevive; con el modo aún solo-grupos se borraría); cancelar un Restaurar ya relanzado NO vuelve a solo-grupos; y una instalación solo-grupos anterior al paso 5 ve un aviso de reinstalar en vez del chooser |
| F · Activar Yala completo → nube | consentimiento → [P] → promover a `complete` (**último paso**, decisión de Jürgen) → E | ídem + backend | **en `2.1` 2026-09-11**, verificada por unit (el plan: la promoción antes de cualquier escritura, y el par `.cloud` antes de persistir [P]). `existing_stable` bloquea sin escribir nada (ticket `full-activation-cloud-adopt-when-account-already-complete`). La promoción real espera **device-QA**. Si se pierde la respuesta del claim, «Reintentar» bloquea (ticket `claim-promotion-lost-response-blocks-the-retry`) |
| F · Cerrar sesión | pushAll grupos → borrar local → neutro | `session-exits-one-verb-per-session` | **en `2.1` 2026-09-11 (paso 9)**, verificada por unit + XCUITest (celda F, hoja `signout_groups_only`): borra personal + sync-meta + grupos por archivos y vuelve al Welcome (antes reabría en la shell de grupos sin sesión). Corregido de paso: su puerta de quiescencia esperaba un «primer import» que un store neutro no emite, y con iCloud Drive activo el cierre no terminaba nunca |
| F · llega una invitación | unirse con la sesión activa | existente | — |
| F · Ya tengo cuenta → iCloud (quiere sus datos privados) | no se ofrece desde F: cerrar sesión → Welcome → restaurar → asociar de nuevo (una nube activa, conmutable) | modelo | — (por diseño) |
| G · cualquier puerta | = A, con neutro duradero ya armado | existente (R4) | — |
| H · tras vaciar en C/D/E | onboarding [P]; en D la asociación sigue | `session-exits-one-verb-per-session` | añadido (D) · **en `2.1` 2026-09-11 (paso 9)**: `DestructiveScopeLogic.wipeLanding` — C/D/E al onboarding personal, F sigue en su shell de grupos |
| **J** · puertas de nube | se ocultan; «Restaurar» dice «nube en pausa» al que tiene faro; grupos con su propio kill | `reentry-killswitch-closes-both-doors` (qa) + política ratificada 6-sep | — |
| **L** · dos cuentas creadas desde el mismo Apple ID | el faro guarda la última reclamada; encamina a esa y sigue ofreciendo «crear otra» | `beacon-routes-only-never-blocks` | nota añadida · **en `2.1` 2026-09-10**: el claim `created` reescribe el faro entero (método + hash), así que guarda la última reclamada sin código nuevo; «Crear otra cuenta» sale siempre en el destino. Device-QA, recorrido 3 |
| **M** · cualquier [I] | error reintentable, sin crear ni borrar nada | existente | — |
| **M** · Cerrar sesión (C/D/E/F) | bloqueado con «un momento más» hasta subir (grupos) / exportar (CloudKit) | `session-exits-one-verb-per-session` | export añadido · **en `2.1` 2026-09-11 (paso 9)**: sin red, la espera del export se agota a los 45 s y el aviso cuenta lo que no llegó («no pudimos confirmar» si no hay número honesto) con salida avisada; los grupos se bloquean sin salida |
| Faro stale (cuenta borrada server-side) | [I] → «no existe» → botón a crear | `restore-beacon-outlives-account-deletion` (kept) + [I] | **en `2.1` 2026-09-10** (`beacon-routes-only-never-blocks`): [I] además LIMPIA el faro cuando lo prueba —mismo hash, o faro de Apple + sesión de Apple sin cuenta— (`BeaconOrphanLogic`, verificada por unit + `CloudIdentityDiscoveryTests`). Con Google y otro hash no hay prueba y el faro se queda; bajo el kill no corre [I]. Por eso `restore-beacon-outlives-account-deletion` sigue abierto |
| Widgets / Siri / Apple Pay / notificaciones en F y tras cerrar sesión | por medir | `after-session-redesign-review-…` | — (después) |

## Lo que la matriz NO cubre a propósito

- El detalle de cada pantalla (copy, layout): va en cada ticket y en `BRAND-VOICE.md`.
- Escenarios de la sesión de visita (M1): retirada por el ADR, ratificada el 2026-09-09.
