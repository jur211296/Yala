# Device QA 2026-08-26

- Check Pro (`subscription-success-without-pro`) cerrado: TF 2.1 build 12, cuenta día a día. Estaba Free. Manage Subscriptions aplicó Pro. Force-quit (swipe up) + reopen: sigue Pro. No se re-probó el sheet de compra in-app desde Free en esta sesión. Cierre por persistencia de entitlement (el fallo en build 11 era force-quit sigue Free + restore silencioso).
- Siguiente en cola: Tendencias comparativa (`trends-comparison-kpi-vs-curve`).
- Hallazgo nuevo: KPI de Balance distinto en Panel vs Distribución (cuenta multi-moneda). Ticket backlog `distribution-balance-kpi-skips-fx`. Números exactos TBD.
- HOLD: A7/M5, store, tag.
- `distribution-balance-kpi-skips-fx`: causa en código 2026-08-26. Panel Balance KPI = `LiveBalanceCalculator` (nativo × TC actual). Distribución hero/header = `TopSpendingCategoriesCalculator` (gasto del período, `amountInPreferredCurrency` histórico). La mitad “suma nativo” queda refutada. Sigue backlog; números TBD; no PASS.
- Jurgen (TF 2.1 build 12): Tendencias Comparativa (Este mes, 3 métricas) OK en device-QA; widget Panel de Comparativa pendiente — `trends-comparison-kpi-vs-curve` no se cierra (MTD-vs-MTD se mantiene). Decisión: KPI de Balance en Distribución se iguala a stock vivo del Panel (`LiveBalanceCalculator` / TC actual); Ingresos/Gastos en Distribución siguen flujo del período (lectura acotada de Frank). Panel no se toca. `distribution-balance-kpi-skips-fx` sigue backlog. No PASS.
